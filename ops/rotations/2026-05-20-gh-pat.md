# GH_PAT rotation — 2026-05-20 (Task #102)

## Reason
During the v0.7.226 emergency ship attempt, the `GH_PAT` secret briefly
appeared in process listings (`ps -p` output) on a shared sandbox. No
evidence of capture, but rotated as a precaution.

## Timeline (UTC)
- **2026-05-20 ~20:27** — Operator on `github.com/settings/tokens` page
  with the old `GH_PAT` (no expiration) row visible. See
  `attached_assets/Screenshot_2026-05-20_202723_1779308941951.png`.
- **2026-05-20 ~20:30** — Operator generated new classic PAT `GH_PAT v2`
  (scopes: `repo`, `workflow`; expiration: 2026-08-18).
- **2026-05-20 20:34** — First Replit-side smoke test ran HTTP 200 against
  `/repos/nanvilow/scripturelive-ai`, scopes `repo, workflow`,
  `permissions.admin=true`. **Note**: this hit was against the OLD token
  value still sitting in the Replit `GH_PAT` secret — the new token had not
  yet been pasted into Replit (`GH_PAT v2` shown as "Never used" in
  follow-up screenshot).
- **2026-05-20 ~20:37** — Operator deleted the old `GH_PAT` row on GitHub.
  Next smoke test returned HTTP 401 (`Bad credentials`) — confirms the
  Replit secret was still holding the old, now-revoked value, and confirms
  the old token is no longer usable.
- **2026-05-20 ~20:43** — Operator regenerated the `GH_PAT v2` value (since
  the original create-time value was never captured into Replit), pasted
  the new value into the Replit `GH_PAT` secret. See
  `attached_assets/Screenshot_2026-05-20_204321_1779309810297.png` for the
  intermediate state (old row deleted; new row present).
- **2026-05-20 20:47** — Final smoke test:
  - URL: `https://api.github.com/repos/nanvilow/scripturelive-ai`
  - Method: `GET` with `Authorization: Bearer $GH_PAT`
  - HTTP: **200**
  - `x-oauth-scopes: repo, workflow`
  - `github-authentication-token-expiration: 2026-06-19 20:45:35 UTC`
  - `permissions: { admin: true, maintain: true, push: true, triage: true, pull: true }`
  - Token fingerprint (sha256 first 12 chars): `245ab5954d9b`
  - Earlier (pre-rotation) fingerprint: `334ffd83cf06` — DIFFERENT value
    confirms the Replit secret now holds a new credential, not the old one.
  - Token shape: `ghp_` prefix, length 40 (classic PAT format).

## Acceptance check
- [x] Fresh fine-scoped PAT generated on GitHub with `repo` + `workflow`
      scopes (and a finite expiration, replacing the prior "no expiration"
      setting — small bonus hardening).
- [x] New value stored as `GH_PAT` in the Repl's secrets (verified via the
      fingerprint change above; secret value never echoed in chat or logs).
- [x] Old token revoked on GitHub (verified two ways: visually absent from
      the token list in screenshot 2, AND the previously-working old-value
      probe now returns 401 Bad credentials).
- [x] Smoke test against `/repos/nanvilow/scripturelive-ai` succeeds with
      the new token (HTTP 200, admin permissions, correct scopes).

## Out of scope / follow-ups
- Secret scanning + push protection are disabled on the repo
  (`security_and_analysis` showed all sub-features `disabled` during the
  smoke test). Filed as follow-up task #103.
- New token expires 2026-06-19. Calendar a refresh before that date or move
  to a fine-grained PAT with rotation automation.
