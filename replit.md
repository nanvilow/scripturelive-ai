# Imported App

A Next.js application providing scripture-related services for web and desktop, enhancing live service experiences, and streamlining administrative tasks.

## User preferences

-   **Autonomy**: Act without asking for consent on routine decisions — always do what's better for the app. Do not ask permission for housekeeping (file trims, refactors, retry logic, dependency bumps, etc.). Only ask when a decision is genuinely destructive or shapes product direction.

## Run & Operate

-   **Install dependencies**: `pnpm install`
-   **Run dev server**: `pnpm dev`
-   **Build app**: `pnpm build`
-   **Typecheck**: `pnpm typecheck`
-   **Codegen**: `pnpm codegen` (for API client)
-   **DB Push**: `pnpm db:push` (for Drizzle schema migrations)
-   **Required Env Vars**: `MAIL_HOST`, `MAIL_USER`, `MAIL_PASS`, `MAIL_FROM`, `SMS_API_KEY`, `SMS_SENDER`

## Stack

-   **Monorepo**: pnpm workspaces
-   **Runtime**: Node.js 24
-   **Language**: TypeScript 5.9
-   **Frontend**: Next.js 16
-   **Backend**: Express 5
-   **Database**: PostgreSQL, SQLite, Replit DB
-   **ORM**: Drizzle ORM, Prisma
-   **Validation**: Zod
-   **Build Tool**: esbuild
-   **API Codegen**: Orval
-   **Speech Recognition**: OpenAI `gpt-4o-mini-transcribe`, Deepgram Nova-3
-   **AI SDK**: OpenAI SDK
-   **NDI Integration**: `koffi` FFI binding to bundled `Processing.NDI.Lib.x64.dll` (NDI 6 runtime ships inside installer as of v0.7.146 — no separate NDI Tools install required)

## Where things live

-   `apps/nextjs-app`: Next.js frontend and Electron wrapper.
-   `apps/api-server`: Express API backend.
-   `packages/`: Shared utilities and components.
-   `artifacts/imported-app/db/custom.db`: SQLite database for the desktop app.
-   `artifacts/imported-app/build-resources/ndi/Processing.NDI.Lib.x64.dll`: bundled NDI runtime (v0.7.146).
-   `public/downloads/manifest.json`: Manifest for dynamic downloads.
-   `openapi.yaml`: API contracts (source of truth for Orval codegen).
-   `drizzle/schema.ts`: Drizzle ORM database schema.
-   `styles/theme/`: Theme-related files.

## Architecture decisions

> Detailed historical entries live in `artifacts/imported-app/CHANGELOG.md`. The list below keeps only the **last three releases in full detail** plus tight one-line guard-rails for older patterns we still want future agents to respect.

### Recent (full detail)

-   **v0.7.217 — Live media-video smoothness: stop 10Hz SSE broadcast storm + stop receiver seek-thrash so secondary display + NDI projector play smoothly (operator $1600-customer escalation, follow-up to v0.7.216: secondary screen + NDI projector micro-freezes during steady playback that EasyWorship didn't have)**: `MediaVideoSurface` writeback effect in `logos-shell.tsx` L420-475 — live-surface writeThreshold MUST be `0.50` (was 0.10 → 5x reduction in broadcast rate ~10Hz → ~2Hz), preview-surface stays `0.25`. To preserve operator-facing responsiveness on transport gestures (pause/scrub/GO LIVE/chapter advance), 4 unconditional event listeners MUST be registered: `play`, `pause`, `seeked`, `loadedmetadata` → each calls `setOwnCurrentTime(v.currentTime)` immediately and bypasses the threshold. Cleanup MUST removeEventListener all 4. Companion fix in `congregation/route.ts` L1593-1623 — receiver drift tolerance MUST be `1.5` (was 0.20 → 7.5x) and the `!IS_NDI` exemption MUST stay (v0.7.194-hotfix.2 NDI invariant). Both fixes together: sender stops bombarding at 10Hz, receiver stops seeking on every jitter spike. Without BOTH, intermediate state (raise sender only, or raise receiver only) is WORSE than v0.7.216 — raise sender threshold to 0.50 with receiver still at 0.20 means every writeback (now 0.50s jumps) trips the receiver's 0.20s seek threshold guaranteed. customBackground / payload schema / NDI exemption / aspect-ratio paths NOT touched. Tests (w)+(x) in `media-preview-isolation.test.ts` source-grep enforce both constants AND all 4 transport listeners AND the NDI exemption. 658/658 PASS. Playwright runtime proof: all 4 transport listeners fire immediately on event dispatch, sub-0.50s timeupdate ticks are suppressed, +0.6s tick crosses threshold and fires writeback. Full text in `artifacts/imported-app/CHANGELOG.md`.

-   **v0.7.216 — Single-clicking a DIFFERENT media tile while LIVE plays a media-video MUST NOT stall the live video; also covers Settings dropdown z-order, GO LIVE auto-play retry, and headphone-button removal (4 follow-ups, full text in CHANGELOG)**: 5 invariants — (A) `MediaVideoSurface` `<video>` `autoPlay` MUST be gated on `!mediaPaused && surface === 'preview'` (not just surface) so the 2nd HW decoder never spins up on a different-media single-click. (B) `library-compact.tsx` sendMediaToPreview MUST detect `liveIsPlayingDifferentMediaVideo` (read liveSlide via v0.7.208 direct-ref-first pattern) and flip `setPreviewMediaPaused(true) + setPreviewMediaCurrentTime(0)` BEFORE `pinPreviewSlide(slide)`; MUST NOT touch any live state (`slides[] / liveSlideIndex / isLive / setLiveAuto`). (C) Play/pause effect MUST register one-shot `canplay` retry with `{once: true}` AND cleanup `removeEventListener('canplay', ...)` so GO-LIVE-swap of paused preview clip auto-plays once new src buffers AND rapid B→C→D swaps don't leak stale listeners that fire `play()` on wrong src. (D) ALL 11 Radix portal primitives (Select/DropdownMenu/Popover/Tooltip/HoverCard/ContextMenu/Menubar/NavigationMenu/Dialog/AlertDialog/Sheet) MUST use `z-[60]`, NEVER bare `z-50` — Settings overlay at z-50 + portal at z-50 = DOM-order tie → portal paints behind on any compositor repaint (live video frames trigger this). Settings overlay MUST stay `fixed inset-0 z-50 bg-background` (load-bearing baseline). (E) LiveDisplayCard's volume toggle MUST flip `setLiveBroadcastAudio` AND the live `<video>` `muted` formula MUST OR-in `!liveBroadcastAudio` so the button actually mutes (was visually toggling state but not affecting audio). PROCESS GR — when an HTML media attribute (`autoplay`/`muted`/`loop`/`preload`) controls a finite HW resource, gate at the ATTRIBUTE layer (synchronous with mount) not just in useEffect (post-mount) — attribute processing runs before any effect commits and the browser may allocate before the effect lands. PROCESS GR — same operator-visible bug ("clicking media B stops live A") with multiple INDEPENDENT failure paths (store mutation v0.7.210, iframe transport dedup v0.7.212, React decoder competition v0.7.216) → fix sequence is CORRECT, not duplicative; each release closes one layer. Forward audit: enumerate every consumer of shadowed state and classify as (1) store mutation, (2) network/SSE transport, (3) DOM/React rendering, (4) browser-resource (decoder/AudioContext) competition. Tests (q)+(r)+(s)+(t)+(u)+(v) + headphone-volume guards in `media-preview-isolation.test.ts`. Full text in `artifacts/imported-app/CHANGELOG.md`.
-   **v0.7.215 — Per-pane Clear buttons on Preview and Live Display (independent, single-click; operator UX request: "add a clear button on both preview and live display that users can clear anything on there with a single click. Both shouldn't interfere with each other.")**: Screenshot circled the empty footer strip at the bottom of each pane in `logos-shell.tsx`. **Root cause / pre-state**: `clearLive` callback at L4044 was only reachable from the GO LIVE / STOP LIVE toggle (when isLive=true) and Escape/Enter-while-live keyboard paths — no UI affordance to wipe Live when the toggle was idle (e.g. AI direct-ref slide held purely in `liveSlide` with `isLive=false`). Preview had ZERO Clear primitive — `clearPinnedPreview()` existed in store L1191 but no button surfaced it, so pins from v0.7.201 `pinPreviewSlide` sat until next pin / setSlides reset. Additionally `clearLive` was incomplete: called `setLiveSlideIndex(-1) + setIsLive(false) + sendToOutput(null,false)` but NOT `clearLiveAuto()` — stale v0.7.203 `liveSlide` direct ref survived every Clear / Stop Live / Escape / Enter-while-live, and `buildOutputPayload`'s `liveSlide ?? slides[liveSlideIndex]` re-rendered the AI slide on the next broadcaster tick. Latent since v0.7.203 (Jul 2025), surfaced now because the operator-facing label "Clear" mandates the pane actually go empty. **Fix #1 (`src/components/layout/logos-shell.tsx` L4044-4058, +6 lines)**: prepend `useAppStore.getState().clearLiveAuto()` to `clearLive`. Now `clearLive = clearLiveAuto() + setLiveSlideIndex(-1) + setIsLive(false) + sendToOutput(null,false)`. **Fix #2 (logos-shell.tsx L1180-1205, ~25 lines net)**: PreviewCard symmetry strip (previously empty `<div className="h-7" />` spacer reserved purely for matching LiveDisplayCard's bottom transport row height since v0.7.157) now hosts an explicit Clear Preview `<Button>` with `CircleSlash` icon. `onClick` calls EXACTLY 2 primitives: `useAppStore.getState().clearPinnedPreview()` + `useAppStore.getState().setPreviewSlideIndex(-1)`. MUST NOT touch `slides[]` (deck survives), `liveSlide` / `liveSlideIndex` (live keeps playing), or `isLive` (broadcast keeps going). `disabled={!previewSlide}` so operator sees at a glance what's clearable. PreviewCard already subscribes to store — no prop drilling. **Fix #3 (logos-shell.tsx L1942-1968, ~18 lines)**: LiveDisplayCard bottom toolbar (Prev | Go Live/Stop Live | Next) gains a 4th button — Clear Live — inserted LEFT of Prev. `onClick={onClearLive}` reuses the existing prop (parent shell wires it to v0.7.215-updated `clearLive` at L4044). Always visible (unlike Stop Live which only appears when isLive=true), `disabled={!liveSlide && !isLive}` so operator can wipe stale direct-ref slide even when toggle is idle. Same `CircleSlash` icon as Clear Preview for visual symmetry. **No change needed in `easyworship-shell.tsx`**: screenshot is logos-shell; EasyWorship shell uses different chassis with Stop Live + Clear already at bottom toolbar. **New regression tests (5, extended `src/lib/media-preview-isolation.test.ts`)**: (l) BEHAVIOURAL — Clear Preview wipes preview but live keeps playing: pin preview verse + setLiveAuto+isLive=true on live verse → call `clearPinnedPreview() + setPreviewSlideIndex(-1)` → assert preview read null, `liveSlide.id`, `liveSlideIndex`, `isLive`, `buildOutputPayload.slide.id`, `slides[0].id` all UNTOUCHED. (m) BEHAVIOURAL — Clear Live wipes live but preview keeps its pin: symmetric setup → call `clearLiveAuto() + setLiveSlideIndex(-1) + setIsLive(false)` → assert `liveSlide=null` (Fix #1 wiped direct ref), `liveSlideIndex=-1`, `isLive=false`, `pinnedPreviewSlide.id`, slides[] all PRESERVED. (n) GUARD — `clearLive` source MUST call `clearLiveAuto()` + setLiveSlideIndex(-1) + setIsLive(false) + sendToOutput(null,false). (o) GUARD — PreviewCard symmetry strip Clear Preview button source MUST call `clearPinnedPreview()` + `setPreviewSlideIndex(-1)` AND MUST NOT call setIsLive / setLiveSlideIndex / clearLiveAuto / setSlides (independence invariant). (p) GUARD — LiveDisplayCard Clear button source MUST `onClick={onClearLive}`, MUST NOT mention onSendLive (separate from GO LIVE toggle), MUST NOT touch clearPinnedPreview / setPreviewSlideIndex inline. **Test results**: 16/16 PASS in `media-preview-isolation.test.ts` (was 11/11 on v0.7.213 — +5). Full suite **650/650 PASS** (was 645/645 on v0.7.214 — +5). 0 regressions. **Replit runtime proof**: tests (l)+(m) exercise EXACT store mutation sequences the new buttons trigger (verified by source-grep guards (o)+(p) that those primitives ARE wired to the buttons' onClick); `buildOutputPayload` assertion in (l) mirrors broadcaster's read path through v0.7.203 output-payload contract. Source-grep guards (n)+(o)+(p) prove fixes live at call sites. Same hybrid proof shape as v0.7.208/v0.7.210/v0.7.212/v0.7.213/v0.7.214 (architect-accepted). **GUARD-RAIL (A) — `clearLive` MUST call `clearLiveAuto()` BEFORE the legacy 3-primitive sequence**: omitting it leaves v0.7.203 `liveSlide` direct ref alive, broadcaster's `liveSlide ?? slides[liveSlideIndex]` re-renders the stale AI verse on next tick — operator presses Clear and the projector keeps showing the slide. Test (n) source-grep enforces. **GUARD-RAIL (B) — Clear Preview button MUST call ONLY `clearPinnedPreview() + setPreviewSlideIndex(-1)`, NOTHING ELSE**: any extra mutation breaks independence. `setSlides([])` would destroy operator's deck (Clear is not "remove all" — bulk-delete is the Scripture Feed's removeAll* affordances). Touching live primitives cascades into live — exact "interference" operator forbade. Test (o) `.not.toMatch(...)` enforces ABSENCE of setIsLive / setLiveSlideIndex / clearLiveAuto / setSlides. **GUARD-RAIL (C) — Clear Live button MUST call ONLY `onClearLive` (the prop), inline button source MUST NOT touch preview-side primitives**: the 4-primitive sequence inside the callback MUST stay symmetric — removing any of the 4 (clearLiveAuto / setLiveSlideIndex / setIsLive / sendToOutput) produces operator-visible drift between React tree, store, and projector. Tests (n)+(p) source-grep enforces. **GUARD-RAIL (D) — Clear Live MUST be visually distinct from Stop Live**: Clear Live uses CircleSlash icon + "CLEAR" label + neutral border; Stop Live uses Square icon + "STOP LIVE" label + rose-bordered muted styling (conditional inverse of rose-filled GO LIVE). Adjacent placement in toolbar avoids conflation; conflating them re-introduces v0.7.214-era "press GO LIVE, toggle becomes Stop Live, press it expecting Clear but only stops broadcast while stale slide stays visible" confusion. **PROCESS GR — When the operator-visible label of an affordance is "Clear", the underlying primitive MUST wipe EVERY layer of the state model that contributes to the rendered output**: for Live = direct ref (v0.7.203 `liveSlide`) + legacy index (`liveSlideIndex`) + broadcast flag (`isLive`) + projector push (`sendToOutput`). For Preview = direct ref (v0.7.201 `pinnedPreviewSlide`) + legacy index (`previewSlideIndex`). Any layer omitted is a latent regression waiting for the state combination where THAT layer drives the pane. Audit: `rg -n "<X>Slide|<X>SlideIndex|<X>:\s*null|set<X>" src/lib/store.ts` THEN enumerate every state field contributing to `buildOutputPayload`'s read path for surface X; Clear primitive wipes ALL of them. **PROCESS GR — A "symmetry strip" empty spacer in a card layout is technical debt that signals a missing affordance**: PreviewCard's L1180-1197 placeholder was load-bearing for layout (matching 16:9 stage size between Preview and Live since v0.7.157) but content-less. v0.7.215's Clear Preview button fills it with the EXACT affordance the layout was reserving space for. Future: any `<div className="h-N" />` aria-hidden placeholder = backlog candidate (height reservation correct, content missing).

-   **v0.7.214 — Voice commands + AI/LLM verse navigation MUST target LIVE display only (one-line GR, full text in CHANGELOG)**: Every voice handler in `speech-provider.tsx` (next_verse / next_chapter / show_verse_n / find_by_quote / LLM classifier) MUST read live anchor via `s.liveSlide ?? (liveIdx >= 0 ? slides[liveIdx] : null)` (v0.7.208 AI direct-ref-first pattern — bare `slides[liveIdx]` returns null when AI is driving live) AND MUST commit via `setLiveAuto(slide) + setLiveActiveVerseIndex(0)`, NEVER the legacy `setSlides + setPreviewSlideIndex + setLiveSlideIndex + setIsLive` combo (clobbers preview + leaves AI liveSlide ref stale). Voice dispatcher switch body MUST contain ZERO `setPreviewSlideIndex(` call sites outside `bible_says` (the ONE voice command that legitimately writes preview — STANDBY only). MUST NOT call `addScheduleItemQuiet` as "preserve manual preview" fallback (v0.7.194-hotfix.11 anti-pattern). Tests (a)-(g) in `ai-live-only.test.ts` enforce all 8 sites. PROCESS GR — when introducing a new direct-ref primitive (`setLiveAuto`/`pinPreviewSlide`), SAME-RELEASE audit MUST enumerate every reader/writer of the SHADOWED state via `rg -n "<shadowed_field>" src/`, not just the headline call site. Full text in `artifacts/imported-app/CHANGELOG.md`.
-   **v0.7.213 — `setSlides` MUST clear `pinnedPreviewSlide` so detected/suggested verses on Preview win goLive (one-line GR)**: `store.ts` setSlides reducer's set() payload MUST include BOTH `liveSlide: null` (v0.7.203 invariant) AND `pinnedPreviewSlide: null` (v0.7.213 invariant). Removing either re-opens a regression class: without `liveSlide: null`, AI direct ref survives a new deck import and stale-renders on live; without `pinnedPreviewSlide: null`, a stale Media-tile pin survives the verse-stage path and v0.7.212's `goLive` pin-first branch promotes the wrong thing. Pattern: any "hard-reset" reducer that takes ownership of Preview MUST clear pin + AI ref. Audit: `rg -n "setSlides:\s*\(s\)\s*=>\s*set\(" src/lib/store.ts` MUST show BOTH fields. Test (j) in `media-preview-isolation.test.ts` source-grep enforces both. Full text in `artifacts/imported-app/CHANGELOG.md`. **New regression tests (3, extended `src/lib/media-preview-isolation.test.ts`)**: (i) load-bearing — pin a stale video, then `setSlides([verse])`, assert `pinnedPreviewSlide === null` post-call, mirror goLive's pin-null fallback to `setLiveSlideIndex(previewSlideIndex) + setIsLive(true)`, assert `buildOutputPayload(state).slide.id === 'rom-8-29'` (verse promoted, not the stale pin). (j) GUARD source-grep on store.ts: `setSlides` reducer's set() payload regex `setSlides:\s*\(s\)\s*=>\s*set\(\{([^}]*)\}\)` MUST match BOTH `pinnedPreviewSlide:\s*null` AND `liveSlide:\s*null` (preserves v0.7.203 invariant). (k) symmetric flow — verse on Preview via setSlides, single-click video → `pinPreviewSlide(vid)` → assert `(pinnedPreviewSlide ?? slides[previewSlideIndex])?.id === 'vid-storm'` (Preview swaps instantly); slides[0] verse survives in the deck (just shadowed by the pin); live untouched (`isLive=false`, `liveSlide=null`). **Test results**: 11/11 PASS in the extended file (was 8/8 on v0.7.212 — +3). Full suite **638/638 PASS** (was 635/635 on v0.7.212 — +3). 0 regressions. **Replit runtime proof**: test (i) exercises the EXACT 3-call sequence the production paths run (`pinPreviewSlide` → `setSlides` → `setLiveSlideIndex+setIsLive`); the assertion `pinnedPreviewSlide === null` post-setSlides directly verifies the store mutation; the final `buildOutputPayload` assertion mirrors the renderer's read path. Test (j) source-grep enforces the fix lives at the store reducer, not in a wrapper that could be bypassed. Same hybrid proof shape as v0.7.210/v0.7.212. **GUARD-RAIL (A) — store.ts `setSlides` reducer's set() payload MUST include BOTH `liveSlide: null` (v0.7.203) AND `pinnedPreviewSlide: null` (v0.7.213)**: removing either field re-opens a regression class. Without `liveSlide: null`, the v0.7.208 AI direct ref survives a new deck import and stale-renders on live (v0.7.210's lead bug). Without `pinnedPreviewSlide: null`, a stale Media pin survives the verse-stage path and v0.7.212's `goLive` promotes the wrong thing (this release's lead bug). Test (j) source-grep enforces both. Audit: `rg -n "setSlides:\s*\(s\)\s*=>\s*set\(" src/lib/store.ts` MUST show BOTH fields in the payload. **GUARD-RAIL (B) — Any future "hard-reset" reducer that takes ownership of the Preview surface MUST clear `pinnedPreviewSlide` AND `liveSlide`**: the pattern is established at L1442 (`setSelectedScheduleItemId(null)`), L1463 (other hard-reset), and L1190 (`setSlides` as of v0.7.213). New reducers like `loadDeck`, `importPresentation`, `resetState` etc. MUST follow the same pattern. **PROCESS GR — When introducing a "pin-first" read pattern (`pinnedPreviewSlide ?? slides[...]`), EVERY writer to the fallback (slides[] via setSlides, setPreviewSlideIndex, etc.) MUST also clear the pin**: v0.7.201 introduced pin-first read in the Preview pane (logos-shell L967). v0.7.212 extended to `goLive`. But the symmetric "writer clears pin" rule was only applied at clearScheduleItem/clearSelection sites, NOT at `setSlides`. Future audit when introducing a new shadowing primitive: enumerate ALL writers to the shadowed field; default to "clear the shadow" unless writer semantics explicitly preserve operator intent (`setPreviewSlideIndex` correctly preserves the pin for arrow-key navigation through the same deck). **PROCESS GR — When operator escalates a follow-up symptom IMMEDIATELY after a release ships, do NOT assume the prior release was wrong; the new symptom is often the NEXT layer of the same bug class**: v0.7.212's pin-first branch in goLive was correct in isolation. The follow-up symptom was caused by a DIFFERENT site (writer didn't clear the pin) — a layer deeper. The right diagnostic question is "given my fix is correct in isolation, what UPSTREAM/DOWNSTREAM site does NOT yet know about the new invariant?" — this produced the 5-minute store.ts fix instead of an unnecessary `goLive` rewrite.

-   **v0.7.212 — Single-click on another Media tile stops the live video AND Go Live button does not promote preview-pinned video to live (one-line GR)**: `output-preview.tsx` `sendNow` MUST hold `lastSentFpRef` and compute a CHEAP identity fingerprint via `JSON.stringify({t:type,b:blanked,l:isLive,dm:displayMode,sl:{id,ty,ti,su,co,mk,mu:mediaUrl.length,mp:mediaPaused},st:settings,au:audio})` — early-return `if (fp === lastSentFpRef.current)` BEFORE `revRef.current += 1` and `postMessage` so identical mirrorLive payloads don't tear down the iframe `<video>`. Fingerprint MUST exclude raw `s.mediaUrl` (only `mediaUrl.length` proxy). v0.7.204 IS_PREVIEW handler `lastRenderKey=''` force-reset MUST stay intact at route.ts L2212. Both `goLive` functions (easyworship-shell + logos-shell) MUST check `pinnedPreviewSlide` and `setLiveAuto(pinned)` BEFORE the `!slides.length` toast guard. PROCESS GR: when a new primitive is introduced, EVERY downstream reader/writer of the shadowed state MUST be audited in the same release. A "subscribe fires on every store change" pattern is ONLY safe when receiver is idempotent for unchanged payloads — dedup at SOURCE not SINK. Full text in `artifacts/imported-app/CHANGELOG.md`.<!-- TRIMMED v0.7.215: was v0.7.212 incumbent full -->
