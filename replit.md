# Imported App

A Next.js application providing scripture-related services for web and desktop, enhancing live service experiences, and streamlining administrative tasks.

## User preferences

-   **Autonomy**: Act without asking for consent on routine decisions — always do what's better for the app. Do not ask permission for housekeeping (file trims, refactors, retry logic, dependency bumps, replit.md trims, etc.). Only ask when a decision is genuinely destructive or shapes product direction.
-   **replit.md upkeep**: Roll the changelog window proactively. When shipping a new release entry, demote the oldest "Recent (full detail)" item to a one-line guard-rail in the same pass — do NOT wait to be asked.

## Run & Operate

-   **Install dependencies**: `pnpm install`
-   **Run dev server**: `pnpm dev`
-   **Build app**: `pnpm build`
-   **Typecheck**: `pnpm typecheck`
-   **Codegen**: `pnpm codegen` (for API client)
-   **DB Push**: `pnpm db:push` (for Drizzle schema migrations)
-   **Required Env Vars**: `MAIL_HOST`, `MAIL_USER`, `MAIL_PASS`, `MAIL_FROM`, `SMS_API_KEY`, `SMS_SENDER`

## Stack

-   **Monorepo**: pnpm workspaces
-   **Runtime**: Node.js 24
-   **Language**: TypeScript 5.9
-   **Frontend**: Next.js 16
-   **Backend**: Express 5
-   **Database**: PostgreSQL, SQLite, Replit DB
-   **ORM**: Drizzle ORM, Prisma
-   **Validation**: Zod
-   **Build Tool**: esbuild
-   **API Codegen**: Orval
-   **Speech Recognition**: OpenAI `gpt-4o-mini-transcribe`, Deepgram Nova-3
-   **AI SDK**: OpenAI SDK
-   **NDI Integration**: `koffi` FFI binding to bundled `Processing.NDI.Lib.x64.dll` (NDI 6 runtime ships inside installer as of v0.7.146 — no separate NDI Tools install required)

## Where things live

-   `artifacts/scripturelive-site/`: marketing site (scriptureliveai.com) — Vite + React + Tailwind v4 + Wouter SPA. Migrated from a standalone Replit project into this monorepo. Builds to `dist/public/`. Deploy target: Cloudflare Pages (free). Build cmd: `pnpm --filter @workspace/scripturelive-site run build`. Output dir: `artifacts/scripturelive-site/dist/public`. Admin pages + API hooks stripped — `src/lib/api-stubs.ts` returns empty results so the home page falls back to hardcoded `DEFAULT_*` constants.
-   `apps/nextjs-app`: Next.js frontend and Electron wrapper.
-   `apps/api-server`: Express API backend.
-   `packages/`: Shared utilities and components.
-   `artifacts/imported-app/db/custom.db`: SQLite database for the desktop app.
-   `artifacts/imported-app/build-resources/ndi/Processing.NDI.Lib.x64.dll`: bundled NDI runtime (v0.7.146).
-   `public/downloads/manifest.json`: Manifest for dynamic downloads.
-   `openapi.yaml`: API contracts (source of truth for Orval codegen).
-   `drizzle/schema.ts`: Drizzle ORM database schema.
-   `styles/theme/`: Theme-related files.

## Sandbox quirks (Replit main agent)

-   **`pnpm install` is blocked** in the main-agent sandbox — pnpm touches `.git/config` and the sandbox classifies it as a destructive git op. To restore a missing `node_modules/@workspace/<lib>` symlink, recreate it manually with `ln -s ../../../../lib/<name> artifacts/imported-app/node_modules/@workspace/<name>`. The `strip:workspace-symlinks` script (run by `pnpm run package*` to flatten symlinks for electron-builder) is a one-way trapdoor in this sandbox — re-running `pnpm install` to restore them isn't possible from here.
-   **`git push` is blocked** in the main-agent sandbox (same `.git/config` touch classification). Ship via the GH Contents API using `GH_PAT` — PUT each changed file to the target `release/v0.7.<N>` branch on `nanvilow/scripturelive-ai`, then trigger `workflow_dispatch` on `release-desktop.yml` (workflow ID `265047930`) with **BOTH** `ref: release/v0.7.<N>` AND `inputs.tag: v0.7.<N>` (the leading `v` is mandatory). The `!cancelled()` job-condition fix for the release-desktop workflow already lives on `release/v0.7.228` (`2e73b7d` + `85d4117`) — cherry-pick onto new release branches if it isn't already there.
-   **CRITICAL workflow_dispatch `inputs.tag` MUST be set (v0.7.247 root cause #2)**: dispatching with `{"ref": "release/v0.7.<N>"}` only — without `inputs.tag` — succeeds the workflow trigger (204) and runs the Next.js + Windows installer builds to completion, but the "Upload installer" step fails with `No files were found with the provided path: artifacts/imported-app/release/ScriptureLive-AI-refs/heads/release/v0.7.<N>-Setup-*.exe`. The Resolve tag step at `release-desktop.yml` L214-229 falls into its else branch when `inputs.tag` is empty: `tag_name="${GITHUB_REF#refs/tags/}"` which leaves the full `refs/heads/release/v0.7.<N>` intact (no `refs/tags/` prefix to strip), then `version=${tag_name#v}` keeps the same string (no leading `v`), then the artifact upload glob (L445) expands to the broken path above. Electron-builder actually produced `ScriptureLive-AI-0.7.<N>-Setup-x64.exe` correctly — the build is fine, only the upload glob is wrong. **Canonical dispatch payload**: `curl -X POST .../actions/workflows/release-desktop.yml/dispatches -d '{"ref":"release/v0.7.<N>","inputs":{"tag":"v0.7.<N>"}}'`. **Operator-detectable signal**: failed run's "Upload installer" step log contains `ScriptureLive-AI-refs/heads/...` in the missing-files path → re-dispatch with `inputs.tag` set, do NOT rebuild from scratch (the artifacts on the runner are already gone, but the build is cheap to re-run — ~22 min on the windows runner).
-   **CRITICAL ship-script EXCLUDE_FILES MUST cover BOTH baked-credential files (v0.7.253 secret-scan failure root cause)**: gitignored locally but the ship-script walker doesn't honour `.gitignore` — it only respects the explicit `EXCLUDE_FILES` set. Required entries: `'src/lib/baked-credentials.ts'` (server SMTP/SMS) AND `'src/lib/keys.baked.ts'` (renderer OpenAI/Deepgram). v0.7.251 → v0.7.253 shipped real Deepgram API key into git history on three release branches because only the server file was in the exclude set. Operator-detectable signal: Secret Scan workflow goes red with `gitleaks` finding `RuleID: deepgram-api-key` (or `openai-api-key`) at `artifacts/imported-app/src/lib/keys.baked.ts:11`. **Mandatory remediation when triggered**: (1) operator rotates the leaked key in the provider console (Deepgram/OpenAI dashboard) — leaked-and-rotated is the only safe state, gitleaks-whitelisting a live key is NEVER acceptable; (2) add the missing path to `EXCLUDE_FILES` in the ship script template for future releases; (3) optionally delete the affected `release/v0.7.<N>` branches via `DELETE /repos/.../git/refs/heads/release/v0.7.<N>` — the GH Release tag + uploaded `.exe` are independent of the branch and survive branch deletion, but the leaked-key commit becomes orphaned and gets GC'd.
-   **CRITICAL ship-drift rule (v0.7.237 root cause)**: every release MUST PUT every drifted file, NOT just `package.json` + `CHANGELOG.md` + `installer.nsh` + `replit.md`. The sandbox's local working tree is the ONLY place feature/fix code lives until explicitly PUT to GitHub — there is no `git push` to discover divergence. v0.7.226 → v0.7.236 all shipped binaries equivalent to v0.7.225 with version bumps because the 4-file PUT loop missed every feature file. **Mandatory pre-ship drift check**: enumerate every file under `artifacts/imported-app/` (excluding `node_modules`, `.next`, `dist`, `src/generated/prisma-client`, `tsconfig.tsbuildinfo`, `next-env.d.ts`, gitignored baked-credentials), compute git blob SHA locally, compare against remote `main` via `GET /repos/.../git/trees/main?recursive=1`, PUT every mismatch + every local-only non-build file to the release branch. The bulk-PUT Python script in v0.7.237 CHANGELOG entry is the canonical reference. **Operator-detectable signal**: if a fix landed in source 3+ releases ago and a freshly-installed binary still doesn't show it, the FIRST diagnostic is `curl /repos/.../contents/<file>?ref=main | base64 -d | grep <fix-marker>` — NOT another reinstall, NOT a state-cleanup walkthrough.

## Architecture decisions

> Detailed historical entries live in `artifacts/imported-app/CHANGELOG.md`. The list below keeps only the **last release in full detail** plus tight one-line guard-rails for older patterns we still want future agents to respect.

### Recent (full detail)

-   **v0.7.256 — Cloud companion migrated off Replit to self-hosted Hetzner (cloud.scriptureliveai.com)**: Operator went fully Replit-independent. The .exe runtime was already 100% Replit-free (auto-updates from GitHub Releases, installer built on GitHub Actions Windows runner, Deepgram/OpenAI keys baked at build via GH Actions secrets, NDI + slides + Bible translations all local). Only remaining dependency was `scripturelive.replit.app` cloud companion (license sync + admin panel + transcribe fallback + telemetry). Migrated to operator-owned Hetzner Cloud CPX22 Nuremberg (~€12/mo with backups): Ubuntu 26.04 + Node 24 + pnpm 11.3 + Next 16 standalone + nginx 1.28 reverse proxy + SQLite at `/var/lib/scripturelive/custom.db` (outside the repo clone so `git pull` never touches it) + Let's Encrypt cert via `certbot --nginx` (auto-renewing). Three URL-default flips this release: `DEFAULT_TRANSCRIBE_PROXY_URL` in `electron/main.ts`, `DEFAULT_TELEMETRY_URL` in `electron/telemetry.ts`, `DEFAULT_CLOUD_BASE` in `src/app/api/license/admin/cloud-sync-test/route.ts` — all flipped from `scripturelive.replit.app` → `cloud.scriptureliveai.com`. Plus three user-facing instruction strings in the same cloud-sync-test route. All three env-var overrides (`TRANSCRIBE_PROXY_URL`, `SCRIPTURELIVE_TELEMETRY_URL`, `SCRIPTURELIVE_CLOUD_BASE`) remain in place at the use sites (NOT module-load), so per-operator host switching takes effect on the next request without a rebuild OR restart. New permanent deliverables: `artifacts/imported-app/scripts/ship-release.mjs` (replaces the `/tmp/ship-v0.7.<N>.mjs` pattern — atomic git-data-API push, mandatory `inputs.tag=v<VERSION>`, BOTH baked-credential paths in `EXCLUDE_FILES`, runs anywhere with `git clone + Node 24 + GH_PAT`) and `artifacts/imported-app/MIGRATION.md` (dependency table, Fly.io/Railway/Render/Hetzner deploy paths, what survives vs dies if Replit disappears). Parallel period: `scripturelive.replit.app` stays alive ~2 weeks until auto-update propagates v0.7.256 to all installs. **GUARD-RAIL (env-var override precedence)**: all three cloud URLs MUST stay read at the use site, NOT at module-load — `process.env.<NAME> || DEFAULT_*` is the canonical pattern. Module-load reads break per-operator-flip-without-restart, which is a load-bearing migration property. Canonical grep: `rg "process\.env\.(TRANSCRIBE_PROXY_URL|SCRIPTURELIVE_TELEMETRY_URL|SCRIPTURELIVE_CLOUD_BASE)" artifacts/imported-app/electron artifacts/imported-app/src/app/api/license` must show the env-read inline in each function/spawn args, not at top-level `const X = process.env...`. **GUARD-RAIL (fresh-clone build sequence)**: on any non-CI machine (Hetzner, dev laptop, contributor PC), `next build` fails with "module-not-found" across every file importing `src/lib/runtime-keys.ts` or `src/lib/baked-credentials.ts` because both are gitignored. Canonical pre-build sequence: `set -a && source .env.production && set +a && node scripts/inject-keys.mjs` BEFORE `next build`. GH Actions runs inject-keys with secrets in job env automatically; manual installs MUST run it explicitly. **GUARD-RAIL (Hetzner pnpm 11.3 preinstall false-positive)**: root `package.json` preinstall `case "$npm_config_user_agent" in pnpm/*)` check false-fires under pnpm 11.3 installed via `npm i -g` (user_agent not populated as expected); every subsequent `pnpm install` fails with `[ELIFECYCLE] Command failed with exit code 1`. Workaround on the Hetzner server only (NOT committed): `node -e "const p=require('./package.json');delete p.scripts.preinstall;require('fs').writeFileSync('package.json',JSON.stringify(p,null,2)+'\n')"`. Does NOT affect CI (GitHub Actions uses corepack-managed pnpm which sets user_agent correctly).

### Older releases (one-line guard-rails only — full text in `artifacts/imported-app/CHANGELOG.md`)

-   **v0.7.255 — Deepgram keyterm overflow broke AI Detection**: Nova-3 streaming WS rejects (HTTP 400 on handshake) above ~140 `keyterm=` query params — COUNT cap, NOT byte budget. KEY_TERMS in `src/hooks/use-deepgram-streaming.ts` MUST stay ≤100 (enforced at append time via `slice(0, KEY_TERMS_LIMIT)`); canonical pre-ship grep `rg "KEY_TERMS_LIMIT|KEY_TERMS\.slice" artifacts/imported-app/src/hooks/use-deepgram-streaming.ts`. When AI Detection shows generic "Check your internet" FIRST diagnostic is NOT "is the key valid" — open DevTools for the WS error code and probe Deepgram with Node `ws` to see JSON body. Probe order: REST `/v1/projects` (auth) → REST `/v1/listen` (model access) → Node `ws` of EXACT streaming URL incl. all keyterms (surfaces real HTTP code+body).
-   **v0.7.254 — Hotfix Deepgram credential rebake after v0.7.251–253 key leak**: pure rotation release. Ship-script `EXCLUDE_FILES` MUST cover BOTH `src/lib/baked-credentials.ts` AND `src/lib/keys.baked.ts` (walker does NOT honour `.gitignore`). When a baked credential leaks, only safe order is (1) rotate key in provider dashboard, (2) update GH Actions secret, (3) bump + ship — Deepgram/OpenAI auto-revoke leaked keys within minutes; whitelisting the gitleaks finding is NEVER acceptable. Win11 Task Manager caches old `FileDescription` after binary overwrite until `explorer.exe` restart — ask operator about that BEFORE re-investigating rcedit on "Electron (N)"-after-install screenshots.
-   **v0.7.253 — Five-fix batch: Task Manager AUMID lockstep + full canonical Bible verse on slides + loop default OFF + AI detection speedup + African English keyterm bias**: SHIPPED A LIVE DEEPGRAM KEY via `src/lib/keys.baked.ts` because ship-script `EXCLUDE_FILES` lacked that path — see v0.7.254 entry for the rotation playbook and the EXCLUDE_FILES guard-rail. Icon 2-place lockstep: `electron-builder.yml` extraResources + `electron/main.ts` BrowserWindow icon (`process.resourcesPath`). Slide canonical-text: `sendDetected()` MUST resolve `v.reference` via `lookupVerse()/lookupRange()` before `splitForSlides()` — reverting re-introduces 4-word-paraphrase-on-Live bug for semantic detections. mediaLoop 4-site lockstep: payload + 2 slide-renderer surfaces + congregation route inline attr + reconcile-block mirror; LT background videos stay intentional always-loop. AI throttle floors: phrase-index ≥ 80 ms, SEMANTIC_THROTTLE_MS ≥ 400 ms. Deepgram keyterm budget: WS query ~8 KB practical limit; current ~75 phrases well under.
-   **v0.7.252 — NDI capture resolution defaults to 720p**: `ndiCaptureResolution` default flipped from `'1080p'` → `'720p'` to match the long-tail mid-range/older sanctuary PC install base (software NDI decode at 1080p saturates a single core on Ivy Bridge mobile / pre-2015 / integrated graphics; vMix/Wirecast/OBS upscale 720p → 1080p in scene composers for free). **GUARD-RAIL (default + fallback lockstep)**: `src/lib/store.ts` `ndiCaptureResolution: '720p'` (DEFAULT_SETTINGS) AND `src/components/views/ndi-output-panel.tsx` `?? '720p'` hook fallback MUST stay in sync — drifting them re-introduces "fresh install gets X, undefined-field upgrade gets Y" inconsistency. Operators who explicitly chose `'1080p'` keep it via Zustand persist (default only seeds fresh installs + pre-v0.7.194-hotfix.3 upgrades where field was never set). Canonical pre-ship grep: `rg "ndiCaptureResolution.*\?\?|ndiCaptureResolution: '" artifacts/imported-app/src/lib/store.ts artifacts/imported-app/src/components/views/ndi-output-panel.tsx`.
-   **v0.7.249 — Professional OBS-grade Windows installer flow (Welcome → License → Install Location → Installing → Completed)**: linear 5-page OBS contract is a CONTRACT — page injection / MessageBox addition / run-checkbox default flip / sidebar+header bitmap swap MUST preserve it. Mid-flow modals BREAK the contract; if operator confirmation is genuinely needed, add to Welcome or Finish page copy, NOT a `MessageBox`. NSIS MUI2 sidebar/header bitmaps MUST be 24-bit uncompressed BMP3 (verify `file foo.bmp` → "PC bitmap, Windows 3.x format, NNN x NNN x 24"); PNGs / BMP4/v5 / palette / ImageMagick's default BMP all surface as black rectangles on Win7/8 and corrupt alpha on Win10/11. Canonical regen: `magick … -type TrueColor -define bmp:format=bmp3 -depth 8 BMP3:foo.bmp`. Run-checkbox default tracks launch-affordance count: 1 affordance ⇒ checked; 2 ⇒ first checked second NOTCHECKED (single-instance flicker was v0.7.143 root cause).
-   **v0.7.248 — Bible Translations DB fallback + Slide Transition default Cut + Settings card re-order**: route handler graceful-degradation try/catch returns CATALOGUE with `download:null` on DB fail (audit `rg "await db\.\w+\.find" src/app/api/**/route.ts`). Slide Transition default flip MUST hit all 6 fallback spots in lockstep (store default + UI fallback `a || 'default'` + payload composer + broadcaster + server renderer; canonical grep `rg "slideTransitionStyle.*\|\|.*'\w+'"`). Settings card re-order is CUT+PASTE, never CSS `order:` (breaks tab-order) or state-driven render order (defeats reconciliation).
-   **v0.7.247 — 7-fix batch shipped via atomic git-data-API**: (A) NDI Full-Screen TRANSPARENT actually strips bg — `congregation/route.ts` L1688 + L1775 gate `safeBg`/`safeBgE` with `if (FS_BG_TRANSPARENT && !isLT)`. (B) Task Manager process name — `package.json` gained `description` + `author` so Windows StringFileInfo shows branded name. (C) Re-mention reference hijack — voice "John 3:16" twice no-ops the second invocation. (D) OPENAI_API_KEY rotation via GH Actions secret; baked-credentials.ts gitignored + regenerated by release-desktop.yml. (E) Transcription buffer 4096→2048 in `use-deepgram-streaming.ts` halves per-chunk latency. (F+G) blank/dup-audio second screen — last `/api/output` POST sites folded into `buildOutputPayload()`. **SHIP PIPELINE GUARD-RAIL (atomic git-data-API replaces serial PUT loop)**: serial concurrent PUTs race on branch HEAD SHA; ship script uses git data API — `POST /git/blobs` per file, ONE `POST /git/trees` with `base_tree: mainSha`, ONE `POST /git/commits`, ONE `PATCH /git/refs/heads/<branch>`. Atomic at ref-update step. Canonical script `/tmp/ship-v0.7.247.mjs`. **SHIP PIPELINE GUARD-RAIL (extended EXCLUDE_DIRS)**: bulk-drift walker MUST exclude `dist-electron`, `dist-electron-ui`, `exports`, `upload`, `uploads`, `download` in addition to `node_modules`, `.next`, `dist`, `release`, `db`. **PROCESS GR (dup audio on second screen / NDI receiver)**: ALWAYS check `liveMonitorAudio` (default false per v0.7.244 + persisted) FIRST — operators with pre-v0.7.244 persisted state still have it true.
### Archived releases (v0.7.231 and older)

Pre-v0.7.232 one-line guard-rails live in `artifacts/imported-app/ARCHIVE.md`. Full release text remains in `artifacts/imported-app/CHANGELOG.md`. When rolling the window forward (demoting a release from "Recent (full detail)" to one-liner), keep only v0.7.232+ entries in this file and append older one-liners to `ARCHIVE.md`.
