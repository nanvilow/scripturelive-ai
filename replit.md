# Imported App

A Next.js application providing scripture-related services for web and desktop, enhancing live service experiences, and streamlining administrative tasks.

## User preferences

-   **Autonomy**: Act without asking for consent on routine decisions — always do what's better for the app. Do not ask permission for housekeeping (file trims, refactors, retry logic, dependency bumps, etc.). Only ask when a decision is genuinely destructive or shapes product direction.

## Run & Operate

-   **Install dependencies**: `pnpm install`
-   **Run dev server**: `pnpm dev`
-   **Build app**: `pnpm build`
-   **Typecheck**: `pnpm typecheck`
-   **Codegen**: `pnpm codegen` (for API client)
-   **DB Push**: `pnpm db:push` (for Drizzle schema migrations)
-   **Required Env Vars**: `MAIL_HOST`, `MAIL_USER`, `MAIL_PASS`, `MAIL_FROM`, `SMS_API_KEY`, `SMS_SENDER`

## Stack

-   **Monorepo**: pnpm workspaces
-   **Runtime**: Node.js 24
-   **Language**: TypeScript 5.9
-   **Frontend**: Next.js 16
-   **Backend**: Express 5
-   **Database**: PostgreSQL, SQLite, Replit DB
-   **ORM**: Drizzle ORM, Prisma
-   **Validation**: Zod
-   **Build Tool**: esbuild
-   **API Codegen**: Orval
-   **Speech Recognition**: OpenAI `gpt-4o-mini-transcribe`, Deepgram Nova-3
-   **AI SDK**: OpenAI SDK
-   **NDI Integration**: `koffi` FFI binding to bundled `Processing.NDI.Lib.x64.dll` (NDI 6 runtime ships inside installer as of v0.7.146 — no separate NDI Tools install required)

## Where things live

-   `apps/nextjs-app`: Next.js frontend and Electron wrapper.
-   `apps/api-server`: Express API backend.
-   `packages/`: Shared utilities and components.
-   `artifacts/imported-app/db/custom.db`: SQLite database for the desktop app.
-   `artifacts/imported-app/build-resources/ndi/Processing.NDI.Lib.x64.dll`: bundled NDI runtime (v0.7.146).
-   `public/downloads/manifest.json`: Manifest for dynamic downloads.
-   `openapi.yaml`: API contracts (source of truth for Orval codegen).
-   `drizzle/schema.ts`: Drizzle ORM database schema.
-   `styles/theme/`: Theme-related files.

## Architecture decisions

> Detailed historical entries live in `artifacts/imported-app/CHANGELOG.md`. The list below keeps only the **last three releases in full detail** plus tight one-line guard-rails for older patterns we still want future agents to respect.

### Recent (full detail)

-   **v0.7.216 — Single-clicking a DIFFERENT media tile while LIVE plays a media-video MUST NOT stall the live video (operator $1600-customer escalation, follow-up to v0.7.210/v0.7.212: "At Media column when users single click on another media, the one that's playing on the live display stops playing.")**: Per the standing v0.7.210 requirement, single-click in Media is preview-only and MUST NEVER interfere with live playback. v0.7.210 fixed the store-mutation path (`sendMediaToPreview` uses `pinPreviewSlide`, not `setSlides`); v0.7.212 fixed the iframe-transport path (`output-preview.tsx` sendNow fingerprint dedup). v0.7.216 fixes the THIRD path — **React-side hardware video decoder competition** in the in-app Live Display. **Root cause / pre-state**: `logos-shell.tsx` LiveDisplayCard (L1841-1857) renders media-video on Live by mounting `<MediaVideoSurface surface="live" src=...>` DIRECTLY (NOT through the mirrorLive iframe — operator's transport bar needs the local HTMLMediaElement to scrub/pause). PreviewCard (L1116-1170) uses the same MediaVideoSurface for `previewIsVideoMedia` slides with `autoPlay={surface === 'preview'}` at L421. When LIVE was playing video A and operator single-clicked a DIFFERENT video B in Media, `sendMediaToPreview(B) → pinPreviewSlide(B)` correctly mutated only `pinnedPreviewSlide` (no `setSlides` / no live mutation). But PreviewCard's re-render mounted a SECOND `<video>` for B with `autoPlay=true` — adding a 2nd hardware video decoder slot. GPUs operators run cap at 2-4 concurrent HW decoders → new decoder competes with live decoder → live `<video>` stalls/pauses mid-playback. v0.7.193-hotfix.2 handled only the SAME-media case (PreviewCard short-circuits to ON-AIR placard at L1132-1147, no 2nd `<video>` mounts); the DIFFERENT-media case was never addressed. Symptoms surfaced now because operator workflows increasingly chain clips — staging next clip while current plays. **Fix #1 (`src/components/layout/logos-shell.tsx` L421-434, MediaVideoSurface `<video>` autoPlay attr, +12 comment-lines / 1 expr change)**: gate preview-only autoplay on `!mediaPaused`. Change `autoPlay={surface === 'preview'}` → `autoPlay={surface === 'preview' && !mediaPaused}`. The `mediaPaused` prop is already wired from store (`previewMediaPaused` for preview surface, `mediaPaused` slide field for live). When `mediaPaused===true` at mount, HTML `autoplay` attr is absent → browser never calls `play()` → no HW decoder slot is requested → no competition. Operator still starts playback explicitly via VideoTransport Play (sets `previewMediaPaused=false`, the existing play/pause `useEffect` at L379-381 then calls `v.play()` — operator's explicit intent). **Fix #2 (`src/components/layout/library-compact.tsx` L1290-1323, `sendMediaToPreview`, ~17 lines net)**: detect LIVE-plays-DIFFERENT-media-video. Read `liveSlide` via v0.7.208 direct-ref-first pattern (`st.liveSlide ?? (st.liveSlideIndex >= 0 ? st.slides[st.liveSlideIndex] : null)`); evaluate `liveIsPlayingDifferentMediaVideo = liveSlide.type==='media' && liveSlide.mediaKind==='video' && liveSlide.mediaUrl && m.kind==='video' && slide.mediaUrl && liveSlide.mediaUrl !== slide.mediaUrl`. When true, **before** `pinPreviewSlide(slide)`, flip `setPreviewMediaPaused(true)` and `setPreviewMediaCurrentTime(0)`. Guarantees new MediaVideoSurface mounts with `mediaPaused=true` from first render → Fix #1's gated `autoPlay` returns false → no autoplay → no 2nd decoder. Three properties hold simultaneously: (a) preview IS pinned (operator sees new clip's first frame, can scrub via transport); (b) live state is COMPLETELY untouched (`liveSlide`, `liveSlideIndex`, `slides[]`, `isLive` all preserved; broadcaster's `buildOutputPayload` returns identical bytes → JSON.stringify dedup short-circuits any SSE re-post → secondary screen / NDI projector continue uninterrupted); (c) GPU HW decoder count stays at 1 instead of climbing to 2. Same-media case unchanged — short-circuits to v0.7.193-hotfix.2 ON-AIR placard, no 2nd `<video>` mounts. **No change needed at broadcaster / SSE / iframe layer**: `buildOutputPayload` never reads `pinnedPreviewSlide` (returns `liveSlide ?? slides[liveSlideIndex]`); identical payload + OutputBroadcaster's stringify-equality early-return → no `/api/output` POST → secondary screen unaffected. v0.7.212's `sendNow` fingerprint dedup already covers the in-app mirrorLive iframe path for non-media-video live slides. **Also widened (`src/lib/theme-bg-visibility.test.ts` L116-127 J GUARD)**: regex `/const sendMediaToPreview[^}]+pinPreviewSlide\(slide\)/s` → `/const sendMediaToPreview[\s\S]*?pinPreviewSlide\(slide\)/` because v0.7.216 body now contains `if {...}` block before the pin call; primitive call still asserted; symmetric widening for `sendMediaToLive` / `setLiveAuto` companion guard. **New regression tests (4, extended `src/lib/media-preview-isolation.test.ts`)**: (q) BEHAVIOURAL — stage LIVE playing video A via legacy GO LIVE path (`slides=[liveVid]`, `liveSlideIndex=0`, `isLive=true`, `previewMediaPaused=false`, `previewMediaCurrentTime=42.5`); simulate EXACT v0.7.216 sendMediaToPreview mutation sequence for DIFFERENT video B (`setPreviewMediaPaused(true) + setPreviewMediaCurrentTime(0) + pinPreviewSlide(B)`); assert `pinnedPreviewSlide.id==='vid-new'` AND `previewMediaPaused===true` AND `previewMediaCurrentTime===0` (preview pinned + paused → autoPlay returns false → no 2nd decoder); assert `slides[0].id==='vid-live'`, `liveSlideIndex===0`, `liveSlide===null`, `isLive===true` (live untouched); assert `buildOutputPayload(state).slide.id==='vid-live'` (broadcaster payload identical → secondary screen unaffected). (r) BEHAVIOURAL — same-media case: stage LIVE playing video A, simulate sendMediaToPreview with same-mediaUrl preview clone; assert `liveIsPlayingDifferentMediaVideo===false`, pause-before-pin branch SKIPPED, `previewMediaPaused` stays `false` (operator's transport state preserved, v0.7.193-hotfix.2 placard handles). (s) GUARD — `logos-shell.tsx` MediaVideoSurface `<video>` `autoPlay` attribute MUST match BOTH `!mediaPaused` AND `surface === 'preview'`. (t) GUARD — `library-compact.tsx` sendMediaToPreview body MUST contain `liveIsPlayingDifferentMediaVideo` detection, `mediaUrl !== slide.mediaUrl` comparison, `setPreviewMediaPaused(true)`, `setPreviewMediaCurrentTime(0)`, AND still call `pinPreviewSlide(slide)`; MUST NOT call `setSlides(` / `setLiveSlideIndex(` / `setIsLive(` / `setLiveAuto(` (re-asserts v0.7.210 independence invariants). **Test results**: 20/20 PASS in `media-preview-isolation.test.ts` (was 16/16 on v0.7.215 — +4). Full suite **654/654 PASS** (was 650/650 on v0.7.215 — +4). 0 regressions. **Replit runtime proof**: test (q) exercises the EXACT 3-call store mutation sequence production sendMediaToPreview performs; the `buildOutputPayload` assertion mirrors the broadcaster's read path through v0.7.203 output-payload contract — proves broadcaster sees identical payload → SSE/iframe stack stays silent → secondary screen unaffected. Source-grep guards (s)+(t) prove fixes live at call sites (logos-shell.tsx L421-434 + library-compact.tsx L1290-1323). Same hybrid proof shape as v0.7.210/v0.7.212/v0.7.213/v0.7.214/v0.7.215. **GUARD-RAIL (A) — MediaVideoSurface `<video>` `autoPlay` attribute MUST be gated on `!mediaPaused` in addition to `surface === 'preview'`**: removing `!mediaPaused` clause re-introduces unconditional preview autoplay → 2nd HW decoder spins up instant a new preview clip mounts → competes with live decoder → live `<video>` stalls. Browser's `autoplay` attribute is processed at element load (synchronously with mount); React's `useEffect` play/pause logic runs AFTER. Without attribute gate, useEffect races browser's autoplay — decoder may still be allocated before effect's `v.pause()` lands. Test (s) source-grep enforces. **GUARD-RAIL (B) — sendMediaToPreview MUST flip `previewMediaPaused=true` + reset clock BEFORE `pinPreviewSlide(slide)` when `liveIsPlayingDifferentMediaVideo===true`**: ordering matters. If pin runs first, React scheduler may commit a frame where `pinnedPreviewSlide` is new clip but `previewMediaPaused` is still old → MediaVideoSurface mounts with `autoPlay=true` for one frame → decoder requested. Pause-then-pin order ensures by mount-read time the flag is already `true`. Test (q) verifies post-state; ordering enforced by source code — future refactors must preserve order. **GUARD-RAIL (C) — sendMediaToPreview MUST NOT touch `slides[]` / `liveSlideIndex` / `isLive` / call `setLiveAuto` under ANY condition (v0.7.210 independence)**: v0.7.216 detection branch ONLY mutates preview-scoped fields (`previewMediaPaused`, `previewMediaCurrentTime`, `pinnedPreviewSlide`). Any future addition that mutates live state under "pause-before-pin" pretext re-opens v0.7.209-era bug class. Test (t) `not.toMatch(...)` enforces ABSENCE of all 4 live-side primitives. **GUARD-RAIL (D) — Same-media case (`mediaUrl === liveSlide.mediaUrl`) MUST NOT enter pause-before-pin branch**: same-media short-circuits at PreviewCard ON-AIR placard (v0.7.193-hotfix.2) — no 2nd `<video>` mounts there, so no decoder competition exists to prevent. Force-pausing operator's existing preview transport state in that case is a surprise regression (operator could be mid-scrub on the same clip for an A/B preview). Test (r) explicitly verifies `previewMediaPaused` stays `false` after same-media re-pin. **GUARD-RAIL (E) — `liveSlide` anchor read inside sendMediaToPreview MUST use v0.7.208 direct-ref-first pattern (`st.liveSlide ?? (st.liveSlideIndex >= 0 ? st.slides[st.liveSlideIndex] : null)`)**: bare `slides[liveSlideIndex]` returns `null` when AI-detected video is live (AI path leaves `liveSlideIndex=-1` and writes directly to `liveSlide` per v0.7.203/v0.7.208), `liveIsPlayingDifferentMediaVideo` evaluates `false` → pause-before-pin branch skips → decoder competition bug re-surfaces for AI-driven live scenarios. Source code at L1308 documents; future audit: any new reader of "currently-live slide" MUST use this pattern. **PROCESS GR — Same operator-visible bug ("clicking media B stops live video A") with multiple INDEPENDENT failure paths (store mutation v0.7.210, iframe transport dedup v0.7.212, React decoder competition v0.7.216) → fix sequence is CORRECT, not duplicative**: each release closed one layer. A "comprehensive single-shot fix" would have required understanding all 3 paths simultaneously, not knowable until each surfaced under operator load. Forward audit pattern when this bug class re-surfaces: enumerate every consumer of shadowed state and classify each as (1) store mutation, (2) network/SSE transport, (3) DOM/React rendering, (4) browser-resource (decoder, AudioContext) competition. Each class needs an independent test guard. **PROCESS GR — When an HTML media attribute (`autoplay`, `muted`, `loop`, `preload`) controls a finite hardware resource, gating it conditionally is preferable to gating only the JS-side `play()` call**: browser's attribute processing runs at element creation, BEFORE React's `useEffect` mount commits. Even a perfectly-correct `useEffect` that calls `v.pause()` immediately may race the browser's attribute-driven allocation. Always gate at attribute layer when attribute exists; reserve `useEffect` for stateful transitions (play → pause → play) after element is mounted.

-   **v0.7.215 — Per-pane Clear buttons on Preview and Live Display (independent, single-click; operator UX request: "add a clear button on both preview and live display that users can clear anything on there with a single click. Both shouldn't interfere with each other.")**: Screenshot circled the empty footer strip at the bottom of each pane in `logos-shell.tsx`. **Root cause / pre-state**: `clearLive` callback at L4044 was only reachable from the GO LIVE / STOP LIVE toggle (when isLive=true) and Escape/Enter-while-live keyboard paths — no UI affordance to wipe Live when the toggle was idle (e.g. AI direct-ref slide held purely in `liveSlide` with `isLive=false`). Preview had ZERO Clear primitive — `clearPinnedPreview()` existed in store L1191 but no button surfaced it, so pins from v0.7.201 `pinPreviewSlide` sat until next pin / setSlides reset. Additionally `clearLive` was incomplete: called `setLiveSlideIndex(-1) + setIsLive(false) + sendToOutput(null,false)` but NOT `clearLiveAuto()` — stale v0.7.203 `liveSlide` direct ref survived every Clear / Stop Live / Escape / Enter-while-live, and `buildOutputPayload`'s `liveSlide ?? slides[liveSlideIndex]` re-rendered the AI slide on the next broadcaster tick. Latent since v0.7.203 (Jul 2025), surfaced now because the operator-facing label "Clear" mandates the pane actually go empty. **Fix #1 (`src/components/layout/logos-shell.tsx` L4044-4058, +6 lines)**: prepend `useAppStore.getState().clearLiveAuto()` to `clearLive`. Now `clearLive = clearLiveAuto() + setLiveSlideIndex(-1) + setIsLive(false) + sendToOutput(null,false)`. **Fix #2 (logos-shell.tsx L1180-1205, ~25 lines net)**: PreviewCard symmetry strip (previously empty `<div className="h-7" />` spacer reserved purely for matching LiveDisplayCard's bottom transport row height since v0.7.157) now hosts an explicit Clear Preview `<Button>` with `CircleSlash` icon. `onClick` calls EXACTLY 2 primitives: `useAppStore.getState().clearPinnedPreview()` + `useAppStore.getState().setPreviewSlideIndex(-1)`. MUST NOT touch `slides[]` (deck survives), `liveSlide` / `liveSlideIndex` (live keeps playing), or `isLive` (broadcast keeps going). `disabled={!previewSlide}` so operator sees at a glance what's clearable. PreviewCard already subscribes to store — no prop drilling. **Fix #3 (logos-shell.tsx L1942-1968, ~18 lines)**: LiveDisplayCard bottom toolbar (Prev | Go Live/Stop Live | Next) gains a 4th button — Clear Live — inserted LEFT of Prev. `onClick={onClearLive}` reuses the existing prop (parent shell wires it to v0.7.215-updated `clearLive` at L4044). Always visible (unlike Stop Live which only appears when isLive=true), `disabled={!liveSlide && !isLive}` so operator can wipe stale direct-ref slide even when toggle is idle. Same `CircleSlash` icon as Clear Preview for visual symmetry. **No change needed in `easyworship-shell.tsx`**: screenshot is logos-shell; EasyWorship shell uses different chassis with Stop Live + Clear already at bottom toolbar. **New regression tests (5, extended `src/lib/media-preview-isolation.test.ts`)**: (l) BEHAVIOURAL — Clear Preview wipes preview but live keeps playing: pin preview verse + setLiveAuto+isLive=true on live verse → call `clearPinnedPreview() + setPreviewSlideIndex(-1)` → assert preview read null, `liveSlide.id`, `liveSlideIndex`, `isLive`, `buildOutputPayload.slide.id`, `slides[0].id` all UNTOUCHED. (m) BEHAVIOURAL — Clear Live wipes live but preview keeps its pin: symmetric setup → call `clearLiveAuto() + setLiveSlideIndex(-1) + setIsLive(false)` → assert `liveSlide=null` (Fix #1 wiped direct ref), `liveSlideIndex=-1`, `isLive=false`, `pinnedPreviewSlide.id`, slides[] all PRESERVED. (n) GUARD — `clearLive` source MUST call `clearLiveAuto()` + setLiveSlideIndex(-1) + setIsLive(false) + sendToOutput(null,false). (o) GUARD — PreviewCard symmetry strip Clear Preview button source MUST call `clearPinnedPreview()` + `setPreviewSlideIndex(-1)` AND MUST NOT call setIsLive / setLiveSlideIndex / clearLiveAuto / setSlides (independence invariant). (p) GUARD — LiveDisplayCard Clear button source MUST `onClick={onClearLive}`, MUST NOT mention onSendLive (separate from GO LIVE toggle), MUST NOT touch clearPinnedPreview / setPreviewSlideIndex inline. **Test results**: 16/16 PASS in `media-preview-isolation.test.ts` (was 11/11 on v0.7.213 — +5). Full suite **650/650 PASS** (was 645/645 on v0.7.214 — +5). 0 regressions. **Replit runtime proof**: tests (l)+(m) exercise EXACT store mutation sequences the new buttons trigger (verified by source-grep guards (o)+(p) that those primitives ARE wired to the buttons' onClick); `buildOutputPayload` assertion in (l) mirrors broadcaster's read path through v0.7.203 output-payload contract. Source-grep guards (n)+(o)+(p) prove fixes live at call sites. Same hybrid proof shape as v0.7.208/v0.7.210/v0.7.212/v0.7.213/v0.7.214 (architect-accepted). **GUARD-RAIL (A) — `clearLive` MUST call `clearLiveAuto()` BEFORE the legacy 3-primitive sequence**: omitting it leaves v0.7.203 `liveSlide` direct ref alive, broadcaster's `liveSlide ?? slides[liveSlideIndex]` re-renders the stale AI verse on next tick — operator presses Clear and the projector keeps showing the slide. Test (n) source-grep enforces. **GUARD-RAIL (B) — Clear Preview button MUST call ONLY `clearPinnedPreview() + setPreviewSlideIndex(-1)`, NOTHING ELSE**: any extra mutation breaks independence. `setSlides([])` would destroy operator's deck (Clear is not "remove all" — bulk-delete is the Scripture Feed's removeAll* affordances). Touching live primitives cascades into live — exact "interference" operator forbade. Test (o) `.not.toMatch(...)` enforces ABSENCE of setIsLive / setLiveSlideIndex / clearLiveAuto / setSlides. **GUARD-RAIL (C) — Clear Live button MUST call ONLY `onClearLive` (the prop), inline button source MUST NOT touch preview-side primitives**: the 4-primitive sequence inside the callback MUST stay symmetric — removing any of the 4 (clearLiveAuto / setLiveSlideIndex / setIsLive / sendToOutput) produces operator-visible drift between React tree, store, and projector. Tests (n)+(p) source-grep enforces. **GUARD-RAIL (D) — Clear Live MUST be visually distinct from Stop Live**: Clear Live uses CircleSlash icon + "CLEAR" label + neutral border; Stop Live uses Square icon + "STOP LIVE" label + rose-bordered muted styling (conditional inverse of rose-filled GO LIVE). Adjacent placement in toolbar avoids conflation; conflating them re-introduces v0.7.214-era "press GO LIVE, toggle becomes Stop Live, press it expecting Clear but only stops broadcast while stale slide stays visible" confusion. **PROCESS GR — When the operator-visible label of an affordance is "Clear", the underlying primitive MUST wipe EVERY layer of the state model that contributes to the rendered output**: for Live = direct ref (v0.7.203 `liveSlide`) + legacy index (`liveSlideIndex`) + broadcast flag (`isLive`) + projector push (`sendToOutput`). For Preview = direct ref (v0.7.201 `pinnedPreviewSlide`) + legacy index (`previewSlideIndex`). Any layer omitted is a latent regression waiting for the state combination where THAT layer drives the pane. Audit: `rg -n "<X>Slide|<X>SlideIndex|<X>:\s*null|set<X>" src/lib/store.ts` THEN enumerate every state field contributing to `buildOutputPayload`'s read path for surface X; Clear primitive wipes ALL of them. **PROCESS GR — A "symmetry strip" empty spacer in a card layout is technical debt that signals a missing affordance**: PreviewCard's L1180-1197 placeholder was load-bearing for layout (matching 16:9 stage size between Preview and Live since v0.7.157) but content-less. v0.7.215's Clear Preview button fills it with the EXACT affordance the layout was reserving space for. Future: any `<div className="h-N" />` aria-hidden placeholder = backlog candidate (height reservation correct, content missing).

-   **v0.7.214 — Voice commands + AI/LLM verse navigation MUST target LIVE display only (operator $1600-customer escalation, follow-up to v0.7.208/v0.7.213: "voice commands should apply to what is LIVE display not preview because AI detection auto-sends to live; user said KJV and it only applied to preview leaving the AI-detected verse on live unchanged; next verse / next chapter / jump-to-N goes for PREVIEW bible text instead of the AI-detected LIVE one")**: v0.7.208 fixed the "KJV switches preview not live" symptom at the `live-translation-sync.tsx` layer. But the 8 voice-command handlers in `speech-provider.tsx` were never audited for the same pattern. When AI auto-detect fires through v0.7.203 `setLiveAuto(slide)`, it writes to the `liveSlide` DIRECT REF and leaves `liveSlideIndex=-1`. Output payload reads `liveSlide ?? slides[liveSlideIndex]` — AI ref wins on screen. But every voice-nav handler still read `slides[liveSlideIndex]` (= `slides[-1]` = `null`) to recover the live passage anchor, and 3 of them then COMMITTED via the legacy `setSlides + setPreviewSlideIndex + setLiveSlideIndex + setIsLive` combo (clobbered operator preview + did not update the AI liveSlide ref). Net behaviour: "next verse" against an AI-detected live verse saw null, fell through to slide-deck fallback (or no-op); "next chapter" / "show verse N" reseated preview to the freshly-loaded verse but live stayed frozen on original AI detection; "find_by_quote" yanked operator pinned preview off-screen. **The 8 problem sites and their fixes (`src/components/providers/speech-provider.tsx`)**: (L609 next_verse/previous_verse anchor read) `const slide = liveIdx >= 0 ? slides[liveIdx] : null` → `const slide = s.liveSlide ?? (liveIdx >= 0 ? slides[liveIdx] : null)` mirroring `logos-shell.tsx` L3929 AI-aware read; (L899-918 find_by_quote commit) removed v0.7.194-hotfix.11 "preserve manual preview" guard which routed to `addScheduleItemQuiet` (silently sent AI-found verse to schedule instead of live) + replaced legacy combo with `setLiveAuto(slide) + setLiveActiveVerseIndex(0)`; (L941 next/previous_chapter anchor read) same liveSlide-first fix; (L989-1003 next/previous_chapter commit) same guard removal + setLiveAuto; (L1136 show_verse_n anchor read) same liveSlide-first fix; (L1204-1219 show_verse_n commit) same guard removal + setLiveAuto; (L1482 LLM classifier context) `const liveSlide = liveIdx >= 0 && liveIdx < slides.length ? slides[liveIdx] : undefined` → `const liveSlide = state.liveSlide ?? (...)` so the classifier receives live-passage context when AI is driving live; (L1110 delete_previous_verse) dropped the `setPreviewSlideIndex(newIdx)` write — voice = LIVE only invariant. **No change needed**: `change_translation` (L1078) only calls `setSelectedTranslation` and routes through the v0.7.208-fixed `live-translation-sync.tsx` watcher; `go_to_reference` (L747) was already on `setLiveAuto`; `bible_says` (L1013) intentionally writes preview only (STANDBY only — operator confirms with Enter / Go Live). **New regression tests (7, extended `src/lib/ai-live-only.test.ts`)**: (a) BEHAVIOURAL — AI auto-detect via setLiveAuto + operator pins Genesis 1:1 to preview; voice next_verse push of John 3:17 via setLiveAuto; assert `liveSlide.id`, `buildOutputPayload`, AND preview untouched. (b)-(e) GUARD source-greps on next_verse/next_chapter/show_verse_n/find_by_quote case bodies — each MUST contain `setLiveAuto`, MUST NOT contain `setPreviewSlideIndex(` / `setLiveSlideIndex(idx)` / `addScheduleItemQuiet(` call sites (strict regex on parens — excludes comment-text mentions), AND anchor reads MUST be liveSlide-first. (f) GUARD ZERO `setPreviewSlideIndex(` call sites outside bible_says case body — uses `setPreviewSlideIndex\(` regex with bibleSays-bounded slice on the dispatcher switch block. (g) GUARD on LLM classifier — anchored on the v0.7.214 comment marker, asserts `state.liveSlide ?? (liveIdx >= 0 ...)`. **Test results**: 16/16 PASS in `ai-live-only.test.ts` (was 9/9 on v0.7.208 — +7). Full suite **645/645 PASS** (was 638/638 on v0.7.213 — +7). 0 regressions. **Replit runtime proof**: test (a) exercises the EXACT store mutation post-214 next_verse performs (`setLiveAuto(stepped) + setLiveActiveVerseIndex(0)`); the `buildOutputPayload` assertion mirrors the renderer read through v0.7.203 output-payload contract. Guards (b)-(g) source-grep directly to prove fixes live at call sites (not in a wrapper). Same hybrid proof shape as v0.7.208/210/212/213. **GUARD-RAIL (A) — Every voice handler in `speech-provider.tsx` that reads the "current live passage anchor" MUST use `s.liveSlide ?? (liveIdx >= 0 ? slides[liveIdx] : null)`**: AI direct-ref path leaves liveSlideIndex=-1; bare-array read returns null and the handler degenerates to slide-deck fallback (no-op or stale step). Tests (b)+(c)+(d) source-grep enforces. **GUARD-RAIL (B) — Every voice handler that COMMITS a freshly-loaded verse to live MUST use `setLiveAuto(slideNew) + setLiveActiveVerseIndex(0)`, NOT the legacy `setSlides + setPreviewSlideIndex + setLiveSlideIndex + setIsLive` combo**: legacy combo clobbers operator preview AND fails to update AI liveSlide ref (live freezes on original detection). `setLiveAuto` is the canonical v0.7.203/v0.7.208/v0.7.210/v0.7.212 direct-ref primitive. Tests (c)+(d)+(e) source-grep enforces. **GUARD-RAIL (C) — Voice dispatcher switch body MUST contain ZERO `setPreviewSlideIndex(` CALL SITES outside `bible_says`**: bible_says is the ONLY voice command that legitimately writes preview (STANDBY only — operator confirms manually). Any other case re-opens this release bug class. Test (f) regex `/setPreviewSlideIndex\(/g` with bibleSays-bounded slice enforces (counts CALLS, not comment-text mentions). **GUARD-RAIL (D) — LLM classifier MUST receive live-passage context derived from `state.liveSlide ?? slides[liveIdx]`**: without direct-ref read, the classifier sees no liveReference when AI is driving live, so "next verse" / "show verse 17" against AI detection fall back to undefined context. Test (g) anchored on v0.7.214 comment marker enforces. **GUARD-RAIL (E) — Voice handlers MUST NOT call `addScheduleItemQuiet` as a "preserve manual preview" fallback**: the v0.7.194-hotfix.11 pattern silently routes operator-requested verses to schedule instead of live — exact OPPOSITE of what voice asked for. With `setLiveAuto` as commit primitive, the preserve-manual-preview guard is unnecessary because setLiveAuto is inherently non-destructive to preview state. Tests (c)+(d)+(e) `not.toMatch(/addScheduleItemQuiet\(/)` enforces. **PROCESS GR — When a "fix N" introduces a new direct-ref primitive (`setLiveAuto`, `pinPreviewSlide`), the SAME-RELEASE audit MUST enumerate every reader/writer of the SHADOWED state, not just the headline call site**: v0.7.208 fixed live-translation-sync.tsx but did not audit speech-provider.tsx 8 sites that read the same shadowed state. v0.7.210 introduced pinPreviewSlide and audited symmetric writers in setSlides (v0.7.213) only after operator escalation. Right release-time question: `rg -n "<shadowed_field>" src/` THEN classify each match (reader / writer / consumer) and verify the new primitive is consulted. **PROCESS GR — Voice command tooltip phrasing MUST match the primitive call**: voice handlers whose intent says "apply to live" (next_verse, next_chapter, show_verse_n, find_by_quote, go_to_reference, change_translation, delete_previous_verse) MUST use `setLiveAuto` / `setLive*` primitives. The ONE case whose intent says "load into standby" (bible_says) MUST use `setPreviewSlideIndex`. Any future voice command added to the dispatcher needs to declare its target at the top of the case body and the primitive call enforced by an `ai-live-only.test.ts` GUARD entry.


-   **v0.7.213 — `setSlides` MUST clear `pinnedPreviewSlide` so detected/suggested verses on Preview win goLive (one-line GR)**: `store.ts` setSlides reducer's set() payload MUST include BOTH `liveSlide: null` (v0.7.203 invariant) AND `pinnedPreviewSlide: null` (v0.7.213 invariant). Removing either re-opens a regression class: without `liveSlide: null`, AI direct ref survives a new deck import and stale-renders on live; without `pinnedPreviewSlide: null`, a stale Media-tile pin survives the verse-stage path and v0.7.212's `goLive` pin-first branch promotes the wrong thing. Pattern: any "hard-reset" reducer that takes ownership of Preview MUST clear pin + AI ref. Audit: `rg -n "setSlides:\s*\(s\)\s*=>\s*set\(" src/lib/store.ts` MUST show BOTH fields. Test (j) in `media-preview-isolation.test.ts` source-grep enforces both. Full text in `artifacts/imported-app/CHANGELOG.md`. **New regression tests (3, extended `src/lib/media-preview-isolation.test.ts`)**: (i) load-bearing — pin a stale video, then `setSlides([verse])`, assert `pinnedPreviewSlide === null` post-call, mirror goLive's pin-null fallback to `setLiveSlideIndex(previewSlideIndex) + setIsLive(true)`, assert `buildOutputPayload(state).slide.id === 'rom-8-29'` (verse promoted, not the stale pin). (j) GUARD source-grep on store.ts: `setSlides` reducer's set() payload regex `setSlides:\s*\(s\)\s*=>\s*set\(\{([^}]*)\}\)` MUST match BOTH `pinnedPreviewSlide:\s*null` AND `liveSlide:\s*null` (preserves v0.7.203 invariant). (k) symmetric flow — verse on Preview via setSlides, single-click video → `pinPreviewSlide(vid)` → assert `(pinnedPreviewSlide ?? slides[previewSlideIndex])?.id === 'vid-storm'` (Preview swaps instantly); slides[0] verse survives in the deck (just shadowed by the pin); live untouched (`isLive=false`, `liveSlide=null`). **Test results**: 11/11 PASS in the extended file (was 8/8 on v0.7.212 — +3). Full suite **638/638 PASS** (was 635/635 on v0.7.212 — +3). 0 regressions. **Replit runtime proof**: test (i) exercises the EXACT 3-call sequence the production paths run (`pinPreviewSlide` → `setSlides` → `setLiveSlideIndex+setIsLive`); the assertion `pinnedPreviewSlide === null` post-setSlides directly verifies the store mutation; the final `buildOutputPayload` assertion mirrors the renderer's read path. Test (j) source-grep enforces the fix lives at the store reducer, not in a wrapper that could be bypassed. Same hybrid proof shape as v0.7.210/v0.7.212. **GUARD-RAIL (A) — store.ts `setSlides` reducer's set() payload MUST include BOTH `liveSlide: null` (v0.7.203) AND `pinnedPreviewSlide: null` (v0.7.213)**: removing either field re-opens a regression class. Without `liveSlide: null`, the v0.7.208 AI direct ref survives a new deck import and stale-renders on live (v0.7.210's lead bug). Without `pinnedPreviewSlide: null`, a stale Media pin survives the verse-stage path and v0.7.212's `goLive` promotes the wrong thing (this release's lead bug). Test (j) source-grep enforces both. Audit: `rg -n "setSlides:\s*\(s\)\s*=>\s*set\(" src/lib/store.ts` MUST show BOTH fields in the payload. **GUARD-RAIL (B) — Any future "hard-reset" reducer that takes ownership of the Preview surface MUST clear `pinnedPreviewSlide` AND `liveSlide`**: the pattern is established at L1442 (`setSelectedScheduleItemId(null)`), L1463 (other hard-reset), and L1190 (`setSlides` as of v0.7.213). New reducers like `loadDeck`, `importPresentation`, `resetState` etc. MUST follow the same pattern. **PROCESS GR — When introducing a "pin-first" read pattern (`pinnedPreviewSlide ?? slides[...]`), EVERY writer to the fallback (slides[] via setSlides, setPreviewSlideIndex, etc.) MUST also clear the pin**: v0.7.201 introduced pin-first read in the Preview pane (logos-shell L967). v0.7.212 extended to `goLive`. But the symmetric "writer clears pin" rule was only applied at clearScheduleItem/clearSelection sites, NOT at `setSlides`. Future audit when introducing a new shadowing primitive: enumerate ALL writers to the shadowed field; default to "clear the shadow" unless writer semantics explicitly preserve operator intent (`setPreviewSlideIndex` correctly preserves the pin for arrow-key navigation through the same deck). **PROCESS GR — When operator escalates a follow-up symptom IMMEDIATELY after a release ships, do NOT assume the prior release was wrong; the new symptom is often the NEXT layer of the same bug class**: v0.7.212's pin-first branch in goLive was correct in isolation. The follow-up symptom was caused by a DIFFERENT site (writer didn't clear the pin) — a layer deeper. The right diagnostic question is "given my fix is correct in isolation, what UPSTREAM/DOWNSTREAM site does NOT yet know about the new invariant?" — this produced the 5-minute store.ts fix instead of an unnecessary `goLive` rewrite.

-   **v0.7.212 — Single-click on another Media tile stops the live video AND Go Live button does not promote preview-pinned video to live (one-line GR)**: `output-preview.tsx` `sendNow` MUST hold `lastSentFpRef` and compute a CHEAP identity fingerprint via `JSON.stringify({t:type,b:blanked,l:isLive,dm:displayMode,sl:{id,ty,ti,su,co,mk,mu:mediaUrl.length,mp:mediaPaused},st:settings,au:audio})` — early-return `if (fp === lastSentFpRef.current)` BEFORE `revRef.current += 1` and `postMessage` so identical mirrorLive payloads don't tear down the iframe `<video>`. Fingerprint MUST exclude raw `s.mediaUrl` (only `mediaUrl.length` proxy). v0.7.204 IS_PREVIEW handler `lastRenderKey=''` force-reset MUST stay intact at route.ts L2212. Both `goLive` functions (easyworship-shell + logos-shell) MUST check `pinnedPreviewSlide` and `setLiveAuto(pinned)` BEFORE the `!slides.length` toast guard. PROCESS GR: when a new primitive is introduced, EVERY downstream reader/writer of the shadowed state MUST be audited in the same release. A "subscribe fires on every store change" pattern is ONLY safe when receiver is idempotent for unchanged payloads — dedup at SOURCE not SINK. Full text in `artifacts/imported-app/CHANGELOG.md`.<!-- TRIMMED v0.7.215: was v0.7.212 incumbent full -->
