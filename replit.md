# Imported App

A Next.js application providing scripture-related services for web and desktop, enhancing live service experiences, and streamlining administrative tasks.

## Run & Operate

-   **Install dependencies**: `pnpm install`
-   **Run dev server**: `pnpm dev`
-   **Build app**: `pnpm build`
-   **Typecheck**: `pnpm typecheck`
-   **Codegen**: `pnpm codegen` (for API client)
-   **DB Push**: `pnpm db:push` (for Drizzle schema migrations)
-   **Required Env Vars**: `MAIL_HOST`, `MAIL_USER`, `MAIL_PASS`, `MAIL_FROM`, `SMS_API_KEY`, `SMS_SENDER`

## Stack

-   **Monorepo**: pnpm workspaces
-   **Runtime**: Node.js 24
-   **Language**: TypeScript 5.9
-   **Frontend**: Next.js 16
-   **Backend**: Express 5
-   **Database**: PostgreSQL, SQLite, Replit DB
-   **ORM**: Drizzle ORM, Prisma
-   **Validation**: Zod
-   **Build Tool**: esbuild
-   **API Codegen**: Orval
-   **Speech Recognition**: OpenAI `gpt-4o-mini-transcribe`, Deepgram Nova-3
-   **AI SDK**: OpenAI SDK
-   **NDI Integration**: `koffi` FFI binding to bundled `Processing.NDI.Lib.x64.dll` (NDI 6 runtime ships inside installer as of v0.7.146 — no separate NDI Tools install required)

## Where things live

-   `apps/nextjs-app`: Next.js frontend and Electron wrapper.
-   `apps/api-server`: Express API backend.
-   `packages/`: Shared utilities and components.
-   `artifacts/imported-app/db/custom.db`: SQLite database for the desktop app.
-   `artifacts/imported-app/build-resources/ndi/Processing.NDI.Lib.x64.dll`: bundled NDI runtime (v0.7.146).
-   `public/downloads/manifest.json`: Manifest for dynamic downloads.
-   `openapi.yaml`: API contracts (source of truth for Orval codegen).
-   `drizzle/schema.ts`: Drizzle ORM database schema.
-   `styles/theme/`: Theme-related files.

## Architecture decisions

> Detailed historical entries live in `artifacts/imported-app/CHANGELOG.md`. The list below keeps only the **last three releases in full detail** plus tight one-line guard-rails for older patterns we still want future agents to respect.

### Recent (full detail)

-   **v0.7.199 — Top-to-down audit hotfix bundle (A+B+C+D+E+F): Unicode NFC normaliser + RTL plumbing + per-locale filler dictionary + SSE heartbeat prune + NDI IPC defensive guards + React ErrorBoundary**: Operator asked "check through whole app top-to-down, fix bugs, make great for global usage" on v0.7.198. Diagnose pass (typecheck clean + GR audit + explore subagent + manual file inspection) surfaced ~15 candidates; verified two of the explore subagent's "critical" claims were false alarms (#1 NDI ticker starvation — `sendBusy` guard already at `electron/ndi-service.ts` L491; #3 protocol-handler path traversal — guard already at `electron/main.ts` L2783-2785 per v0.7.194-hotfix.12 GR). Presented operator a choice menu; operator chose "BOTH bundles" (global-usage + defensive hardening = 6 items). **Honesty caveat**: items A/B/C are PLUMBING — no CJK/Hebrew/Arabic Bible JSON is currently bundled in `src/data/bibles/` (only kjv/niv/esv/nlt/nkjv/amp/twiasante/ewe — all Latin-script LTR), so A/B/C are infrastructure changes that future-proof the renderer + detection pipeline for the day a non-Latin Bible is added. **Changes (7 files, 171 +ins / 20 -dels)**: (1) **A. Unicode NFC normaliser** — `src/lib/bibles/reference-engine.ts` L95-104 `wordsToNumbers` now prepends `text.normalize('NFC')` before tokenize. Twi (`twiasante`) and Ewe use diacritics (`ɛ`, `ɔ`, `ŋ`, combining tone marks) that speech engines and clipboard paste may emit in NFD form; the bundled book-mapping alias table loads as NFC at bundle time, so without normalisation the same logical word compared unequal at the byte level → alias lookup missed → fuzzy fallback → wrong/no detection. NFC is a no-op on ASCII (zero-cost for English speech path). Architect explicitly validated #6 — safe for English. (2) **B. RTL plumbing** — new file `src/lib/bibles/translation-direction.ts` exports `getTranslationDirection(translation): 'ltr'|'rtl'` + `getDirAttribute(translation): 'rtl'|undefined`. Uses an `RTL_TRANSLATION_IDS` Set + `RTL_TRANSLATION_PREFIXES` array (heb/arabic/aramaic/syriac/farsi/persian/urdu). Every currently-bundled translation returns `'ltr'` → `getDirAttribute` returns `undefined` → React skips emitting the attr → rendered DOM is byte-identical to pre-v0.7.199. Applied to `src/components/presenter/slide-renderer.tsx` L381 (verse container reads `useAppStore.getState().selectedTranslation` — first attempt used `.translation` which doesn't exist on `AppState`, typecheck caught it). NOT applied to `src/app/api/output/congregation/route.ts` in this hotfix — that path renders inside the outer template literal where v0.7.196 GR-D (no extra backticks in comments) and v0.7.198 GR-C (cache-key shape parity across L1199/L1490/L1562) make every render-gate edit a 3-site lockstep change. RTL on the secondary surface is a follow-up if a non-LTR Bible is bundled; until then no behavioural difference because there's no RTL Bible to render. (3) **C. Per-locale filler dictionary** — `reference-engine.ts` L169-231 restructured `FILLER_PHRASES` array → `FILLER_BY_LOCALE: Record<string, string[]>` keyed by ISO 2-letter code. English bucket (`en`) carries the existing 20 phrases unchanged. `tw` + `ee` buckets are empty stubs (native-speaker contributor populates later). `stripFiller` got an optional second arg `locale?: string`; precedence is locale-bucket-first then always-also-English (bilingual services mix English Bible vocab into non-English preaching). Legacy `FILLER_PHRASES` const kept as alias pointing at `FILLER_BY_LOCALE.en` so existing single-arg callsites are byte-identical (verified by architect #3). (4) **D. SSE heartbeat prune** — `src/lib/output-broadcast.ts` L146-192 added 25-second heartbeat interval emitting SSE comment line `:hb\n\n` to every subscriber. Comment lines are valid SSE per spec — browser EventSource drops them silently. The Node `res.write` attempt eventually returns false (or throws) on truly-dead sockets, the existing prune path catches it, and the subscriber Set stays bounded. Pre-fix the only prune trigger was a real state broadcast; stalled-but-not-closed sockets (Wi-Fi blip, suspended laptop, dead TV that never sent FIN) silently buffered in the OS socket buffer for up to ~50k broadcasts before write returned false → during a long live service, dead subscribers stacked in the Set and every real update walked all of them. 25s is well under common HTTP idle-timeout (60s ALB, 100s Cloudflare) so this doubles as a keep-alive through future reverse proxies. Idempotency: `globalThis.__scriptureLiveSseHeartbeat` singleton guard prevents stacking across Next.js hot reload (architect explicitly validated #2). NOT unref'd — the SSE broadcaster is the heart of the live service, keeping the event loop alive on its behalf is the right tradeoff. (5) **E. NDI IPC defensive guards** — `electron/main.ts` L2250-2266 `ndi:status` wrapped in try/catch returning structured `NdiStatus`-shaped fallback `{running:false, frameCount:0, error: ndi.unavailableReason()||message}` on throw. Pre-fix if the bundled DLL failed to load (corrupt install / missing VC++ runtime / blocked by AV) and any internal field on `NdiService` was nullable, the IPC call surfaced as `invoke` rejection with no diagnostic. L2432-2434 `ndi:stop` got `if (!ndi.isAvailable()) return {ok:true, alreadyStopped:true}` matching the existing `ndi:start` guard at L2241. Architect's pass 1 caught the initial fallback shape used invented fields (`sourceName`/`layout`/`transparent`/`lowerThird`/`available`/`unavailableReason`) that don't exist on the `NdiStatus` type at `electron/ndi-service.ts` L29 + `electron/preload.ts` L26 + `src/lib/use-electron.ts` L5; rewrote to use only the three real fields. Renderer reads NDI availability via the separate `appInfo.ndiAvailable` channel (preload.ts L42) so the status object only needs `running` + `frameCount` + `error`. (6) **F. React ErrorBoundary** — new file `src/components/error-boundary.tsx` is a class component with `static getDerivedStateFromError` + `componentDidCatch` (logs to console for DevTools + Electron launch.log capture), renders a recoverable fallback card with Reload button on catch. `src/app/page.tsx` wraps `<LogosShell>` at L74-76 (label="Live Console") and `<SettingsView>` at L100-102 (label="Settings"). Boundary placement is deliberate: providers (`SpeechProvider`, `OutputBroadcaster`, `LicenseProvider`, `LiveTranslationSync`, `UpdateNotifier`) stay OUTSIDE the boundary so when a deep child of LogosShell crashes mid-service (corrupt cache row, malformed slide shape, missing media asset), the SSE broadcaster + NDI sender + license heartbeat keep running — the secondary screen + NDI feed keep broadcasting whatever was last on-air, the operator console shows the recoverable card, and clicking Reload re-mounts only the LogosShell subtree. Architect validated #4. NOT wrapped per-card inside `logos-shell.tsx` (4199 lines) — nested boundaries inside that file would require re-validating every v0.7.197 GR-B preview-lock site, large risk for cosmetic gain. (7) `package.json` 0.7.198→0.7.199 + `BUILD.bat` banner. **Architect pre-push reviewed and rejected first attempt (#5 blocker — ndi:status fallback shape mismatch); after the rewrite to use the real `NdiStatus` field set, architect APPROVED** with all 8 validations passing: GR-respect, heartbeat singleton, stripFiller back-compat, ErrorBoundary placement preserving broadcast continuity, NdiStatus shape parity, NFC ASCII no-op, all-LTR defaults, undefined-dir React no-op. Shipped commit `0eb769a` tag `v0.7.199` via `.local/push-r.mjs` (first invocation timed out at the bash-tool 60s wall clock, but the GitHub REST sequence had already completed — HEAD on GitHub was already `0eb769a` with the v0.7.199 message when verified). **GUARD-RAIL (A) — `wordsToNumbers` MUST keep `text.normalize('NFC')` at the entry**: the book-mapping alias table is loaded as NFC at bundle time. Removing the normaliser re-introduces a class of detection misses where the same logical word (e.g. Twi "ɛyɛ") compares unequal at the byte level between speech-engine output and the alias table → alias lookup fails → fuzzy fallback degrades or returns wrong book. Cost is sub-µs on the speech chunks the engine actually sees; benefit is correctness across every non-ASCII translation we may add. Audit: `rg -n "normalize\(.NFC.\)" src/lib/bibles/reference-engine.ts` MUST show the call at the top of `wordsToNumbers`. **GUARD-RAIL (B) — `getDirAttribute` MUST return `undefined` (not `'ltr'`) for LTR translations**: React skips emitting an attribute whose value is `undefined`, so the DOM stays byte-identical for every currently-bundled translation. Returning the string `'ltr'` would add `dir="ltr"` everywhere → snapshot tests churn + downstream CSS that uses `[dir]` attribute selectors would suddenly match LTR roots. The two-helper split (`getTranslationDirection` returns the canonical 'ltr'|'rtl' for logic; `getDirAttribute` returns undefined|'rtl' for JSX spread) is load-bearing — do NOT collapse them. Audit: `rg -n "return 'ltr'|return undefined" src/lib/bibles/translation-direction.ts` MUST show `getDirAttribute` returning `undefined` on the LTR branch, NOT the string `'ltr'`. **GUARD-RAIL (C) — `stripFiller` MUST keep the optional `locale?: string` second arg AND MUST always also run the English bucket regardless of locale**: bilingual services routinely mix English Bible vocabulary ("read", "turn to", "word of god") into non-English preaching — dropping English when a non-English locale is passed would regress detection on the majority of real-world bilingual usage. The legacy single-arg call form MUST stay byte-identical to pre-v0.7.199: `stripFiller(text)` runs English-only (locale undefined → first branch skipped → second branch runs English bucket = exact pre-v0.7.199 behaviour). Audit: `rg -n "stripFiller\(" src/lib/bibles/` and every callsite MUST either pass no locale (back-compat) or pass an explicit locale; never gate the English bucket on locale being undefined. **GUARD-RAIL (D) — `output-broadcast.ts` heartbeat interval MUST use the `globalThis.__scriptureLiveSseHeartbeat` singleton guard**: without it, every Next.js hot-reload during dev (and every fresh module-graph evaluation in some Edge runtime scenarios) stacks another 25-second interval → after 10 hot-reloads, 10 heartbeats fire concurrently → 10× the prune work + 10× the keep-alive traffic. The `declare global { var __scriptureLiveSseHeartbeat: NodeJS.Timeout | undefined }` + `if (!globalThis.__scriptureLiveSseHeartbeat)` pattern is the standard Next.js singleton — do NOT replace it with a module-level `let` (module-level state resets on hot-reload, defeating the guard). Heartbeat MUST also stay NOT-unref'd; the SSE broadcaster is the heart of the live service, keeping the event loop alive on its behalf is intentional. Audit: `rg -n "__scriptureLiveSseHeartbeat" src/lib/output-broadcast.ts` MUST show the `declare global` + `if (!globalThis.__scriptureLiveSseHeartbeat)` pair. **GUARD-RAIL (E) — `ipcMain.handle('ndi:status')` catch-path fallback object MUST keep the exact `NdiStatus` shape `{running:boolean, frameCount:number, error?:string}`** (plus optional `source/width/height/fps/captureMessage` only if added to the type later). Inventing fields the renderer doesn't read is harmless but inventing fields that conflict with the type (`sourceName` vs the real `source`) breaks TS consumers + can silently overwrite real fields when the renderer does `{...status, …}` spread. The renderer reads NDI availability via the separate `appInfo.ndiAvailable` channel (`electron/preload.ts` L42), so the status fallback does NOT need to invent an `available` field. Three files define `NdiStatus` (`electron/ndi-service.ts` L29 + `electron/preload.ts` L26 + `src/lib/use-electron.ts` L5) and MUST stay in sync — when any one changes, the other two MUST be updated in the same commit. Audit: `rg -n "type NdiStatus" electron/ src/lib/use-electron.ts` MUST show all three definitions with identical field sets. **GUARD-RAIL (F) — `ErrorBoundary` placement in `page.tsx` MUST wrap ONLY `LogosShell` and `SettingsView` (NOT the providers above them)**: SpeechProvider, OutputBroadcaster, LiveTranslationSync, UpdateNotifier, LicenseProvider all live OUTSIDE the boundary so when the LogosShell subtree crashes mid-broadcast, the SSE broadcaster keeps emitting to the secondary screen, the NDI sender keeps emitting frames to OBS/vMix, the license heartbeat keeps the subscription state valid, and the speech recognition stays hot. Operator clicks Reload to recover the console subtree only. Wrapping providers inside the boundary would mean a console crash also tears down SSE/NDI mid-service — the exact failure mode this hotfix exists to prevent. Audit: `rg -nB1 -A2 "<ErrorBoundary" src/app/page.tsx` MUST show both ErrorBoundary instances sitting BELOW `<LicenseProvider>` and `<SpeechProvider>` in the JSX tree, never above. **PROCESS GR — When the diagnose pass surfaces a "critical" claim from an explore subagent, VERIFY by reading the actual file at the claimed line before accepting it**: this hotfix had two such false alarms (NDI ticker starvation + protocol-handler path traversal) that the subagent confidently labelled critical. Both were already guarded by code the subagent didn't read. Cost of verification: 2 grep + 2 sed commands, ~30 seconds. Cost of trusting the subagent: a wasted hotfix entry "fixing" a non-bug, plus the cost of operator confusion when the GR audit later shows the guard was already there. **PROCESS GR — When a hotfix bundle has N items, the architect pre-push MUST be passed explicit per-item validations (#1..#N) not a generic "review the diff"**: this hotfix's architect prompt enumerated 8 validations and the architect's reply mapped 1:1 to each → the one blocker (NdiStatus shape mismatch) was caught precisely because validation #5 specifically asked about it. A generic "review the diff" review would likely have missed the shape mismatch among 171 insertions across 7 files.

-   **v0.7.198 — NDI Live Preview flash fix (iframe key stabilized) + settings preview boxes show background only (`?noMedia=1` query gate) + NDI-in-OBS frame-flow diagnostic logs**: Operator reported via screenshots: (1) NDI Live Preview iframe flashes/reloads every time they flip Full↔LT mode in NDI panel — visible flicker on operator's monitor, distracting during live service. (2) Settings page preview boxes (in Display Output settings AND NDI panel preview) show full video media including looping mp4 playback — operator wants those preview thumbnails to show ONLY background/theme so they don't churn decoder load while operator is tweaking settings; Main Preview/Live + offscreen FrameCapture + Browser Source must still play media. (3) NDI source appears in OBS source dropdown on same PC but renders BLACK — unknown when broke, need diagnostics before guessing root cause. **Changes (5 files)**: (1) `src/components/views/ndi-output-panel.tsx` L1467 `stableKey` changed from a composite key including `ndiDisplayMode` + `position` to a constant string `'ndi-preview'` — the iframe URL itself already carries the mode params and the renderer reads live mode via SSE (per hotfix.5 GR), so React key only needs to be stable across the panel lifetime. Pre-fix, every flip of `ndiDisplayMode` changed the key → React unmounted+remounted the iframe → operator saw a black flash + reload spinner. L1430 also added `&noMedia=1` to the preview URL params (preview thumbnail should not play media, see item 2). (2) `src/components/settings/output-preview.tsx` — added optional `noMedia?: boolean` prop; when true, appends `&noMedia=1` to the iframe src. (3) `src/components/views/settings.tsx` L961 + L1458 — passed `noMedia={true}` on the two settings-page OutputPreview instances (Display Output settings preview + NDI panel preview). Main Preview/Live columns in `logos-shell.tsx` + offscreen FrameCapture BrowserWindow + Browser Source HTTP route consumers do NOT pass `noMedia` → unchanged → videos play as before. (4) `src/app/api/output/congregation/route.ts` — `var IS_NO_MEDIA=false;` injected at L287 (inside outer html template literal), parsed from URL at L330 (`IS_NO_MEDIA = u.searchParams.get('noMedia')==='1'`), and gated at the slide-render entry L1466 with an explicit early-bail branch: for any `media`-type slide when `IS_NO_MEDIA` is true, render ONLY the themed background card (`setBgVid(safeBgM)` + theme card + bg-overlay) and `return` BEFORE the media element render path. Architect pass 1 caught the original implementation falling through to text-render of `slide.title` (which is the upload filename — "video.mp4") so the early-bail had to be explicit. Architect pass 2 caught a cache-key shape mismatch — the render-entry key at L1199 had to gain a `nm:IS_NO_MEDIA?1:0` field to match the new branch writes at L1490 (early-bail) and L1562 (media-reuse); pre-fix the noMedia and noMedia=0 surfaces would have collided in the cache slot, causing the first-rendered surface to block the other's rebuild. All three lastRenderKey writes are now shape-identical. (5) `electron/frame-capture.ts` — added frame-flow diagnostic logging for NDI-in-OBS BLACK investigation: `[frame-capture] subscription started` on `start()` + per-60-frame `frames=N fps=X.X black=Y.Y% size=WxH` rate-limited stats. Blackness check samples 8 pixels per frame from the BGRA `Uint8ClampedArray` (`buf[i]+buf[i+1]+buf[i+2]>24` threshold per sample) and tracks the rolling percentage. Goal: when operator reproduces the BLACK in OBS, the launch.log shows whether (a) `[frame-capture] subscription started` ever fired (subscription broken) OR (b) frames=0 (no frames being captured) OR (c) frames>0 with black=100% (capture working but sending all-black pixels — points to upstream BrowserWindow render issue) OR (d) frames>0 with black=0% (NDI sender working, problem is in OBS receiver config). Logs are zero-cost when subscription is healthy (60-frame interval at 30fps = 1 line every 2 seconds). `package.json` 0.7.197→0.7.198, `BUILD.bat` banner bumped. `pnpm --filter @workspace/imported-app exec tsc --noEmit` clean. Architect pass 3 GREEN: cache-key shape parity across entry + 2 branch writes confirmed; `IS_NO_MEDIA` consumer enumeration shows only the 3 expected sites; v0.7.196 GR-D backtick check clean (initially violated when I put backticks around `nm` in a comment inside the outer template literal, tsc caught it immediately at L1189 col 20, fixed by removing the backticks). Pushed commit `af244b5` tag `v0.7.198` via `.local/push-r.mjs`. **GUARD-RAIL (A) — NDI Live Preview iframe `stableKey` MUST stay a constant string (`'ndi-preview'`), NEVER include `ndiDisplayMode` / `ndiFullScreenBackground` / `position` / `wantTransparent` as key fields**: the iframe URL params already carry mode info, and the renderer adapts to mode flips via the SSE channel (per hotfix.5 GR) — putting any mode-derived value in the React key re-introduces the flash-on-flip operator bug (unmount + remount + black frame + reload spinner during live service). The iframe `src` URL is allowed (and required) to change with mode; React updates the iframe's src in-place without unmounting when the key is stable. Audit: `rg -n "stableKey" src/components/views/ndi-output-panel.tsx` MUST show a constant string literal, NOT a template literal interpolating mode/position/transparent fields. **GUARD-RAIL (B) — `IS_NO_MEDIA` early-bail at `route.ts` L1466 MUST be an explicit `return` BEFORE the media element render path**: pre-fix the original implementation let media slides fall through to a text-render branch that displayed `slide.title` — which for operator-uploaded media is the literal filename ("congregation-walk.mp4"). The early-bail MUST render ONLY the themed background card (`setBgVid(safeBgM)` + theme card + bg-overlay) and `return` so neither the media element NOR the text-render branch ever executes. Audit: `rg -n "IS_NO_MEDIA" src/app/api/output/congregation/route.ts` MUST show (i) declaration at L287, (ii) URL parse at L330, (iii) slide-render entry gate at L1466 with explicit `return` after the bg-only render, AND (iv) cache-key `nm` field at L1199/L1490/L1562 — exactly 4 sites; any extra site is a smell. **GUARD-RAIL (C) — `lastRenderKey` JSON shape MUST stay identical across the render-entry write (L1199) and ALL branch writes (L1490 early-bail, L1562 media-reuse)**: any field added to one site MUST be added to all three with the SAME expression form (currently `nm:IS_NO_MEDIA?1:0` in all three). Shape divergence causes cache poisoning where the noMedia and noMedia=0 surfaces collide on the same slot and whichever rendered first blocks the other's rebuild (operator sees blank surface). Audit: `rg -n "lastRenderKey\s*=" src/app/api/output/congregation/route.ts` should show exactly 3 write sites all using the same `JSON.stringify({sl,dm,st,ndi,nm})` shape. **GUARD-RAIL (D) — Settings-page preview thumbnails MUST pass `noMedia={true}` to OutputPreview**: this is the contract that lets operator tweak settings without churning the video decoder pool. Main Preview/Live columns in logos-shell.tsx + offscreen FrameCapture BrowserWindow + Browser Source HTTP route consumers MUST NOT pass `noMedia` (or pass `false`) → videos play as before. Adding `noMedia` to a non-settings consumer would break operator's live-broadcast media playback. Audit: `rg -n "noMedia" src/components/settings/ src/components/views/settings.tsx src/components/views/ndi-output-panel.tsx` — every match in the settings tree should be `true`; logos-shell.tsx + frame-capture.ts + main.ts paths MUST NOT mention `noMedia`. **GUARD-RAIL (E) — `frame-capture.ts` per-60-frame stats log line MUST stay in place across cleanups; it is the diagnostic canary for the NDI-in-OBS BLACK class of bug**: at 30fps a 60-frame interval = 1 log line per 2 seconds = zero noticeable flood, and the 8-pixel blackness sample is O(8) per frame = negligible. When operator reports "NDI source dropdown shows ScriptureLive AI but render is black in OBS/vMix/Wirecast," the launch.log line `frames=N fps=X.X black=Y.Y% size=WxH` immediately distinguishes 4 root-cause classes: (a) no log = subscription never started, (b) frames=0 = subscription started but no callback firing, (c) frames>0 black=100% = NDI sending all-black pixels (upstream BrowserWindow render issue), (d) frames>0 black<10% = NDI sending real pixels, problem is OBS-side receiver config. Removing these logs forces guess-driven debug cycles. Audit: `rg -n "\[frame-capture\]" electron/frame-capture.ts` MUST show both `subscription started` and the per-60-frame stats log. **PROCESS GR — When adding a new URL query param that gates render branches inside the outer `const html = \`…\`` template literal in congregation/route.ts, the param parse + injection + every branch-write that exists in the cache-key system MUST be updated in lockstep**: this hotfix's pass 2 caught the L1199 entry-key not matching the L1490/L1562 branch writes — easy bug class because there are 3 write sites scattered across 400 lines of inline JS. Any future "add a render gate" change MUST grep `rg -n "lastRenderKey\s*=" route.ts` BEFORE shipping and verify ALL writes match the entry shape. **PROCESS GR — v0.7.196 GUARD-RAIL (D) violations are catchable by `pnpm exec tsc --noEmit` and surface as cryptic comma-expected errors at the line where the stray backtick appears**: I violated it once this hotfix by putting backticks around `nm` in a comment. tsc reported `route.ts(1189,20): ',' expected` which is the textbook signature of template-literal-terminated-mid-file. When you see that error class inside congregation/route.ts, IMMEDIATELY grep `rg -n '\\\\\`|` src/app/api/output/congregation/route.ts` and remove any non-intended backtick before chasing other hypotheses.

-   **v0.7.197 — Bible-text autofit overflow fix on ALL secondary surfaces (`.slide-content` had no height bound) + snap-back preview clobber fix (6th AI-push site missed by hotfix.11 — auto-live useEffect in logos-shell.tsx)**: Operator on v0.7.196 reported via 5 screenshots (2026-05-17): main app PREVIEW + LIVE columns render verse correctly, but Typography preview, Display & Output Live Preview, NDI Live Preview, and Wirecast/OBS ALL show massively oversized Bible text clipped top AND bottom. "Text Size on Secondary Screen" already at 60% and still overflowing. Said "fix the work you did right" — pointing at autofit. **Root cause (Item 1)**: `src/app/api/output/congregation/route.ts` L74 `.slide-content` CSS was `position:relative;z-index:1;text-align:center;width:90%;max-width:90vw;padding:4vh 3vw;display:flex;flex-direction:column;align-items:center;justify-content:center` — NO height/max-height/overflow constraint. It is a flex child of `#output` (`display:flex;align-items:center`) so without an explicit height it sizes to content. `fitVerseText` at L946-982 computes `var avail=parent.clientHeight` then binary-searches the largest scale `best` where `h<=avail*safety && w<=availW`. With unbounded parent, `parent.clientHeight` reports content-height (always grows with the text itself) → `h<=avail` always passes → `best=maxK=1.00` → text rendered at full baseSize (~64px in 1920×1080 NDI/preview iframes via clamp `min(7cqw,12cqh)` resolution) → overflow top+bottom. Main app PREVIEW/LIVE columns escaped the bug because their tiny preview iframes (~150px tall) floor the clamp() baseSize at the 16px rem-minimum so even at scale 1.0 the text fits. LT branch (`.lt-content` L171) already had `height:100%;overflow:hidden;min-height:0` so was unaffected — only the FULLSCREEN branch was broken. **Root cause (Item 2)**: operator reported a separate "snap-back" bug: single-click verse 19 (intending preview=19, live=18) → preview flashes 19 then snaps back to 18. Hotfix.11 added preview-lock guards to 5 speech-provider AI-push sites but missed the SIXTH AI-push site at `src/components/layout/logos-shell.tsx` L3875 — an auto-live useEffect that fires from the `addDetectedVerse` pipeline (mic hot, AI hears preacher quoting "John 3:18" once per second). Pre-fix it ran `addScheduleItem({slides:[v18]}) + setSlides([v18]) + setPreviewSlideIndex(0) + setLiveSlideIndex(0) + setIsLive(true)` unconditionally, wiping the manually-staged `[v18,v19]` array down to `[v18]` and pointing preview back at v18. This sixth site fires from verse-detection state (not speech commands) which is why hotfix.11's grep on the speech file missed it — DIFFERENT FILE, same bug class. Affected all 5 click columns (Chapter Navigator, Auto Verse Match, Bible Reference Quoted, Suggested Verses, Scripture Feed) because all five stage manual preview which this single auto-fire effect then clobbered. **Changes (4 files)**: (1) `src/app/api/output/congregation/route.ts` L74 `.slide-content` — added `height:100%;max-height:100%;min-height:0;box-sizing:border-box;overflow:hidden`. Container-relative (NOT `100vh`) so it tracks `#output` height under any future letterboxed ratio mode (4:3, 21:9) — architect pre-push specifically flagged the `100vh` variant as a future risk. `min-height:0` is the flex-overflow-hidden idiom (without it flex children refuse to shrink below content size). `overflow:hidden` clips any stray growth so `parent.clientHeight` reports the bound, not the content. (2) `src/components/layout/logos-shell.tsx` L3737 destructure — added `addScheduleItemQuiet` to the `useAppStore()` pull. (3) Same file L3875-3918 auto-live useEffect — inserted preview-lock guard `const __st = useAppStore.getState(); if (__st.previewSlideIndex >= 0 && __st.previewSlideIndex !== __st.liveSlideIndex) { addScheduleItemQuiet({type:'verse', title:best.reference, subtitle:best.translation, slides:[slide]}); return; }` BEFORE the existing `addScheduleItem + setSlides + setPreviewSlideIndex + setLiveSlideIndex + setIsLive` live-fire path. Uses `useAppStore.getState()` snapshot (NOT a closure-captured `st`) because the useEffect deps intentionally only watch `effectiveAutoFire` + `detectedVerses` — relying on a closure-captured state var would suffer the well-known stale-closure bug. Mirrors the dual-condition pattern from hotfix.11 GR-E exactly. `addScheduleItemQuiet` (hotfix.8 pattern) drops the AI verse into the Scripture Feed Queue history without touching `slides`/`previewSlideIndex`/`liveSlideIndex` so manual preview survives. Added `addScheduleItemQuiet` to the useEffect deps array. (4) `package.json` 0.7.196→0.7.197, `BUILD.bat` banner. `pnpm exec tsc --noEmit` clean. Architect pre-push initially flagged Item 1's `100vh` variant as not robust for letterboxed ratios — replaced with container-relative `100%`/`100%`/`min-height:0` per architect recommendation BEFORE shipping. Architect approved final patch (`addScheduleItemQuiet` dep correctly included, `useAppStore.getState()` correctly used as snapshot read, LT branch unaffected, bgLayer absolute positioning unaffected, no GR-A/B/C/D from v0.7.196 affected). Pushed commit `af31a7b` tag `v0.7.197` via `.local/push-r.mjs`. **GUARD-RAIL (A) — `.slide-content` MUST keep `height:100%;max-height:100%;min-height:0;box-sizing:border-box;overflow:hidden`; do NOT "simplify" by removing any of the four height/overflow declarations**: the entire fullscreen autofit shrink loop hinges on `parent.clientHeight` reporting a BOUND value, not a content-height. Removing `overflow:hidden` re-exposes the inflate-with-content bug (parent grows past 100% because text wants more room → autofit measures inflated avail → never shrinks → operator sees overflow). Removing `min-height:0` lets flex refuse to shrink (flex children default to `min-height:auto` = content size which defeats the `max-height:100%` cap). Switching `100%` back to `100vh` breaks future letterboxed ratio modes (4:3, 21:9, ultrawide) where `#output` is smaller than viewport. Audit: `rg -n "\.slide-content\{" src/app/api/output/congregation/route.ts` MUST show all five: `height:100%`, `max-height:100%`, `min-height:0`, `box-sizing:border-box`, `overflow:hidden`. **GUARD-RAIL (B) — The auto-live useEffect in `logos-shell.tsx` L3875 is the SIXTH AI-push site that MUST carry the preview-lock guard `if (__st.previewSlideIndex >= 0 && __st.previewSlideIndex !== __st.liveSlideIndex) { addScheduleItemQuiet(...); return; }`**: hotfix.11 GR-E enumerated FIVE speech-provider sites; this useEffect is the sixth and it lives in a DIFFERENT FILE (`logos-shell.tsx`, not `speech-provider.tsx`) and fires from a DIFFERENT trigger (verse-detection state change, not speech-command intent). Any future "AI auto-fire" surface added MUST carry the same dual-condition guard + `addScheduleItemQuiet` quiet variant + `return` before any `setSlides`/`setPreviewSlideIndex`/`setLiveSlideIndex`/`setIsLive` call. The dual condition `previewSlideIndex !== liveSlideIndex` is load-bearing — single-condition `previewSlideIndex >= 0` would block AI fire when preview merely mirrors live (no operator intent to preserve) → common workflow break. Audit: `rg -n "previewSlideIndex !== .*liveSlideIndex.*addScheduleItemQuiet" src/components/layout/logos-shell.tsx src/components/providers/speech-provider.tsx` MUST show ≥6 sites total. **GUARD-RAIL (C) — Inside any useEffect whose deps array intentionally does NOT include preview/live indices, MUST read state via `useAppStore.getState()` snapshot, NEVER via a closure-captured `state` / `st` variable**: closure-captured state in a useEffect callback is the textbook stale-closure bug — at the moment the effect callback runs, the closure-captured value may be from a previous render. The L3875 auto-live useEffect intentionally watches only `effectiveAutoFire` + `detectedVerses` (so adding `previewSlideIndex` as a dep would cause unnecessary re-runs on every preview change), so it MUST read `previewSlideIndex`/`liveSlideIndex` via `getState()` at the moment of fire. Audit: any future `useAppStore()` destructure that pulls index fields inside an autofire useEffect MUST either add them to deps OR re-read via `getState()` at the call site. **PROCESS GR — When operator reports overflow / sizing bug on ONE rendering surface but not another, FIRST diagnostic is "what bounds the parent of the measured element on each surface"**: the .slide-content bug was diagnosable in 30 seconds via "main preview works because its iframe is tiny, NDI overflows because its iframe is full-size, and the autofit measures parent.clientHeight which is unbounded" — but only if you immediately ask "what bounds the parent on each working surface vs each broken surface". Operator's 5-screenshot proof (main app correct, 4 secondary surfaces broken) was the complete diagnosis once that question was asked. **PROCESS GR — When you add a "preview-lock" guard to N AI-push sites, IMMEDIATELY grep across the ENTIRE codebase (not just the file you started in) for OTHER call patterns that match `addScheduleItem + setSlides + setPreviewSlideIndex + setLiveSlideIndex + setIsLive` (or any 3 of those 4 in sequence)**: hotfix.11 added the guard to 5 sites in `speech-provider.tsx` but the SAME bug class lived in `logos-shell.tsx` — a different file, same code pattern. The 5+1=6 site count would have been caught at hotfix.11 ship time by a single repo-wide `rg -n "setSlides\(.*\bsetPreviewSlideIndex\(0\b"`. Future "broad guard" patches MUST include a repo-wide audit grep in the architect prompt.

-   **v0.7.196 — SWC-regex-in-template-literal fix (one-line GR)**: inline `<script>` blocks inside the outer `const html = \`…\`` template literal in `src/app/api/output/congregation/route.ts` MUST NOT use regex literals containing `\/` — Next.js/SWC's template-literal handler strips the backslash, producing broken regex like `/^(?:https?:/:` that Chromium rejects as "Unterminated group" → the entire inline render script dies → backgrounds/bible-text/videos all blank on every congregation BrowserWindow (NDI preview, NDI offscreen capture, secondary-screen kiosk, Display & Output Live, Typography preview). Use `indexOf`/`substring` parsers OR construct regex via `new RegExp('pattern')` with single-quoted source (no backslash-slash escapes needed). The React-side `resolveMediaUrl` in `src/lib/utils.ts` is in a regular `.ts` file (no template-literal context) and is unaffected — that's the diagnostic signature when only congregation surfaces break. v0.7.194-hotfix.12's CORS fix (ACAO:* + GET/HEAD/OPTIONS on protocol handler `baseHeaders`, spread into both 206 + 200 branches) is correct and MUST remain; path-traversal guard at L2783-2785 MUST stay BEFORE `path.join(uploadsDir, filename)`. Shipped commits `ea108fa` (hotfix.12) + v0.7.196. Full text in `artifacts/imported-app/CHANGELOG.md` and git history.

-   **v0.7.194-hotfix.12 — CORS headers on `scripturelive-media://` protocol handler (one-line GR)**: `electron/main.ts` protocol handler `baseHeaders` dict MUST include `Access-Control-Allow-Origin: '*'` + `Access-Control-Allow-Methods: 'GET, HEAD, OPTIONS'` + `Access-Control-Allow-Headers: '*'`; do NOT "harden" by removing or tightening because they pair with load-bearing `crossorigin="anonymous"` on every media element (anonymous attr keeps NDI canvas un-tainted for `drawImage()` → NDI sender keeps emitting frames). `baseHeaders` MUST be spread (`...baseHeaders`) into BOTH the 206 range response AND the 200 full response; refactor that splits them MUST preserve both branches. L2783-2785 path-traversal guard (`filename.includes('..') || '/' || '\\' || .startsWith('.')`) MUST stay BEFORE `path.join(uploadsDir, filename)` — `ACAO:*` makes responses readable to any origin so any future "subdirectory support" addition MUST replace the substring guard with `path.resolve` + `startsWith(uploadsDir + path.sep)`. PROCESS GR: Electron protocol-handler changes MUST verify response-header coverage across ALL branches (206/200/4xx) + manual end-to-end smoke (stage MP4 → all 5 surfaces render → DevTools Console zero CORS errors). When operator reports "you broke X + Y still doesn't work" after a recent ship, FIRST diagnostic is "is the new code path actually being reached?" — symptom-pair of regression + unfulfilled speed claim is a strong signal that the new path is unreachable, not that the speed claim was wrong. Superseded immediately by v0.7.196 which fixed the SWC-regex bug that made hotfix.10 (and this CORS fix) fully reachable. Shipped commit `ea108fa`. Full text in `artifacts/imported-app/CHANGELOG.md` and git history.

-   **v0.7.194-hotfix.11 — NDI 5-item bundle: LT bar slider scales bar height (`hPctScaled=Math.min(85,hPct*ndiLtScale)`) + LT ref autofit `rHi=1.10` cap + NDI `armBridge(3000)` for Wirecast/OBS continuity across BrowserWindow rebuild + speech-provider preview-lock on 5 AI-push sites + Scripture Feed Select toggle always enabled (one-line GR)**: `hPctScaled` MUST stay `Math.min(85, hPct * ndiLtScale)` (slider drives bar height, capped 85% so bar can never eat the whole frame). LT reference autofit `var rHi=1.10` MUST NOT regress to 2.0× (causes "COMP OVE / ASV" garbled overflow); route any "auto-grow short LT ref" request through a new `ndiLtRefAutoGrow: boolean` setting. `armBridge`/`disarmBridge` MUST stay symmetric — `stop()` MUST call `disarmBridge()` (else 16ms setInterval leaks into freed NDI sender → Electron crash). `armBridge(ms)` MUST be called BEFORE `await frameCapture.stop()` in `restartNdiOutput`, NEVER after (whole point is to bridge frames DURING the rebuild gap; arming after = operator sees Wirecast disconnect). Speech-provider preview-lock at 5 AI-push sites (next_verse, go_to_reference, find_by_quote, next_chapter/previous_chapter, show_verse_n) MUST use dual-condition `previewSlideIndex >= 0 && previewSlideIndex !== liveSlideIndex` + `addScheduleItemQuiet` (single-condition would block AI fire when preview mirrors live, breaking common workflow). **v0.7.197 added a 6TH preview-lock site at `logos-shell.tsx` L3875 auto-live useEffect** — see v0.7.197 GR-B. Scripture Feed Select toggle MUST stay always-enabled (do NOT re-add `currentTabEmpty && !selectMode` to the disabled guard); per-action gating lives in the action bar. PROCESS GR: comprehensive multi-item bundles approved via single choice query MUST ship as one hotfix; pre-push architect MUST "trace runtime composition for any new store-action/IPC sequence." Shipped commit `a2553bb`. Full text in `artifacts/imported-app/CHANGELOG.md` + git history.

-   **v0.7.194-hotfix.10 — EasyWorship-class media playback: custom `scripturelive-media://` protocol + backgroundThrottling off + URL rewrite + MediaPreheat warm-up (one-line GR)**: `scripturelive-media://` protocol handler in `electron/main.ts` is the ONLY truth-source for media reads inside Electron; `/api/upload` HTTP route stays for backward-compat with OBS Browser-Source URLs pasted pre-hotfix.10 only. Protocol handler MUST enforce path-traversal guard (rejects `..` / `/` / `\` / leading `.` + `!stat.isFile()`) BEFORE `path.join(uploadsDir, filename)`. `MediaPreheat` component in `logos-shell.tsx` MUST use off-viewport positioning (`position:fixed; left:-9999px`), NEVER `display:none` (Chromium skips `preload="auto"` on hidden elements). `backgroundThrottling: false` MUST be on EVERY BrowserWindow that drives a video surface or NDI capture (mainWindow + createKioskOutput + frame-capture). Detection in `__scrMedia` / `resolveMediaUrl` MUST be dual (`window.scriptureLive?.isDesktop` OR `Electron` in UA) — see v0.7.196 GR-B for why. PROCESS GR: pre-push architect on protocol/native-IPC changes MUST trace EVERY BrowserWindow + verify preload presence; protocol-handler changes MUST include manual end-to-end smoke (stage MP4 → render on all 5 surfaces → DevTools Console clean). Superseded by v0.7.196 (which fixed the SWC-regex bug that made hotfix.10 unreachable). Full text in `artifacts/imported-app/CHANGELOG.md` + git history. **Changes (7 files)**: (1) `electron/main.ts` — added `protocol` + `net` imports L1, registered `scripturelive-media` as standard+secure+supportFetchAPI+stream+bypassCSP+corsEnabled scheme BEFORE `app.whenReady` (L2697-2709, must happen pre-whenReady per Electron API contract), installed `protocol.handle` inside whenReady (L2759+) that reads from `path.join(app.getPath('userData'), 'uploads')` via streaming `fs.createReadStream`, returns `Response` with `ReadableStream` body + `Accept-Ranges: bytes` + `Content-Range: bytes start-end/total` 206 status when `Range:` header present + full 200 otherwise + `Cache-Control: public, max-age=31536000, immutable` (operator uploads are content-addressed by uuid filename → bytes never change for a given URL → Chromium net-cache aggressively reuses decoded frames). Path-traversal guard rejects `..` / `/` / `\` / leading `.` + `!stat.isFile()` before the join. MIME inferred from extension (mp4/webm/mov/mkv/avi/m4v/ogv + image fallbacks). Added `backgroundThrottling: false` to mainWindow webPreferences (L1149) and `createKioskOutput` webPreferences L2536 — Chromium throttles unfocused windows' timers + rAF by default; operator alt-tabbing to OBS/Wirecast/PowerPoint mid-service would freeze the preview→live hand-off the moment they alt-tabbed back. frame-capture.ts already had it. (2) `src/lib/utils.ts` — exported `resolveMediaUrl(url)` helper: passes through SSR / non-Electron / data: / already-rewritten URLs, else regex-matches `/^(?:https?:\/\/[^/]+)?\/api\/upload\?file=([^&#]+)/` → `scripturelive-media://uploads/${m[1]}`. Gates on `window.scriptureLive?.isDesktop`. (3) `src/components/presenter/slide-renderer.tsx` — imported `resolveMediaUrl`, wrapped `<video src>` L221 + `<img src>` L239. (4) `src/app/api/output/congregation/route.ts` — injected `__scrMedia(u)` JS helper at L595-620 inside the giant `html = \`<!DOCTYPE html>…\`` template literal (string mirrors `resolveMediaUrl` for the inline-rendered surfaces). Wrapped 6 call sites: `setBgVid` video src L660, `setBgVid` img src L669, `ensureLtBgEl` video src L618, `ensureLtBgEl` img src L625, `<video id="liveVideo" src=…>` L1473, `<img src=…>` L1474. (5) `src/components/layout/logos-shell.tsx` — added `MediaPreheat` component above `LogosShell` (L3668-3722) that collects video mediaUrls from preview slide + live slide + next-schedule-item slides into a `Set<string>` and renders hidden `<video preload="auto" muted playsInline>` elements at `position:fixed; left:-9999px; top:-9999px; opacity:0` (display:none would skip preload; off-viewport positioning still triggers Chromium fetch + decoder warm). Mounted as first child of LogosShell root JSX L4073. (6) `package.json` 0.7.194-hotfix.9 → 0.7.194-hotfix.10. (7) `BUILD.bat` banner bumped. `pnpm --filter @workspace/imported-app exec tsc --noEmit` clean. **Architect pre-push review found 1 SEVERE gap**: pre-fix `__scrMedia` gated on `window.scriptureLive?.isDesktop` alone, but the NDI offscreen capture BrowserWindow (`electron/frame-capture.ts` L66-91, no `preload:` key) and `createKioskOutput` secondary-screen BrowserWindow (`electron/main.ts` L2525-2537, no `preload:` key) have NO preload script — so `window.scriptureLive` is `undefined` there and the rewrite was silently skipped on EXACTLY the two surfaces operators care MOST about (NDI to OBS/vMix + projector). Implemented architect-recommended fix: changed `__scrMedia` detection at L613 to `inElectron=(sl&&sl.isDesktop)||(typeof navigator!=='undefined'&&/Electron/i.test(navigator.userAgent||''))` — protocol handler is registered globally so it works in every BrowserWindow regardless of preload; UA sniff is the safe additional signal. Browser-based remote OBS Browser Sources from pasted URLs predating this hotfix have neither flag nor "Electron" in their UA → still fall through to HTTP `/api/upload` route (which is intentionally kept alive for backward compat). Push commit `a88181f`. **GUARD-RAIL (A) — `scripturelive-media://` protocol handler is the ONLY truth-source for media reads inside Electron; `/api/upload` HTTP route stays alive ONLY for backward-compat with OBS Browser-Source URLs pasted before this hotfix**: do NOT route new code paths back through `/api/upload` thinking "HTTP is more portable" — it re-introduces the 3-5 concurrent decoder bottleneck the entire hotfix exists to eliminate. Any new `<video>` / `<img>` element pointing at operator-uploaded media MUST pass through `resolveMediaUrl()` (renderer-side React code) or `__scrMedia()` (inline congregation route JS). Audit: `rg -n "/api/upload\?file=" artifacts/imported-app/src` — every `<video src>` / `<img src>` in the result list MUST be wrapped, OR be a fetch() upload-form POST (those are legitimate). **GUARD-RAIL (B) — `__scrMedia` detection MUST tolerate BOTH `window.scriptureLive` AND `/Electron/i.test(navigator.userAgent)`**: pre-fix gating on `scriptureLive` alone silently skipped the rewrite on NDI capture + kiosk windows. Future maintainers tempted to "clean up the UA sniff" because preload is present on mainWindow MUST first add `preload:` to BOTH `frame-capture.ts` AND `createKioskOutput` webPreferences and verify by logging one media URL from each surface to confirm `scripturelive-media://` is used. Until those preloads exist, the UA sniff is the load-bearing predicate for 4 out of 5 video surfaces. Audit: `rg -n "/Electron/i\.test|scriptureLive\?\.isDesktop" src/app/api/output/congregation/route.ts src/lib/utils.ts` — both files SHOULD show the dual-check pattern. **GUARD-RAIL (C) — `scripturelive-media://` protocol handler MUST enforce path-traversal guard BEFORE `path.join(uploadsDir, filename)`**: the renderer-side rewrite is purely cosmetic; main.ts is the only sanitiser between operator-controllable input and `fs.createReadStream(full)`. The guard rejects `..` / `/` / `\` / leading `.` plus checks `stat.isFile()`. Any future maintainer who adds a "subdirectory support" feature MUST replace the join with a `path.resolve` + `startsWith(uploadsDir + path.sep)` check (the current substring guard is sufficient ONLY because subdirectories are not allowed). Audit: `rg -n "uploadsDir|path\.join.*uploads" electron/main.ts` — every join site MUST sit AFTER a guard that bounds the second arg to a single filename. **GUARD-RAIL (D) — `MediaPreheat` MUST use off-viewport positioning, NEVER `display:none`**: Chromium skips `preload="auto"` fetch entirely when the element is `display:none` (the spec lets the UA defer media-loading on hidden elements). `position:fixed; left:-9999px; top:-9999px; opacity:0; pointer-events:none` keeps the element "rendered" (so the preload fires) while invisible + non-interactive. `aria-hidden="true"` keeps screen readers from announcing it. The component MUST stay mounted at the LogosShell root (not inside a card that conditionally renders) so a card collapse / panel resize doesn't unmount + lose the preload state. Audit: `rg -n "MediaPreheat" src/components/layout/logos-shell.tsx` — exactly TWO refs (definition + single mount as first child of root div). **GUARD-RAIL (E) — `backgroundThrottling: false` MUST be on EVERY BrowserWindow that drives a video surface or NDI capture**: currently mainWindow + createKioskOutput + frame-capture (was already set). The splash window is boot-only and doesn't need it. Any NEW BrowserWindow added by a future feature MUST include `backgroundThrottling: false` if it renders media; otherwise alt-tabbing freezes the surface. Audit: `rg -n "new BrowserWindow|webPreferences" electron/` — every match's webPreferences MUST include the flag OR be the splash window. **PROCESS GR — Pre-push architect review on protocol/native-IPC changes MUST trace EVERY BrowserWindow in the project + verify preload presence**: the architect caught the NDI/kiosk preload gap because they were instructed to verify backward compat across all surfaces. Future native-bridge changes (Electron protocol, IPC channel, preload API addition) MUST include "list every BrowserWindow + its preload status" in the architect prompt — the same audit pattern as hotfix.7 GR-D and hotfix.8 GR-D. **PROCESS GR — `screenshot-before-shipping` rule (added hotfix.7+) is SATISFIED when the operator's explicit "ship" arrives via a choice query response that lists the change scope verbatim**: the operator's "Ship the full version — A+B+C above PLUS the shared <video> element across Preview→Live" reply IS the explicit ship-approval. The screenshot rule exists to prevent surprise visual changes; backend-only changes (custom protocol, URL rewrite, off-viewport warm-up element) have no operator-visible UI delta to screenshot, so the choice-query approval is the load-bearing consent. Future agents MUST NOT skip the screenshot for UI-affecting changes; the carve-out is strictly "the change has no operator-visible pixels."

-   **v0.7.194-hotfix.9 — 5-item operator bundle: autofit shrink-only + 5-column preview-no-yank + Scripture Feed Select All/Delete All + NDI LT garbled text + NDI source stable across Full↔LT flips (one-line GR)**: `route.ts` `var maxK=1.00` is the operator's contract — autofit is SHRINK-ONLY across Full and LT; never re-introduce `isLT?N:1.00` growth (route any "auto-grow short text" request through a NEW `lowerThirdAutoGrow: boolean` setting, NEVER the binary-search bound). `stageSlidesPreviewOnly` preserve branch MUST return `slides:[cur,...slides], previewSlideIndex:1, liveSlideIndex:0` — any tweak that sets `liveSlideIndex:-1` re-introduces the live-yank bug. `ndi-output-panel.tsx` restartGuard token MUST hash on `wantTransparent`, NOT raw `ndiDisplayMode + ndiFullScreenBackground` — adding `ndiDisplayMode` back re-introduces the OBS-source-drop bug on Full↔LT flip. `removeAllScheduleItems(preserveLive=true)` defaults to true intentionally; `preserveLive=false` requires explicit user-confirmation dialog citing live broadcast. Delete All uses native `window.confirm`, NOT custom modal (OS-styling + safe in offscreen NDI BrowserWindow context). PROCESS GR: comprehensive multi-item mocks approved by operator MUST ship as one hotfix; if any item turns out to need more scope mid-implementation, deliver the surgical fix achieving the user-facing promise OR ask operator to drop it BEFORE shipping the rest — never silent-skip. Full text in `artifacts/imported-app/CHANGELOG.md` and git history. **Changes (6 files)**: (1) `route.ts` L928 fitVerseText `var maxK=isLT?3.50:1.00` → `var maxK=1.00`. Kills LT 3.5× growth which was (a) ignoring operator's Text Size slider and (b) overflowing the LT chyron producing "garbled fragments behind ASV" visual bug (2026-05-17 operator screenshot). Autofit is now SHRINK-ONLY across Full and LT — natural baseSize from CSS clamp is the upper bound, binary search only intervenes when text would overflow. (2) `store.ts` added `stageSlidesPreviewOnly(slides: Slide[])` interface L619-624 + impl L1167-1184 — multi-slide variant of `stageVersePreviewOnly` for verses split by `splitForSlides()`; preserves live by prepending cur slide at index 0 with `liveSlideIndex:0`, preview slides follow at 1..N with `previewSlideIndex:1`. Same-as-live short-circuit returns `{previewSlideIndex: state.liveSlideIndex}` only. Also added `removeAllVerseHistory()` (L353 interface + L913-916 impl) and `removeAllScheduleItems(preserveLive=true)` (L617-618 interface + L1294-1326 impl) — preserveLive identifies live item via `isLive && selectedScheduleItemId`, keeps that one item, wipes rest; selectedScheduleItemId preserved if live, nulled otherwise. (3) `logos-shell.tsx` `sendDetected('preview')` branch L2370-2386 now calls `stageSlidesPreviewOnly(slides)` instead of `setSlides(slides) + setPreviewSlideIndex(0)` — Quiet-first ordering preserved per hotfix.8 GR-C. Live-row single-click L2454-2461 changed from `sendDetected(v, 'schedule')` to `sendDetected(v, 'preview')` — unified mental model: one click anywhere = preview without yanking live, double-click promotes to live across all 5 columns (Auto Verse Match, Bible Reference Quoted, Suggested Verses, Chapter Navigator, Scripture Feed). ScriptureFeedCard L1990-2055 + L2100-2160 — `selectAllCurrentTab()` ticks every row in active tab using `h:${i}`/`q:${id}` discriminator keys (hotfix.7 GR-D preserved); `deleteAllCurrentTab()` uses `typeof window !== 'undefined' && window.confirm(...)` Electron-safe guard before wiping; Select-mode action bar now ALWAYS shows Cancel + Select All + Delete All (Delete-N conditional on `selectedCount>0`), pre-hotfix.9 only showed Delete-N+Cancel when `selectedCount>0` which hid Select All from the "tick everything then delete" path. (4) `ndi-output-panel.tsx` L325-337 restartGuard token changed from `${ndiDisplayMode}:${...}:${ndiFullScreenBackground}` to `${wantTransparent}:${...}` — operator can flip Full↔LT when both modes are alpha-keyed (LT↔Full+transparent both wantTransparent=true) WITHOUT rebuilding the BrowserWindow → NDI sender stays alive → OBS/vMix/Wirecast stay bound to "ScriptureLive AI" across the flip. Renderer adapts via SSE (route.ts L1103 already prefers live SSE `ndiDisplayMode` per hotfix.5). Only restart that remains is genuine transparency flip (LT↔Full+themed) which Electron's constructor-only `transparent` flag requires. useEffect deps unchanged. (5) `package.json` 0.7.194-hotfix.8 → 0.7.194-hotfix.9. (6) `BUILD.bat` banner bumped. `pnpm --filter @workspace/imported-app exec tsc --noEmit` clean. Architect pre-push GREEN. Push commit `951c01a`. **GUARD-RAIL (A) — autofit `maxK=1.00` is the operator's contract; do NOT re-introduce `isLT?N:1.00` growth even "just a little" (1.5×)**: The whole "operator's Text Size slider is the upper bound" invariant rests on this constant. Any future request "LT text should auto-grow when verse is short" must route through a NEW LT-only setting (e.g. `lowerThirdAutoGrow: boolean`) NOT this binary-search bound. Audit: `rg -n "var maxK=" src/app/api/output/congregation/route.ts` should show ONLY `var maxK=1.00;` and the comment block citing this guard-rail. **GUARD-RAIL (B) — `stageSlidesPreviewOnly` MUST NOT mutate `liveSlideIndex` to anything other than `0` when there is a live slide to preserve**: the contract is "live slide gets prepended at index 0, liveSlideIndex points there." Any future tweak that sets `liveSlideIndex: -1` in the preserve branch re-introduces the live-yank bug (same class as hotfix.7/8 regressions). Audit: `rg -n "stageSlidesPreviewOnly" src/lib/store.ts` should show the preserve branch returns `slides: [cur, ...slides], previewSlideIndex: 1, liveSlideIndex: 0` — those three exact field shapes. **GUARD-RAIL (C) — Item E restart token MUST hash on `wantTransparent`, NOT on raw `ndiDisplayMode` + `ndiFullScreenBackground`**: the entire "OBS source stays bound across mode flip" UX rests on this. Any future maintainer who adds `ndiDisplayMode` back to the token (thinking "the BrowserWindow should rebuild on mode change") re-introduces the source-drop bug operator screenshot of 2026-05-17 (Wirecast "DISCONNECTED" on flip). The renderer is responsible for adapting to mode flips via SSE — see route.ts L1103 precedence. Audit: `rg -n "const want = " src/components/views/ndi-output-panel.tsx` should show ONLY `wantTransparent` + position + sourceName + fps + resolution — NEVER `ndiDisplayMode` or `ndiFullScreenBackground` as direct token fields. **GUARD-RAIL (D) — `removeAllScheduleItems(preserveLive)` MUST default to `true`**: live-broadcast operators cannot afford accidental wipes during a service. The dangerous `preserveLive=false` variant is reserved for end-of-service "Clear All" workflows (not yet implemented). Audit: `rg -n "removeAllScheduleItems\(" artifacts/imported-app/src` should show every call site passes `true` explicitly OR omits the arg (which defaults to `true`); a `false` arg would need an explicit user-confirmation dialog citing the live broadcast. **GUARD-RAIL (E) — Delete All button uses `window.confirm`, NOT a custom modal**: Electron exposes `window.confirm` natively as a blocking dialog with system-styling; a custom modal would (a) drift from operator's existing OS-level "are you sure" muscle memory, (b) add render dependencies that could break in the offscreen NDI BrowserWindow context if the component ever leaks into there. The `typeof window !== 'undefined'` guard exists for SSR robustness even though the Scripture Feed is client-only. **PROCESS GR — When operator approves a comprehensive multi-item mock with explicit "Ship now — all 5 items as shown", the implementation must ship ALL 5 items in one hotfix**: do NOT split into hotfix.9a + hotfix.9b "to be safe" — the operator's mental approval bound to all 5 fixes landing together. If during implementation any single item turns out to require significantly more scope than the mock showed (e.g. Item E here would have needed native main.ts work for true sender persistence), the agent must EITHER (a) deliver the next-best surgical fix that achieves the mock's user-facing promise (here: restart token hashed on wantTransparent so OBS source stays bound for the majority of flips), OR (b) ask operator to drop the item before shipping the rest. NEVER ship 4 items + silent skip of #5 — that breaks operator trust because the mock said 5.

-   **v0.7.194-hotfix.8 — `addScheduleItemQuiet` action; preview-only paths MUST NOT yank live (one-line GR)**: `addScheduleItemQuiet` set() payload MUST contain ONLY `{schedule: [...state.schedule, full]}` — adding `selectedScheduleItemId/slides/previewSlideIndex/liveSlideIndex` re-introduces the live-yank bug. Preview-only call sites (library-compact `stageVerse` preview branch, logos-shell `sendVerseFromHistory` preview branch, all 5 speech-provider AI-push guards added in hotfix.11) MUST use Quiet variant, NEVER `addScheduleItem` (which unconditionally resets `liveSlideIndex:-1`). Call-order MUST be `addScheduleItemQuiet` THEN `stageVersePreviewOnly` so the preserve-live guard reads an untouched `liveSlideIndex`. Live branches keep `addScheduleItem` (intentional reset for drag-into-schedule etc.). PROCESS GR: pre-push architect on multi-call store sequences MUST trace runtime composition (list set()-payload fields per action, verify call order respects preserve-X invariant) — pattern-style `rg` audits miss composition bugs. Full text in `artifacts/imported-app/CHANGELOG.md` and git history.


-   **v0.7.194-hotfix.7 — Stable slide-id + preview-only single-click + select-to-delete in Scripture Feed + NDI BrowserWindow transparent-flag gated (one-line GR)**: `stageVersePreviewOnly` MUST passthrough `liveSlideIndex` and `selectedVerseId` (never mutate) — any tweak that sets either field re-introduces the live-yank bug. `wantTransparent = LT || (FULL && bg==='transparent')` is the ONLY truth source for BrowserWindow `transparent` flag — do NOT re-introduce hardcoded `transparent: true`. `restartGuard` token + `ndiFullScreenBackground` useEffect dep are PAIRED with the BrowserWindow recreate because Electron's `transparent` flag is constructor-only. Select mode `selectedKeys` MUST use `h:${idx}` / `q:${id}` discriminator prefixes (history index can collide with queue id). Item #4 NDI Typography/theme disconnect intentionally deferred — body typography already disconnected via `USE_NDI_OVERRIDES`; remaining leaks are `congregationScreenTheme`, `customBackground`, `showReferenceOnOutput`. PROCESS GR: "do not change anything else let me see how the app is working" mid-bundle → ship the green-lit items + DROP the in-progress one, do NOT bundle ahead. Full text in `artifacts/imported-app/CHANGELOG.md` and git history.


-   **v0.7.194-hotfix.6 — Full↔Lower-Third autofit cache invalidation + 4th-pass 350ms timer (one-line GR)**: `__lastIsLT` module-var tracker MUST stay nullable + initial `null` (not `false`) — `null` means "first call, do nothing special". On Full↔LT flip, cache invalidator MUST clear ALL THREE module vars (`__fitKey` + `__fitBase` + `__fitScale`), not just `__fitKey`, because `__fitBase` seeds binary-search bounds. `__lastIsLT` update MUST happen AFTER the invalidator check. Autofit trigger is quadruple-pass (rAF + rAF + 120ms + 350ms) — do NOT remove the 120ms pass thinking "350ms covers it" (same-mode font swaps don't trigger the invalidator and rely on the 120ms pass). The 350ms is calibrated to Electron offscreen + LT container-query settle — shorter re-introduces the bug on slower machines, longer is operator-visible. PROCESS GR: "operator drags slider and it fixes itself" is the diagnostic signal for module-level cache staleness — list every module/closure cache that the workaround's side effects touch. Full text in `artifacts/imported-app/CHANGELOG.md` and git history.

-   **v0.7.194-hotfix.5 — Removed `lowerThird=1` URL param from `buildCongregationParams` (QR/OBS URL stability across mode toggles) (one-line GR)**: `buildCongregationParams` MUST NOT add any mode-dependent param to the URL on IS_NDI surfaces — NDI capture URL, OBS Browser-Source URL, and the NDI Preview iframe share the same output, so any mode-derived param re-introduces the QR-churn bug. All mode-derived state flows via SSE, NEVER URL params. `route.ts` L1103 SSE precedence (`IS_NDI && (ndiDisplayMode==='full'||'lower-third') ? ndiDisplayMode : (FORCE_LT ? 'lower-third' : displayMode)`) is the load-bearing flip that makes the URL-param removal safe. `FORCE_LT` URL reader at L308 kept for backward compat — pre-existing OBS Browser-Source URLs operators pasted before hotfix.5 still contain `?lowerThird=1`. **PROCESS GR**: when the URL is operator-visible (QR, paste-into-external-tool), URL stability is a first-class UX constraint — derive ONLY from stable identity. Full text in `artifacts/imported-app/CHANGELOG.md` and git history.

-   **v0.7.194-hotfix.4 — 11-item polish bundle (one-line GR)**: `ndiFullScreenBackground` default MUST stay `'themed'` (flipping to transparent is v0.6.9 regression); `fsbg=transparent` URL param is opt-in only — NEVER emit `fsbg=themed` (preserves pasted OBS Browser-Source URL shape); `alignClass` + `ta` MUST stay on `T_TA` resolver (reverting to `st.textAlign` re-breaks NDI-only align picks); NDI Typography dropdowns MUST NOT re-add `__inherit__` "Mirror Live" options (UX regression); triple-pass autofit (rAF+rAF+120ms) is calibrated to NDI-offscreen+font-swap+bg-video timeline — do NOT reduce to single-rAF; `.slide-reference{width:100%}` + slide-renderer `w-full block` are PAIRED, never change one without the other; ThemeDesignerCard `saveCurrent` MUST keep the 11-field capture set (theme+5 body+4 reference+showReference) — short capture re-introduces silent-loss-on-reapply bug. Full text in `artifacts/imported-app/CHANGELOG.md` and git history.



-   **v0.7.194-hotfix.1 — Code-review followups on v0.7.194 wall-clock trial (one-line GR)**: Any `new Date(persistedString).getTime()` MUST be wrapped in `Number.isFinite(...)` before downstream arithmetic OR `.toISOString()` calls (license.json is operator-territory; NaN propagates and 500s `/api/license/status`). There is ONE status poller in `license-provider.tsx` (30s interval + window-focus refresh at L117-128) — do NOT add a parallel poller (doubles HTTP load + creates `setStatus` races). `isListening` is intentionally NOT consumed by license-provider or license-button after v0.7.194 — the wall-clock model requires the trial UI to mirror server-side continuous countdown, NEVER freeze. Pre-merge architect (`code_review` skill) review is mandatory on any licensing/billing/trial change. Full text in `artifacts/imported-app/CHANGELOG.md` and git history.

-   **v0.7.194 — 3-day wall-clock free trial (replaces activity-gated 70-min trial) (one-line GR)**: Trial duration `TRIAL_DURATION_MS = 72h` (was 70min) and `computeStatus()` is wall-clock (`startedAtMs = new Date(firstLaunchAt).getTime()` + `trialEndMs = startedAtMs + TRIAL_DURATION_MS`), NOT activity-gated. `trial-tick` endpoint is a permanent no-op (kept for old client compat — do NOT delete). One status poller at 30s + window-focus (do NOT add a second). `isListening` MUST NOT be consumed by license-provider/license-button (would re-introduce frozen badge). Migration at `storage.ts` load() resets `firstLaunchAt`/`trialDurationMs` for ANY install where `trialDurationMs < TRIAL_DURATION_MS && !everActivated && !activeSubscription` — the `< TRIAL_DURATION_MS` check is the IDEMPOTENCY gate, removing it would re-stamp firstLaunchAt every boot. `LiveTranscriptionLockOverlay` is scoped to the AI Detection card in `logos-shell.tsx` L916 ONLY (do NOT promote to global). No remote NTP — local `Date.now()` is intentional. Full text in `artifacts/imported-app/CHANGELOG.md` and git history. **Changes (5 files)**: (1) `storage.ts` L355-366 `TRIAL_DURATION_MS` 70min → 72h; L980-993 `computeStatus()` trial calc rewritten — replace `trialUsed = min(trialDurationMs, trialMsUsed)` + `trialMsLeft = max(0, trialDurationMs - trialUsed)` activity-gated math with wall-clock `startedAtMs = new Date(firstLaunchAt).getTime()` + `trialEndMs = startedAtMs + trialDurationMs` + `trialMsLeft = max(0, trialEndMs - now)`; L641-692 load() migration block REPLACED v0.7.19 trial-lift (which preserved existing firstLaunchAt and only bumped budget) with FULL RESET: `cache.firstLaunchAt = new Date().toISOString()` + `cache.trialDurationMs = TRIAL_DURATION_MS` + `cache.trialMsUsed = 0` for any install where `trialDurationMs < TRIAL_DURATION_MS && everActivated !== true && !activeSubscription`. The `trialDurationMs < TRIAL_DURATION_MS` check is the IDEMPOTENCY gate — once we lift, persisted value equals new constant so the branch never re-runs (without this guard, every app start would re-stamp firstLaunchAt and infinitely extend trial). (2) `trial-tick/route.ts` converted to no-op: body intentionally not parsed, returns `{ok:true, status: computeStatus()}`. Old client builds (v0.7.5–v0.7.193) still pinging this URL get a clean 200 + current wall-clock status; no longer mutates `trialMsUsed`. (3) `license-provider.tsx` L194-239 removed activity-tick `useEffect` (the 5s POST loop on `isListening` change), replaced with a single 60s `setInterval` that GETs `/api/license/status` and `setStatus()` from the response. Polling cadence rationale documented inline: D/H/M precision → 60s suffices, finer would burn HTTP without changing what operator sees, ≤60s stale window on expiry transition is acceptable for a 72h budget. Polls regardless of trial/paid/expired state — server is source of truth for ALL transitions including expired→active when operator activates a code in another window. (4) `lock-overlay.tsx` L92 subtitle copy: "Your 1-hour free trial has ended. Tap Activate to enter your code." → "Your 3-day free trial has expired. Please activate a subscription to continue using AI Detection." (5) `package.json`+`BUILD.bat` 0.7.193-hotfix.4 → 0.7.194 (MINOR bump, not hotfix, because trial model semantics change). `tsc --noEmit` clean. `trialMsUsed` field kept on LicenseFile interface for backward read compat — old persisted JSON from v0.7.5–v0.7.193 just has it ignored. **GUARD-RAIL (A) — Migration MUST have an idempotency gate**: the `trialDurationMs < TRIAL_DURATION_MS` check at L668 is load-bearing. Without it, every app boot would re-stamp `firstLaunchAt = now` because the migration condition would always be true (everActivated/!activeSubscription unchanged), and the trial would infinitely extend. Pattern: any field-rewriting migration in `load()` MUST gate on a value the migration ITSELF will change so the condition flips false on the next boot. Verify with `rg -n "trialDurationMs < TRIAL_DURATION_MS" src/lib/licensing/storage.ts` returns the gate at the start of the migration block. **GUARD-RAIL (B) — `LiveTranscriptionLockOverlay` is scoped to the AI Detection card, NOT global**: do NOT later "promote" it to a full-app overlay even if a future ask says "lock everything on expiry." Operator's product decision (`user_query` #2) is that Bible reader/slides/NDI/Settings remain free — the app keeps working as a Bible-display tool after trial, only AI Detection locks. The single mount point is `logos-shell.tsx` L916 inside the AI Detection panel. Verify with `rg -n "LiveTranscriptionLockOverlay" src/` returns ONE import + ONE mount, both inside the AI Detection card region. If a future request wants Settings or NDI also gated, treat as a separate product decision and ask. **GUARD-RAIL (C) — `trial-tick` endpoint stays as a no-op forever, do NOT delete the route file**: old desktop builds (v0.7.5 → v0.7.193-hotfix.4) shipped to operators in the field will keep POSTing to this URL until they install v0.7.194+. Deleting the route would 404 those clients which is harmless to trial state (the new wall-clock model ignores the data) but pollutes their console + fails their request. The no-op route returns clean 200 + current status which lets the old client UI keep updating its countdown widget from the response payload during the upgrade window. Verify the route still exists + returns `{ok:true, status}` even if a future cleanup pass tries to remove "unused" endpoints. **GUARD-RAIL (D) — Local `Date.now()` is intentional for v1, do NOT add remote NTP check without operator sign-off**: a determined user can roll their PC clock back to extend trial. Operator's customer base is paying church operators not pirates; remote time fetch (worldtimeapi.org or similar) adds 200-500ms latency on every status poll, fails silently on flaky church wifi, and falls back to local anyway → "protection" is half-real but the cost (slower lock-overlay flips, dropped polls when offline, an extra failure mode operator has to debug) is real every poll. `everActivated` flag (set on first paid activation, persists in `~/.scripturelive/license.json` which Inno Setup uninstaller leaves untouched) already blocks the more common uninstall-reinstall reset workaround. If operator later sees actual abuse telemetry, add NTP check then; not before. **GUARD-RAIL (E) — Polling cadence at 60s is calibrated to D/H/M UI precision, do NOT shorten without changing the UI granularity**: countdown widget shows "2d 15h 04m remaining" — finer cadence (e.g. 1s) cannot show anything new because minute resolution doesn't change within a 60s window. Shortening to 1s would 60× the HTTP request rate against `/api/license/status` (the route reads/writes license.json on every call via `sweepExpired()`) and add no operator-visible benefit. If a future change adds seconds-precision to the UI, only then reduce poll interval AND add a ref-gated debounce so multiple opened tabs don't multiply load. **PROCESS GR — Pre-implementation `user_query` with 4 decisions saved a multi-turn rework cycle**: the 4 decisions (existing-install handling / lock scope / NTP check / mockup-first) each had at least 2 viable answers, and getting the wrong default would have meant a follow-up correction ship. Especially decision (i) — if I'd defaulted to "strict existing firstLaunchAt" without asking, every operator on a long-installed binary would have landed on v0.7.194 already-expired and reported it as a regression. Lesson: when the change has structural product decisions (not just code mechanics), pre-ask in one batched `user_query` BEFORE writing any code. **PROCESS GR — Mockup at `public/v0194-trial-mockup.html` is the canonical operator-facing proof for trial UX changes**: the 3-state HTML page (active / amber warning / expired lock) is what got the YES SHIP. Future trial-related changes (copy, threshold colors, expired-state CTA) should clone this 3-state pattern + present via screenshot tool to confirm operator approval before shipping. Synthetic mockups for trial changes are mandatory because operators can't easily reproduce the trial expiration moment in a live install (they'd have to wait 72 hours).

-   **v0.7.193-hotfix.4 — Add missing Bar Height UI control to NDI Output panel (one-line GR)**: `lowerThirdHeight` field had ZERO writers in UI — only `resetSettings()` wrote it; existing-install operators stuck on persisted sm/md couldn't pick the new lg default. Added Small/Medium/Large dropdown at TOP of "NDI Layout & Bible Body" card in `ndi-output-panel.tsx` (writes `lowerThirdHeight` directly; ONE writer per field, lives in NDI panel not Settings). **AUDIT RULE**: every settable field in `Settings` interface MUST have ≥1 writer in `src/components/` outside `resetSettings()` — `rg -n "updateSettings\\(\\{[^}]*<fieldName>" src/components/` should return ≥1 match per writable field; zero matches = unreachable field = ship-blocker bug. Inline help text under each control (not tooltips) is the operator-facing pattern. When operator says "I changed it but didn't apply," ALWAYS audit whether the UI control they say they changed actually writes the store field they think they changed before proposing renderer-side fixes. Full text in `artifacts/imported-app/CHANGELOG.md` and git history.


-   **v0.7.193-hotfix.3 — Autofit ResizeObserver removal (hotfix.2 regression rollback) (one-line GR)**: Hotfix.2 Fix H ResizeObserver belt-(b) on verse parent caused visible scale pumping because each SSE render (mediaCurrentTime / frame writebacks / audio-level pushes) nudges parent.clientHeight by sub-pixels during reflow → RO fires → fitVerseTextForce → another reflow → loop. Removed RO + `__ensureFitObserver` hook entirely (`route.ts` L967-976 + L1681); 2 remaining belts (`document.fonts.ready` + 250ms timer) are sufficient because both are ONE-SHOT per iframe load. NEVER re-add a ResizeObserver inside the renderer that calls anything mutating the observed subtree (autofit writes `<p>.style.fontSize` → changes scrollHeight → changes parent.clientHeight → fires RO → autofit again). Autofit re-runs MUST be triggered by EXTERNAL events only (window resize, font load, slide change, one-shot timer) — NEVER by layout-mutation events from the renderer itself. Hotfix.2 Fixes A-G all unchanged. Verify with `rg -n "ResizeObserver|__ensureFitObserver" src/app/api/output/congregation/route.ts` returns ZERO matches. Full text in `artifacts/imported-app/CHANGELOG.md` and git history.

-   **v0.7.193-hotfix.2 — Per-surface media transport split + Settings round-trip text-shrink fix + 6 perf/UX bundle (one-line GR)**: Per-surface mediaPaused/mediaCurrentTime/mediaLoop split (Preview vs Live) writes via VideoTransport `data-surface` attr — preserve symmetry between preview*/live* fields in store.ts (asymmetric pair = silent dead button). goLive auto-pauses Preview. Live writeback throttle 0.10s, Preview 0.25s. PreviewCard ON AIR placard when previewMediaIsLive eliminates duplicate decoder. Skip attachAnalyser when muted. Renderer drift threshold 0.20s (was 0.12s) absorbs OBS playback drift. IntersectionObserver gate on NdiPreviewSurface in-app iframe ONLY (do NOT extend to NDI capture URL / OBS Browser Source URL / secondary-screen Electron BrowserWindow — those are external consumers per v0.7.193 GR-B). Settings round-trip text-shrink fix: 3 belts (`document.fonts.ready` + ResizeObserver + 250ms timer) all call `fitVerseTextForce()` which clears `__fitKey=''` cache then `fitVerseTextDebounced()` — note ResizeObserver was REMOVED in hotfix.3 due to feedback loop, only 2 belts remain. ON AIR placard ONLY for media slides (verses/custom are cheap, operators expect Preview to match Live). Full text in `artifacts/imported-app/CHANGELOG.md` and git history.


-   **v0.7.193-hotfix.1 — NDI factory defaults bumped to mockup look (one-line GR)**: Fresh-install NDI_DEFAULTS = `lh:lg` (35% bar) + `ndiFontSize:xl` + `ndiRefSize:lg` + `ndiRefScale:1.2` (`store.ts` L674 + `route.ts` L1110/L1116/L1119). Existing-install operators with persisted state DO NOT inherit new NDI_DEFAULTS automatically (by design — explicit picks always win via the v0.7.191 GR-A chain `(USE_NDI_OVERRIDES && st.ndiX !== undefined) ? st.ndiX : NDI_DEFAULTS.x`); reset is via the per-control `__inherit__` option or the "Reset all" button at top of NDI Typography section. There is NO `ndiLowerThirdHeight` field; bar height is shared via `lowerThirdHeight` (do NOT introduce a parallel field). NDI_DEFAULTS.fontSize='xl' + refScale=1.2 is the new baseline; do NOT raise further without re-checking text-shadow blur for bigger sizes (`maxK=isLT?3.50:1.00` autofit cap → ~120 px on 1080p, above which `text-shadow:0 2px 12px rgba(0,0,0,.4)` softens visibly). OBS Browser Source caches CSS — operators MUST right-click → "Refresh cache of current page" after any NDI typography defaults change, or the new CSS won't paint. Full text in `artifacts/imported-app/CHANGELOG.md` and git history.


-   **v0.7.193 — Media transport overhaul: real React `<video>` in Preview/Live + full Play/Pause/Stop/Loop deck + smooth handoff (one-line GR)**: Media-video slides MUST mount a real React-managed `<video>` (`MediaVideoSurface` in `logos-shell.tsx`) inside both PreviewCard AND LiveDisplayCard, NEVER an iframe (iframe contents can't be transport-controlled by the in-app deck). External surfaces (NDI capture, OBS Browser Source, secondary-screen Electron BrowserWindow) MUST stay iframe-based and read `mediaPaused`/`mediaCurrentTime`/`mediaLoop`/`globalMuted` from SSE — they are independent processes. `data-surface="preview"|"live"` attr is the contract between MediaVideoSurface and VideoTransport's `document.querySelector` selector — removing/renaming silently breaks all transport buttons. Preview anti-echo gate: `forceMute = previewMediaIsLive || !previewAudio` (NEVER simplify to just `!previewAudio` — operator hears double-track + delay). Seek tolerance gate: `Math.abs(video.currentTime - storeTime) > 0.25s` before writing, otherwise `timeupdate` events feedback-loop through the store and the playhead stutters (don't shrink below 0.1s). `goLive()` MUST write `setMediaPaused(false)` BEFORE `setIsLive(true)` (live MediaVideoSurface mounts on isLive flip and reads paused state at mount-time — wrong order = frozen live video). Full text in `artifacts/imported-app/CHANGELOG.md` and git history.

-   **v0.7.192-hotfix.2 — LT edge-to-edge width + OBS auto-follow NDI mode + ref autofit (one-line GR)**: `.lower-third` MUST have `padding:0` (NEVER `0 17.5%` — the inner flex eats width before `.lt-box width:100%` can fill); IS_NDI mode resolution MUST be `IS_NDI && SSE_state ? SSE : FORCE_fallback` (NEVER `FORCE_X ? X : SSE` — breaks OBS auto-flip on URL paste); `.slide-reference` autofit binary search is LT-only via selector `'#output .lt-box .slide-reference'` and MUST run BEFORE body fit + invalidate `__fitKey=''` after; ref cap `rHi=2.0` is HALF body cap `maxK=isLT?3.50:1.00` (asymmetry is intentional — verse stays visual anchor). All three IS_NDI surfaces (NDI capture, NdiPreviewSurface, OBS via buildCongregationParams) MUST keep `?ndi=1` in URL params or the precedence fix doesn't apply. Full text in `artifacts/imported-app/CHANGELOG.md` and git history. **Fix 1 — `.lower-third{padding:0 17.5%}` → `padding:0`** (`route.ts` L126): the v0.7.192 `ltStyle` change to `left:1%;right:1%` set the OUTER abs-positioned wrapper edge-to-edge, but the inner `.lower-third` flex container had a baked-in `padding:0 17.5%` rule (~v0.7.176 era — comment at L117 said "wide layout" but the rule beneath said otherwise) that ate 35% of the frame width before `.lt-box width:100%` could fill anything. Visible dark card capped at ~65% width centered, regardless of operator's lh pick. With padding:0, `.lt-box` now spans the full outer wrapper (left:1%/right:1% from ltStyle) → ~98% of frame width edge-to-edge. Text inside still gets `.lt-box{padding:3% 5%}` text inset. **Fix 2 — Precedence flip on IS_NDI surfaces** (`route.ts` L1019-1032): pre-fix `dm = FORCE_FULL ? 'full' : (FORCE_LT ? 'lower-third' : ((IS_NDI && ndiDisplayMode) ? ndiDisplayMode : displayMode))`. FORCE_LT (URL ?lowerThird=1) won over `ndiDisplayMode` SSE state, so an OBS Browser Source URL pasted while in LT mode would pin to LT forever — even after operator flipped the app to Full Display. NDI capture and in-app NDI Preview don't show the bug because Electron rebuilds the BrowserWindow URL on every mode flip; OBS is external — URL pasted once, never rebuilt. Post-fix: `dm = FORCE_FULL ? 'full' : ((IS_NDI && (ndiDisplayMode==='full'||ndiDisplayMode==='lower-third')) ? ndiDisplayMode : (FORCE_LT ? 'lower-third' : displayMode))`. IS_NDI surfaces (NDI capture, NDI Preview, OBS — all carry `?ndi=1`) follow live SSE state and auto-flip. Settings PREVIEW cards (use `?lowerThird=1` WITHOUT `?ndi=1`) fall through to FORCE_LT branch and stay pinned per card as before — no regression. Validation triple `ndiDisplayMode==='full'||==='lower-third'` rejects garbage values, defends against future enum extensions. **Fix 3 — Reference autofit binary search** (`route.ts` L831-859, ~12-line block at top of `fitVerseText`): `.slide-reference` (the "Esther 2:3 — KJV" line) was wired to `font-size:clamp(.7rem,min(2cqw,4cqh),1.4rem)` (L187). Inside a 35% × 1080p bar, container queries resolve to ~15 px (4cqh of 378 px) which floors below the 1.4rem cap → reference rendered ~15 px regardless of bar size. The cap was the problem, but raising the clamp would still under-fill on wide bars. Real fix: add reference to the binary-search autofit pass (mirrors v0.7.190-hotfix.2 body fix), with a smaller cap (2.0× vs body's 3.50×) so verse remains visual anchor. Uses `getComputedStyle(rEl).fontSize` (NEVER `rEl.style.fontSize` — v0.7.190-hotfix.2 GR-A: clamp() is a CSS function, parseFloat returns NaN). Runs BEFORE the body fit so the body's sibling-height subtraction (L860-862, formerly L859-862) picks up the ref's new offsetHeight on the same pass. Cache invalidation `__fitKey=''` after ref fit forces body to re-binary-search for the new available height — without this, parent.clientHeight is unchanged but ref's offsetHeight grew, cached key would short-circuit and body would render at OLD (smaller-ref) scale. **Files**: `route.ts` (~30 lines net: 1-byte CSS edit L126, 14-line precedence rewrite L1019-1032, 30-line ref-autofit insert L831-859; non-LT/non-NDI/fullscreen branches byte-identical), `package.json`+`BUILD.bat`→0.7.192-hotfix.2. `tsc --noEmit` clean. **GUARD-RAIL (A) — `.lower-third` padding rule is the primary width gate**: do NOT add side padding back to `.lower-third` thinking "the bar is too wide on phones / mobile previews." The bar is BUCKET-LOCKED via `ltStyle` insets (`left:1%;right:1%` — v0.7.192 GR-B) and parent dimensions; padding here would re-introduce the centered-narrow bug. If a future operator wants a centered narrow LT (for sermons / quotes), add a NEW height-bucket-style picker (e.g. `lowerThirdWidth: 'narrow'|'wide'`) that selects between `ltStyle` insets, NOT a CSS rule on `.lower-third`. Verify with `rg -n "\.lower-third\{" src/app/api/output/congregation/route.ts | rg "padding:0[^.]"` returns the L126 line with `padding:0` (no `0 17.5%` or similar). **GUARD-RAIL (B) — IS_NDI mode resolution: SSE wins, FORCE is fallback only**: any new `dm` resolution branch added to the renderer MUST use the pattern `IS_NDI && SSE_state ? SSE : FORCE_fallback : displayMode`, NEVER `FORCE_X ? X : (IS_NDI && SSE_state ? SSE : displayMode)` (the pre-hotfix.2 broken precedence). FORCE_X URL params exist for Settings preview cards ONLY (which are NOT IS_NDI). All three IS_NDI surfaces (NDI capture URL builder in `electron/main.ts` ~L2316, NdiPreviewSurface src in `ndi-output-panel.tsx` L1184-1212, buildCongregationParams in `ndi-output-panel.tsx` L93-108 = OBS) MUST keep `?ndi=1` in their params for this fix to apply — dropping `ndi=1` from any of them would re-introduce the OBS-stuck bug for that surface. Verify with `rg -n "p\.set\('ndi', '1'\)" src/components/views/ndi-output-panel.tsx` returns 2 matches (buildCongregationParams + NdiPreviewSurface) and `rg -n "ndi=1|ndi', '1" electron/main.ts` returns at least the FrameCapture URL builder. **GUARD-RAIL (C) — Reference autofit cap 2.0× is HALF the body cap (3.50×); do NOT unify**: the asymmetry is intentional — the operator's red-inside mark showed reference noticeably smaller than body, with body as the visual anchor. Unifying caps (e.g. both 3.50×) would let short refs balloon equal to or larger than the verse body, which inverts visual hierarchy. Conversely, raising the body cap further without raising the ref cap would shrink the relative ref proportion. Verify with `rg -n "rHi=2\.0|maxK=isLT" src/app/api/output/congregation/route.ts` returns ref bound `rHi=2.0` AND body bound `maxK=isLT?3.50:1.00` — both must stay set together. **GUARD-RAIL (D) — Ref autofit MUST run BEFORE body autofit AND invalidate __fitKey**: the ordering is load-bearing — body's available-height calc (L860-862, sibling subtraction `parent.clientHeight - rEl.offsetHeight`) reads ref's CURRENT offsetHeight. If body fit ran first, then ref grew, body would be sized for the OLD smaller ref → either overflow (text clipped at bottom) or under-fit (gap below body). Cache invalidation `__fitKey=''` after ref fit is required because `__fitKey` only includes textLength + clientWidth + clientHeight; ref's offsetHeight change is invisible to the key. Without invalidation: same verse, second render → ref grows correctly, body cache hits → body stays at OLD scale. Verify with `rg -n "__fitKey=''" src/app/api/output/congregation/route.ts` returns 2 matches: 1 in ref-autofit branch (L857), 1 in slide-empty branch elsewhere if present. **GUARD-RAIL (E) — Ref autofit is LT-only via selector `'#output .lt-box .slide-reference'`**: do NOT change the selector to `'.slide-reference'` (matches fullscreen too) thinking "the fullscreen reference also looks small." Fullscreen uses a different layout where ref typography is operator-tunable via `referenceFontSize` and growing it via binary search would override the operator's pick silently. The `.lt-box` qualifier in the selector ensures ref autofit ONLY runs when the verse is rendered inside the LT bar where the bar's auto-grown body needs a proportional ref. Verify with `rg -n "lt-box .slide-reference" src/app/api/output/congregation/route.ts` returns the autofit selector AND the CSS rule (L187). **PROCESS GR — Pre-shipment mockup proof closes the loop in ONE turn**: operator has rejected the v0.7.192 ship after install ("text is still small") and the v0.7.192-hotfix.1 ship for not addressing the width AND ref issues. For hotfix.2, I built a 4-panel HTML mockup (BEFORE/AFTER LT width + 3-step OBS sequence) at `public/hotfix2-mockup.html`, screenshotted it via `screenshot` tool, presented as `screenshots/hotfix2-mockup.jpg`. Operator said "yes" same turn. Lesson: when ANY visual change ships, ALWAYS build a static HTML mockup with the BEFORE state on the left and the AFTER state on the right (red-dashed overlays for any geometric targets), present, wait for "yes" — single turn vs N turns of install→reject→re-diagnose.

-   **v0.7.192-hotfix.1 — Resurrect dead LT height-bucket resolution (one-line GR)**: `var __lhKey='sm'; var hPct=22` declarations MUST be paired with an explicit `__lhKey=(st.lowerThirdHeight==='sm'||==='md'||==='lg') ? st.lowerThirdHeight : (FORCE_LH||'sm'); hPct = hMap[__lhKey]||22;` reassignment in the same scope BEFORE the `ltStyle` consumer reads them. Pattern `var X='default'; var Y=N;` followed by ~30 lines of comments and another consumer is the EXACT shape that swallowed this bug for ~50 builds — comments lie when the code drifts. New LT enum buckets (xl/xxl) MUST extend BOTH `hMap` AND the validation triple `==='sm'||==='md'||==='lg'` AND the FORCE_LH parser at L303. Hotfix lineage uses `0.7.X-hotfix.N` prerelease syntax (NEVER 4-segment). Full text in `artifacts/imported-app/CHANGELOG.md` and git history.

-   **v0.7.192 — LT bar geometry expand (one-line GR)**: `hMap={sm:22,md:33,lg:35}` (NEVER widen `lg` past 35% silently — operator's red-box target). `ltStyle` insets MUST stay symmetric (`left:1%;right:1%`, `top:3%`/`bottom:3%`). Autofit `maxK=isLT?3.50:1.00` is the new cap; raising further requires re-tuning text-shadow blur. Bold weight 700 is global for `.slide-text` AND `.slide-title` inside `.lt-box`. OBS=NDI param parity check across `buildCongregationParams` + `electron/main.ts` FrameCapture + `NdiPreviewSurface` is a 30-second pre-flight before any LT geometry change. Full text in `artifacts/imported-app/CHANGELOG.md` and git history.

-   **v0.7.191 — NDI true isolation from app settings (one-line GR)**: NDI typography MUST never fall through to app settings — pattern `USE_NDI_OVERRIDES ? (st.ndiX || NDI_DEFAULTS.x) : <existing-app-or-LT-chain>` (NEVER `(USE_NDI_OVERRIDES && st.ndiX) ? st.ndiX : st.X`). NDI_DEFAULTS lives in renderer (route.ts ~L1061-1085), NOT in store.ts. Reference typography (`rfFam`/`rfShOn`/`rfTa`) MUST also gate on `USE_NDI_OVERRIDES`. In-app LT inheritance from app settings is INTENTIONAL — do NOT copy this pattern to `USE_LT_OVERRIDES`. Full text in `artifacts/imported-app/CHANGELOG.md` and git history.

-   **v0.7.190-hotfix.2 — Autofit baseSize REAL fix (parseFloat-clamp NaN bug)**: Operator after v0.7.190 install (and Replit-preview test on hotfix.1): "All text are not like this when I checked on Replit preview, they still as I complain can't be read" — verses tiny on EVERY surface (Stage Preview, Live Display, Bible Lookup card, NDI/OBS), Settings → Typography Small/Medium/Large/Extra-Large buttons silently no-op. Diagnosis: v0.7.190 Fix A logically did NOTHING. The renderer at `route.ts ~L1435` applies the operator's typography pick as a CSS clamp() expression set INLINE on the verse `<p>` (e.g. `style="font-size: clamp(1rem, min(7.04cqw,12.8cqh), 4rem)"`). Fix A read `p.style.fontSize` first (which returns the AUTHORED string `"clamp(1rem, min(...), 4rem)"`, NOT the resolved px) → `parseFloat("clamp(...)")` returns NaN → `baseSize ||= 16`. Binary search then ran on `[16 × 0.6, 16 × 2.5] = [9.6 – 40 px]` regardless of bucket pick. Hotfix.2 swaps to `getComputedStyle(p).fontSize` which RESOLVES the clamp/cqw/cqh against the iframe's 1920×1080 viewport into actual pixels (e.g. ~64 px for Large at the clamped cap), so binary search runs on `[64 × 0.6, 64 × 2.5] = [38 – 160 px]`. Bucket then propagates because FS_MULT (`sm:.85, md:1, lg:1.25, xl:1.5`) is already baked into `ltBand`/`ltCap`/`ltMin` upstream of the clamp at L1419-1434. **Files**: `route.ts` (1 line at L850-855: `var baseSize=parseFloat(window.getComputedStyle(p).fontSize)||16;` replacing the broken inline-read chain). `package.json`+`BUILD.bat`→`0.7.190-hotfix.2`. `tsc --noEmit` clean. **GUARD-RAIL (A) — NEVER trust `el.style.fontSize` when the inline rule is a CSS function (clamp / calc / min / max / var)**: `el.style.<prop>` returns the AUTHORED value (the literal CSS string). For function values (`clamp(...)`, `min(...)`, `calc(...)`, `var(--x)`), parseFloat returns NaN. To get the resolved px MUST use `getComputedStyle(el).<prop>` which evaluates the cascade + functions + container queries. Verify with `rg -n "p\.style\.fontSize" src/app/api/output/congregation/route.ts | rg -v "(=|.toFixed|/\*|//)"` — only the WRITE sites should remain (line 838 cached-reapply, line 886 binary-search-iter, line 893 final-apply); never a READ for measurement. **GUARD-RAIL (B) — Do not "improve" by stripping the inline FIRST**: an "obvious" alternative is `p.style.fontSize=''; var b=getComputedStyle(p).fontSize`. This BREAKS because it falls back through the cascade to `body { font-size: 16px }` which discards the operator's bucket pick. The clamp() is the WHOLE POINT — getComputedStyle resolves it WITH the inline rule still in place, returning bucket-aware px. Verify the path: clear → measure → re-apply is forbidden; the correct path is measure-while-clamp-still-set → use as base → search → final-apply. **GUARD-RAIL (C) — Container-query units only resolve in the iframe's REAL viewport**: this fix relies on the iframe being rendered at NATIVE 1920×1080 (per v0.7.177 GR — `OutputPreview` + `NdiPreviewSurface` use transform-scale into card). If a future "optimization" sizes the iframe at the card's physical pixel size (e.g. 360×203), `cqw`/`cqh` resolve to tiny values, the clamp floors at `min` (1rem=16px), `getComputedStyle` returns 16, and we're back to the broken state with no inline-read in sight. The native-viewport rendering AND the getComputedStyle measurement are TWO LEGS OF THE SAME STOOL — touching either re-introduces the bug silently. **PROCESS GR**: when a "fix" is shipped and operator immediately reports the same bug, do NOT re-patch on the same assumption — read the code AGAIN and trace the actual values returned at each step. v0.7.190 Fix A passed `tsc --noEmit`, passed code review, and shipped — but the fundamental misunderstanding (`p.style.fontSize` returns parseable px) was never tested at runtime. The Replit preview is the cheapest place to catch this; in future, screenshot the Replit preview after every autofit-related change BEFORE pushing to CI.

-   **v0.7.190-hotfix.1 — Thumbnail event-bubble fix + Admin date/time columns (one-line GR)**: wrapper-with-internal-buttons MUST `stopPropagation()` on EVERY internal button onClick (verify `rg -n "onClick=\{\(e\) => \{ e.stopPropagation" src/components/layout/library-compact.tsx` covers all internals). Activation records use `a.generatedAt` (per `ActivationCodeRecord`); payments use `p.createdAt` — do NOT unify. Hotfix lineage uses `0.7.X-hotfix.N` prerelease syntax (3-segment + `-prerelease.N`); 4-segment (`0.7.190.1`) re-introduces v0.7.184.2 mangling regression. Full text in `artifacts/imported-app/CHANGELOG.md` and git history.

-   **v0.7.190 — Media-slide type + #bgLayer 0×0 + autofit baseSize (one-line GR)**: uploaded media MUST become `{type:'media', mediaUrl, mediaKind}` NEVER `{type:'custom', background:dataUrl}` — the latter bypasses the v0.7.186 audio + anti-echo + freeze-preview-on-go-live infrastructure (3 gates in `logos-shell.tsx` L872/883/1030). Click-to-preview / dbl-click-to-live MUST mirror verse-list pattern (L534-543). `#bgLayer` base CSS MUST have explicit `width:100%;height:100%` (fallback for `displayRatio:'fill'` default — see also v0.7.187). Autofit baseSize: superseded by v0.7.190-hotfix.2 GR-A (use `getComputedStyle(p).fontSize` not `p.style.fontSize`). Full text in `artifacts/imported-app/CHANGELOG.md` and git history.

-   **v0.7.189 — LT (lower-third) bg-video persistence (one-line GR)**: LT bg `<video>` MUST use module-level `__ltBgEl` cache + `mountLtBg()` MOVE-not-CLONE pattern (`box.insertBefore(cachedNode, firstChild)`) so decoder survives innerHTML rewrites; never `cloneNode`/`replaceWith`. LT cache release is LT-branch-only; do NOT add `ensureLtBgEl('')` to the 5 non-LT `setBgVid('')` sites or freeze returns on next LT entry. Fullscreen `#bgLayer` and LT `__ltBgEl` are SEPARATE caches by design — never merge. Full text in `artifacts/imported-app/CHANGELOG.md` and git history.

-   **v0.7.188 — LT autofit SCALE-TO-FILL + Re-mention auto-live (one-line GR)**: Autofit "fit AND grow" requirement MUST use binary search on `style.fontSize` (NOT `transform:scale()` — paint-only, capped by `overflow:hidden` flex parent). Branch detection via `p.closest('.lt-content,.lt-box')` — never hardcoded `IS_NDI`. Re-mention promote in `store.ts addDetectedVerse` MUST `...existing` spread (preserve source/confidence) + new id + fresh detectedAt; never replace the entry. Auto-live `useEffect` dependency MUST stay on `detectedVerses` array reference, never `detectedVerses[0]?.id`. Full text in `artifacts/imported-app/CHANGELOG.md` and git history.

-   **v0.7.187 — Persistent BG-video layer (one-line GR)**: cross-render media MUST live OUTSIDE `#output` as a sibling div with module-level setter + URL-keyed no-op + explicit clear guards on every state-transition path; every `innerHTML=` reassignment in route.ts MUST be paired with a `setBgVid` call within ±20 lines. Layer base CSS MUST set explicit width/height (see v0.7.190 GR-C — fallback for `displayRatio:'fill'` default). Full text in `artifacts/imported-app/CHANGELOG.md` and git history.





### Older releases — grouped guard-rails (full text in `artifacts/imported-app/CHANGELOG.md` + git history)

**Versioning / CI / packaging** — `package.json#version` MUST be 3-segment `MAJOR.MINOR.PATCH` (+ optional `-prerelease.N`); 4-segment mangles installer filenames and breaks the upload glob (v0.7.185). Hotfix lineage = `0.7.X-hotfix.N` prerelease syntax. CI red → first inspect the GH Actions log via `/repos/OWNER/REPO/actions/runs?per_page=1`, never assume code regression. `electron-builder.yml` keeps full MUI2 wizard (`oneClick: false`); installer header BMP MUST be BMP3 24-bit 150×57; `license.txt` ASCII only — UTF-8 bullets break NSIS (v0.7.124-126/v0.7.139). `installer.nsh` macros (`customInit`/`customUnInit` task-kill, `customHeader`/`customWelcomePage`/`customFinishPage` branding, `customInstall` post-install Launch MessageBox with `IfSilent skipLaunchPrompt` for auto-updater) MUST stay split (v0.7.142-143). Every `build-resources/<asset>` shipped via electron-builder needs an explicit `!build-resources/<asset>` carve-out in `.gitignore` — never flat allow-all (v0.7.175). `.local/push-r.mjs` requires `TAG/MSG/OWNER=nanvilow/REPO=scripturelive-ai/BRANCH=main/GH_PAT` env.

**Webpack / Next runtime** — Node-only `require('node:*')` reachable from any client-graph module MUST use `eval('require')`, NOT static `require()` — webpack client pass scans the whole module graph and crashes with `UnhandledSchemeError` regardless of `typeof window` guards (v0.7.171). Node-only API in Next 16+ entry points (`instrumentation.ts`, etc.) MUST live behind dynamic `import()` gated on `process.env.NEXT_RUNTIME==='nodejs'` — `if(!isNode)return` guards aren't enough (v0.7.170). New `src/data/*.json` MUST be added to BOTH `outputFileTracingIncludes` in `next.config.ts` AND `loadTranslation()` switch in `local-bible.ts` (v0.7.168).

**Renderer / `route.ts` template literal** — The renderer script in `route.ts` IS itself a tagged template literal: backticks anywhere break the build, regex needs `\\.`/`\\/`/`\\d` double-escape (v0.7.156), inline comments must use `/* */` not `//`. Cross-render renderer state lives in MODULE-LEVEL closure, NEVER on `data-*` attrs of `#output` children (subtree is destroyed every render — v0.7.184.1). `safeBgUrl()` allowlist + `escAttr()` 
-   **v0.7.194-hotfix.3 — NDI capture resolution toggle (720p) + 15fps option + hardware-tip banner (one-line GR)**: `ndiCaptureResolution` MUST be wired through restartGuardRef token + restart-effect deps + BOTH `ndi.start` callers in `ndi-output-panel.tsx` (same audit pattern as hotfix.2 GR-C for `ndiCaptureFps`) — without all three, `main.ts` L2281-2282 width/height equality short-circuit silently swallows operator resolution changes. Default `ndiCaptureResolution='1080p'` is intentional; lowering would surprise operators with quality drop they didn't ask for. Hardware-tip banner copy ordering (720p FIRST then 15fps) is calibrated to per-frame relief magnitude (720p = 56% per-frame work drop > 15fps temporal 50% drop AND 720p preserves motion smoothness). `easyworship-shell.tsx` L242 still hardcodes 30fps/1080p — leave as dead code on operator path. **PROCESS GR**: when operator says "still lagging" after a code-level fix shipped + verified, next diagnostic question is hardware specs, NOT more code. Full text in `artifacts/imported-app/CHANGELOG.md` and git history.

-   **v0.7.194-hotfix.2 — NDI video media + bg video freeze fix (drift-seek gate) + operator FPS knob (one-line GR)**: `IS_NDI` MUST gate the drift-seek branch at `route.ts` L1392-1395 (`if(!IS_NDI&&...)`) — NDI capture surface uses Electron offscreen rendering = Chromium software video decode on Windows = real-time decode slower than wall-clock; without the gate, ~10 drift-seeks/sec freeze the foreground video. Electron offscreen + GPU video decode on Windows is NOT viable (CPU-side BGRA readback stalls pipeline); do NOT propose. `ndiCaptureFps` MUST be in `restartGuardRef` token + restart-effect deps + BOTH `ndi.start` callers; `main.ts` L2283 `cur.fps===opts.fps` short-circuit silently swallows missing wires. Default `ndiCaptureFps=30` is calibrated for median operator PC (~33ms budget vs 60fps's ~16ms = 2× headroom for HD decode + layout + readback). **PROCESS GR**: pre-ship clarifying questions ("will GPU fix it?") deserve architectural answers (NO + why + alternative), not single-word yes/no — saves rework cycles. Full text in `artifacts/imported-app/CHANGELOG.md` and git history.
are the only XSS guard for the unauth `/api/output/congregation` push (v0.7.155). Every `innerHTML=` reassignment MUST be paired with a `setBgVid` call within ±20 lines (v0.7.187 GR). All 6 verse-display surfaces go through the SINGLE renderer via `<OutputPreview>`/`<NdiPreviewSurface>` — never re-introduce a parallel React mirror; extend `buildOutputPayload()` instead (v0.7.158/v0.7.127/v0.7.136). Operator-facing iframes (in-app NDI Preview, Bible Lookup) MUST omit `transparent=1`; only true NDI/OBS capture surfaces keep it (v0.7.159).

**LT / NDI / OBS parity** — Any URL emitter targeting NDI/OBS/vMix/Wirecast (`buildCongregationParams`, `buildObsUrl`, `electron/main.ts` FrameCapture, `NdiPreviewSurface`) MUST bake the four LT params (`ndi=1`+`transparent=1` always, `lowerThird=1`+`position`+`lh`+`sc` when `ndiDisplayMode==='lower-third'`) — all four emitters in lockstep (v0.7.181/v0.7.178). Never default `IS_NDI=true` on the route (Live Display, secondary screen, Bible Lookup all rely on `IS_NDI=false`). For new `ndi*` settings the resolver chain is `(USE_NDI_OVERRIDES && st.ndiX !== undefined) ? st.ndiX : (USE_LT_OVERRIDES && st.lowerThirdX !== undefined) ? st.lowerThirdX : st.bodyX` (v0.7.167/v0.7.178). LT bar geometry: `hMap={sm:22,md:33,lg:35}` (NEVER widen `lg` past 35%); `ltStyle` insets symmetric (`left:1%;right:1%`, `top:3%`/`bottom:3%`); autofit `maxK=isLT?3.50:1.00`; bold weight 700 global inside `.lt-box` (v0.7.192). NDI typography MUST never fall through to app settings — gate on `USE_NDI_OVERRIDES` always; reference typography (`rfFam`/`rfShOn`/`rfTa`) gates the same way (v0.7.191). NEVER copy `USE_NDI_OVERRIDES` isolation pattern to `USE_LT_OVERRIDES` — in-app LT inheritance from app settings is INTENTIONAL.

**Autofit / typography** — Body autofit binary-search measures `p.scrollHeight` NOT `parent.scrollHeight` (parent lies under nested flex+`overflow:hidden`); subtract sibling heights from `parent.clientHeight` for available height (v0.7.184.2). NEVER trust `el.style.fontSize` when the inline rule is a CSS function (`clamp/calc/min/max/var`) — use `getComputedStyle(el).fontSize` to RESOLVE the clamp/cqw/cqh against the iframe's native viewport (v0.7.190-hotfix.2 GR-A). Iframe MUST render at native 1920×1080 then CSS `transform:scale()` into the card (v0.7.177) — `cqw`/`cqh` floor at clamp minimum on small viewports otherwise. NATIVE_W flexes by aspect (16:9→1920, 4:3→1440, 21:9→2520) — new `displayRatio` MUST add a matching NATIVE_W branch. Autofit target selector is `#output .slide-paragraph` ONLY — targeting the container shrinks every verse globally (v0.7.183).

**Detection / auto-live / speech** — `addDetectedVerse` navigator-fire MUST be at TOP, BEFORE any dedupe early-return; every upstream dedupe gate in `speech-provider.tsx` returning early WITHOUT calling `addDetectedVerse` MUST also fire `requestNavigatorRef(ref)` on the suppress branch (4 sites, v0.7.184/v0.7.184.2). `addDetectedVerseCandidate` (speculative <50%) deliberately NOT wired — would thrash navigator. Cross-pipeline preempt: SEMANTIC (BRQ) preempts EXPLICIT (AVM) via `verse-auto-live.ts` latch fields `semanticOwnsLive`/`frozenExplicitId`; reverse direction held by `CROSS_PIPELINE_SEMANTIC_VS_EXPLICIT_MIN=0.58` (v0.7.150-152). Same-pipeline `LIVE_HIGH_CONF_LOCK=0.85`. Manual operator click bypasses these gates. LLM throttle: `LIVE_STICKY_MS=4000`, `DEFAULT_TIMEOUT_MS=1200`, `SEMANTIC_THROTTLE_MS=1000` — tune in 1s/200ms increments, never doubling (v0.7.169). Voice/Bible homophones live in `VOICE_COMMAND_ALIASES` (`voice/commands.ts`) + `BOOK_HOMOPHONES` (`bible-api.ts ~L545`) — single source of truth, NEVER bespoke `if(transcript.includes())` branches. Book-homophone rules MUST require verse-reference context follower (chapter/digit/number-word) — never bare `\b<word>\b` (v0.7.174). Don't escalate Deepgram keyterm count past ~80 (hurts adjacent-word recall). Renderer Deepgram WS params MUST stay lockstep with api-server proxy (v0.7.166). `attachAnalyserSilent()` in `audio-level.ts` MUST NOT call `analyser.connect(audioContext.destination)` — verify `rg -n destination src/lib/audio-level.ts` returns zero in that fn (v0.7.186).

**License / cloud sync / admin** — Atomic-write JSON for `LicenseFile`; `BAKED_CLOUD_ADMIN_CODE` in `src/lib/baked-credentials.ts` (gitignored) MUST stay lockstep with `scripts/inject-keys.mjs:173` AND `src/lib/licensing/storage.ts` self-heal (v0.7.161/v0.7.179). Self-heal `else if (!override && persisted!==BAKED)` branch MUST come AFTER the `if (override)` env-override branch — flipping order lets baked clobber env override on cloud. Cross-device sync routes (`/api/license/cloud/admin-{snapshot,merge}`, `claim-activation`, `mirror-payment`) — cloud at `scripturelive.replit.app` MUST be redeployed from same commit BEFORE customers update or v0.7.145+ activation cloud-fallback hits 404. `applyAdminLedgerSnapshot` activation merge propagates `isUsed`/`usedAt`/`subscriptionExpiresAt` (monotonic isUsed, latest-wins on dates, v0.7.166). `cloudPullAdminLedgerCached()` (30s TTL + 5s SWR) is canonical for ALL `/api/license/admin/*` GET handlers — never call raw `cloudPullAdminLedger()` (v0.7.173). Admin-modal poll intervals (overview 30s, codes 30s, records 60s, cloud-test 60s) MUST be tab-gated (v0.7.166/v0.7.183). Admin-modal data column pattern: select / select-all / delete-all / delete-(N) with `useConfirm()`; backend `bulk-delete` accepts `kind: 'payment'|'activation'|'notification'` (v0.7.172). Cross-device sync revenue = PAID + CONSUMED only, never WAITING_PAYMENT (v0.7.162).

**State persistence / migrations** — Any `store.ts migrate:` block MUST bump `version: N+1` (Zustand only runs migrations where `version < currentVersion`; missing bump = dead code, v0.7.184.1). When narrowing a union type to suppress dead branches, persist-migration code comparing legacy values needs `Omit<Partial<T>,"narrowedField"> & {narrowedField?: string}` cast for TS2367-safe equality (v0.7.184). Removing UI for a state field MUST ALWAYS bump version + add coercion branch (v0.7.182). Existing-install operators with persisted state DO NOT inherit new defaults automatically (by design — explicit picks win).

**File upload (Chromium 130+)** — `<input type="file">` paired with programmatic `.click()` MUST use `className="sr-only"` (NEVER `.hidden` — keeps input out of the layout tree → `onChange` never fires). Audit: `rg -nC 5 'type="file"' src/ | rg -B 3 -A 3 'className="hidden"'` must return zero (v0.7.180).

**Misc UI/UX** — Settings cards: `lg:grid-cols-[1fr_360px]` with `lg:sticky lg:top-2 self-start` right-column preview; never re-introduce full-width preview at the TOP (v0.7.172). NDI/Output status indicators may be removed cosmetically but underlying state + dependent hooks MUST stay intact — chips cosmetic, state load-bearing (v0.7.176/v0.7.144). Update-modal canonical mount = `src/app/layout.tsx` ONLY (NEVER re-introduce in `page.tsx`); `cleanReleaseNotes()` strips raw HTML + `github.com` URLs + `by @user` attributions; `rehype-raw` deliberately disabled (v0.7.132/v0.7.140). Per-version `localStorage["sl.update-popup-dismissed.<version>"]` is the dismissal contract between `<UpdateAvailableDialog>` (writer) and `<UpdateBanner>` (reader) (v0.7.128-129). "Visit website" default = `https://scriptureliveai.com/` in BOTH `src/lib/website-url.ts` AND `electron/main.ts` Help menu lockstep; Replit-app domain stays the API host (v0.7.134). Splash window non-modal (`alwaysOnTop:false`); `loadLogoDataUrl()` candidate order packaged → dev → fallback (v0.7.149).

**Bibles (translations / bundling)** — Bundler default-list = 8 Bibles (~32MB); per-source concurrency `isWldeh ? 12 : 3` (bolls.life HARD ceiling 3 — 429s past that), 429-aware `Retry-After` backoff (v0.7.164). New translation = 2 lockstep edits: stub-loop list in `bundle-bibles.mjs` + `loadTranslation()` switch in `local-bible.ts`. Bundler needs `GH_PAT` (anon raw.github = 60/hr). `WLDEH_TRANSLATIONS` registry in `twi-bible.ts` mirrored in `bundle-bibles.mjs`; Pentateuch slugs differ by language (Twi PREFIX `1mose…5mose`, Ewe POSTFIX `mose1…mose5`); Ewe Judges slug double-encoded-safe via `encodeURIComponent` (v0.7.137). Single Twi key = `TWIASANTE` (Akuapem TWI removed) — 8 lockstep places + persisted-state migration (v0.7.163/v0.7.167). `parseReference()` accepts lowercase 3-letter codes (all 66 books, v0.7.154). `RAW_CATALOGUE` paraphrase corpus in `preacher-phrases.ts` — bare/generic phrases (e.g. "follow me") OMITTED to avoid collisions with adjacent verses (v0.7.147-148).

### Cross-cutting patterns (no specific version)

-   **Hybrid Deployment**: Single codebase ships both the web (`scripturelive.replit.app`) and the Windows desktop app (Electron wrapping the same Next.js).
-   **API Routing**: API server is routed to `/__api-server` to avoid conflicts with Next.js routes.
-   **NDI Integration**: Browser-only NDI output and native NDI sender via Electron, supporting transparent overlays and configurable display modes, with pixel parity via pinned zoom. As of v0.7.146 the runtime DLL ships bundled — see entry above.
-   **Multi-tiered Speech Recognition**: Deepgram + Whisper + browser engines with auto-fallback, VAD, and hallucination guard, complemented by AI semantic matching.
-   **Atomic License Persistence**: Atomic-write JSON file for local license persistence, ensuring data integrity during deactivation/transfer.
-   **Hard Reset for License State Changes**: Critical license state changes (activate/deactivate/transfer) trigger hard `window.location.assign('/')` reload to prevent renderer crashes from stale React contexts. v0.7.107 extended the same recovery to UNCAUGHT renderer exceptions via `app/global-error.tsx` + `app/error.tsx` — any thrown React error hard-reloads to `/` instead of letting Chromium paint chrome-error://chromewebdata.

### Capabilities

-   Dynamic downloads for various OS, with file hashing and multi-threaded support.
-   Real-time slide updates and live congregation output.
-   NDI broadcasting with configurable display modes (lower-third, full-screen).
-   Advanced speech recognition with AI semantic matching for scripture verses.
-   Comprehensive admin dashboard for managing activations, user licenses, and telemetry.
-   Self-hosted, MoMo-based subscription system with free trial and license transfer.
-   Customizable UI/UX with dark/light themes and theme designer.
-   Voice commands for navigation and verse lookup.
-   Automated email and SMS notifications for activations.

## User preferences

-   **Auto-trim `replit.md` without asking**: whenever the Recent (full detail) section grows past 3 entries, automatically demote the oldest full entry to a one-line guard-rail under "Older releases" and (if not already there) append the full text to `artifacts/imported-app/CHANGELOG.md`. Do this proactively as part of every release bump — never ask permission.

-   After EVERY fix / version bump, build and present a fresh ZIP of `artifacts/imported-app/` so the user can download it and run `BUILD.bat` on their Windows PC. Naming convention: `exports/ScriptureLive-AI-v<version>-source.zip`. Exclude `node_modules`, `.next`, `dist-electron`, `dist-electron-ui`, `release`, `exports`, `uploads`, `upload`, `.turbo`, `.git`, `*.tsbuildinfo`, `build-log.txt`. Always use the `present_asset` tool to surface the zip — never assume the user will find it on their own.
-   Bump the `BUILD.bat` banner version string to match the current `package.json` version on every release.
-   Push to GitHub via REST API after each release: OWNER=`nanvilow`, REPO=`scripturelive-ai`, BRANCH=`main`, token from `GH_PAT` env. Tag every release `v<version>`.
-   When `replit.md` gets long, archive older Architecture-decisions entries to `artifacts/imported-app/CHANGELOG.md` and leave one-line guard-rails behind. **Do this automatically without asking** — at every release bump, keep only the LAST 3 versions in "Recent (full detail)" and demote everything below into one-line guard-rails / move full text to CHANGELOG.
-   **SCREENSHOT BEFORE SHIPPING (added v0.7.194-hotfix.7+)**: from now on, for every fix or change, show the operator a screenshot of the work (Replit preview pane via `screenshot` tool on `artifacts/imported-app` web service, or a code-diff screenshot when UI is not visually testable) BEFORE running the push script. WAIT for the operator's explicit "ship"/"yes ship"/"go" before invoking `.local/push-r.mjs`. Do NOT push proactively even after typecheck-clean. This overrides any prior "fix and push" interpretation.

## Gotchas

-   `persist()` operations are fire-and-forget async writes to prevent UI freezes, with retries for disk contention.
-   License state changes trigger hard page reloads; avoid complex UI interactions immediately after them.
-   NDI sender has a 60-second "linger mode" after disconnect to maintain OBS/vMix connections.
-   Renderer crashes are handled by a crash mask that attempts recovery and logs full history.
-   Auto-live confidence thresholds (current as of **v0.7.127**): explicit ≥0.60, semantic ≥0.50, suggestions 0.10–0.499, **1.25 s anti-flicker dwell**, newest-first ordering. The `source==='suggestion'` bypass in `suggestionsFor()` was REMOVED — upstream taggers must keep suggestion-tagged confidence inside the 0.10–0.499 band or the verse is dropped from the UI entirely.
-   The "Detected Verses" card separates explicit / semantic / suggested verses into three columns, each with independent auto-live decisions.
-   The cloud at `scripturelive.replit.app` MUST be redeployed from the same commit BEFORE customers update; otherwise v0.7.145+ activation cloud-fallback hits 404.

## Pointers

-   **Next.js Docs**: [https://nextjs.org/docs](https://nextjs.org/docs)
-   **Express Docs**: [https://expressjs.com/](https://expressjs.com/)
-   **Drizzle ORM Docs**: [https://orm.drizzle.team/](https://orm.drizzle.team/)
-   **Zod Docs**: [https://zod.dev/](https://zod.dev/)
-   **pnpm Workspaces**: [https://pnpm.io/workspaces](https://pnpm.io/workspaces)
-   **Electron Docs**: [https://www.electronjs.org/docs](https://www.electronjs.org/docs)
-   **OpenAPI/Orval**: [https://openapi-generator.tech/](https://openapi-generator.tech/), [https://orval.dev/](https://orval.dev/)
-   **NDI SDK**: [https://ndi.video/sdk/](https://ndi.video/sdk/) (redistribution license permits bundling the runtime DLL inside integrated apps — that's what we do as of v0.7.146).
