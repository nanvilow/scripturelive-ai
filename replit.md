# Imported App

A Next.js application providing scripture-related services for web and desktop, enhancing live service experiences, and streamlining administrative tasks.

## Run & Operate

-   **Install dependencies**: `pnpm install`
-   **Run dev server**: `pnpm dev`
-   **Build app**: `pnpm build`
-   **Typecheck**: `pnpm typecheck`
-   **Codegen**: `pnpm codegen` (for API client)
-   **DB Push**: `pnpm db:push` (for Drizzle schema migrations)
-   **Required Env Vars**: `MAIL_HOST`, `MAIL_USER`, `MAIL_PASS`, `MAIL_FROM`, `SMS_API_KEY`, `SMS_SENDER`

## Stack

-   **Monorepo**: pnpm workspaces
-   **Runtime**: Node.js 24
-   **Language**: TypeScript 5.9
-   **Frontend**: Next.js 16
-   **Backend**: Express 5
-   **Database**: PostgreSQL, SQLite, Replit DB
-   **ORM**: Drizzle ORM, Prisma
-   **Validation**: Zod
-   **Build Tool**: esbuild
-   **API Codegen**: Orval
-   **Speech Recognition**: OpenAI `gpt-4o-mini-transcribe`, Deepgram Nova-3
-   **AI SDK**: OpenAI SDK
-   **NDI Integration**: `koffi` FFI binding to bundled `Processing.NDI.Lib.x64.dll` (NDI 6 runtime ships inside installer as of v0.7.146 — no separate NDI Tools install required)

## Where things live

-   `apps/nextjs-app`: Next.js frontend and Electron wrapper.
-   `apps/api-server`: Express API backend.
-   `packages/`: Shared utilities and components.
-   `artifacts/imported-app/db/custom.db`: SQLite database for the desktop app.
-   `artifacts/imported-app/build-resources/ndi/Processing.NDI.Lib.x64.dll`: bundled NDI runtime (v0.7.146).
-   `public/downloads/manifest.json`: Manifest for dynamic downloads.
-   `openapi.yaml`: API contracts (source of truth for Orval codegen).
-   `drizzle/schema.ts`: Drizzle ORM database schema.
-   `styles/theme/`: Theme-related files.

## Architecture decisions

> Detailed historical entries live in `artifacts/imported-app/CHANGELOG.md`. The list below keeps only the **last three releases in full detail** plus tight one-line guard-rails for older patterns we still want future agents to respect.

### Recent (full detail)

-   **v0.7.190-hotfix.2 — Autofit baseSize REAL fix (parseFloat-clamp NaN bug)**: Operator after v0.7.190 install (and Replit-preview test on hotfix.1): "All text are not like this when I checked on Replit preview, they still as I complain can't be read" — verses tiny on EVERY surface (Stage Preview, Live Display, Bible Lookup card, NDI/OBS), Settings → Typography Small/Medium/Large/Extra-Large buttons silently no-op. Diagnosis: v0.7.190 Fix A logically did NOTHING. The renderer at `route.ts ~L1435` applies the operator's typography pick as a CSS clamp() expression set INLINE on the verse `<p>` (e.g. `style="font-size: clamp(1rem, min(7.04cqw,12.8cqh), 4rem)"`). Fix A read `p.style.fontSize` first (which returns the AUTHORED string `"clamp(1rem, min(...), 4rem)"`, NOT the resolved px) → `parseFloat("clamp(...)")` returns NaN → `baseSize ||= 16`. Binary search then ran on `[16 × 0.6, 16 × 2.5] = [9.6 – 40 px]` regardless of bucket pick. Hotfix.2 swaps to `getComputedStyle(p).fontSize` which RESOLVES the clamp/cqw/cqh against the iframe's 1920×1080 viewport into actual pixels (e.g. ~64 px for Large at the clamped cap), so binary search runs on `[64 × 0.6, 64 × 2.5] = [38 – 160 px]`. Bucket then propagates because FS_MULT (`sm:.85, md:1, lg:1.25, xl:1.5`) is already baked into `ltBand`/`ltCap`/`ltMin` upstream of the clamp at L1419-1434. **Files**: `route.ts` (1 line at L850-855: `var baseSize=parseFloat(window.getComputedStyle(p).fontSize)||16;` replacing the broken inline-read chain). `package.json`+`BUILD.bat`→`0.7.190-hotfix.2`. `tsc --noEmit` clean. **GUARD-RAIL (A) — NEVER trust `el.style.fontSize` when the inline rule is a CSS function (clamp / calc / min / max / var)**: `el.style.<prop>` returns the AUTHORED value (the literal CSS string). For function values (`clamp(...)`, `min(...)`, `calc(...)`, `var(--x)`), parseFloat returns NaN. To get the resolved px MUST use `getComputedStyle(el).<prop>` which evaluates the cascade + functions + container queries. Verify with `rg -n "p\.style\.fontSize" src/app/api/output/congregation/route.ts | rg -v "(=|.toFixed|/\*|//)"` — only the WRITE sites should remain (line 838 cached-reapply, line 886 binary-search-iter, line 893 final-apply); never a READ for measurement. **GUARD-RAIL (B) — Do not "improve" by stripping the inline FIRST**: an "obvious" alternative is `p.style.fontSize=''; var b=getComputedStyle(p).fontSize`. This BREAKS because it falls back through the cascade to `body { font-size: 16px }` which discards the operator's bucket pick. The clamp() is the WHOLE POINT — getComputedStyle resolves it WITH the inline rule still in place, returning bucket-aware px. Verify the path: clear → measure → re-apply is forbidden; the correct path is measure-while-clamp-still-set → use as base → search → final-apply. **GUARD-RAIL (C) — Container-query units only resolve in the iframe's REAL viewport**: this fix relies on the iframe being rendered at NATIVE 1920×1080 (per v0.7.177 GR — `OutputPreview` + `NdiPreviewSurface` use transform-scale into card). If a future "optimization" sizes the iframe at the card's physical pixel size (e.g. 360×203), `cqw`/`cqh` resolve to tiny values, the clamp floors at `min` (1rem=16px), `getComputedStyle` returns 16, and we're back to the broken state with no inline-read in sight. The native-viewport rendering AND the getComputedStyle measurement are TWO LEGS OF THE SAME STOOL — touching either re-introduces the bug silently. **PROCESS GR**: when a "fix" is shipped and operator immediately reports the same bug, do NOT re-patch on the same assumption — read the code AGAIN and trace the actual values returned at each step. v0.7.190 Fix A passed `tsc --noEmit`, passed code review, and shipped — but the fundamental misunderstanding (`p.style.fontSize` returns parseable px) was never tested at runtime. The Replit preview is the cheapest place to catch this; in future, screenshot the Replit preview after every autofit-related change BEFORE pushing to CI.

-   **v0.7.190-hotfix.1 — Thumbnail event-bubble fix + Admin date/time columns**: Architect code review of v0.7.190 caught HIGH severity bug — new media thumbnail wrapper (`library-compact.tsx ~L1361`) had `onClick={sendMediaToPreview}` / `onDoubleClick={sendMediaToLive}` but contained 3 internal buttons (Delete L1377, "+ Schedule" L1386, "Set BG" L1393) whose clicks bubbled UP to the wrapper → tapping Delete also previewed the media before deleting; tapping "+ Schedule" double-fired schedule + preview; tapping "Set BG" double-fired bg-set + preview. Fix: `e.stopPropagation()` added to all three internal handlers. Operator request bundled in same hotfix: Date & Time column added to Recent Payments table (`admin-modal.tsx ~L2099+L2122`, uses `p.createdAt` → `toLocaleString()`, with full-ISO `title=` tooltip + `whitespace-nowrap` so the column doesn't wrap into two lines on narrow viewports) and Generated column added to Recent Activations (`admin-modal.tsx ~L2222+L2253`, uses `a.generatedAt` since `ActivationCodeRecord` interface in `storage.ts:49` declares `generatedAt: string // ISO`, NOT `createdAt` — payments and activations use different field names by design, do not "unify" them). **Files**: `library-compact.tsx` (3 lines: `e.stopPropagation()` injection at L1377/1386/1393), `admin-modal.tsx` (4 lines: 2 `<th>` headers + 2 `<td>` cells), `package.json` + `BUILD.bat` → `0.7.190-hotfix.1`. tsc --noEmit clean. **GUARD-RAIL (A) — Wrapper-with-internal-buttons MUST stopPropagation on every internal click**: any future "click thumbnail = action X" pattern where the thumbnail is a wrapper containing other clickable controls MUST add `e.stopPropagation()` (or equivalent) to EVERY internal button's onClick. Verify with `rg -n "onClick=\{\(e\) => \{ e.stopPropagation" src/components/layout/library-compact.tsx` returns matches for all internal buttons. Forgetting one re-introduces the bug for that one button silently — operator hits it once on the live console and loses trust. **GUARD-RAIL (B) — Activation records use `generatedAt` NOT `createdAt`**: when adding date columns or sort/filter logic for activation rows in admin UI, use `a.generatedAt` (per `ActivationCodeRecord` interface). Payment records DO use `p.createdAt`. The asymmetry is historical (activations were minted explicitly by operator action = "generated"; payments came in passively from gateway = "created"); do NOT rename one to match the other in code or types — at least 6 admin endpoints + 2 cloud-sync paths + the licensing-storage migrations all key off the existing names. **GUARD-RAIL (C) — Hotfix prerelease syntax + glob lockstep (re-affirms v0.7.185 GR)**: `0.7.190-hotfix.1` is valid 3-segment + `-prerelease.N` per electron-builder; the `release-desktop.yml` upload glob `ScriptureLive-AI-${ver}-Setup-*.exe` (where `ver=${tag_name#v}` → `0.7.190-hotfix.1`) matches the produced installer `ScriptureLive-AI-0.7.190-hotfix.1-Setup-x64.exe` correctly. Do NOT switch hotfix lineage to 4-segment (`0.7.190.1`) or the v0.7.184.2 mangling regression returns. **PROCESS GR**: when architect (code review) returns HIGH severity findings on a JUST-shipped release, do NOT silently push hotfix — surface the finding to operator with proposed 1-2 line fix, get explicit "yes hotfix" before the build, and bundle any operator-requested additions into the SAME hotfix to keep CI runs minimal (one CI build per operator install cycle).

-   **v0.7.190 — Media-slide type fix (unblocks audio meter + anti-echo + freeze-preview-on-go-live) + click-to-preview / dbl-click-to-live + #bgLayer 0×0 + autofit baseSize regression**: Operator after v0.7.189 install: "Bible verse video backgrounds still freeze AND lag, audio indicator gone, echo back from media playback, and the click=preview / 2nd click=live media flow is broken." Diagnosis: ONE upstream bug (`library-compact.tsx ~L1227 addToSchedule`) was creating uploaded media items as `type:'custom'` slides with the video shoved into `slide.background` instead of `type:'media'` with `slide.mediaUrl + slide.mediaKind`. The entire v0.7.186 audio + anti-echo machinery is gated on `slide.type === 'media'` (HiddenMediaMeter mount at `logos-shell.tsx ~L1031` checks `previewSlide.type === 'media'`; effectivePreviewSlide URL match at L870-878 checks both sides are `type:'media'`). With `type:'custom'`, no gate ever matched → meter never mounted, `mediaPaused:true` was never spliced into preview payload on go-live → BOTH preview iframe AND live-display iframe rendered the same `<video>` as a verse-with-bg → two decoders fighting over the same source → echo from doubled audio + dropped frames + freeze. **Fix C** (`library-compact.tsx ~L1230 buildMediaSlide`): factored slide construction; `addToSchedule` now produces `{type:'media', mediaUrl:m.dataUrl, mediaKind:m.kind}`. `useAsBackground` (the explicit Set-BG button) is unchanged — operators who DELIBERATELY want a video as a verse background can still do that via `customBackground` settings. **Fix D** (`library-compact.tsx ~L1264-1280` + thumbnail wrapper at L1361): added `sendMediaToPreview` (single click — schedule + setSlides + setPreviewSlideIndex(0); leaves Live alone) and `sendMediaToLive` (double click — same plus setLiveSlideIndex(0) + setIsLive(true)). Mirrors verse-list pattern at L534-543. The `+ Schedule` hover button kept for operators who just want to queue without previewing. **Fix A** (`route.ts ~L844-855 fitVerseText`): autofit baseSize was being computed AFTER `style.fontSize=''` had stripped the operator's typography-button setting, so every verse measured against browser-default 16 px instead of the operator's Small/Medium/Large/Extra-Large pick → buttons effectively no-op. Reordered: `const inlineFs=p.style.fontSize; const cs=getComputedStyle(p); const fromInline=inlineFs?parseFloat(inlineFs):NaN; const baseSize=Number.isFinite(fromInline)?fromInline:parseFloat(cs.fontSize)||16` BEFORE clearing inline fontSize for the binary search. Restores operator-controlled sizing while preserving v0.7.188 binary-search-on-fontSize (GR-A) for fill behaviour. **Fix B** (`route.ts ~L68 #bgLayer base CSS`): added `width:100%;height:100%` to the persistent bg-video sibling layer. Was 0×0 because no rule set dimensions — when `displayRatio:'fill'` (the default in `store.ts ~L670` on every fresh install), `applyRatio()` doesn't apply a ratio class with dimensions, so the layer collapsed to 0×0. Browsers throttle decoders for invisible elements → operator-perceived freeze even WITH the v0.7.187 persistent-element pattern intact. The cached `<video>` was correctly preserved across renders; it just couldn't render visibly OR keep its decoder running smoothly. **Files**: `library-compact.tsx` (~55 lines: buildMediaSlide + 2 helpers + thumbnail wrapper), `route.ts` (~15 lines: 2 surgical edits — autofit reorder + #bgLayer dimensions), `package.json`+`BUILD.bat`→0.7.190. `tsc --noEmit` clean. **GUARD-RAIL (A) — Uploaded media MUST become `type:'media'` slides, NEVER `type:'custom'+background:dataUrl`**: any future code path that wraps a media-library item into a slide MUST use `{type:'media', mediaUrl, mediaKind}`, never the legacy `{type:'custom', background:dataUrl}` shape. The `custom`-with-bg shape silently bypasses the entire v0.7.186 audio + anti-echo + freeze-preview-on-go-live infrastructure (all gated on `type === 'media'` at 3 sites in `logos-shell.tsx`: L872, L883, L1030). Verify with `rg -n "type: ?'custom'" src/components/layout/library-compact.tsx` returns zero matches in media-related code paths. The operator's explicit "Set BG" button (`useAsBackground` ~L1282) is the ONE legitimate path to `customBackground` for video-as-bg use. **GUARD-RAIL (B) — Click-to-preview / dbl-click-to-live MUST stay parity with verse list**: media thumbnail click handlers (`sendMediaToPreview` / `sendMediaToLive`) MUST follow the same single/double-click pattern as the verse list (L534-543). Operators have muscle memory across verses + media — diverging the patterns (e.g. single click = live for media but preview for verses) breaks live workflows. Verify both surfaces use the same `useAppStore.getState().setSlides + setPreviewSlideIndex(0)` for preview, and add `setLiveSlideIndex(0) + setIsLive(true)` for live. **GUARD-RAIL (C) — `#bgLayer` MUST have explicit dimensions in base CSS**: the v0.7.187 persistent-bg-video layer relies on `applyRatio()` to set ratio classes that mostly carry their own width/height. But when no ratio class applies (e.g. `displayRatio:'fill'` which is the default), the layer falls back to base CSS — that base CSS MUST include `width:100%;height:100%` or the layer is 0×0 and the browser throttles the decoder. Verify with `rg -n "#bgLayer" src/app/api/output/congregation/route.ts | head -5` shows the base rule has both width and height. **GUARD-RAIL (D) — Autofit MUST capture operator's inline fontSize BEFORE measurement**: any code in `fitVerseText` that calls `p.style.fontSize=''` to clear for measurement MUST first read `const inlineFs=p.style.fontSize` and use that as `baseSize` (parsed as float; fallback to computed style only if inline is empty). Otherwise the operator's typography-button setting (which writes to `style.fontSize`) is stripped before measurement → autofit measures against browser-default 16px → buttons silently no-op. **PROCESS GR**: when an operator reports MULTIPLE seemingly-unrelated bugs in one ship (audio gone + echo + freeze + missing UI flow), look for ONE upstream type/shape bug that bypasses an entire infrastructure layer BEFORE patching each symptom individually. v0.7.190 fixed all four with TWO files touched.

-   **v0.7.189 — LT (lower-third) bg-video persistence (v0.7.187 follow-up)**: Operator after v0.7.188 install: "Bible verse video backgrounds still freeze." Diagnosis: v0.7.187's `#bgLayer` hoist only covered the FULLSCREEN verse branch (`route.ts ~L1469 setBgVid(safeBg)`) and the themed-empty branch (~L1033). The **lower-third (LT) branch was missed** — line 1389 still emitted an inline `<video class="lt-bg" ...>` INSIDE the chyron card, and the entire `<div class="lt-box">` was reassigned via `innerHTML=` at L1447 on every render → `<video>` destroyed and recreated → t=0 restart-flash on every speech tick / SSE poll → operator-perceived "freeze". Fix: same pattern as v0.7.187 but kept SEPARATE because the LT bg lives INSIDE the chyron card (positioned), not behind the whole stage — can't reuse the global `#bgLayer`. New module-level `__ltBgUrl/__ltBgEl/__ltBgOverlay` cache + `ensureLtBgEl(url)` (key by URL, no-op on same, release+rebuild on URL change) + `mountLtBg(box,url)` (calls ensureLtBgEl then `box.insertBefore(bg, firstChild)` — `insertBefore` MOVES the existing node, does NOT clone, so decoder state is preserved across innerHTML rewrites). LT branch (~L1438-1509): `ltInnerBg=''` (template no longer carries bg tag); after `$('output').innerHTML=...` runs, `setBgVid('')` releases fullscreen #bgLayer (irrelevant in LT mode), then `mountLtBg(__ltBox, safeLtBg)` slots the cached element into the freshly-built `.lt-box`. `lower-third-black` mode and empty-bg case both call `ensureLtBgEl('')` to release the cache. Non-LT branches (fullscreen / media / clear) leave the cache dangling intentionally — the element is auto-detached when `#output` innerHTML wipes, browser pauses the off-DOM `<video>` decoder, and re-entering LT re-attaches the SAME node via `insertBefore` (no t=0 restart). **Files**: `route.ts` (~60 lines: cache + ensureLtBgEl + mountLtBg above setBgVid; 1 line strip of inline ltInnerBg tag; 5-line mount block after LT innerHTML). `package.json`+`BUILD.bat`→0.7.189. `tsc --noEmit` clean. **GUARD-RAIL (A) — LT bg uses MOVE-not-CLONE pattern**: any future "persistent media inside a per-render container" requirement MUST use `parent.insertBefore(cachedNode, firstChild)` (a MOVE op when the node already has a parent, or an attach when detached). Element-cloning (`cloneNode(true)` + `replaceWith`) creates a NEW `<video>` which restarts decode at t=0 — the very bug we're fixing. Verify with `rg -n "cloneNode" src/app/api/output/congregation/route.ts` should return zero matches in the bg paths. **GUARD-RAIL (B) — LT cache must NOT be released by every non-LT setBgVid call**: do NOT add `ensureLtBgEl('')` alongside the 5 existing `setBgVid('')` sites (no-slide / blanked / clear / NDI alpha-key / foreground-media) — releasing on every transition would force a fresh `<video>` build the next time operator returns to LT mode, restoring the freeze. The cached element gets auto-detached from DOM when `#output` innerHTML wipes; the off-DOM video is harmless (browser pauses decoder, ~negligible memory). The LT branch itself is the ONLY place that should call `ensureLtBgEl('')` (when entering LT with `lower-third-black` or empty bg). **GUARD-RAIL (C) — fullscreen `#bgLayer` and LT `__ltBgEl` are SEPARATE caches**: do NOT merge them into a single function. The fullscreen `#bgLayer` is a GLOBAL sibling of `#output` (positioned by `applyRatio()` to fill the stage); the LT bg is INSIDE `.lt-box` (positioned by the chyron's pos+size class). Same-URL-different-cache lets both surfaces share the operator's `customBackground` field without one path stealing the element from the other on mode-flip.

-   **v0.7.188 — LT autofit SCALE-TO-FILL + Re-mention auto-live to broadcast**: Two fixes shipped together with the v0.7.187 BG-video work and v0.7.186 Twi+media work (all four bundled into one push to avoid four CI runs). **(1) NDI Lower-Third autofit SCALE-TO-FILL** — `route.ts ~L756-840 fitVerseText` rewritten from `transform: scale()` to **binary search on `font-size`**. Old behaviour (v0.7.184.2): `transform:scale(k)` with `k ∈ [0.60, 1.00]` — could only ever shrink, never grow. Operator wanted: short verses ("Jesus wept.") fill the bar, long verses shrink to fit. Why scale couldn't grow: under the LT branch's `.lt-content { display:flex; flex-direction:column; overflow:hidden; min-height:0 }`, scaling UP makes the `<p>` overflow its already-constrained flex parent, but the flex parent has `overflow:hidden` so the visual size is capped at the parent — `scale(1.5)` on a flex-shrunk child looks identical to `scale(1.0)`. Font-size DOES change layout (parent.scrollHeight reflects the actual size), so binary search converges. New impl: 10-iteration binary search on `font-size` multiplier, branch detected via `parent.closest('.lt-content,.lt-box')`: LT bounds `[0.60, 2.50]`, fullscreen bounds `[0.60, 1.00]` (shrink-only on main display — operator-explicit, no balloon). Cache via module-level `__fitKey/__fitBase/__fitScale`: same verse re-renders reapply cached scale via `style.fontSize` without re-measuring. Per-verse lock preserved. Sibling subtraction (chyron) preserved. Floor 0.60 preserved. **(2) Re-mention auto-live to broadcast** — `store.ts addDetectedVerse ~L836` dedupe gate `if (existingRank >= incomingRank) return {}` changed to PROMOTE: spread `...existing` + new `id: det-rementioned-${Date.now()}-${rand}` + fresh `detectedAt`, then return `{ detectedVerses: [promoted, ...filtered].slice(0, 100) }`. Operator scenario: speaker says Amos 1:3 (live), then John 3:4 (live), then Amos 1:3 again — pre-fix: nothing happened because `addDetectedVerse` returned `{}` → `detectedVerses` array reference unchanged → auto-live `useEffect` in `logos-shell.tsx ~L3339` watching `detectedVerses` never re-ran → broadcast stayed on John 3:4. Post-fix: array reference changes → useEffect re-fires → new `id` ≠ `lastAutoVerseId.current` (the v0.7.181-era gate that prevents same-verse re-fire) → `shouldFireAutoLiveStable` picks Amos as front-of-array → broadcast updates. `requestNavigatorRef` already fires at the top of the reducer (v0.7.184 GR), so navigator was already correct on re-mention; only the broadcast path was broken. `addDetectedVerseCandidate` (speculative <50%) intentionally NOT touched — would thrash broadcast on every speech tick. Cross-source rank protection preserved (semantic doesn't downgrade explicit) because `...existing` spread keeps original `source`+`confidence`. Stability gate (≥3 frames per source) and 500 ms LIVE_HOLD_MS in `verse-auto-live.ts` still apply — accidental tick won't snap-back, real re-mention will. **Files**: `route.ts` (~85 lines: binary-search rewrite), `store.ts` (~28 lines: ~22 of comment + ~6 of code at the dedupe branch), `package.json`+`BUILD.bat`→0.7.188. `tsc --noEmit` clean. **GUARD-RAIL (A) — Autofit fill needs font-size, not transform**: any future "make this auto-fit AND grow" requirement MUST use `style.fontSize` binary search, NOT `transform: scale()`. Transform is paint-only (doesn't change layout) and inside any `overflow:hidden` flex parent it cannot grow visibly past the parent. The float math: `lo=0.6, hi=2.5; for(10 iter) { mid=(lo+hi)/2; p.style.fontSize=baseSize*mid+'px'; (parent.scrollHeight<=avail) ? lo=mid : hi=mid; } scale=lo` — `lo` is the largest-still-fitting; final apply is `style.fontSize=baseSize*lo+'px'`. **GUARD-RAIL (B) — Branch detection for autofit bounds**: LT vs fullscreen branch MUST be detected via `p.closest('.lt-content,.lt-box')` (truthy = LT). Hardcoded `IS_NDI` flag is wrong — both NDI broadcast AND OBS browser source go through the same renderer, both can hit either branch depending on `ndiDisplayMode`. **GUARD-RAIL (C) — Re-mention promote MUST preserve source/confidence**: the promoted entry MUST `...existing` spread, NEVER replace with the incoming `v`. Replacing would let a low-confidence semantic re-mention overwrite a high-confidence explicit original — operator would see column source-pill flicker. Spread + new id + fresh detectedAt is the entire change; everything else in the entry stays. **GUARD-RAIL (D) — Auto-live useEffect dependency MUST stay on `detectedVerses` array reference**: do NOT change the dependency to `detectedVerses[0]?.id` "to be more efficient" — the promote fix relies on the array reference changing per re-mention. Switching to a per-id dep would break re-mention even with the promote fix in place because the same id (pre-fix) wouldn't trigger; a new id (post-fix) would, but the protection chain would be re-entrant on rank-tied insertions.

-   **v0.7.187 — Persistent BG-video layer (one-line GR)**: cross-render media MUST live OUTSIDE `#output` as a sibling div with module-level setter + URL-keyed no-op + explicit clear guards on every state-transition path; every `innerHTML=` reassignment in route.ts MUST be paired with a `setBgVid` call within ±20 lines. Layer base CSS MUST set explicit width/height (see v0.7.190 GR-C — fallback for `displayRatio:'fill'` default). Full text in `artifacts/imported-app/CHANGELOG.md` and git history.




### Older releases — guard-rails only (full text in `artifacts/imported-app/CHANGELOG.md` and git history)
-   **v0.7.185 — Version-string semver hotfix**: `package.json#version` MUST be exactly 3-segment `MAJOR.MINOR.PATCH` (optional `-prerelease.N`). Never 4-segment (`0.7.184.2`) — electron-builder mangles to `ScriptureLive-AI-0.7.18-4.2-Setup-x64.exe` and the `release-desktop.yml` upload glob misses the file → cascade-skipped Upload latest.yml + blockmap + macOS notarize + GitHub Release publish. Hotfix lineage uses prerelease syntax (`0.7.185-hotfix.1`) AND lockstep update of the upload glob. **PROCESS GR**: when CI is "red" after a push, FIRST inspect the actual GH Actions log (most-recent run via `/repos/OWNER/REPO/actions/runs?per_page=1`) — do NOT assume code regression.
-   **v0.7.186 — Twi homophone aliases + Hidden media VU meter / preview-vs-live echo cancellation**: 6 new entries in VOICE_COMMAND_ALIASES table in voice/commands.ts (NEVER bespoke if(transcript.includes()) branches in speech-provider.tsx — table is single source of truth + shared with keyterm-pruner). attachAnalyserSilent() in audio-level.ts MUST NOT call analyser.connect(audioContext.destination) — read-only or you double-route audio + cause echo (verify rg -n destination src/lib/audio-level.ts returns zero in attachAnalyserSilent). HiddenMediaMeter (1×1 px off-screen video) drives audioLevelPreview + previewVideoPlaying flags; AudioMeter gates bar height on playing flag so silent/paused = dead-flat zero. effectivePreviewSlide URL match in PreviewCard MUST normalise (strip query, lowercase scheme) — preview keeps playing otherwise.
-   **v0.7.184.2 — Re-mention nav-fire at every dedupe gate + autofit measurement target = `<p>` not parent**: store-level fix in `addDetectedVerse` is necessary but NOT sufficient — every upstream dedupe gate in `speech-provider.tsx` that returns early WITHOUT calling `addDetectedVerse` MUST also fire `requestNavigatorRef(ref)` on the suppress branch (4 sites: Reference Engine v2 ~L1561, regex-detector ~L2068, AI cosine-matcher ~L1968, keyword text-search ~L1820). Verify with `rg -n "processed.*Ref.*has\(|continue" src/components/providers/speech-provider.tsx | rg -B 2 "continue"` — every new `continue` must be preceded by `requestNavigatorRef`. `addDetectedVerseCandidate` deliberately NOT wired (speculative <50% would thrash navigator). Autofit MUST measure `p.scrollHeight` not `parent.scrollHeight` — the parent measurement silently lies under nested flex+overflow:hidden (the LT branch). Subtract sibling heights (chyron) from `parent.clientHeight`. **PROCESS GR**: when operator reports "doesn't work at all" after a verified fix, trace the FULL call path before re-patching — v0.7.184/184.1 wasted two iterations re-patching the same site instead of finding the upstream gates.
-   **v0.7.184.1 — Persist version + module-level autofit cache**: any persist-state migration block in `store.ts migrate:` MUST be paired with `version: N+1` bump (Zustand only runs migrations where `version < currentVersion`; missing bump = dead code). Cross-render state for the renderer script MUST live in module-level closure state, NEVER on a `data-` attribute on a child of `#output` (output subtree is destroyed+recreated on every render). ANY new code added inside the renderer-script template literal in `route.ts` MUST be backtick-free (the script is itself a tagged template literal — backticks anywhere break the build). **PROCESS GR**: code review (architect) MUST run after every multi-file ship.
-   **v0.7.184 — In-app LT removal + Auto-route detected refs**: `addDetectedVerse` navigator-fire MUST be at TOP, BEFORE dedupe early-return. In-app LT removal is permanent — `DisplayMode='full'` type narrow + v4→v5 migration coercion are coupled, never roll one back without the other. OBS URL must bake the four LT params lockstep across FrameCapture / NdiPreviewSurface / buildObsUrl / buildCongregationParams. Persist-migration code comparing legacy values needs `Omit<Partial<T>,"narrowedField"> & {narrowedField?: string}` cast for TS2367-safe equality.
-   **v0.7.183 — Autofit single-shot math + admin-modal poll-rate gates**: superseded by v0.7.188 GR-A (binary-search font-size for fill, not iterative scale-shrink). Floor 0.60 still operator-explicit — pathologically long verses (>1.66× overflow) require operator-action, NOT a lower floor. Autofit target selector is `#output .slide-paragraph` ONLY — targeting the container shrinks every verse globally. Admin-modal poll intervals (overview 30s, codes 30s, records 60s, cloud-sync-test 60s) are gated by `tab === 'overview' / 'codes'` — gates from v0.7.166 are load-bearing.
-   **v0.7.182 — In-app LT removal + auto-route + autofit**: see v0.7.184 / v0.7.184.2 / v0.7.188 GRs above. Detection-pipeline auto-nav: authoritative (≥50%, addDetectedVerse) → fire `requestNavigatorRef`; speculative (<50%, addDetectedVerseCandidate) → DO NOT fire (would thrash navigator on every speech tick). When narrowing a union type to suppress dead branches, prefer single-literal narrow (`type X = 'full'`) — less likely to be re-widened. `stable-stage.tsx isLive` prop is now no-op cosmetically but MUST stay in component signature (caller-compat). When removing UI for a state field with persist migration, ALWAYS bump version + add coercion branch.
-   **v0.7.181 — OBS URL bakes NDI LT params + strip OBS mentions from Settings UI**: `buildCongregationParams()` in `ndi-output-panel.tsx ~L93-109` bakes `ndi=1`+`transparent=1` always, plus `lowerThird=1`+`position`+`lh`+`sc` when `ndiDisplayMode==='lower-third'`. **GR**: any new `/api/output/congregation` URL emitter targeting NDI/OBS/vMix/Wirecast MUST bake the four LT params, in lockstep with three existing emitters: `electron/main.ts` FrameCapture, `NdiPreviewSurface`, `buildObsUrl()`. Use `useAppStore.getState().settings.<field>` (lazy read) for TDZ-safe access when a function sits above the hook subscription, but ONLY when the component is already subscribed elsewhere. Do NOT remove OBS/vMix/Wirecast mentions from `ndi-output-panel.tsx` itself — Settings-page strip only.
-   **v0.7.180 — File upload fix (Chromium 130+ trusted-UI hardening on hidden inputs)**: `<input type="file" className="hidden">` paired with programmatic `.click()` relay is broken in Chromium 130+ packaged Electron — `onChange` never fires. Fix is `className="sr-only"` (keeps input in layout tree) plus optionally `<label htmlFor>` wrapping a visible trigger (`settings.tsx` Custom Background pattern) or just the class swap if multi-trigger (`logos-shell.tsx` Media Library pattern). **GR**: NEVER use `.hidden` on a file input that's targeted by a JS `.click()` relay; codebase audit `rg -nC 5 'type="file"' src/ | rg -B 3 -A 3 'className="hidden"'` must return zero matches. Also drop `console.error` in upload-handler catch blocks — `toast.error()` doesn't help when handler never fires. **PROCESS GR**: every operator-reported bug fix from v0.7.180 forward MUST follow diagnose → ASK targeted question → propose with proof → wait for explicit "Yes ship" → ship. v0.7.179 shipped without showing operator first and was pushed back; v0.7.180 followed the new flow and was approved before any code changed.
-   **v0.7.179 — Per-PC masterCode default = baked cloud admin code**: `freshFile()` + `load()` chains in `licensing/storage.ts` now default per-PC `masterCode` to `BAKED_CLOUD_ADMIN_CODE` (= cloud canonical = `SL-MASTER-HETEVT56-HCKTTS74`); v0.7.172 self-heal extended with `else if (!override && persisted!==BAKED)` branch silently rewriting stale random per-PC codes on first boot of v0.7.179. Cross-Device Sync now auto-connects out-of-the-box without operator setup. **GR**: `BAKED_CLOUD_ADMIN_CODE` lockstep across THREE call sites (cloud-sync.ts, cloud-sync-test/route.ts, licensing/storage.ts) + `inject-keys.mjs:173`. Self-heal `else if` MUST come AFTER `if (override)` env-override branch — flipping order would let baked rebake clobber env override on cloud deployments.
-   **v0.7.178 — OBS URL → NDI parity (superseded by v0.7.181 bake)**: added `?ndi=1` to `buildObsUrl()` / `browserFallbackUrl` in `ndi-output-panel.tsx` so route's `IS_NDI` gating fires for OBS surface. Half-fix — missed the URL-bake half v0.7.5.1 already proved necessary for FrameCapture; v0.7.181 finishes the job. **GR**: NEVER make `IS_NDI` default to true on the route — Live Display window, secondary screen, projector, in-app Settings preview, Bible Lookup preview ALL rely on `IS_NDI=false`. When adding a NEW `ndi*` setting, wire its T_* resolver as the 3-tier chain `(USE_NDI_OVERRIDES && st.ndiNew !== undefined) ? st.ndiNew : (USE_LT_OVERRIDES && st.lowerThirdNew !== undefined) ? st.lowerThirdNew : st.bodyEquivalent`.
-   **v0.7.177 — In-App LT Preview WYSIWYG (native 1920×1080 transform-scale)**: `OutputPreview` (`src/components/settings/output-preview.tsx`, used by every Settings card preview + Live Display) now renders iframe at NATIVE 1920×1080 then CSS `transform: scale()` into the card — ported byte-for-byte from `NdiPreviewSurface`. Fix B carved LT off body fallback in `route.ts` ~line 827: `USE_LT_OVERRIDES` branch clamps `lowerThirdBibleLineHeight` at `Math.max(1.2,...)` and unset default = 1.4. **GR**: any new in-app preview surface loading `/api/output/congregation` MUST render at NATIVE_W × NATIVE_H + transform-scale, NOT iframe-fits-parent (cqw/cqh/vw font math floors at clamp minimum on small viewports, verses crash). NATIVE_H pinned at 1080; NATIVE_W flexes by aspect (16:9→1920, 4:3→1440, 21:9→2520) — new `displayRatio` MUST add matching NATIVE_W branch. Do NOT merge `NdiPreviewSurface` and `OutputPreview` — intentional duplicates (NDI broadcast WYSIWYG vs every-other-surface WYSIWYG).
-   **v0.7.176 — LT Frame Lock + Text Auto-Fit + Status Badge Removal + Cloud MasterCode Rotation**: locked `__lhKey='sm'`/`hPct=22` UNCONDITIONALLY across every surface in `route.ts` (operator-explicit; bucket buttons now wired to typography only — never re-introduce `hMap[__lhKey]` lookup). Restored dark-gradient `.lt-box` plate as default + 6 themed gradients; NDI alpha keying opts OUT via `ndiLowerThirdTransparent`, never by flipping global default. Cloud admin masterCode rotated to `SL-MASTER-HETEVT56-HCKTTS74` in `baked-credentials.ts:26` + `inject-keys.mjs:173` lockstep — see v0.7.179 GR-A for the now 3-site lockstep playbook including per-PC masterCode. Removed "NDI/Output Active" badge from `easyworship-shell.tsx` ~line 1541 (state `outputActive` + Radio import RETAINED — drives hardware). **GR**: `hPct` change MUST re-tune `ltBand`/`ltCap`/`ltMin` ramp in lockstep (test fixture: 3 verses ≤90/~180/~320 chars at 1280×720 must not overflow). When removing a status indicator anywhere, keep underlying state variable + dependent hooks intact — chip cosmetic, state load-bearing.
-   **v0.7.175 — Untrack installer artwork from `.gitignore`**: blanket `build-resources/*` rule had only 4 carve-outs (notarize.js, icon.ico, icon.png, entitlements.mac.plist); v0.7.173 CI red on NSIS phase exposed missing `installerHeader.bmp`/`installerSidebar.bmp`/`uninstallerSidebar.bmp`/`installer.nsh`/`license.txt` + 28MB NDI DLL (`build-resources/ndi/`). Added 6 explicit `!build-resources/<asset>` carve-outs. **GR**: every new `build-resources/` file shipped in installer (referenced from `electron-builder.yml`/`installer.nsh`/`extraResources`) MUST get a matching `!` carve-out — verify with `git --no-optional-locks check-ignore -v <path>`. NEVER convert to flat `!build-resources/` allow-all (electron-builder cache junk like `release/`, `win-unpacked/` would bloat repo by hundreds of MB/release).
-   **v0.7.174 — Bible-book homophone normaliser**: BOOK_HOMOPHONES table + normalizeBookHomophones() in src/lib/bible-api.ts (~line 545); 10 mishears (Ruth←root/roach/route, Job←joe, Jude←dude, Amos←ammos, Titus←tightest, Habakkuk←"had a cook", Malachi←malarkey, Joel←jewel, Acts←axe, Hosea←hoseah). MUST require verse-reference context follower (chapter/digit/number-word) — never bare \b<word>\b → <book> rule (would clobber sermon prose). Wired into normalizeSpokenNumbers() FIRST step so downstream collapser sees corrected book name. New mishears go in the table — do NOT escalate Deepgram keyterm count past ~80 (hurts adjacent-word recall).

-   **v0.7.173 — Admin slowness cache + LT frame frozen + baked cloud rotation (superseded)**: `cloudPullAdminLedgerCached()` 30s TTL + 5s stale-while-revalidate in `src/lib/licensing/cloud-pull-cache.ts` is canonical for ALL `/api/license/admin/*` GET handlers — never call raw `cloudPullAdminLedger()`. `.lower-third` flex parent MUST use `align-items:stretch` (not `center`) so child `.lt-box` `height:100%` doesn't collapse to intrinsic on some Chromium layout passes (kept in v0.7.176). Transparent-LT-default reverted in v0.7.176 — DARK gradient is now the default again, NDI alpha keying opts OUT via `ndiLowerThirdTransparent`. Cloud admin code rotated again in v0.7.176 — see GR D there.
-   **v0.7.172 — Settings UX + notif bulk-delete + cloud env-override**: every "Settings + Preview" card MUST use `lg:grid-cols-[1fr_360px]` with `lg:sticky lg:top-2 self-start` right-column preview — never re-introduce a full-width preview at the TOP of a settings card. Any new admin-modal data column MUST follow the v0.7.130 select / select-all / delete-all / delete-(N) pattern with `useConfirm()`; backend `bulk-delete` route already accepts `kind: 'payment' | 'activation' | 'notification'`. Cloud-side masterCode MUST be sourced from `SCRIPTURELIVE_MASTER_CODE` env var (per-PC `licenseFile.masterCode` is for Electron clients only); `load()` self-heals stale on-disk values silently.
-   **v0.7.171 — webpack `node:` scheme fix**: any new Node-only `require()` (`node:fs`, `node:path`, `node:child_process`, etc.) reachable from a client component MUST be resolved via `eval('require')`, not static `require()`. The `typeof window` runtime guard is necessary but NOT sufficient — webpack's CLIENT pass scans the whole module graph and crashes with `UnhandledSchemeError` on any unhandled scheme it sees, regardless of whether the call would ever execute. Symptom: dev (Turbopack) passes, production webpack build fails. Verify locally by running `package:win` or watching the GitHub Actions run; `tsc --noEmit` passes either way.
-   **v0.7.170 — Quiet Edge-Runtime warnings**: extracted Node-only body of `instrumentation.ts` `register()` into `src/instrumentation-node.ts` and dynamic-imported it behind `process.env.NEXT_RUNTIME === 'nodejs'`. Turbopack's edge-bundle static scanner ignores runtime guards and prints `⚠ A Node.js API is used (process.on)` on every compile, which fills the workflow card and turns it red. Any new Node-only API (`fs`, `process.*`, `child_process`, `nodemailer`) in a Next 16+ entry-point file MUST live behind a dynamic `import()` gated on `NEXT_RUNTIME==='nodejs'`; a top-of-function `if(...) return` guard is necessary but NOT sufficient.
-   **v0.7.169 — LLM throttle rollbacks**: `LIVE_STICKY_MS` 8000→4000, `DEFAULT_TIMEOUT_MS` 800→1200 ms (Ghana p90 Wi-Fi), `SEMANTIC_THROTTLE_MS` 1500→1000 ms. Tune in 1 s / 200 ms increments, never doubling — v0.7.117/v0.7.93 mistake was 2× changes overshooting field-usable range. Did NOT touch 0.58 cross-pipeline corroboration floor or `semanticOwnsLive` latch.
-   **v0.7.168 — Offline Bibles in Packaged Electron**: `outputFileTracingIncludes` in `next.config.ts` for `src/data/bibles/*.json` + server-side `fsFallback()` in `local-bible.ts` (reads cwd / standalone dir / `__dirname` resolve) when `require()` returns empty stub. Any new `src/data/*.json` MUST be added to BOTH `outputFileTracingIncludes` AND `loadTranslation()` switch. v0.7.171 rewrote the fallback's `require('node:fs')`/`'node:path'` to `eval('require')` due to webpack `UnhandledSchemeError` on CLIENT pass.
-   **v0.7.167 — Independent LT typography + TWI migration**: split `USE_NDI_OVERRIDES` (NDI-only) from `USE_LT_OVERRIDES` (in-app LT-only) in `route.ts`; added 7 `lowerThird*` settings + 3-tier chain (NDI → LT → body); persist `version 3 → 4` migration rewrites `'TWI'` → `'TWIASANTE'` in 3 persisted keys. Any new lower-third-like display mode propagates via `dm.indexOf('lower-third')===0`; every key in `route.ts` resolution chain MUST also be in `settingsRenderKey()`; any future `TRANSLATIONS_INFO` removal MUST ship a persist-migration bump.
-   **v0.7.166 — Activation lifecycle cross-device + admin tab-gated reload + Deepgram alignment**: `applyAdminLedgerSnapshot()` activation merge propagates `isUsed`/`usedAt`/`subscriptionExpiresAt` (monotonic isUsed, latest-wins on dates); admin-modal `/admin/list` poll gated to `tab === 'overview'`; renderer direct Deepgram WS params lockstep with api-server proxy. Any new SHARED `ActivationCodeRecord` field MUST be added to the merge; every `<Dialog>` auto-refresh MUST gate on its tab; renderer + proxy Deepgram params change in lockstep. (LT-typography GR superseded by v0.7.167 split.)
-   **v0.7.164 — Bundle NKJV/NLT/AMP offline**: bundler default-list = 8 Bibles (~32 MB); per-source concurrency split (`isWldeh ? 12 : 3` — bolls.life 429s past 3); 429-aware backoff honours `Retry-After`. Every NEW translation needs 2 lockstep edits: (a) stub-loop list in `bundle-bibles.mjs`, (b) `loadTranslation()` switch in `local-bible.ts`. v0.7.168's `outputFileTracingIncludes` wildcard covers JSON copy. Bolls concurrency pin (3) is a HARD ceiling.
-   **v0.7.163 — Single Twi key (TWIASANTE) + drop Akuapem TWI + ordinal voice aliases**: 11 ordinal aliases (`3 version`, `3rd version`, `three version`, …) all → `TWIASANTE`. TWI removal lives in 8 lockstep places: `bible-api.ts` TRANSLATIONS_INFO + `twi-bible.ts` WldehKey/WLDEH_TRANSLATIONS/isWldehKey + `local-bible.ts` switch + `bundle-bibles.mjs` default-list + WLDEH map + `voice/commands.ts` aliases + `voice/llm-classifier.ts` prompt. Bundler needs GH_PAT auth (anonymous raw.github throttled 60/hr). v0.7.167 finished the job with the persisted-state migration.
-   **v0.7.162 — Cloud Sync stats**: cloud-sync-test buckets MUST stay in sync with `storage.computeCodeStatus()` precedence (deleted > cancelled > master > never-used > active/expired/used). Revenue is PAID + CONSUMED only — never include WAITING_PAYMENT.
-   **v0.7.159 — NDI Live Preview background**: any iframe for OPERATOR consumption MUST omit `transparent=1` — only true NDI/OBS capture surfaces keep transparency.
-   **v0.7.158 — Single-renderer across all 6 surfaces**: NEVER re-introduce a parallel React mirror of the slide composite — every verse-display surface goes through `<OutputPreview>` (or `<NdiPreviewSurface>`). Extend `buildOutputPayload()` or add a flag.
-   **v0.7.157 — OBS card + cross-device sync UI**: `browserFallbackUrl = ${window.location.origin}/api/output/congregation?transparent=1` is the canonical fallback for hybrid web/Electron renderers when Electron IPC data is null. NEVER outer-`&&`-guard a card on optional Electron-IPC data.
-   **v0.7.156 — Output-window regex hotfix**: regex inside `route.ts` giant template literal must double-escape backslashes (`\\.`, `\\/`, `\\d`); verify with `node --check` on extracted inline script after every regex edit.
-   **v0.7.155 — Video backgrounds**: `safeBgUrl()` allowlist (`http://`, `https://`, `data:image/`, `data:video/`, `/`) + `escAttr()` are the XSS guard for the unauthenticated `/api/output/congregation` state-push. Inline comments inside the giant template literal MUST use `/* */`, never embed unescaped backticks.
-   **v0.7.161 — Auto-sync OOTB (bake cloud masterCode)**: `BAKED_CLOUD_ADMIN_CODE` + `getCloudAdminCode()` in `src/lib/baked-credentials.ts`; resolution chain (per-PC `cloudAdminCode` → env `SCRIPTURELIVE_CLOUD_ADMIN_CODE` → baked) in `adminSyncCode()` and `cloud-sync-test/route.ts`. `baked-credentials.ts` is gitignored; rotate masterCode in BOTH that constant AND `scripts/inject-keys.mjs` hardcoded fallback. Per-PC override must stay at the top of the chain.
-   **v0.7.160 — Cross-device admin sync visible + auto-pull on config read**: `admin/config` GET now mirrors the list/codes pattern (4 s `cloudPullAdminLedger` + `applyAdminLedgerSnapshot`); admin-modal header pill (`connected`/`wrong key`/`unreachable`/`not set up`) + Sync Now button. Any new admin-data read endpoint MUST start with the same pull block, and per-PC fields stay on the storage strip-list. v0.7.153 cloud routes still required.
-   **v0.7.154 — 3-letter book aliases**: `parseReference()` accepts lowercase 3-letter codes + whitespace separator. All 66 books aliased.
-   **v0.7.153 — Cross-device admin-panel sync transport**: cloud routes `/api/license/cloud/admin-{snapshot,merge}` (masterCode-auth, idempotent merge by `updatedAt`); helpers in `src/lib/licensing/cloud-sync.ts` no-op when `cloudAdminCode` unset — v0.7.157 added the UI to set it. Cloud must be redeployed before customers update.
-   **v0.7.150–v0.7.152 — Cross-pipeline preempt direction**: SEMANTIC (BRQ) preempts EXPLICIT (AVM) in `src/lib/verse-auto-live.ts`; latch fields `semanticOwnsLive` / `frozenExplicitId`; reverse direction held by 0.58 floor (`CROSS_PIPELINE_SEMANTIC_VS_EXPLICIT_MIN`). v0.7.150/151 had direction backwards — final shipped direction is v0.7.152.
-   **v0.7.149 — Boot Splash**: `electron/main.ts` splash is non-modal (`alwaysOnTop: false`, `skipTaskbar: false`, dwell 0); `loadLogoDataUrl()` candidate order packaged → dev → fallback; main-window minimize/restore mirrored to splash.
-   **v0.7.147–v0.7.148 — Paraphrase corpus**: `RAW_CATALOGUE` in `src/lib/bibles/preacher-phrases.ts` — Matt 4:19 (53 entries, all carrying distinctive signatures), Psalm 124:7 (26), 399-paraphrase pulpit corpus. Bare "out of the snare" / generic "follow me" omitted (collide with 2 Tim 2:26 / Matt 9:9 etc).
-   **v0.7.146 — Bundled NDI Runtime**: `build-resources/ndi/Processing.NDI.Lib.x64.dll` shipped via `electron-builder.yml` `extraResources`; `findNdiDll()` order in `ndi-service.ts`: env → bundled → dev → legacy. Bundled wins over system NDI Tools.
-   **v0.7.145 — Cross-Machine Activation + Payment Cloud Sync**: `cloudClaimActivation` / `cloudMirrorPayment` in `src/lib/licensing/cloud-sync.ts`; cloud routes `/api/license/cloud/{claim-activation,mirror-payment}`; `activate/route.ts` falls back to cloud only on `/not recognis|not found|unknown|invalid/i`. Cloud must be redeployed before customers update.
-   **v0.7.144 — Live-Mic Pulsing Dot**: `logos-shell.tsx` Card `badge` is a single `<span>` with `engineDotColor`. Never delete `engineLabel`/`engineTitle`/`preferredEngine`/`DropdownMenu` imports — speech-provider chain still consumes `preferredEngine`.
-   **v0.7.142–v0.7.143 — Post-install launch**: `customInstall` MessageBox in `installer.nsh` (primary; `IfSilent skipLaunchPrompt` for auto-updater); `MUI_FINISHPAGE_RUN_NOTCHECKED` Run-checkbox is backup. Launch via `Exec '"$INSTDIR\ScriptureLive AI.exe"'`.
-   **v0.7.140 — Single Update Modal + HTML Strip**: canonical mount = `src/app/layout.tsx` (NEVER re-introduce in `page.tsx`); `cleanReleaseNotes()` strips raw HTML; `rehype-raw` deliberately disabled.
-   **v0.7.139 — Installer Polish**: `installerHeader.bmp` MUST be BMP3 24-bit 150×57; `license.txt` uses `(a)…(f)` ASCII (UTF-8 bullets break NSIS).
-   **v0.7.138 — Admin Recent-Activations**: `RecentActivation` reads `paymentRef` off `row.generatedFor?.paymentRef`, NOT `row.paymentRef`; `installId` lives on `LicenseFile` only.
-   **v0.7.137 — Twi/Asante/Ewe Bibles**: `WLDEH_TRANSLATIONS` registry in `src/lib/bibles/twi-bible.ts` mirrored in `scripts/bundle-bibles.mjs`. Pentateuch slugs differ by language (Twi PREFIX `1mose…5mose`, Ewe POSTFIX `mose1…mose5`); Ewe Judges slug double-encoded-safe via `encodeURIComponent`.
-   **v0.7.136 — Bible Lookup Preview**: Bible Lookup tab uses `<OutputPreview>` iframe — never re-introduce a parallel React mirror in any verse-display surface.
-   **v0.7.131–v0.7.135 — Auto-live thresholds**: `CROSS_PIPELINE_SEMANTIC_VS_EXPLICIT_MIN = 0.58` (uniform); SAME-pipeline `LIVE_HIGH_CONF_LOCK = 0.85`. Manual operator click bypasses these gates.
-   **v0.7.134 — Domain Flip**: "Visit website" default = `https://scriptureliveai.com/` in BOTH `src/lib/website-url.ts` and `electron/main.ts` Help menu (keep in lockstep). Replit-app domain remains the API host.
-   **v0.7.132 — GitHub Sweep**: `cleanReleaseNotes()` strips github.com URLs and `by @user in <url>` attributions; `src/lib/github-repo.ts` kept only for electron-updater feed URL parsing.
-   **v0.7.128–v0.7.129 — Update Modal Dismissal + Read-Lock**: per-version `localStorage["sl.update-popup-dismissed.<version>"]` is the shared dismissal contract between `<UpdateAvailableDialog>` (writer) and `<UpdateBanner>` (reader); 8s read-lock blocks SAME-pipeline near-misses but cross-pipeline check runs FIRST.
-   **v0.7.127 — Single-Renderer Output**: `<OutputPreview>` is a thin `<iframe src="/api/output/congregation?preview=1">`; `buildOutputPayload()` in `src/lib/output-payload.ts` is the shared helper for SSE + preview. Suggestions enforced strictly in 0.10–0.499 band for ALL sources.
-   **v0.7.124–v0.7.126 — Installer/Confirm/Font**: `electron-builder.yml` full MUI2 wizard (`oneClick: false`); destructive prompts use `useConfirm()` (Radix `<AlertDialog>`); `fullScreenClamp()` in `src/lib/fonts.ts` is the canonical font-size formula consumed by `fitFont()`. `customInit`/`customUnInit` task-kill macros + `customHeader`/`customWelcomePage`/`customFinishPage` branding macros all live in `installer.nsh` — never collapse them.

### Cross-cutting patterns (no specific version)

-   **Hybrid Deployment**: Single codebase ships both the web (`scripturelive.replit.app`) and the Windows desktop app (Electron wrapping the same Next.js).
-   **API Routing**: API server is routed to `/__api-server` to avoid conflicts with Next.js routes.
-   **NDI Integration**: Browser-only NDI output and native NDI sender via Electron, supporting transparent overlays and configurable display modes, with pixel parity via pinned zoom. As of v0.7.146 the runtime DLL ships bundled — see entry above.
-   **Multi-tiered Speech Recognition**: Deepgram + Whisper + browser engines with auto-fallback, VAD, and hallucination guard, complemented by AI semantic matching.
-   **Atomic License Persistence**: Atomic-write JSON file for local license persistence, ensuring data integrity during deactivation/transfer.
-   **Hard Reset for License State Changes**: Critical license state changes (activate/deactivate/transfer) trigger hard `window.location.assign('/')` reload to prevent renderer crashes from stale React contexts. v0.7.107 extended the same recovery to UNCAUGHT renderer exceptions via `app/global-error.tsx` + `app/error.tsx` — any thrown React error hard-reloads to `/` instead of letting Chromium paint chrome-error://chromewebdata.

### Capabilities

-   Dynamic downloads for various OS, with file hashing and multi-threaded support.
-   Real-time slide updates and live congregation output.
-   NDI broadcasting with configurable display modes (lower-third, full-screen).
-   Advanced speech recognition with AI semantic matching for scripture verses.
-   Comprehensive admin dashboard for managing activations, user licenses, and telemetry.
-   Self-hosted, MoMo-based subscription system with free trial and license transfer.
-   Customizable UI/UX with dark/light themes and theme designer.
-   Voice commands for navigation and verse lookup.
-   Automated email and SMS notifications for activations.

## User preferences

-   **Auto-trim `replit.md` without asking**: whenever the Recent (full detail) section grows past 3 entries, automatically demote the oldest full entry to a one-line guard-rail under "Older releases" and (if not already there) append the full text to `artifacts/imported-app/CHANGELOG.md`. Do this proactively as part of every release bump — never ask permission.

-   After EVERY fix / version bump, build and present a fresh ZIP of `artifacts/imported-app/` so the user can download it and run `BUILD.bat` on their Windows PC. Naming convention: `exports/ScriptureLive-AI-v<version>-source.zip`. Exclude `node_modules`, `.next`, `dist-electron`, `dist-electron-ui`, `release`, `exports`, `uploads`, `upload`, `.turbo`, `.git`, `*.tsbuildinfo`, `build-log.txt`. Always use the `present_asset` tool to surface the zip — never assume the user will find it on their own.
-   Bump the `BUILD.bat` banner version string to match the current `package.json` version on every release.
-   Push to GitHub via REST API after each release: OWNER=`nanvilow`, REPO=`scripturelive-ai`, BRANCH=`main`, token from `GH_PAT` env. Tag every release `v<version>`.
-   When `replit.md` gets long, archive older Architecture-decisions entries to `artifacts/imported-app/CHANGELOG.md` and leave one-line guard-rails behind. **Do this automatically without asking** — at every release bump, keep only the LAST 3 versions in "Recent (full detail)" and demote everything below into one-line guard-rails / move full text to CHANGELOG.

## Gotchas

-   `persist()` operations are fire-and-forget async writes to prevent UI freezes, with retries for disk contention.
-   License state changes trigger hard page reloads; avoid complex UI interactions immediately after them.
-   NDI sender has a 60-second "linger mode" after disconnect to maintain OBS/vMix connections.
-   Renderer crashes are handled by a crash mask that attempts recovery and logs full history.
-   Auto-live confidence thresholds (current as of **v0.7.127**): explicit ≥0.60, semantic ≥0.50, suggestions 0.10–0.499, **1.25 s anti-flicker dwell**, newest-first ordering. The `source==='suggestion'` bypass in `suggestionsFor()` was REMOVED — upstream taggers must keep suggestion-tagged confidence inside the 0.10–0.499 band or the verse is dropped from the UI entirely.
-   The "Detected Verses" card separates explicit / semantic / suggested verses into three columns, each with independent auto-live decisions.
-   The cloud at `scripturelive.replit.app` MUST be redeployed from the same commit BEFORE customers update; otherwise v0.7.145+ activation cloud-fallback hits 404.

## Pointers

-   **Next.js Docs**: [https://nextjs.org/docs](https://nextjs.org/docs)
-   **Express Docs**: [https://expressjs.com/](https://expressjs.com/)
-   **Drizzle ORM Docs**: [https://orm.drizzle.team/](https://orm.drizzle.team/)
-   **Zod Docs**: [https://zod.dev/](https://zod.dev/)
-   **pnpm Workspaces**: [https://pnpm.io/workspaces](https://pnpm.io/workspaces)
-   **Electron Docs**: [https://www.electronjs.org/docs](https://www.electronjs.org/docs)
-   **OpenAPI/Orval**: [https://openapi-generator.tech/](https://openapi-generator.tech/), [https://orval.dev/](https://orval.dev/)
-   **NDI SDK**: [https://ndi.video/sdk/](https://ndi.video/sdk/) (redistribution license permits bundling the runtime DLL inside integrated apps — that's what we do as of v0.7.146).
