# Imported App

A Next.js application providing scripture-related services for web and desktop, enhancing live service experiences, and streamlining administrative tasks.

## User preferences

-   **Autonomy**: Act without asking for consent on routine decisions — always do what's better for the app. Do not ask permission for housekeeping (file trims, refactors, retry logic, dependency bumps, replit.md trims, etc.). Only ask when a decision is genuinely destructive or shapes product direction.
-   **replit.md upkeep**: Roll the changelog window proactively. When shipping a new release entry, demote the oldest "Recent (full detail)" item to a one-line guard-rail in the same pass — do NOT wait to be asked.

## Run & Operate

-   **Install dependencies**: `pnpm install`
-   **Run dev server**: `pnpm dev`
-   **Build app**: `pnpm build`
-   **Typecheck**: `pnpm typecheck`
-   **Codegen**: `pnpm codegen` (for API client)
-   **DB Push**: `pnpm db:push` (for Drizzle schema migrations)
-   **Required Env Vars**: `MAIL_HOST`, `MAIL_USER`, `MAIL_PASS`, `MAIL_FROM`, `SMS_API_KEY`, `SMS_SENDER`

## Stack

-   **Monorepo**: pnpm workspaces
-   **Runtime**: Node.js 24
-   **Language**: TypeScript 5.9
-   **Frontend**: Next.js 16
-   **Backend**: Express 5
-   **Database**: PostgreSQL, SQLite, Replit DB
-   **ORM**: Drizzle ORM, Prisma
-   **Validation**: Zod
-   **Build Tool**: esbuild
-   **API Codegen**: Orval
-   **Speech Recognition**: OpenAI `gpt-4o-mini-transcribe`, Deepgram Nova-3
-   **AI SDK**: OpenAI SDK
-   **NDI Integration**: `koffi` FFI binding to bundled `Processing.NDI.Lib.x64.dll` (NDI 6 runtime ships inside installer as of v0.7.146 — no separate NDI Tools install required)

## Where things live

-   `artifacts/scripturelive-site/`: marketing site (scriptureliveai.com) — Vite + React + Tailwind v4 + Wouter SPA. Migrated from a standalone Replit project into this monorepo. Builds to `dist/public/`. Deploy target: Cloudflare Pages (free). Build cmd: `pnpm --filter @workspace/scripturelive-site run build`. Output dir: `artifacts/scripturelive-site/dist/public`. **Content model**: fully static — all marketing copy lives in six JSON files under `src/content/` (features, pricing, testimonials, page-content, downloads, hero-buttons). `src/lib/content-snapshot.ts` imports them and re-exports as typed constants; `src/lib/api-stubs.ts` returns those to the existing `useList*` hooks so `home.tsx` renders unchanged. **To edit content** (two options): (1) Visual editor at `scriptureliveai.com/admin/` — Decap CMS bootstrap in `public/admin/index.html`, collections defined in `public/admin/config.yml`, GitHub OAuth via Cloudflare Pages Functions at `functions/oauth/auth.js` + `functions/oauth/callback.js`. Requires GitHub OAuth App (callback: `https://scriptureliveai.com/oauth/callback`) + `GITHUB_CLIENT_ID` and `GITHUB_CLIENT_SECRET` env vars on the CF Pages project. (2) Edit JSON directly on GitHub.com (pencil icon). Both paths commit to `main`, triggering a CF Pages rebuild in ~90s. The legacy admin at the old Replit deployment is dead post-DNS-flip. Live admin content was snapshotted on 2026-05-27 (32 records before Daniel-Mensah dedup → 31).
-   `apps/nextjs-app`: Next.js frontend and Electron wrapper.
-   `apps/api-server`: Express API backend.
-   `packages/`: Shared utilities and components.
-   `artifacts/imported-app/db/custom.db`: SQLite database for the desktop app.
-   `artifacts/imported-app/build-resources/ndi/Processing.NDI.Lib.x64.dll`: bundled NDI runtime (v0.7.146).
-   `public/downloads/manifest.json`: Manifest for dynamic downloads.
-   `openapi.yaml`: API contracts (source of truth for Orval codegen).
-   `drizzle/schema.ts`: Drizzle ORM database schema.
-   `styles/theme/`: Theme-related files.

## Sandbox quirks (Replit main agent)

-   **`pnpm install` is blocked** in the main-agent sandbox — pnpm touches `.git/config` and the sandbox classifies it as a destructive git op. To restore a missing `node_modules/@workspace/<lib>` symlink, recreate it manually with `ln -s ../../../../lib/<name> artifacts/imported-app/node_modules/@workspace/<name>`. The `strip:workspace-symlinks` script (run by `pnpm run package*` to flatten symlinks for electron-builder) is a one-way trapdoor in this sandbox — re-running `pnpm install` to restore them isn't possible from here.
-   **`git push` is blocked** in the main-agent sandbox (same `.git/config` touch classification). Ship via the GH Contents API using `GH_PAT` — PUT each changed file to the target `release/v0.7.<N>` branch on `nanvilow/scripturelive-ai`, then trigger `workflow_dispatch` on `release-desktop.yml` (workflow ID `265047930`) with **BOTH** `ref: release/v0.7.<N>` AND `inputs.tag: v0.7.<N>` (the leading `v` is mandatory). The `!cancelled()` job-condition fix for the release-desktop workflow already lives on `release/v0.7.228` (`2e73b7d` + `85d4117`) — cherry-pick onto new release branches if it isn't already there.
-   **CRITICAL workflow_dispatch `inputs.tag` MUST be set (v0.7.247 root cause #2)**: dispatching with `{"ref": "release/v0.7.<N>"}` only — without `inputs.tag` — succeeds the workflow trigger (204) and runs the Next.js + Windows installer builds to completion, but the "Upload installer" step fails with `No files were found with the provided path: artifacts/imported-app/release/ScriptureLive-AI-refs/heads/release/v0.7.<N>-Setup-*.exe`. The Resolve tag step at `release-desktop.yml` L214-229 falls into its else branch when `inputs.tag` is empty: `tag_name="${GITHUB_REF#refs/tags/}"` which leaves the full `refs/heads/release/v0.7.<N>` intact (no `refs/tags/` prefix to strip), then `version=${tag_name#v}` keeps the same string (no leading `v`), then the artifact upload glob (L445) expands to the broken path above. Electron-builder actually produced `ScriptureLive-AI-0.7.<N>-Setup-x64.exe` correctly — the build is fine, only the upload glob is wrong. **Canonical dispatch payload**: `curl -X POST .../actions/workflows/release-desktop.yml/dispatches -d '{"ref":"release/v0.7.<N>","inputs":{"tag":"v0.7.<N>"}}'`. **Operator-detectable signal**: failed run's "Upload installer" step log contains `ScriptureLive-AI-refs/heads/...` in the missing-files path → re-dispatch with `inputs.tag` set, do NOT rebuild from scratch (the artifacts on the runner are already gone, but the build is cheap to re-run — ~22 min on the windows runner).
-   **CRITICAL ship-script EXCLUDE_FILES MUST cover BOTH baked-credential files (v0.7.253 secret-scan failure root cause)**: gitignored locally but the ship-script walker doesn't honour `.gitignore` — it only respects the explicit `EXCLUDE_FILES` set. Required entries: `'src/lib/baked-credentials.ts'` (server SMTP/SMS) AND `'src/lib/keys.baked.ts'` (renderer OpenAI/Deepgram). v0.7.251 → v0.7.253 shipped real Deepgram API key into git history on three release branches because only the server file was in the exclude set. Operator-detectable signal: Secret Scan workflow goes red with `gitleaks` finding `RuleID: deepgram-api-key` (or `openai-api-key`) at `artifacts/imported-app/src/lib/keys.baked.ts:11`. **Mandatory remediation when triggered**: (1) operator rotates the leaked key in the provider console (Deepgram/OpenAI dashboard) — leaked-and-rotated is the only safe state, gitleaks-whitelisting a live key is NEVER acceptable; (2) add the missing path to `EXCLUDE_FILES` in the ship script template for future releases; (3) optionally delete the affected `release/v0.7.<N>` branches via `DELETE /repos/.../git/refs/heads/release/v0.7.<N>` — the GH Release tag + uploaded `.exe` are independent of the branch and survive branch deletion, but the leaked-key commit becomes orphaned and gets GC'd.
-   **CRITICAL ship-drift rule (v0.7.237 root cause)**: every release MUST PUT every drifted file, NOT just `package.json` + `CHANGELOG.md` + `installer.nsh` + `replit.md`. The sandbox's local working tree is the ONLY place feature/fix code lives until explicitly PUT to GitHub — there is no `git push` to discover divergence. v0.7.226 → v0.7.236 all shipped binaries equivalent to v0.7.225 with version bumps because the 4-file PUT loop missed every feature file. **Mandatory pre-ship drift check**: enumerate every file under `artifacts/imported-app/` (excluding `node_modules`, `.next`, `dist`, `src/generated/prisma-client`, `tsconfig.tsbuildinfo`, `next-env.d.ts`, gitignored baked-credentials), compute git blob SHA locally, compare against remote `main` via `GET /repos/.../git/trees/main?recursive=1`, PUT every mismatch + every local-only non-build file to the release branch. The bulk-PUT Python script in v0.7.237 CHANGELOG entry is the canonical reference. **Operator-detectable signal**: if a fix landed in source 3+ releases ago and a freshly-installed binary still doesn't show it, the FIRST diagnostic is `curl /repos/.../contents/<file>?ref=main | base64 -d | grep <fix-marker>` — NOT another reinstall, NOT a state-cleanup walkthrough.

## Architecture decisions

> Detailed historical entries live in `artifacts/imported-app/CHANGELOG.md`. The list below keeps only the **last release in full detail** plus tight one-line guard-rails for older patterns we still want future agents to respect.

### Recent (full detail)

-   **v0.7.264 — HISTORY/QUEUE consecutive-duplicate suppression + admin BIN bulk-delete + cross-device sync root-cause fix**: Three batched operator-reported fixes. **(1) Duplicate spam in HISTORY + QUEUE panels** (operator screenshots showed "Habakkuk 1:1 MSG" ×4, "John 3:16" ×2 in HISTORY; "Romans 8:28" ×3 in QUEUE). Root cause: the speech pipeline re-emits the same final hypothesis every frame and AUTO LIVE re-fires the same reference frame-after-frame (v0.7.187.2 re-mention promotion mints a fresh id each time), so `addToVerseHistory` (`src/lib/store.ts`) and `addScheduleItemQuiet` both appended identical consecutive rows (`addDetectedVerse` already deduped; these two did not). **Fix**: `addToVerseHistory` skips the append when the MOST-RECENT entry matches incoming `reference`+`translation`; `addScheduleItemQuiet` skips when the LAST schedule item matches `type`+`title`+`subtitle` and returns the existing row id. A DIFFERENT entry in between breaks the run, so a verse can legitimately reappear later. `addScheduleItem` (manual Go-Live append) is intentionally NOT deduped. **(2) Admin BIN bulk delete**: added a select-all header Checkbox + per-row Checkboxes to the soft-deleted (Bin) table in `admin-modal.tsx`, plus a bin-specific bulk bar doing `bulkDelete('activation', …, {permanent:true})` (the active-view soft-delete bulk bar is gated `!codesShowBin`). Selection (`codesSelected`) is cleared on EVERY view switch — both the Bin toolbar toggle AND the stat-tile drill-down — and the bin delete is defensively scoped to `codesData.bin ∩ codesSelected` so a stale active-view selection can never become a permanent-delete target. **(3) "Records show differently on PCs"**: ROOT CAUSE — `DEFAULT_CLOUD_BASE` in `src/lib/licensing/cloud-sync.ts` was still `https://scripturelive.replit.app` while v0.7.256 only flipped the DIAGNOSTIC `cloud-sync-test` route to Hetzner. Desktop installs (with `SCRIPTURELIVE_CLOUD_BASE` unset) were syncing real admin records against the DEAD Replit cloud while the badge showed "connected" → each PC diverged. Flipped the default to `https://cloud.scriptureliveai.com`. This is a CLIENT-SIDE (.exe) fix — the Hetzner cloud is the unchanged source of truth, so NO Hetzner redeploy was needed (none of the changed files are server-side cloud code or in `deploy-hetzner.yml`'s paths filter). Verified: full vitest **723/723** green (+5 new dedup regression specs in `src/lib/history-queue-dedup.test.ts`), `tsc --noEmit` exit 0, architect review PASS (caught + fixed the stat-tile cross-view selection bleed before ship). **GUARD-RAIL (consecutive-dedup is LAST-entry only, never global)**: `addToVerseHistory` / `addScheduleItemQuiet` compare ONLY against the single most-recent entry — do NOT widen to a full-list scan or a verse can never reappear after an intervening one. `addScheduleItem` stays un-deduped (manual operator intent). **GUARD-RAIL (admin selection MUST clear on every view switch)**: any new code path that toggles `codesShowBin` or the active/bin filter MUST `setCodesSelected(new Set())`; the bin permanent-delete MUST stay scoped to `codesData.bin ∩ codesSelected`. **GUARD-RAIL (cloud-base default lockstep)**: `DEFAULT_CLOUD_BASE` (`cloud-sync.ts`) and the `cloud-sync-test` route default MUST both point at `cloud.scriptureliveai.com`; a future cloud move MUST flip BOTH plus the three v0.7.256 URL defaults. Self-loop note: the Hetzner cloud sets `SCRIPTURELIVE_CLOUD_BASE` to itself so it talks to itself on admin reads — benign/convergent today (echo-push suppressed), but the `REPLIT_DEPLOYMENT_ID` self-loop guard does NOT fire on Hetzner; if cloud-side self-traffic ever matters, add an explicit `SCRIPTURELIVE_IS_CLOUD` env guard.

### Older releases (one-line guard-rails only — full text in `artifacts/imported-app/CHANGELOG.md`)

-   **v0.7.263 — Live-detection hot-path perf: per-frame regex recompilation eliminated**: `parseVerseReference` (`src/lib/bible-api.ts`) and `findTranslationAlias` (`src/lib/voice/commands.ts`) run on EVERY transcript frame — both had their RegExps hoisted to module scope (`VERSE_PATTERNS_NONGLOBAL` strips only `g`/keeps `i`; `TRANSLATION_ALIAS_REGEX` precompiled longest-first). GUARD-RAIL: any RegExp inside a per-frame detection fn MUST be compiled once at module load, NEVER `new RegExp(...)` per call; shared-instance reuse is safe ONLY for non-global (no `g`/`y`) patterns. Rejected (do NOT re-suggest): (1) `optimizePackageImports`/`lucide-react` in the Electron build = v0.7.45 GHA EPERM regression (Cloud-Run-only BY DESIGN); (2) narrowing broad `useAppStore()` subs in `logos-shell.tsx`/`easyworship-shell.tsx` = stale-UI risk; (3) rewriting `verse-auto-live.ts` `pickAutoLiveBySource` = fragile tie-break; (4) throttling interim to `isFinal` = deliberate behaviour change; (5) `isPopulated` O(N)→O(1) = NOT hot.

-   **v0.7.262 — Live Display chevrons preserve pinned Preview media**: `onPrev`/`onNext` (the `<`/`>` chevrons beside GO LIVE in the Live transport strip) MUST NOT touch `previewSlideIndex` when `pinnedPreviewSlide` exists — the pin is sticky against deck navigation; `>` with a pin routes through `goLive()` to promote. GUARD-RAIL: every code path that mutates the Preview pane (`setPreviewSlideIndex` / `clearPinnedPreview` / deck-nav / auto-advance / keyboard / hotkey / AI-fire) MUST check `useAppStore.getState().pinnedPreviewSlide` and short-circuit unless the gesture was Clear Preview, GO LIVE-promote, or pin-replace. Canonical grep: `rg "setPreviewSlideIndex" artifacts/imported-app/src/components/layout/logos-shell.tsx`.

-   **v0.7.261 — loadTranslation() lowercases the translation key**: store passes `'KJV'` (uppercase display form) but bundled JSON is `kjv.json`; `loadTranslation` body MUST begin with `const t = String(tRaw).trim().toLowerCase()` BEFORE the cache check, or every `lookupVerse` / `lookupRange` returns null and AUTO LIVE silently leaks the trigger paraphrase to the projector. Regression contract: `src/lib/bibles/local-bible.test.ts` 8/8 (incl. Matthew 14:21 / Mark 5:25 / Esther 6:1 KJV) — do NOT relax. Canonical grep: `rg "function loadTranslation" artifacts/imported-app/src/lib/bibles/local-bible.ts`.

-   **v0.7.260 — AUTO LIVE useEffect mirrors v0.7.253 sendDetected() lookup chain**: AUTO LIVE useEffect at logos-shell.tsx ~L4842 was building slide.content INLINE from `best.text` with zero lookup, bypassing v0.7.253 sendDetected() resolution. Mirrored `parseVerseReference → lookupRange/lookupVerse → fallback best.text` chain into the useEffect with defensive try/catch. Necessary but insufficient until v0.7.261 fixed the loadTranslation case bug. **Two-site lockstep guard-rail**: any future verse-slide construction site (hotkey-fire, gesture-fire, remote-fire) MUST do the same lookup-then-fallback or paraphrase-leak returns for BRQ detections. Canonical grep: `rg "type: 'verse' as const" artifacts/imported-app/src/components/layout/logos-shell.tsx`. `best.text` for `source: 'semantic'` is ALWAYS the trigger phrase, NEVER the canonical verse — only safe render path is re-fetch via `lookupVerse/lookupRange(v.reference, v.translation || 'KJV')`.

-   **v0.7.259 — Fresh explicit utterance preempts semantic re-fire on same-frame tiebreak**: Operator screenshot 2026-05-28 16:03 — speaker said "let's go to Genesis 2:1" but Live stayed on older "Matthew 14:25 — Jesus walked on the sea" semantic match. Root cause: `store.ts` v0.7.187.2 re-mention promotion gives every re-detected semantic verse a fresh `detectedAt` every frame, and the v0.7.152 unconditional semantic-wins tiebreak handed it the projector back every frame — explicit could never win when ANY background semantic activity was happening. Fix: guarded escape clause BEFORE the v0.7.152 branch in `shouldFireAutoLiveStable` — when both columns fire same frame AND `detectedAtMs(explicitFire) > detectedAtMs(semanticFire)` (strict `>`), explicit wins. Asymmetric `>` preserves same-tick ties for semantic per v0.7.152 stale-AVM protection. Canonical pre-ship grep: `rg "detectedAtMs\(explicitFire\) > detectedAtMs\(semanticFire\)" artifacts/imported-app/src/lib/verse-auto-live.ts` — exactly one site, one strict `>`. Both this fix AND the v0.7.187.2 re-mention promotion depend on `detectedAt: new Date()` being stamped at regex-hit time in `speech-provider.tsx` (explicit) and at promotion time in `store.ts` ~L1143 (semantic) — stale-stamped detections regress in opposite directions. Three test specs in `verse-auto-live.test.ts` (fresh-explicit-wins + stale-explicit-loses + same-tick-goes-to-semantic).

-   **v0.7.258 — Operator dashboard density pass: "ALL" default + footer removal + slim transport bar + Media-narrower bottom row**: Four batched UX changes from operator screenshots 2026-05-27 22:00 reclaiming vertical/horizontal chrome for the bottom-row Chapter Nav / Detected Verses / Media cards. (1) `bibleOnlyTranscription` default flipped `true` → `false` (fresh installs land on full transcript with "ALL" pill un-highlighted; existing operators keep persisted choice). (2) Bottom `<footer h-7>` "Powered by WassMedia" branding REMOVED (lives in top-left app header only — re-adding a bottom strip negates the claw-back). (3) `TransportBar` compressed h-14 → h-9 with buttons h-10 → h-7 — single source of truth in `easyworship-shell.tsx`, imported by `logos-shell.tsx`. (4) Bottom-row column widths 33/33/34 → 39/39/22 (Media narrowed, Chapter Nav + Detected Verses widened); all three `ResizablePanel.defaultSize` props MUST sum to 100 in lockstep, `autoSaveId='logos-shell-bottom-cols'` keeps existing operator drags.

-   **v0.7.257 — AVM auto-fire unfreezes on fresh re-utterance + reference typography & Bible line-height defaults**: v0.7.152 semantic-owns-live latch release clause extended to also fire when `detectedAtMs(explicitTop) > gate.lastFireAtMs` — audio-buffer-lag re-runs carry the original timestamp (stale, latch holds), but fresh new utterances carry a newer timestamp (latch releases) so a re-spoken address (e.g. operator says "Ephesians 6:11" twice with a paraphrase in between) actually fires live. Audit-once before any explicit-pipeline refactor: `rg "detectedAt.*Date\.now|detectedAt:.*now" artifacts/imported-app/src/components/providers/speech-provider.tsx` — every explicit detection ingestion site MUST stamp `detectedAt: Date.now()` at regex-hit time (NOT store-merge time, NOT inherited from the source utterance buffer); stale-stamped detections regress to v0.7.152 behaviour. Default flips (Bible line-height 1.20 → 1.10 + referenceFontSize 'xl' + referenceTextScale 1.55) follow the v0.7.252 lockstep pattern — `store.ts` DEFAULT_SETTINGS + `settings.tsx` `??` fallback in sync. Existing operators keep persisted values via Zustand persist.

### Archived releases (v0.7.252 and older)

Older one-line guard-rails live in `artifacts/imported-app/ARCHIVE.md` (newest at top). Full release text remains in `artifacts/imported-app/CHANGELOG.md`. When rolling the window forward, demote the oldest "Older releases" one-liner(s) into the top of `ARCHIVE.md` to keep this file lean — current cutoff is v0.7.257.
