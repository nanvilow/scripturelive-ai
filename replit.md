# Imported App

A Next.js application providing scripture-related services for web and desktop, enhancing live service experiences, and streamlining administrative tasks.

## User preferences

-   **Autonomy**: Act without asking for consent on routine decisions — always do what's better for the app. Do not ask permission for housekeeping (file trims, refactors, retry logic, dependency bumps, replit.md trims, etc.). Only ask when a decision is genuinely destructive or shapes product direction.
-   **replit.md upkeep**: Roll the changelog window proactively. When shipping a new release entry, demote the oldest "Recent (full detail)" item to a one-line guard-rail in the same pass — do NOT wait to be asked.

## Run & Operate

-   **Install dependencies**: `pnpm install`
-   **Run dev server**: `pnpm dev`
-   **Build app**: `pnpm build`
-   **Typecheck**: `pnpm typecheck`
-   **Codegen**: `pnpm codegen` (for API client)
-   **DB Push**: `pnpm db:push` (for Drizzle schema migrations)
-   **Required Env Vars**: `MAIL_HOST`, `MAIL_USER`, `MAIL_PASS`, `MAIL_FROM`, `SMS_API_KEY`, `SMS_SENDER`

## Stack

-   **Monorepo**: pnpm workspaces
-   **Runtime**: Node.js 24
-   **Language**: TypeScript 5.9
-   **Frontend**: Next.js 16
-   **Backend**: Express 5
-   **Database**: PostgreSQL, SQLite, Replit DB
-   **ORM**: Drizzle ORM, Prisma
-   **Validation**: Zod
-   **Build Tool**: esbuild
-   **API Codegen**: Orval
-   **Speech Recognition**: OpenAI `gpt-4o-mini-transcribe`, Deepgram Nova-3
-   **AI SDK**: OpenAI SDK
-   **NDI Integration**: `koffi` FFI binding to bundled `Processing.NDI.Lib.x64.dll` (NDI 6 runtime ships inside installer as of v0.7.146 — no separate NDI Tools install required)

## Where things live

-   `apps/nextjs-app`: Next.js frontend and Electron wrapper.
-   `apps/api-server`: Express API backend.
-   `packages/`: Shared utilities and components.
-   `artifacts/imported-app/db/custom.db`: SQLite database for the desktop app.
-   `artifacts/imported-app/build-resources/ndi/Processing.NDI.Lib.x64.dll`: bundled NDI runtime (v0.7.146).
-   `public/downloads/manifest.json`: Manifest for dynamic downloads.
-   `openapi.yaml`: API contracts (source of truth for Orval codegen).
-   `drizzle/schema.ts`: Drizzle ORM database schema.
-   `styles/theme/`: Theme-related files.

## Sandbox quirks (Replit main agent)

-   **`pnpm install` is blocked** in the main-agent sandbox — pnpm touches `.git/config` and the sandbox classifies it as a destructive git op. To restore a missing `node_modules/@workspace/<lib>` symlink, recreate it manually with `ln -s ../../../../lib/<name> artifacts/imported-app/node_modules/@workspace/<name>`. The `strip:workspace-symlinks` script (run by `pnpm run package*` to flatten symlinks for electron-builder) is a one-way trapdoor in this sandbox — re-running `pnpm install` to restore them isn't possible from here.
-   **`git push` is blocked** in the main-agent sandbox (same `.git/config` touch classification). Ship via the GH Contents API using `GH_PAT` — PUT each changed file to the target `release/v0.7.<N>` branch on `nanvilow/scripturelive-ai`, then trigger `workflow_dispatch` on `release-desktop.yml` (workflow ID `265047930`) with `ref` set to that branch. The `!cancelled()` job-condition fix for the release-desktop workflow already lives on `release/v0.7.228` (`2e73b7d` + `85d4117`) — cherry-pick onto new release branches if it isn't already there.

## Architecture decisions

> Detailed historical entries live in `artifacts/imported-app/CHANGELOG.md`. The list below keeps only the **last release in full detail** plus tight one-line guard-rails for older patterns we still want future agents to respect.

### Recent (full detail)

-   **v0.7.235 — Brightness on receivers + strict-follow sync rewrite + branded icon + voice cmds (verse/vest/chapter N) + paraphrase engine 100→582**: batch close on all 5 operator escalations against v0.7.234. **Fix A (brightness on foreground media)**: `route.ts` L1880 `mediaStyle` template literal now appends `opacity:var(--bg-opacity,1)` so foreground `<video id="liveVideo">` (sermon clips, ID videos, hymn backings) inherits the brightness slider's `--bg-opacity` var. v0.7.227 wired the var but only bound it to bg selectors; v0.7.230 widened to the bg layer chain; this completes the chain to the foreground media surface. Operator-perception "slider doesn't work on NDI/OBS/secondary" closes. **Fix B (strict-follow sync rewrite — pause/scrub-back desync on OBS/Wirecast)**: `route.ts` L1966-2002 rewritten to a transport-event detection model with `playbackRate` trim. HARD-SNAP triggers (`currentTime = snapshot.time` immediately, NDI included): pause-state change, backward jump >0.3s (scrub-back), forward jump >2s (scrub-fwd or seek). SOFT-TRIM (rate 0.97 if ahead / 1.03 if behind / 1.0 if within ±0.15s): routine drift inside ±0.5s tolerance now uses smooth rate-trim instead of seek-correction — no more decoder-flush stutter on every micro-correction. Rate reverts to 1.0 on convergence (no oscillation). **Fix C (branded Electron icon)**: `build-resources/` (icon.ico 181KB + icon.png 443KB + 3 installer BMPs + installer.nsh + license.txt + notarize.js + entitlements.mac.plist) uploaded to main via GH Contents API in prior session; operators must reinstall (not auto-update) to see new icon on existing installs — Windows caches the exe icon at install time. **Fix D (voice cmds verse/vest/chapter N)**: THREE layers updated. `commands.ts` — new `CommandKind: show_chapter_n` with `chapterNumber?` field; `detectShowVerseCommand` regex widened to `verse|verses|vest|vs` (vest = most-common Deepgram + gpt-4o-mini-transcribe mishearing of "verse"); new `detectShowChapterCommand` mirrors verse-N grammar at chapter granularity (bounds 1-150). `llm-classifier.ts` — `show_chapter_n` in `LLM_INTENT_KINDS` + Zod field + dispatch label + 2 prompt rules (explicit "vest" mishearing guidance + chapter-N rule, prefers `go_to_reference` when book is also named). `speech-provider.tsx` L1214 — new dispatcher case `show_chapter_n` reads live slide → `parseExplicitReference(slide.title)` → validates N against `bibleStructure[book].length` → `lookupVerse` + `fetchBibleVerse` fallback → `setLiveAuto` (matches v0.7.214 LIVE-only semantics). Toast feedback on no-live / unparseable / out-of-range. **Fix F (paraphrase engine 100→582)**: new `src/lib/ai/paraphrase-verses.ts` exports `PARAPHRASE_VERSES: readonly PopularVerse[]` with 582 curated short modern paraphrases organized canonically (Genesis through Revelation; Beatitudes + Lord's Prayer + Sermon on the Mount + I AM statements + Romans Road + 1 Cor 13 + fruit of the Spirit + armor of God + Hebrews 11 all covered line-by-line). Folded into `semantic-matcher.ts` L380 `ensureCache` builder alongside `POPULAR_VERSES_KJV` + `preacherCatalogueAsVerses()` — same v0.7.67 "more surface forms per reference = more cosine recall" pattern. Cache grows from ~300 → ~880 vectors; one-time boot cost ~$0.001. **GUARD-RAIL (foreground media fallback `1`, bg fallback `.85`)**: foreground `mediaStyle` opacity binding MUST use `var(--bg-opacity,1)`; bg selectors MUST keep fallback `.85`. At operator-picked brightness=85 both render identically; asymmetry only matters before `applyRender()` writes the var — bg already-dimmed-to-baseline (no flash on receiver init) AND foreground at-full (no surprise dim on fresh transport `<source>`). Swapping either breaks one of those first-paint contracts. **GUARD-RAIL (text immunity preserved)**: NO opacity binding on `.slide-text` / `.verse-text` / `.reference-text`. WCAG-AA depends on text-shadow stack. Do NOT add `opacity:var(--bg-opacity)` to any text selector without first widening the text-shadow stack. **GUARD-RAIL (NDI exemption is for ROUTINE drift, not transport events)**: the `!IS_NDI` gate on the drift-correction path MUST apply to soft-trim/seek-correction only. HARD-SNAP on explicit transport events (pause-state change, scrub-back >0.3s, scrub-fwd >2s) MUST fire on NDI receivers too. NDI's software-decode compositor drifts past tight tolerance within seconds → routine drift correction would flush the decoder on every check (v0.7.194 lesson). But operator-initiated pause/scrub is the OPPOSITE of routine — receivers must follow immediately. The two paths are now structurally separate; preserve that separation. **GUARD-RAIL (playbackRate trim bounds)**: rate values MUST stay in `[0.97, 1.03]` for soft-trim. Wider (0.90/1.10) produces audible pitch artefacts on hymn backings + OBS Browser source audio capture. Tighter (0.99/1.01) takes 10+ seconds to converge 0.3s drift. Current envelope converges 0.3s in ~10s, no audible pitch shift. **GUARD-RAIL (pair-bound throttle invariant preserved from v0.7.234)**: `logos-shell.tsx` L490 writeback `0.20s` < `route.ts` L1913 drift tolerance `0.50s` MUST hold (2.5x headroom). Rate-trim respects same envelope. **GUARD-RAIL (chapter detector runs AFTER verse detector)**: `detectCommand` dispatch order MUST be verse-then-chapter. Chapter regex tail `(\d+)` would otherwise capture "5" from "verse 5" and mis-fire `show_chapter_n`. Verse detector's stricter prefix (`verse|verses|vest|vs`) is the only thing keeping the two grammars disjoint at regex level — reordering breaks every "verse N" command silently. **GUARD-RAIL ("vest" stays alternation, NOT fuzzy)**: `detectShowVerseCommand` MUST keep `verse|verses|vest|vs` as literal alternation, NOT a `verse?` or Levenshtein-1 match. Fuzzy matching would let "best", "rest", "west", "first" all trigger verse-jumps — those are normal sermon words. The alternation is a curated whitelist of the only ASR mishearings worth accepting. **GUARD-RAIL (PARAPHRASE_VERSES book strings MUST match popular-verses.ts canonical)**: `Psalms` not `Psalm`, `1 Samuel` not `I Samuel`, `Song of Solomon` not `Song of Songs`. Drift causes re-fetch in non-bundled translation to fail silently via `fetchBibleVerse` → operator sees chip but click-to-live errors. **GUARD-RAIL (paraphrase duplicates of references in POPULAR_VERSES_KJV are INTENTIONAL)**: dedupe on reference is INTENTIONAL at the matcher AND chip-display layer — but NOT at the embedding cache layer. Each surface form (canonical KJV vs short paraphrase vs preacher-phrasing) embeds to a different vector with the same reference; cosine winner returns reference, chip layer collapses on reference at display. Removing duplicates from embedding cache CUTS recall, doesn't improve it. **PROCESS GR (curated > LLM-generated for paraphrases)**: full-Bible paraphrase coverage (31,102 verses × 5-20 paraphrases = 150K-600K mappings) quality-cliffs hard on the long-tail. LLM-generated paraphrases for verses like Num 26:34's census detail produce low-information variants that match too many things and pollute top-K. Curated beats auto-generated 10:1 in matcher precision because every curated entry is a memorable, frequently-quoted line worth a vector. Tier 3 (full-Bible HNSW pack) is a separate project. **Tests**: `pnpm exec tsc --noEmit` clean.

-   **v0.7.234-condensed**: paired throttle drop `logos-shell.tsx` L490 live writeback `0.50s→0.20s` AND `congregation/route.ts` L1913 receiver drift tolerance `1.5s→0.5s`; preview 0.25s untouched; NDI `!IS_NDI` drift exemption preserved (rewritten in v0.7.235 to apply to routine drift only, not transport events). Module-local broadcast suppression gate `src/lib/broadcast-gate.ts` (`setBroadcastSuppressed`/`isBroadcastSuppressed`/`onSuppressionChange`); `Slider` (`ui/slider.tsx`) `onPointerDown`→gate true + window-level pointerup one-shot release; `OutputBroadcaster.schedule()` short-circuits on suppression, `onSuppressionChange` subscriber IMMEDIATE flush on release (bypasses 16ms debounce). Operator Preview/Live cards update locally during drag (read Zustand directly); only receivers stay calm. **GUARD-RAIL pair-bound throttle invariant**: `logos-shell.tsx` L490 writeback < `congregation/route.ts` L1913 drift tolerance MUST hold; current pair `0.20s/0.50s` = 2.5x headroom; both move together. **GUARD-RAIL window-level pointerup**: MUST attach to `window` NOT Slider Root (Radix lifts pointer capture outside track hit-test on wide drag — Root-level handler misses release, gate stuck-true silently freezes every SSE broadcast app-wide). **GUARD-RAIL unmount cleanup releases gate**: Slider `useEffect` cleanup MUST call `setBroadcastSuppressed(false)` unconditionally. **GUARD-RAIL immediate flush on release**: `onSuppressionChange` subscriber MUST skip 16ms `schedule()` debounce and call `flush()` directly. **GUARD-RAIL suppression check FIRST in schedule**: order MUST be `if(cancelled) return; if(isBroadcastSuppressed()) return; if(flushTimerRef.current) return; setTimeout(...)`. **GUARD-RAIL broadcast gate is module-local NOT Zustand**: gate MUST live in `broadcast-gate.ts` as plain mutable module state — Zustand would itself trip `useAppStore.subscribe` on every flip. **PROCESS GR (proof-first investigation before patching)**: every operator escalation MUST be traced to a file:line root cause with quoted code shown to user BEFORE any edit. Speculative fixes forbidden. **PROCESS GR (operator perception ≠ bug)**: "X looks wrong on receiver Y" reports MUST be validated against the math BEFORE patching the rendering path — perception bugs caused by sync lag get mis-attributed to brightness/colour/contrast.

-   **v0.7.233 — Upload-time poster extraction for library video tiles + NDI alpha-fill SIMD optimisation**: `library-compact.tsx` L1156/L1226/L1273/L1555 — `extractFirstFrameJpeg(file)` via `URL.createObjectURL(file)` (NOT FileReader dataURL — 200ms vs 3-5s on 50MB clips), 5s timeout, settled-guard, releases URL on every settle; `handleFiles` awaits BEFORE addMediaItem; tile JSX `poster={m.posterUrl}` + `preload={posterUrl ? "none" : "auto"}`. freezeBg pattern preserved as `!posterUrl` legacy fallback. NDI alpha-force `ndi-service.ts` L1108 replaced per-byte `slot[i]=0xff` loop with `Uint32Array` view + `|= 0xff000000` bitwise (V8 SIMD-vectorises, ~4x). **GUARD-RAIL**: `Uint32Array` constructor MUST take all 3 args `(slot.buffer, slot.byteOffset, slot.length>>>2)` — dropping byteOffset aliases neighbouring slot (silent memory corruption). LE assumption for `0xff000000` mask is correct on x64/arm64; BE port flips to `0x000000ff`. Bulk uploads MUST be serialised (`for (const file of queue)` + `await extractFirstFrameJpeg(file)`, NOT `forEach(async)`) — parallel extraction holds finite HW decoder slots that compete with foreground Preview/Live `<video>` surfaces (v0.7.216 + v0.7.222 invariant).
-   **v0.7.231 — Reverse v0.7.227/v0.7.222 standby chrome + paint media-tile thumbnails**: dropped amber STANDBY badge + "Play preview" CTA overlays (kept freezeBg poster `<video>` above). `VideoTransport.onPlay` flips `setMediaPaused(false)` BEFORE `findVideo()` (standby poster omits `data-surface="preview"`). `library-compact.tsx` video-tile back to v0.7.225 freezeBg pattern. **GUARD-RAIL**: freezeBg `<video>` lifecycle (`preload="auto"` + `loadeddata→play().then(pause).catch(pause)` + `onPlay→setTimeout(pause,0)`, muted, playsInline, no autoplay/loop) is FOURTH site in lockstep with route.ts setBgVid + standby poster + setPreheatVid + library tile. v0.7.233 added a FIFTH site (upload-time poster extraction at `library-compact.tsx` L1226 `extractFirstFrameJpeg`) — any change MUST be made across all five.
-   **v0.7.230 — `resolveMediaFit` 5-way helper + foreground media brightness binding + OBS BGRA opt-in**: `src/lib/media-fit.ts` canonical 5-case `mediaFit → {objectFit, aspect}` consumed by `MediaVideoSurface` + standby poster + container `aspectRatio` (4-surface lockstep with `route.ts` + `slide-renderer.tsx`). `--bg-opacity` foreground binding fallback `1` vs bg `.85` is pair-invariant (bg dims at baseline, fg stays 100% until applyRender writes var). NO opacity binding on text selectors — WCAG-AA depends on text-shadow stack. `ndiForceBgraForObs` plumbed 5 layers (settings type + default false, ndi-service FourCC + stop reset, main.ts forward, preload + use-electron types, panel UI + 2 ndi.start callsites + restart guard token). **GUARD-RAIL**: sender-create-time settings (FourCC, resolution, fps, sourceName, transparent flag) MUST be plumbed through all 5 layers AND included in restart guard token AND reset in `stop()`. **PROCESS GR**: main-agent ship pipeline = `pnpm exec tsc --noEmit` → bump → CHANGELOG prepend → replit.md demote-on-ship → GH Contents API PUT to `release/v0.7.<N>` branch → workflow_dispatch on `release-desktop.yml` (workflow ID `265047930`) with `ref` set to that branch. `!cancelled()` job-condition fix lives on `release/v0.7.228` (`2e73b7d` + `85d4117`) — cherry-pick if not on target branch.
### Older releases (one-line guard-rails only — full text in `artifacts/imported-app/CHANGELOG.md`)

-   **v0.7.232 — Card video-stage 4-property invariant**: every Card body mounting an aspect-ratio'd video frame + shrink-0 footer rows (transport bar, LiveBottomControls, Clear-Preview) MUST use the full `flex-1 min-h-0 overflow-hidden flex items-stretch` quartet on its stage div (PreviewCard logos-shell.tsx L1340 + LiveDisplayCard L2202). Dropping `overflow-hidden` lets aspect-ratio's intrinsic `width/ratio` contribution beat `min-h-0`'s 0 lower bound and push the stage past its `flex-1` allocation, clipping the shrink-0 transport sibling. Card primitive (L153) does NOT enforce this — stages WITHIN the body are an artifact-author responsibility, copy the quartet from PreviewCard or LiveDisplayCard. Don't pad the transport — VideoTransport root already has shrink-0 at L1690 since v0.7.193; the fix is upstream (clip the stage), not downstream (pad the transport).
-   **v0.7.229 — Standby-poster click-side predicate widening + per-clip mediaFit on standby poster (shipped inside v0.7.230)**: TWO sites in `library-compact.tsx` + `logos-shell.tsx` L3810 MUST keep the URL-tail regex `/\.(mp4|webm|mov|m4v|mkv|ogv)(\?|#|$)/i` in lockstep with the render-side gate at `logos-shell.tsx` L1197 (v0.7.222 Fix #8). Adding a new extension to one site WITHOUT updating all THREE re-opens the dual-decoder contention bug. Standby poster per-clip fit now flows through the v0.7.230 `resolveMediaFit` helper — see v0.7.230 entry above for the 4-surface lockstep.
-   **v0.7.228 — Output/NDI startup-delay preheat via `setPreheatVid`**: `output-payload.ts` MUST emit `preheatMediaUrl` only when `pinnedPreviewSlide` is a media-video AND its `mediaUrl !== cur.mediaUrl`. `congregation/route.ts` `setPreheatVid` MUST use `preload="auto"`, kick `play().then(pause)` in `loadeddata` (NOT `loadedmetadata`), `setTimeout(pause, 0)` in `play` listener. `#preheatLayer` MUST stay off-screen (left/top:-99999px, opacity:0, pointer-events:none) so compositor skips the layer. URL dedup via `__preheatUrl` module-local MUST come BEFORE teardown loop.
-   **v0.7.227 — `bgBrightness` CSS-var system + freezeBg standby-poster `<video>`**: `--bg-opacity` / `--bg-scrim` driven by `settings.bgBrightness`, mapping `scrim=(1-opacity)*0.333`, slider `min={10}`. Var fallback baseline `.85`/`.05` (v0.7.226 visual baseline). Freeze-bg poster `<video>` MUST keep `preload="auto"` (NOT `'metadata'`/`'none'`), no autoplay, no loop, `muted`, `playsInline`, `loadeddata→play().then(pause).catch(pause)`, `play→setTimeout(pause,0)`. WCAG-AA legibility at low brightness depends on `.slide-text` `text-shadow` stack. v0.7.230 extended `--bg-opacity` to foreground media (fallback `1`); pair invariant + lockstep now spans 4 selectors not 3.
-   **v0.7.226 — Operator brightness pick `.85 / .05`**: var fallback baseline (3 pair-bound selectors). Pair invariant: opacity + scrim alpha move together on the same surface. v0.7.227 enforces by source (single slider drives all). WCAG-AA legibility at `.85/.05` depends on `.slide-text` `text-shadow` stack — do NOT remove the shadow at this brightness.
-   **v0.7.225 — freezeBg first-frame paint**: `route.ts` setBgVid branch MUST use `v.preload='auto'` (NOT `'metadata'`/`'none'`), MUST kick `v.play().then(pause).catch(pause)` in `loadeddata`, MUST `setTimeout(pause, 0)` in `play` listener (NOT synchronous), AND MUST NOT call synchronous `v.pause()` after `layer.appendChild(v)`. Re-used by v0.7.227 standby poster AND v0.7.228 `setPreheatVid`.
-   **v0.7.224 — goLive resume seek**: `logos-shell.tsx` + `easyworship-shell.tsx` goLive pinned-path MUST call `setLiveAuto(pinned)` BEFORE `setLiveMediaCurrentTime(resumeFrom)` (atomic reset in setLiveAuto would clobber otherwise). `MediaVideoSurface` play-effect `canplay` handler MUST re-apply pending seek from `useAppStore.getState()` directly (NOT closed-over `mediaCurrentTime`) before `play()`.
-   **v0.7.223 — NDI opaque + BGRX FourCC**: `main.ts` `wantTransparent` AND URL-param branch BOTH MUST be `opts.transparent === true` (NOT `!== false`). `ndi-service.ts` `nativeSendFrame` MUST pick `senderTransparent ? FOURCC_BGRA : FOURCC_BGRX`; opaque path MUST force `slot[i] = 0xff` at i=3,7,11,... for old-receiver BGRA fallback. `stop()` MUST reset `senderTransparent = false`. v0.7.230 added `forceBgraForObs` override path — see v0.7.230 entry.
-   **v0.7.222 — Single-click STANDBY gate (5-condition AND gate)**: `logos-shell.tsx` `previewStandbyForLive` MUST stay intact. URL-tail regex fallback for legacy persisted slides (mediaKind missing) MUST stay in `liveIsMediaVideo` upstream — and now ALSO in BOTH click-side handlers per v0.7.229 lockstep (3 sites total). Predicate widening in render-side without click-side widening is silently broken.
-   **v0.7.220 — NDI async send + buffer pool**: `ndi-service.ts` hot path MUST use `send_send_video_async_v2` NOT sync v2, `clock_video:false`, 2-slot `videoBufferPool` with `(i+1)%2` advance. `armBridge` setInterval MUST be `Math.max(16, Math.floor(1000/fps))`. `lingerStop`/`gracefulStop` fade loops MUST `await setTimeout(frameMs)` between sends.
-   **v0.7.219 — MediaPreheat link tag**: warm-up MUST use `<link rel="preload" as="video" crossOrigin="anonymous">` in `document.head` (NOT hidden `<video preload="auto">` — silently allocates HW decoder slots). Warm-up URL set MUST union `pinnedPreviewSlide` + `liveSlide` direct refs alongside `slides[]`. `crossOrigin` MUST match eventual `<video crossOrigin="anonymous">`. v0.7.228 added a SECOND preheat path inside the output renderer (`setPreheatVid` — see v0.7.228 entry).
-   **v0.7.217 — SSE writeback throttle**: `MediaVideoSurface` live-surface `writeThreshold` MUST be `0.50`, preview MUST stay `0.25`. 4 unconditional transport listeners (play/pause/seeked/loadedmetadata) MUST register + cleanup. `congregation/route.ts` receiver drift tolerance MUST be `1.5` with `!IS_NDI` exemption intact.
-   **v0.7.216 — autoPlay gate + canplay retry**: `MediaVideoSurface` `<video>` `autoPlay` MUST be `!mediaPaused && surface === 'preview'`. `library-compact.tsx` `sendMediaToPreview` MUST flip `setPreviewMediaPaused(true) + setPreviewMediaCurrentTime(0)` BEFORE `pinPreviewSlide` when live plays different media-video. Play/pause effect MUST register one-shot `canplay` retry `{once:true}` + cleanup. Radix portals MUST use `z-[60]` never bare `z-50`. LiveDisplayCard volume MUST flip `setLiveBroadcastAudio` AND live `<video>` `muted` MUST OR-in `!liveBroadcastAudio`.
-   **v0.7.215 — Per-pane Clear buttons**: `clearLive` MUST call `clearLiveAuto()` BEFORE the 4-primitive sequence. PreviewCard "Clear Preview" MUST call ONLY `clearPinnedPreview() + setPreviewSlideIndex(-1)` (independence — MUST NOT touch live primitives).
-   **v0.7.214 — Voice/AI targets LIVE only**: every voice handler in `speech-provider.tsx` MUST read live anchor via `s.liveSlide ?? (liveIdx >= 0 ? slides[liveIdx] : null)` AND commit via `setLiveAuto(slide) + setLiveActiveVerseIndex(0)`. Voice dispatcher MUST contain ZERO `setPreviewSlideIndex(` outside `bible_says`. MUST NOT use `addScheduleItemQuiet` as fallback.
-   **v0.7.213 — setSlides clears pin**: `store.ts` `setSlides` reducer set() payload MUST include BOTH `liveSlide: null` AND `pinnedPreviewSlide: null`. Pattern: any "hard-reset" reducer taking ownership of Preview MUST clear pin + AI ref.
