# Imported App

A Next.js application providing scripture-related services for web and desktop, enhancing live service experiences, and streamlining administrative tasks.

## User preferences

-   **Autonomy**: Act without asking for consent on routine decisions — always do what's better for the app. Do not ask permission for housekeeping (file trims, refactors, retry logic, dependency bumps, replit.md trims, etc.). Only ask when a decision is genuinely destructive or shapes product direction.
-   **replit.md upkeep**: Roll the changelog window proactively. When shipping a new release entry, demote the oldest "Recent (full detail)" item to a one-line guard-rail in the same pass — do NOT wait to be asked.

## Run & Operate

-   **Install dependencies**: `pnpm install`
-   **Run dev server**: `pnpm dev`
-   **Build app**: `pnpm build`
-   **Typecheck**: `pnpm typecheck`
-   **Codegen**: `pnpm codegen` (for API client)
-   **DB Push**: `pnpm db:push` (for Drizzle schema migrations)
-   **Required Env Vars**: `MAIL_HOST`, `MAIL_USER`, `MAIL_PASS`, `MAIL_FROM`, `SMS_API_KEY`, `SMS_SENDER`

## Stack

-   **Monorepo**: pnpm workspaces
-   **Runtime**: Node.js 24
-   **Language**: TypeScript 5.9
-   **Frontend**: Next.js 16
-   **Backend**: Express 5
-   **Database**: PostgreSQL, SQLite, Replit DB
-   **ORM**: Drizzle ORM, Prisma
-   **Validation**: Zod
-   **Build Tool**: esbuild
-   **API Codegen**: Orval
-   **Speech Recognition**: OpenAI `gpt-4o-mini-transcribe`, Deepgram Nova-3
-   **AI SDK**: OpenAI SDK
-   **NDI Integration**: `koffi` FFI binding to bundled `Processing.NDI.Lib.x64.dll` (NDI 6 runtime ships inside installer as of v0.7.146 — no separate NDI Tools install required)

## Where things live

-   `apps/nextjs-app`: Next.js frontend and Electron wrapper.
-   `apps/api-server`: Express API backend.
-   `packages/`: Shared utilities and components.
-   `artifacts/imported-app/db/custom.db`: SQLite database for the desktop app.
-   `artifacts/imported-app/build-resources/ndi/Processing.NDI.Lib.x64.dll`: bundled NDI runtime (v0.7.146).
-   `public/downloads/manifest.json`: Manifest for dynamic downloads.
-   `openapi.yaml`: API contracts (source of truth for Orval codegen).
-   `drizzle/schema.ts`: Drizzle ORM database schema.
-   `styles/theme/`: Theme-related files.

## Sandbox quirks (Replit main agent)

-   **`pnpm install` is blocked** in the main-agent sandbox — pnpm touches `.git/config` and the sandbox classifies it as a destructive git op. To restore a missing `node_modules/@workspace/<lib>` symlink, recreate it manually with `ln -s ../../../../lib/<name> artifacts/imported-app/node_modules/@workspace/<name>`. The `strip:workspace-symlinks` script (run by `pnpm run package*` to flatten symlinks for electron-builder) is a one-way trapdoor in this sandbox — re-running `pnpm install` to restore them isn't possible from here.
-   **`git push` is blocked** in the main-agent sandbox (same `.git/config` touch classification). Ship via the GH Contents API using `GH_PAT` — PUT each changed file to the target `release/v0.7.<N>` branch on `nanvilow/scripturelive-ai`, then trigger `workflow_dispatch` on `release-desktop.yml` (workflow ID `265047930`) with `ref` set to that branch. The `!cancelled()` job-condition fix for the release-desktop workflow already lives on `release/v0.7.228` (`2e73b7d` + `85d4117`) — cherry-pick onto new release branches if it isn't already there.
-   **CRITICAL ship-drift rule (v0.7.237 root cause)**: every release MUST PUT every drifted file, NOT just `package.json` + `CHANGELOG.md` + `installer.nsh` + `replit.md`. The sandbox's local working tree is the ONLY place feature/fix code lives until explicitly PUT to GitHub — there is no `git push` to discover divergence. v0.7.226 → v0.7.236 all shipped binaries equivalent to v0.7.225 with version bumps because the 4-file PUT loop missed every feature file. **Mandatory pre-ship drift check**: enumerate every file under `artifacts/imported-app/` (excluding `node_modules`, `.next`, `dist`, `src/generated/prisma-client`, `tsconfig.tsbuildinfo`, `next-env.d.ts`, gitignored baked-credentials), compute git blob SHA locally, compare against remote `main` via `GET /repos/.../git/trees/main?recursive=1`, PUT every mismatch + every local-only non-build file to the release branch. The bulk-PUT Python script in v0.7.237 CHANGELOG entry is the canonical reference. **Operator-detectable signal**: if a fix landed in source 3+ releases ago and a freshly-installed binary still doesn't show it, the FIRST diagnostic is `curl /repos/.../contents/<file>?ref=main | base64 -d | grep <fix-marker>` — NOT another reinstall, NOT a state-cleanup walkthrough.

## Architecture decisions

> Detailed historical entries live in `artifacts/imported-app/CHANGELOG.md`. The list below keeps only the **last release in full detail** plus tight one-line guard-rails for older patterns we still want future agents to respect.

### Recent (full detail)

-   **v0.7.242 — Voice search catalog +600 paraphrases (operator-supplied Tier 1D, OT + NT, 1081→1681 entries)**: operator delivered 600 starter paraphrased Bible quotations (Old + New Testament) as flat `text. — Book chapter:verse` list. Parsed + appended verbatim to `src/lib/ai/paraphrase-verses.ts` (1250 → 1852 lines) as single contiguous block tagged `// ── v0.7.242 Tier 1D — operator-supplied 600 starter paraphrases (Old + New Testament) ──`. Catalog grew **1081 → 1681** entries (~55% more surface forms), expanding cosine recall + `matchTranscript()` Map coverage on the live phrase-index pipeline that v0.7.241 fix (D) wired in FIRST on every speech chunk. Append-time parser normalization: smart quotes (`'`, `"`, `—`, `–`) → ASCII for TS string-literal safety; book-name canonical contract enforced (`Psalm` → `Psalms`, `Song of Songs` → `Song of Solomon`, `I/II/III X` → `1/2/3 X`); string-quote-style auto-select (`"` for entries with `'`, `'` otherwise); 0/600 lines failed parsing. Dedupe scope is `(reference, text)` tuple ONLY — cross-dedupe against prior 1081 INTENTIONALLY off (file header doc: "more surface forms per reference = more cosine recall"). Zero consumer-side code changes: `phrase-index.ts` `warmPhraseIndex()` reads `PARAPHRASE_VERSES` directly, useEffect `[]` deps per v0.7.241 GR-D, Map build stays O(n=1681) sub-50ms cold-start. **GUARD-RAIL (book-name canonical REINFORCED)**: any future bulk-paraphrase append MUST run the same normalizer at WRITE time, NOT runtime. `Psalm` slipping through breaks cache-key parity with `popular-verses.ts` — chip dedupe shows two refs, re-fetch fails (provider catalog uses `Psalms`). Mapping table in `/tmp/append_paraphrases.py` `BOOK_CANONICAL` — preserve verbatim. **GUARD-RAIL (cross-dedup OFF for paraphrase appends)**: do NOT add "skip if reference already in prior N" check. Duplicate refs with different surface forms is the architectural REASON the file is separate from `popular-verses.ts`. Dedupe is intentionally `(ref, text)` tuple-only. **GUARD-RAIL (write-time normalization is load-bearing)**: runtime normalizer would (1) pay cost on every install cold-start, (2) drift Map keys from `popular-verses.ts` keys silently, (3) hide non-canonical source files from agents inspecting them. Write-time normalization makes the file itself the canonical contract. **GUARD-RAIL (Tier-tag header comments)**: every bulk-append MUST carry `// ── vX.Y.Z Tier <N> — <provenance> ──` delimiter. v0.7.235 = Tier 1A. v0.7.239 = Tier 1B/1C. v0.7.242 = Tier 1D. Next = Tier 1E. **PROCESS GR (catalog-only ship signature)**: when only diff is appended entries to a curated data file, bulk-drift PUT detects EXACTLY 3 changed files (`package.json` + `CHANGELOG.md` + data file) + `replit.md`. More than that = uncurated drift from prior session — investigate BEFORE PUT.

-   **v0.7.241 — Video lag in OBS Browser Source (HTTP Range / 206 Partial Content) + 4 stacked fixes** (demoted by v0.7.242 ship): five fixes — (A) `electron-builder.yml` removed `signAndEditExecutable: false` (was disabling rcedit's icon + StringFileInfo stamping; signing-disable knob is `forceCodeSigning: false`, NOT this). (B) `store.ts` `pinPreviewSlide` atomic single `set` (pin + reset paused=false + currentTime=0); go-live MUST prepend `setLiveMediaPaused(false)` BEFORE `setLiveAuto(true)`. (C) `speech-provider.tsx` outer fetch timeout 1000→1500 ms, MUST exceed inner LLM `DEFAULT_TIMEOUT_MS=1200` by ≥ 200 ms — bump together. (D) `matchTranscript()` from `phrase-index.ts` dispatched FIRST per chunk (not fallback), minN=2 + score floor 0.65 + 200 ms throttle, `warmPhraseIndex()` useEffect MUST stay `[]` deps, `matchTranscript()` MUST stay synchronous. (E) `/api/upload` GET now parses `Range` header + emits 206 + `Content-Range` per RFC 7233 single-range (no multipart), applied to BOTH >20 MB streaming branch AND small-file branch; `Accept-Ranges: bytes` is a PROMISE not a hint — removing while bug exists is WORSE (Chromium tightens buffer policy). **PROCESS GR**: operator-reported "video lags in OBS Browser Source" FIRST diagnostic is `curl -I -H "Range: bytes=0-1000" <upload-url>` — 200 with full body proves Range not honoured regardless of advertised header. NDI lag (architectural CPU-readback per v0.7.234) ≠ OBS Browser Source lag (implementation per v0.7.241) — DIFFERENT root causes, treat separately.

-   **v0.7.240 — Live-footer popovers auto-close (Mic + Master Volume)** (demoted by v0.7.241 ship): `LiveBottomAudioControls` in `logos-shell.tsx` — both sliders (mic gain 0–200%, master volume 0–100%) schedule 600 ms close on `onPointerUp` / `onKeyUp` ONLY (not `onChange` — drag is intent envelope, pointer-up is commit); slider `onChange` cancels any pending timer so long drag never snaps panel away mid-adjust; one-shot Mute/Unmute + "100%" reset close immediately; timer refs cleared in `useEffect` unmount AND on every `onOpenChange → closed` transition. **GUARD-RAIL**: 600 ms close MUST be scheduled from pointer-up/key-up, NEVER from `onChange`. **GUARD-RAIL**: timer refs MUST be cleared in `onOpenChange → closed` useEffect — without cleanup, re-opening within 600 ms fires stale timer over new session. **GUARD-RAIL**: Mute toggle + "100%" reset stay immediate-close (binary commits, not envelopes). **PROCESS GR**: operator escalations phrased "anytime users finish X this should auto close" are ALWAYS about drag-intent vs commit — fix is NEVER a debounced `onChange` close. Look for platform's value-committed signal (pointer-up/key-up) and schedule from there with a small read-the-value delay.

-   **v0.7.238 — Transport bar / live controls + voice "verse N / chapter N" word-form**: Card primitive gained `footer?: ReactNode` slot rendered as section-level sibling OUTSIDE body's overflow-hidden region (logos-shell.tsx L111-178); PreviewCard + LiveDisplayCard moved shrink-0 footer chrome (symmetry strip, LiveBottomControls, VideoTransports) into footer prop. v0.7.232 stage quartet stays — the two fixes solve DIFFERENT clipping paths (intrinsic-size vs sibling overflow). commands.ts L940+L1024 capture widened `(\d{1,3})` → `(\d{1,3}|[a-z][a-z\s-]{1,40})` + new `parseEnglishNumber()` helper (1-19, tens incl. ASR "fourty", compound "twenty-one", hundreds "one hundred and twelve"). Range gates verse 1-200 chapter 1-150. Regex permissive, parser strict. **GUARD-RAIL (footer slot is section-level NOT body-level)**: moving footer INTO body re-introduces overflow-hidden clipping. **GUARD-RAIL (parseEnglishNumber stays shared between verse + chapter detectors)**: keep SMALL/TENS tables in one place; v0.7.239 LLM word-form list MUST match this table exactly.


-   **v0.7.237 — Bulk-sync release: every code fix since v0.7.226 finally reaches the binary**: 11 prior releases shipped binaries equivalent to v0.7.225 with version bumps because the GH Contents API PUT loop only pushed 4 files (`package.json`, `CHANGELOG.md`, `installer.nsh`, `replit.md`). Feature code stayed in sandbox local working tree. Fixed by bulk-diff Python script PUTting all 33 drifted files + 4 curated new files (media-fit.ts, broadcast-gate.ts, paraphrase-verses.ts, bg-compare.html) to release/v0.7.237. Gitignored baked-credentials (keys.baked.ts, baked-credentials.ts) NOT PUT — synthesized by release-desktop.yml from GH secrets at build time. Prisma-generated client + tsbuildinfo + next-env.d.ts + .so.node + .wasm NOT PUT — regenerated by build. **GUARD-RAIL (MANDATORY pre-ship drift check)**: every release MUST enumerate every file under `artifacts/imported-app/` (excluding node_modules, .next, dist, generated/prisma-client, tsconfig.tsbuildinfo, next-env.d.ts, gitignored baked-credentials), compute git blob SHA locally, compare against remote main via `GET /repos/.../git/trees/main?recursive=1`, PUT every mismatch + every local-only non-build file to release branch. The 4-file PUT loop is a one-way trap. **GUARD-RAIL (operator-detectable signal)**: if fix landed in source 3+ releases ago and freshly-installed binary still doesn't show it, FIRST diagnostic is `curl /repos/.../contents/<file>?ref=main | base64 -d | grep <fix-marker>` — NOT another reinstall, NOT state-cleanup walkthrough. **GUARD-RAIL (curated-new-file allowlist)**: media-fit.ts, broadcast-gate.ts, ai/paraphrase-verses.ts, bg-compare.html referenced by drifted-file imports MUST be PUT alongside. **GUARD-RAIL (build-output exclusions)**: generated/prisma-client/*, tsconfig.tsbuildinfo, next-env.d.ts, *.so.node, *.wasm MUST NOT be PUT. **GUARD-RAIL (gitignored secret-bearing files)**: keys.baked.ts + baked-credentials.ts MUST NOT be PUT — gitignored on purpose; build workflow writes them from GH Actions secrets. **PROCESS GR (verification cadence)**: every release-desktop run MUST be followed by single-spot-check `curl` against published asset OR Contents-API SHA verify of ≥1 feature-bearing source file on release branch — confirms build carried the diff, not just `package.json`.

-   **v0.7.236 — Clean uninstall (EasyWorship / Wirecast parity)**: operator escalation against v0.7.235 — "when users uninstall the app delete everything that include the app just like other apps do (EasyWorship, Wirecast, etc.)". Root cause: previous installer removed program files only; Electron user-data at `%APPDATA%\ScriptureLive AI\` survived uninstall, so a fresh reinstall on the same PC loaded the OLD persisted Zustand/electron-store/SQLite back into the new build — operator on PC #2 saw "most of the fixes did not apply" because v0.7.235 binary was running on top of state objects from earlier versions that lacked fields the new code reads (mediaKind, bgBrightness, throttle envelope, paraphrase cache schema). **Fix**: `build-resources/installer.nsh` adds a new `customUnInstall` macro alongside existing `customInit`/`customUnInit`/`customInstall`/`customFinishPage`. Prompts (default YES) "Do you also want to delete your ScriptureLive AI settings, library, saved schedules, and cached data?" and on YES recursively wipes four canonical user-data paths via `RMDir /r`: `$APPDATA\ScriptureLive AI` (Electron userData — electron-store, Zustand persist, IndexedDB, LocalStorage, Service Worker caches, Network cookies, GPUCache), `$LOCALAPPDATA\ScriptureLive AI` (code cache, partition data, crash dumps, bundled Next server logs), `$APPDATA\@workspace\imported-app` (pre-v0.7.x namespace — long-time operators still have state here from migration window), `$LOCALAPPDATA\@workspace\imported-app` (same, LocalAppData side). `ClearErrors` at macro end so RMDir's "folder did not exist" code on clean machines does not poison installer exit code. **GUARD-RAIL (silent uninstall MUST skip wipe)**: `IfSilent skipDataWipe` BEFORE the MessageBox is LOAD-BEARING. electron-updater's auto-update path runs the uninstaller with `/S` as part of every in-app update — without the silent guard, EVERY update would erase the operator's library, weekly schedule, NDI settings, and recent-files list (catastrophic Sunday-morning data loss). The guard MUST be the FIRST instruction inside `customUnInstall`. Manual uninstalls via Control Panel / Settings → Apps run interactively and hit the prompt; auto-update uninstalls run silently and skip the wipe entirely. **GUARD-RAIL (default action on dialog is YES = wipe)**: matches operator intent — they're uninstalling because they don't want the app on this PC. Operators about to reinstall a different build can click No. `/SD IDYES` for silent-but-interactive default. **GUARD-RAIL (path list MUST stay synced with Electron `app.setPath`)**: if `main.ts` ever calls `app.setPath("userData", customPath)` to relocate user data, the `customUnInstall` RMDir list MUST be updated in lockstep — otherwise uninstall leaves orphan state at the custom path while wiping the now-empty default path. Electron's default `userData = %APPDATA%\<productName>` is the current contract; any change is a 2-site edit. **GUARD-RAIL (productName drives the path — do NOT rename without migration)**: `electron-builder.yml` `productName: ScriptureLive AI` is the source of truth; Electron derives `app.getPath("userData")` from it. Renaming the product without a one-time migration orphans every operator's existing state under the OLD productName folder while the new build writes to a fresh empty folder. If a future rename is needed, ship a `main.ts` migration that copies the old folder forward BEFORE the first window opens. **GUARD-RAIL (RMDir paths use `$APPDATA` / `$LOCALAPPDATA` NSIS variables, NOT hard-coded `C:\Users\...`)**: NSIS resolves `$APPDATA` to the invoking-user's Roaming folder; hard-coding the path would break on non-English Windows installs (where `%USERPROFILE%` differs) and on machines with redirected AppData (corporate / OneDrive Known Folder Move). **PROCESS GR (persisted user data is the silent root cause of "fix didn't apply on PC #N" reports)**: when an operator reports a fix landed on PC #A but not PC #B and version-check confirms BOTH PCs run the same build, the next step is ALWAYS to verify user-data state, not to chase a code-level bug. Old Zustand store snapshots (pre-throttle-envelope, pre-bgBrightness, pre-paraphrase-cache-schema) loaded by the new binary look like a regression but are actually schema drift. v0.7.236 fixes the trap at the installer level; before this, operators had to manually `RMDir /r %APPDATA%\ScriptureLive AI` between reinstalls. **Tests**: `pnpm exec tsc --noEmit` clean (installer.nsh is preprocessor-only — no TS impact). NSIS macro syntax verified against existing `killRunningApp` / `customInit` / `customFinishPage` patterns in same file.

-   **v0.7.235 — Receiver brightness + strict-follow sync + voice verse/vest/chapter N + 582-entry paraphrase engine**: foreground `<video id="liveVideo">` MUST NOT carry `opacity:var(--bg-opacity)` (route.ts L1877) — `bgBrightness` is for text-over-background readability, NOT for dimming full-frame sermon clips; bg image/bg video/lower-third bg bindings intentionally stay. NO opacity binding on text selectors — WCAG-AA depends on text-shadow stack. Strict-follow sync uses transport-event detection with HARD-SNAP (pause-change, scrub-back >0.3s, scrub-fwd >2s) on ALL surfaces including NDI, SOFT-TRIM (`playbackRate` 0.97/1.03/1.0) on routine drift `!IS_NDI` only — NDI exempt from routine drift to avoid decoder-flush stutter, but HARD-SNAP transport events MUST fire on NDI too. Rate envelope bounded `[0.97, 1.03]` — wider causes pitch artefacts, tighter takes >10s to converge. Verse-N detector regex MUST keep `verse|verses|vest|vs` as literal alternation NOT fuzzy match. Chapter detector dispatched AFTER verse detector (chapter regex tail `(\d+)` would capture "5" from "verse 5" if reordered). PARAPHRASE_VERSES book strings MUST match popular-verses.ts canonical (Psalms not Psalm, 1 Samuel not I Samuel, Song of Solomon not Song of Songs). Curated > LLM-generated for paraphrases (full-Bible auto-gen pollutes top-K with low-info variants).


-   **v0.7.233 — Upload-time poster extraction for library video tiles + NDI alpha-fill SIMD optimisation**: `library-compact.tsx` L1156/L1226/L1273/L1555 — `extractFirstFrameJpeg(file)` via `URL.createObjectURL(file)` (NOT FileReader dataURL — 200ms vs 3-5s on 50MB clips), 5s timeout, settled-guard, releases URL on every settle; `handleFiles` awaits BEFORE addMediaItem; tile JSX `poster={m.posterUrl}` + `preload={posterUrl ? "none" : "auto"}`. freezeBg pattern preserved as `!posterUrl` legacy fallback. NDI alpha-force `ndi-service.ts` L1108 replaced per-byte `slot[i]=0xff` loop with `Uint32Array` view + `|= 0xff000000` bitwise (V8 SIMD-vectorises, ~4x). **GUARD-RAIL**: `Uint32Array` constructor MUST take all 3 args `(slot.buffer, slot.byteOffset, slot.length>>>2)` — dropping byteOffset aliases neighbouring slot (silent memory corruption). LE assumption for `0xff000000` mask is correct on x64/arm64; BE port flips to `0x000000ff`. Bulk uploads MUST be serialised (`for (const file of queue)` + `await extractFirstFrameJpeg(file)`, NOT `forEach(async)`) — parallel extraction holds finite HW decoder slots that compete with foreground Preview/Live `<video>` surfaces (v0.7.216 + v0.7.222 invariant).
-   **v0.7.231 — Reverse v0.7.227/v0.7.222 standby chrome + paint media-tile thumbnails**: dropped amber STANDBY badge + "Play preview" CTA overlays (kept freezeBg poster `<video>` above). `VideoTransport.onPlay` flips `setMediaPaused(false)` BEFORE `findVideo()` (standby poster omits `data-surface="preview"`). `library-compact.tsx` video-tile back to v0.7.225 freezeBg pattern. **GUARD-RAIL**: freezeBg `<video>` lifecycle (`preload="auto"` + `loadeddata→play().then(pause).catch(pause)` + `onPlay→setTimeout(pause,0)`, muted, playsInline, no autoplay/loop) is FOURTH site in lockstep with route.ts setBgVid + standby poster + setPreheatVid + library tile. v0.7.233 added a FIFTH site (upload-time poster extraction at `library-compact.tsx` L1226 `extractFirstFrameJpeg`); v0.7.239 added a SIXTH site (`extractFirstFrameJpegFromSrc` + 2 render branches + uploadOne + backfill useEffect in `logos-shell.tsx`) — any change MUST be made across all six.
### Older releases (one-line guard-rails only — full text in `artifacts/imported-app/CHANGELOG.md`)

-   **v0.7.232 — Card video-stage 4-property invariant** (demoted by v0.7.239 ship): every aspect-ratio'd video stage inside a Card body MUST keep the full `flex-1 min-h-0 overflow-hidden flex items-stretch` quartet (PreviewCard + LiveDisplayCard in logos-shell.tsx) — dropping `overflow-hidden` lets aspect-ratio's intrinsic-size win over `min-h-0` and clip the shrink-0 transport sibling. Card primitive does NOT enforce it; stage clipping is an artifact-author responsibility.
-   **v0.7.230 — `resolveMediaFit` 5-way helper + foreground brightness binding + OBS BGRA opt-in** (demoted by v0.7.241 ship): `media-fit.ts` canonical 5-case `mediaFit → {objectFit, aspect}` consumed by 4 surfaces in lockstep (MediaVideoSurface + standby poster + container aspectRatio + route.ts). `--bg-opacity` foreground fallback `1` vs bg `.85` is pair-invariant. NO opacity binding on text selectors — WCAG-AA depends on text-shadow stack. `ndiForceBgraForObs` plumbed 5 layers (settings type + default false, ndi-service FourCC + stop reset, main.ts forward, preload + use-electron types, panel UI + 2 ndi.start callsites + restart guard token). Sender-create-time settings (FourCC, resolution, fps, sourceName, transparent flag) MUST be plumbed through all 5 layers AND included in restart guard token AND reset in `stop()`. **PROCESS GR (main-agent ship pipeline)**: `pnpm exec tsc --noEmit` → bump → CHANGELOG prepend → replit.md demote-on-ship → GH Contents API bulk-drift PUT to `release/v0.7.<N>` → workflow_dispatch on `release-desktop.yml` (workflow ID `265047930`) with both `ref: release/v0.7.<N>` AND `inputs.tag: v0.7.<N>` → curl spot-check on a feature-bearing file at release ref. `!cancelled()` job-condition fix lives on `release/v0.7.228` (`2e73b7d` + `85d4117`) — cherry-pick if not on target branch.

### Archived releases (v0.7.229 and older)

Pre-v0.7.230 one-line guard-rails live in `artifacts/imported-app/ARCHIVE.md`. Full release text remains in `artifacts/imported-app/CHANGELOG.md`. When rolling the window forward (demoting a release from "Recent (full detail)" to one-liner), keep only v0.7.230+ entries in this file and append older one-liners to `ARCHIVE.md`.
