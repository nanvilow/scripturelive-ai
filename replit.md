# Imported App

A Next.js application providing scripture-related services for web and desktop, enhancing live service experiences, and streamlining administrative tasks.

## User preferences

-   **Autonomy**: Act without asking for consent on routine decisions — always do what's better for the app. Do not ask permission for housekeeping (file trims, refactors, retry logic, dependency bumps, etc.). Only ask when a decision is genuinely destructive or shapes product direction.

## Run & Operate

-   **Install dependencies**: `pnpm install`
-   **Run dev server**: `pnpm dev`
-   **Build app**: `pnpm build`
-   **Typecheck**: `pnpm typecheck`
-   **Codegen**: `pnpm codegen` (for API client)
-   **DB Push**: `pnpm db:push` (for Drizzle schema migrations)
-   **Required Env Vars**: `MAIL_HOST`, `MAIL_USER`, `MAIL_PASS`, `MAIL_FROM`, `SMS_API_KEY`, `SMS_SENDER`

## Stack

-   **Monorepo**: pnpm workspaces
-   **Runtime**: Node.js 24
-   **Language**: TypeScript 5.9
-   **Frontend**: Next.js 16
-   **Backend**: Express 5
-   **Database**: PostgreSQL, SQLite, Replit DB
-   **ORM**: Drizzle ORM, Prisma
-   **Validation**: Zod
-   **Build Tool**: esbuild
-   **API Codegen**: Orval
-   **Speech Recognition**: OpenAI `gpt-4o-mini-transcribe`, Deepgram Nova-3
-   **AI SDK**: OpenAI SDK
-   **NDI Integration**: `koffi` FFI binding to bundled `Processing.NDI.Lib.x64.dll` (NDI 6 runtime ships inside installer as of v0.7.146 — no separate NDI Tools install required)

## Where things live

-   `apps/nextjs-app`: Next.js frontend and Electron wrapper.
-   `apps/api-server`: Express API backend.
-   `packages/`: Shared utilities and components.
-   `artifacts/imported-app/db/custom.db`: SQLite database for the desktop app.
-   `artifacts/imported-app/build-resources/ndi/Processing.NDI.Lib.x64.dll`: bundled NDI runtime (v0.7.146).
-   `public/downloads/manifest.json`: Manifest for dynamic downloads.
-   `openapi.yaml`: API contracts (source of truth for Orval codegen).
-   `drizzle/schema.ts`: Drizzle ORM database schema.
-   `styles/theme/`: Theme-related files.

## Architecture decisions

> Detailed historical entries live in `artifacts/imported-app/CHANGELOG.md`. The list below keeps only the **last three releases in full detail** plus tight one-line guard-rails for older patterns we still want future agents to respect.

### Recent (full detail)

-   **v0.7.225 — Settings preview boxes: first frame of bg video MUST paint instantly, not 1-3s after mount (operator escalation following v0.7.224)**: Operator on v0.7.224: "Video background delays to appear on all preview columns it should be fast as users upload it." Clarified to the SETTINGS preview boxes (Display & Output Live Preview, Typography preview, NDI Live Preview, Custom Background thumbnail). After picking a video tile, the box stayed black-with-text for 1-3 seconds before the first frame painted, then sat there frozen as designed. **Root cause**: v0.7.221's freezeBg path (`route.ts` setBgVid L834) mounted the bg `<video>` with `preload='metadata'` (fetches headers + dimensions only, NOT enough bytes to decode a frame) AND called `v.pause()` synchronously immediately after `layer.appendChild(v)` — which puts the element in a paused-before-any-data-loaded state where several Chromium HW decoder paths assume the element will never need a frame and sleep the decoder permanently. The `#t=0.1` media fragment was a HINT at the desired display time but did nothing without a load that actually reached that frame. **Fix** (`route.ts` L834-888) — 4 coordinated changes to the freezeBg branch: (1) `v.preload='metadata'` → `'auto'` so the browser fetches the first GOP up-front; (2) `loadeddata` handler MUST kick a one-shot `v.play().then(pause).catch(pause)` to force the first decoded frame onto the compositor (Chromium refuses to paint a paused video that has never been told to play, even with `preload='auto'`, on several HW decoder paths — the play() registers intent to draw, the .then(pause) immediately stops further frames; decoder slot held only for the brief play→pause window then released); (3) `play` listener MUST `setTimeout(pause, 0)` (was synchronous `v.pause()`) so the loadeddata-triggered `play().then(pause)` resolves cleanly first (synchronous pause inside the play listener races the .then(pause) and produces AbortError spam); (4) REMOVED the synchronous `try{v.pause()}catch(_e){}` after `layer.appendChild(v)` (re-adding it puts the element in a never-draw state). **GUARD-RAIL (freezeBg preload)**: `IS_FROZEN_BG` branch MUST set `v.preload='auto'`, NOT `'metadata'` or `'none'`. **GUARD-RAIL (loadeddata play kick)**: handler MUST call `v.play().then(pause).catch(pause)`, NOT bare `v.pause()`. **GUARD-RAIL (play listener async pause)**: MUST schedule the pause via `setTimeout(_, 0)`. **GUARD-RAIL (no synchronous pause on append)**: the synchronous `v.pause()` immediately after `layer.appendChild(v)` MUST stay removed. **GUARD-RAIL (NDI/Broadcast path untouched)**: the `else` broadcast branch (Live Display, Secondary Screen, NDI FrameCapture) MUST stay byte-identical to v0.7.224 — v0.7.225 is scoped to freezeBg-only; the v0.7.220 async send + v0.7.223 BGRX FourCC + buffer pool invariants on the broadcast path are preserved. **PROCESS GR — `preload` attribute lies about its costs in BOTH directions on Chromium HW decoder paths**: v0.7.218/v0.7.222 found `preload='auto'` allocates a decoder slot eagerly (bad for FOREGROUND preview while LIVE decoder is on a different slot). v0.7.225 finds the OPPOSITE failure on the BACKGROUND layer: `preload='metadata'` + immediate-pause-on-append leaves the element in a never-draw state. The two findings are symmetric, not contradictory — preload's documented behaviour does NOT predict whether a frame paints. Reliable signal: "did the element get a play() call after loadeddata". Every freezeBg-style "paint one frame and stop" pattern MUST include the play().then(pause) kick. **PROCESS GR — when an operator says "delays to appear on all preview columns" without specifying which column, ASK before assuming**: initial diagnosis assumed the main Preview pane (MediaVideoSurface). Clarification revealed the SETTINGS preview boxes (OutputPreview + freezeBg renderer path). Same operator-visible failure mode (black box with text) but entirely different code paths — fixing the wrong one would have left the escalation open AND risked re-opening the v0.7.218/v0.7.222 LIVE-stall regression by relaxing the wrong preload gate. Tests: 670/670 PASS (no new test — freezeBg is HTTP-route inline JS, source-grep guards live in route-level tests; existing media-preview-isolation suite covers the orthogonal MediaVideoSurface path). Typecheck clean. NDI runtime + Electron offscreen capture untestable in Replit — operator field validation closes the loop. Full text in `artifacts/imported-app/CHANGELOG.md`.

-   **v0.7.224 — GO LIVE while preview plays video MUST resume on live (operator $1600-customer escalation; same workflow that v0.7.212/v0.7.216/v0.7.221 each took one layer off; third-layer fix)**: Operator on v0.7.223 reported "The user clicking on go live also sends video from the preview to the live display to immediately start playing. still not working." Pre-fix live mounted on frame 0 (or stalled on first frame) instead of resuming at the preview's current watch point. Two independent layers remained. **Root cause #1 (call-order clobber)**: `logos-shell.tsx` goLive pinned path L4366 (and `easyworship-shell.tsx` L1589) called `setLiveMediaCurrentTime(pv.currentTime)` FIRST, then `setLiveAuto(pinned)` LAST. But v0.7.221's store contract for `setLiveAuto` (store L1233-1236) atomically resets `liveMediaCurrentTime=0 + liveMediaPaused=false` whenever the new live URL differs from the current one — designed as defence-in-depth for every OTHER setLiveAuto caller (AI auto-fire, speech-provider × 5, live-translation-sync × 2, library-compact double-click) where there's no preview playback context to inherit. That reset silently clobbered the goLive pinned path's just-captured preview time. **Root cause #2 (Chromium fresh-element seek)**: Even with correct ordering, `MediaVideoSurface` mounts FRESH on the live URL change (`key={liveSlide.mediaUrl}` from v0.7.221 ensures clean remount). The play effect (L388) attempts a seek at the top of the body — but Chromium silently no-ops `v.currentTime = X` on a fresh element before metadata loads. The v0.7.216 `canplay` one-shot retry called `v.play()` but did NOT re-apply the pending seek, so even with correct ordering the first frame on screen was frame 0. **Fix part A**: `logos-shell.tsx` goLive pinned path L4378+ (and `easyworship-shell.tsx` L1616+) — capture `pv.currentTime` into a local `resumeFrom`, call `setPreviewMediaPaused(true)`, then `setLiveAuto(pinned)` (lets the atomic reset apply on the new URL), then if `resumeFrom > 0` call `setLiveMediaCurrentTime(resumeFrom)` to override the just-reset 0. The captured time becomes the LAST write before the live `<video>` mounts, so the store reflects the correct resume target. **Fix part B**: `logos-shell.tsx` MediaVideoSurface play effect L413+ — the one-shot `canplay` listener now reads `useAppStore.getState().liveMediaCurrentTime` / `previewMediaCurrentTime` directly (NOT the closed-over `mediaCurrentTime` hook value, which may be the stale 0 from `setLiveAuto`'s atomic reset captured at effect-setup time), applies the seek BEFORE calling `v.play()`. By the time canplay fires, metadata has loaded so the seek lands; play resumes from the resume point, not frame 0. Reading the store directly closes the race where `setLiveAuto` reset → `setLiveMediaCurrentTime(resumeFrom)` resolves AFTER the effect has already closed over its dependencies but BEFORE canplay fires. Guarded by `targetTime > 0 && |v.currentTime - targetTime| > 0.5` to skip redundant seeks. **GUARD-RAIL (goLive pinned-path ordering)**: `setLiveAuto(pinned)` MUST come BEFORE `setLiveMediaCurrentTime(resumeFrom)` in BOTH `logos-shell.tsx` and `easyworship-shell.tsx` goLive pinned paths. Inverting the order re-opens the v0.7.224 regression (captured time clobbered by atomic reset). **GUARD-RAIL (canplay seek)**: `MediaVideoSurface` play-effect's `canplay` handler MUST re-apply the pending seek before calling `play()`, AND MUST read `useAppStore.getState()` directly (not the closed-over `mediaCurrentTime`) so an in-flight `setLiveMediaCurrentTime` from goLive lands even if the effect closed over a stale 0. **GUARD-RAIL (store contract preserved)**: v0.7.221 `setLiveAuto` atomic transport reset MUST stay in place — it's correct for every OTHER caller. The goLive call-order fix is the right scope of intervention; the store contract is unchanged. **PROCESS GR — when a fix touches store-level state with downstream renderer-mount semantics, audit BOTH the store contract AND the renderer's first-frame side effects in the same pass**: v0.7.221 added the atomic transport reset to setLiveAuto (correct for AI callers) but did not re-audit the operator GO LIVE call sites, which were silently broken by their own store action's atomicity. **PROCESS GR — re-fired escalation = re-trace EVERY layer of the user gesture against the actual code**: v0.7.221's store-contract fix solved one layer; v0.7.216's canplay retry solved another. The combination still left the actual symptom (live starts from frame 0) because the seek and the play are independent operations and only one of them was wired to the canplay event. v0.7.224 makes them both run on canplay. Tests: 670/670 PASS — new (jj) source-grep guard with 13 invariants covering pinned-path ordering in both shells + canplay handler contract. Test (u) v0.7.216 regex relaxed (canplay handler is no longer a one-liner) but the v0.7.216 invariant it enforces (canplay calls play()) is preserved. Typecheck clean. NDI runtime + GPU HW decoder slot contention still untestable in Replit — operator field validation closes the loop. Untouched: all v0.7.223 NDI fixes; v0.7.222 STANDBY + MediaCard.onItemClick refactor; v0.7.221 monotonic timecode + `key={liveSlide.mediaUrl}` + atomic setLiveAuto store contract (THIS IS PRESERVED — fix is at the caller); v0.7.220 async send + buffer pool; v0.7.219 MediaPreheat `<link>`; v0.7.218 preload gating; v0.7.217 SSE writeback throttle; v0.7.216 canplay retry + autoPlay gate. Full text in `artifacts/imported-app/CHANGELOG.md`.

-   **v0.7.223 — NDI output: opaque by default + EW-class smoothness via BGRX FourCC (operator $1600-customer escalation; v0.7.220 closed producer-side gaps but receiver-side stutter and transparency persisted)**: Operator on v0.7.222 reported TWO symptoms in one escalation — "still freezing and not smooth" AND "video background on NDI output is TRANSPARENT (shouldn't be at all)". Two independent root causes. **Root cause #1 (transparency)**: `electron/main.ts` L2288 `wantTransparent = wantLayout === 'ndi' && opts.transparent !== false` defaulted ANY NDI session to TRANSPARENT — the offscreen BrowserWindow was alpha-matted, the renderer page received `?transparent=1`, and BGRA frames carried real alpha values wherever the renderer's compositor produced non-opaque pixels (`<video>` fetching frames, contain-fit letterbox regions, inter-frame gaps). Receivers that honour alpha (vMix, Wirecast, OBS) then showed video backgrounds as see-through in their program composite. **Root cause #2 (smoothness)**: `electron/ndi-service.ts` L54 sent every frame with `FOURCC_BGRA` — receivers MUST run per-pixel alpha-composite (~62M ops/sec at 1080p30) before mixing into their program output. EasyWorship advertises BGRX (B,G,R,X with the 4th byte explicitly ignored padding) precisely so receivers skip this work — that is the EW-class smoothness trick that v0.7.220 producer-side fixes alone could not deliver. **Fix #1**: `main.ts` L2307 `wantTransparent` MUST be `opts.transparent === true` (explicit opt-in, NOT `!== false`) AND L2391 URL-param `transparent` branch MUST also be `opts.transparent === true`. The two flags MUST stay in lockstep (BrowserWindow attribute + renderer page CSS background) — pre-fix mismatch produced partial-transparency artefacts. **Fix #2**: `ndi-service.ts` L69 added `FOURCC_BGRX = 0x58524742`; L635 `nativeSendFrame` MUST pick `this.senderTransparent ? FOURCC_BGRA : FOURCC_BGRX`. New `senderTransparent` field set in `start()` from new `NdiStartOptions.transparent` (passed from main.ts via `await ndi.start({ ...opts, transparent: wantTransparent })`) and reset in `stop()`. Memory layout byte-identical so buffer pool + line stride + async send contract are unchanged. **Fix #3 (belt-and-suspenders)**: `sendFrame` after `bgraBuffer.copy(slot)`, when `!senderTransparent`, run `for (let i = 3; i < len; i += 4) slot[i] = 0xff` — protects older NDI 4 receivers that don't recognise BGRX and fall back to BGRA interpretation. Cost: ~0.3ms per 1080p frame at memory bandwidth (invisible vs 33ms budget). Hand-rolled loop, NOT Uint32Array pattern fill (would re-allocate per frame, defeats v0.7.220 buffer pool). **GUARD-RAIL (transparent default)**: both `wantTransparent` AND the URL-param `transparent` branch MUST be `opts.transparent === true`. Reverting either to `!== false` re-opens the see-through video background regression. **GUARD-RAIL (FourCC selection)**: opaque path MUST be BGRX, transparent path MUST be BGRA — swapping either breaks a distinct operator workflow. **GUARD-RAIL (alpha forcing)**: `sendFrame` MUST force `slot[i] = 0xff` for `i = 3, 7, 11, ...` when `!senderTransparent` (BGRX alone insufficient — old receivers fall back to BGRA). **GUARD-RAIL (state reset)**: `stop()` MUST reset `senderTransparent = false` (prevents start→toggle→stop→start from inheriting stale transparent flag). **PROCESS GR — when an operator describes TWO symptoms in one escalation, treat them as INDEPENDENT until proven coupled**: the freezing and transparency complaints shared only an output surface, not a mechanism — solving one would have left an escalation on the other. **PROCESS GR — EW-class smoothness has THREE layers (producer pacing, wire format, receiver work)**: v0.7.217 closed producer (in-app SSE rate); v0.7.220 closed producer (async send + buffer pool); v0.7.223 closes receiver (BGRX skip alpha composite). All three required to match EW. **PROCESS GR — when adding an opt-in feature with receiver-visible consequences, default MUST be the safe / common case**: the v0.6.6 transparent toggle was added for the 5%-lower-third workflow but defaulted ON for ALL NDI, silently breaking the 95%-fullscreen workflow. Pattern `opts.X !== false` reads as "default true" — for receiver-visible features, default MUST be `opts.X === true`. Tests: 34/34 PASS in `media-preview-isolation.test.ts` (unchanged — NDI runtime untestable in Replit per v0.7.220 documented limit; relies on operator field validation with actual vMix/OBS/Wirecast receiver). Untouched: all v0.7.222 fixes (MediaCard.onItemClick refactor, STANDBY placard, preload='none' across surfaces, updateFit direct-ref-first, liveIsMediaVideo URL-extension fallback); v0.7.221 monotonic timecode + jitter telemetry; v0.7.220 async send + buffer pool + paced loops + `clock_video:false`; v0.7.219 MediaPreheat `<link>`; v0.7.218→v0.7.222 preload chain; v0.7.103 linger; v0.7.56 mDNS retract; v0.7.12 keep-alive + sendBusy mutex; FLTP audio path. Full text in `artifacts/imported-app/CHANGELOG.md`.

-   **v0.7.222 — Single-click on another media tile while LIVE plays a media-video MUST NOT stall the live video (operator $1600-customer escalation; v0.7.216 / v0.7.218 / v0.7.221 each closed one layer but the underlying root cause survived all three releases)**: v0.7.221's `key={liveSlide.mediaUrl}` on the LIVE `MediaVideoSurface` was correct for swap-live but ORTHOGONAL to the operator's actual workflow (single-click clip B while clip A plays live — `liveSlide.mediaUrl` does NOT change on a preview-pin, so the key stays stable and the live `<video>` is never remounted). Root cause is the PREVIEW pane's `<video>` element: even with v0.7.218's `preload="metadata"` and v0.7.216's `autoPlay={false}`, Chromium still does an HTTP range request + container demux + transient decoder-capability probe on mount, which evicts the live decoder on low-end Windows GPUs (2-4 HW slots, 1 used by Live + 1 by NDI offscreen capture = no headroom). FIX #1: `logos-shell.tsx` L~544 `MediaVideoSurface` `<video>` `preload` attr MUST be `'none'` (NOT `'metadata'`) when `surface === 'preview' && mediaPaused` — `'none'` is the only value that does ZERO network + ZERO demux + ZERO decoder probe. Shape of the gate is unchanged from v0.7.218 (preview-only + paused-only); only the truthy literal moved `'metadata'` → `'none'`. Live surface and playing-preview path both keep `preload='auto'` (correct — they MUST decode). FIX #2 (defense in depth): `logos-shell.tsx` PreviewCard L~1165 `previewStandbyForLive` predicate MUST be a 5-condition AND (`previewSlide?.type === 'media'` + `previewSlide.mediaKind === 'video'` + `previewSlide.mediaUrl` truthy + `liveIsMediaVideo` + `liveSlide?.mediaUrl !== previewSlide.mediaUrl` + `previewMediaPaused`) — when true, PreviewCard L~1333 MUST render a STANDBY placard branch that mounts ZERO `<video>` / ZERO `<MediaVideoSurface>` — the operator-visible UI is an amber "Standby" pill + slide title + "Live is on air — press Play below to monitor without stalling Live" + a `Play preview` button wired to `setPreviewMediaPaused(false)` which releases the gate so the real surface mounts on next render (operator explicitly opts into the contention at that point). Removing any of the 5 conditions re-opens a regression class — dropping (4) renders placard when LIVE is on a verse (no contention exists, UX regression); dropping (5) clobbers the v0.7.216 ON-AIR placard for same-media; dropping (6) makes the placard sticky after Play. Tests (bb)-(ff) in `media-preview-isolation.test.ts` are 4 new + 1 updated (test (y) v0.7.218 migrated `'metadata'` → `'none'` with cross-reference to (bb) for rationale). 665/665 PASS. Replit cannot reproduce HW decoder slot eviction (Linux + software decoders + no NDI runtime) — fix is structural so source-grep guards are strongest pre-ship verification; operator field validation closes the loop. **GUARD-RAIL (preload value)**: `MediaVideoSurface` `<video>` `preload` MUST be `'none'` (not `'metadata'`, not `'auto'`, not bare) when `surface === 'preview' && mediaPaused`. The v0.7.218 assumption that `'metadata'` was decoder-free has been disproved by operator escalation. Any future relaxation MUST be tested on the operator's hardware with the real NDI capture window active, not on Replit. Test (bb) source-grep enforces. **GUARD-RAIL (predicate completeness)**: `previewStandbyForLive` is a 5-condition AND — weakening any one is a behavioural regression. Test (cc) source-grep enforces all 5. **GUARD-RAIL (branch ordering)**: a NEW preview-pane render branch added in the future MUST be added INSIDE the same ternary chain (and after both the `previewMediaIsLive` ON-AIR branch AND the `previewStandbyForLive` STANDBY branch) so the zero-`<video>` guarantee holds for the LIVE-contention case. A new branch added BEFORE the STANDBY check would re-open the regression. **PROCESS GR — when an operator escalation re-fires on the release immediately following a fix attempt, the FIRST diagnostic question MUST be "does my fix actually address the operator's workflow, or only an adjacent one I assumed was the same path?"**: v0.7.221's `key` fix was correct for swap-live but the operator's actual workflow was single-click-preview, which doesn't change `liveSlide.mediaUrl`. The two workflows superficially share the symptom but exercise completely different code paths. Re-fired escalation = re-trace the user gesture step-by-step against the actual code path, do not assume the previous fix's diagnosis was right. **PROCESS GR — defense-in-depth for finite-HW-resource bugs (HW decoder slots, audio context nodes, WebGL contexts, MediaStream tracks) MUST stack render-layer gates ON TOP OF attribute-layer gates, not instead of them**: the v0.7.216 → v0.7.218 → v0.7.222 progression is the canonical example (autoPlay gate → preload="metadata" gate → preload="none" gate + zero-element placard). Attribute gates are necessary but not sufficient — the browser still owns the element lifecycle and can do unexpected work behind the attribute façade. The render-layer gate is the only one that guarantees ZERO HW resource allocation. FIX #4 (architect code-review surfaced this as a SECOND live-stop path the initial v0.7.222 attempt missed): `logos-shell.tsx` `MediaCard.onItemClick` L~3643-3713 first-click MUST use `pinPreviewSlide` (not `setSlides([slide]) + setLiveSlideIndex(-1) + setIsLive(false)` — that wiped live BY DESIGN; same legacy code-path bug that library-compact's `sendMediaToPreview` migrated away from in v0.7.210 + v0.7.216 but the in-shell sibling carried until now). New contract identical to `sendMediaToPreview`: live direct-ref read (`liveSlide ?? slides[liveSlideIndex]`) + pause-before-pin when `liveIsPlayingDifferentMediaVideo` + `pinPreviewSlide` + second-click via `setLiveAuto` (NOT legacy `setLiveSlideIndex(0) + setIsLive(true)` — the legacy index-promote only worked because old first-click stuffed slide into `slides[0]`, now that first-click pins instead, slides[] may not contain it). Test (gg) is a 9-assertion source-grep guard (strips `//` comments via per-line regex so historical "MUST NOT call X" documentation doesn't false-positive negative checks). 666/666 PASS. Audit pattern for future similar refactors: `rg -n "onItemClick|sendMediaToPreview" src/components/layout/` — any time a fix touches one, audit the others against the same contract. Untouched: LIVE surface attributes (zero changes); v0.7.221 LIVE `key={liveSlide.mediaUrl}` (KEEP — closes the legitimate swap-live decoder-handoff path); `customBackground` / `freezeBg=1` payload schema; `mirrorLive` / SSE semantics; v0.7.216 paused-before-pin sequence + canplay retry; v0.7.217 writeThreshold + drift tolerance; v0.7.219 MediaPreheat refactor; v0.7.220 NDI async send + buffer pool. Full text in `artifacts/imported-app/CHANGELOG.md`.

-   **v0.7.220 — NDI smoothness on receiving apps (OBS/vMix/Wirecast): async send + 2-slot buffer pool + paced bridge/fade loops — close last gap to EasyWorship-class NDI output (operator $1600-customer escalation; v0.7.217 closed in-app smoothness, this closes downstream-receiver smoothness)**: `electron/ndi-service.ts` — added koffi binding for `NDIlib_send_send_video_async_v2` (L297-313), hot path `nativeSendFrame` MUST call `send_send_video_async_v2` NOT `send_send_video_v2` (L595-599); `NDIlib_send_create` settings MUST have `clock_video: false` (L470-481) — async send has its own NDI worker pacing the wire, `clock_video:true` would queue a 2nd pacing layer that re-blocks main thread. Added 2-slot `videoBufferPool` + `videoBufferIndex` + `videoBufferCapacity` state (L182-197); `sendFrame` MUST memcpy into `videoBufferPool[videoBufferIndex]` then advance `(i+1)%2`, MUST NOT call `Buffer.allocUnsafe(bgraBuffer.length)` per frame — eliminates ~250MB/s GC churn at 1080p30 AND satisfies async send's buffer-lifetime contract (slot NDI is reading is NEVER the slot next sendFrame writes; sendBusy mutex ensures ≤1 outstanding async send). `stop()` MUST reset `videoBufferPool=[]` (release ~16MB/sender on teardown). Architect medium-risk caveat (addressed in same release): switching the hot path to async ALSO removed the implicit sync-blocking pacing that 3 OTHER call sites depended on — `armBridge` setInterval MUST be `Math.max(16, Math.floor(1000/(this.status.fps||30)))` NOT bare `16` (would burst at 62fps under async); `lingerStop` AND `gracefulStop` fade-to-black loops MUST `await setTimeout(frameMs)` between sends (tight for-loop would coalesce all N fade frames into a single black-flash on receiver). PROCESS GR — when a sync primitive provides "free" pacing as a side effect of blocking, switching it to async MUST come with audit of EVERY call site that relied on that implicit pacing: `rg -n "<primitive_invocation>" .` THEN classify each site as explicit-depends OR implicit-depends-via-loop. v0.7.220 found 3 implicit sites; without the audit they'd ship as latent regressions ("fade isn't a fade, just a flash" / "bridge re-emit hammering CPU"). PROCESS GR — when architect flags a same-release-addressable caveat, address immediately, do not defer (next release would be a hotfix). Audio path (`send_send_audio_v3`) intentionally left sync — tiny frames, no observed bandwidth/jitter issue, gratuitous risk to convert. Untouched: customBackground / payload schema / `!IS_NDI` exemption / aspect-ratio paths / `ndiCaptureFps` default / keep-alive ticker / `lastFrame` caching / linger semantics / v0.7.216-219 fixes. Guard test `(aa)` in `src/lib/media-preview-isolation.test.ts` source-grep enforces all 7 invariants (FFI binding loaded + bindings-object exposed + nativeSendFrame uses async-not-sync + clock_video:false + pool used + Buffer.allocUnsafe absent + pool freed + armBridge fps-paced + lingerStop awaits + gracefulStop awaits). 661/661 PASS. No NDI runtime test possible in Replit (Windows DLL + actual NDI receivers required) — relies on operator validation. Full text in `artifacts/imported-app/CHANGELOG.md`.

-   **v0.7.219 — NDI/Preview decoder-slot competition: MediaPreheat MUST use `<link rel="preload" as="video">` NOT hidden `<video preload="auto">` AND warm-up set MUST include `pinnedPreviewSlide` + `liveSlide` direct refs (one-line GR, full text in CHANGELOG)**: `src/components/layout/logos-shell.tsx` MediaPreheat (L~3815) — pre-fix used hidden `<video preload="auto">` per upcoming media URL which SILENTLY allocated 1-3 HW decoder slots competing with real Preview / Live / NDI decoders (GPU caps at 2-4 streams on low-end hardware — was the root of the multi-release live-stall saga). Post-fix uses `<link rel="preload" as="video" crossOrigin="anonymous" data-media-preheat="<url>">` injected into `document.head` imperatively via useEffect (Chromium's preload scanner only honours head-level preload; tag with `data-media-preheat` for precise add/remove): warms HTTP cache only, ZERO decoder cost. Warm-up URL set MUST union `pinnedPreviewSlide` (v0.7.201) + `liveSlide` (v0.7.203) direct refs alongside upcoming slides[] — pre-fix MediaPreheat only read `slides[idx]` so single-clicked AND AI-routed media never got byte-warmed, causing cold network fetch on GO LIVE. `crossOrigin="anonymous"` MUST match eventual `<video crossOrigin="anonymous">` (CORS-mode mismatch invalidates cache hit). 660/660 PASS + Playwright PART A verified (2 `<link data-media-preheat>` in head with both URLs, 0 offscreen `<video>` warmup elements remain). Full text in `artifacts/imported-app/CHANGELOG.md`.

-   **v0.7.218 — MediaVideoSurface `<video>` `preload` attribute MUST be gated by `previewMediaPaused` (one-line GR, full text in CHANGELOG; SUPERSEDED BY v0.7.222 — truthy literal moved from `'metadata'` → `'none'` because `'metadata'` still triggered decoder-capability probe on operator's hardware; gate SHAPE preserved)**: `src/components/layout/logos-shell.tsx` L~524 — `MediaVideoSurface` `<video>` MUST set `preload={surface === 'preview' && previewMediaPaused ? 'none' : 'auto'}` (was `'metadata'` pre-v0.7.222) — symmetric to v0.7.216 `autoPlay` gate. Pre-fix `preload="auto"` on a paused preview surface still allocated an HW decoder slot during the metadata-fetch phase (browsers create the decoder eagerly to prepare for play()), competing with live + NDI decoders even though the user explicitly paused. Gating to `"metadata"` when paused tells the browser to fetch dimensions/duration only — no decoder allocation. Same root pattern as v0.7.216 PROCESS GR ("HTML media attributes that control finite HW resources must be gated at the attribute layer, not just in useEffect"). 659/659 PASS + Playwright verified. Full text in `artifacts/imported-app/CHANGELOG.md`.

-   **v0.7.217 — Live media-video smoothness: stop 10Hz SSE broadcast storm + receiver seek-thrash (one-line GR, full text in CHANGELOG)**: `MediaVideoSurface` writeback effect in `logos-shell.tsx` L420-475 — live-surface `writeThreshold` MUST be `0.50` (5x reduction in broadcast rate ~10Hz → ~2Hz), preview MUST stay `0.25`. 4 unconditional transport event listeners (`play`/`pause`/`seeked`/`loadedmetadata`) MUST be registered + cleaned up so transport gestures bypass the threshold for operator responsiveness. `congregation/route.ts` L1593-1623 receiver drift tolerance MUST be `1.5` (was 0.20 → 7.5x) AND `!IS_NDI` exemption MUST stay (v0.7.194-hotfix.2 NDI invariant). Intermediate states (raise sender only OR receiver only) are WORSE than v0.7.216. Tests (w)+(x) source-grep enforce. Full text in CHANGELOG.

-   **v0.7.216 — Single-clicking a DIFFERENT media tile while LIVE plays a media-video MUST NOT stall live; + Settings dropdown z-order + GO LIVE auto-play retry + headphone-button removal (one-line GR, full text in CHANGELOG)**: 5 invariants — (A) `MediaVideoSurface` `<video>` `autoPlay` MUST be `!mediaPaused && surface === 'preview'` (gate at attribute layer, not just useEffect, to prevent 2nd HW decoder spin-up); (B) `library-compact.tsx` sendMediaToPreview MUST flip `setPreviewMediaPaused(true) + setPreviewMediaCurrentTime(0)` BEFORE `pinPreviewSlide(slide)` when live plays different media-video, MUST NOT touch live state; (C) play/pause effect MUST register one-shot `canplay` retry `{once: true}` AND cleanup `removeEventListener('canplay', ...)`; (D) ALL 11 Radix portal primitives MUST use `z-[60]` NEVER bare `z-50` (Settings overlay is z-50, equal-z = portal paints behind on compositor repaint); (E) LiveDisplayCard volume toggle MUST flip `setLiveBroadcastAudio` AND live `<video>` `muted` MUST OR-in `!liveBroadcastAudio`. PROCESS GR — HTML media attributes controlling finite HW resources MUST be gated at attribute layer (synchronous with mount), not just useEffect. Tests (q)-(v) + headphone-volume guards. Full text in CHANGELOG.

-   **v0.7.215 — Per-pane Clear buttons on Preview + Live (independent single-click, no interference) (one-line GR, full text in CHANGELOG)**: `clearLive` MUST call `clearLiveAuto()` BEFORE `setLiveSlideIndex(-1) + setIsLive(false) + sendToOutput(null,false)` (4-primitive sequence) — omitting `clearLiveAuto()` leaves v0.7.203 `liveSlide` direct ref alive, broadcaster re-renders stale AI slide. PreviewCard symmetry-strip Clear Preview button MUST call ONLY `clearPinnedPreview() + setPreviewSlideIndex(-1)` (independence invariant — MUST NOT touch live primitives or `setSlides([])`). LiveDisplayCard Clear Live button MUST call `onClick={onClearLive}` (prop reuses parent's 4-primitive callback). PROCESS GR — when operator-visible label is "Clear", the primitive MUST wipe EVERY state layer contributing to rendered output for that surface (direct ref + legacy index + broadcast flag + projector push). PROCESS GR — "symmetry strip" empty spacers in card layouts are backlog candidates (height reservation correct, content missing). Tests (l)-(p). Full text in CHANGELOG.

-   **v0.7.214 — Voice commands + AI/LLM verse navigation MUST target LIVE display only (one-line GR, full text in CHANGELOG)**: Every voice handler in `speech-provider.tsx` (next_verse / next_chapter / show_verse_n / find_by_quote / LLM classifier) MUST read live anchor via `s.liveSlide ?? (liveIdx >= 0 ? slides[liveIdx] : null)` (v0.7.208 AI direct-ref-first pattern — bare `slides[liveIdx]` returns null when AI is driving live) AND MUST commit via `setLiveAuto(slide) + setLiveActiveVerseIndex(0)`, NEVER the legacy `setSlides + setPreviewSlideIndex + setLiveSlideIndex + setIsLive` combo (clobbers preview + leaves AI liveSlide ref stale). Voice dispatcher switch body MUST contain ZERO `setPreviewSlideIndex(` call sites outside `bible_says` (the ONE voice command that legitimately writes preview — STANDBY only). MUST NOT call `addScheduleItemQuiet` as "preserve manual preview" fallback (v0.7.194-hotfix.11 anti-pattern). Tests (a)-(g) in `ai-live-only.test.ts` enforce all 8 sites. PROCESS GR — when introducing a new direct-ref primitive (`setLiveAuto`/`pinPreviewSlide`), SAME-RELEASE audit MUST enumerate every reader/writer of the SHADOWED state via `rg -n "<shadowed_field>" src/`, not just the headline call site. Full text in `artifacts/imported-app/CHANGELOG.md`.
-   **v0.7.213 — `setSlides` MUST clear `pinnedPreviewSlide` so detected/suggested verses on Preview win goLive (one-line GR)**: `store.ts` setSlides reducer's set() payload MUST include BOTH `liveSlide: null` (v0.7.203 invariant) AND `pinnedPreviewSlide: null` (v0.7.213 invariant). Removing either re-opens a regression class: without `liveSlide: null`, AI direct ref survives a new deck import and stale-renders on live; without `pinnedPreviewSlide: null`, a stale Media-tile pin survives the verse-stage path and v0.7.212's `goLive` pin-first branch promotes the wrong thing. Pattern: any "hard-reset" reducer that takes ownership of Preview MUST clear pin + AI ref. Audit: `rg -n "setSlides:\s*\(s\)\s*=>\s*set\(" src/lib/store.ts` MUST show BOTH fields. Test (j) in `media-preview-isolation.test.ts` source-grep enforces both. Full text in `artifacts/imported-app/CHANGELOG.md`. **New regression tests (3, extended `src/lib/media-preview-isolation.test.ts`)**: (i) load-bearing — pin a stale video, then `setSlides([verse])`, assert `pinnedPreviewSlide === null` post-call, mirror goLive's pin-null fallback to `setLiveSlideIndex(previewSlideIndex) + setIsLive(true)`, assert `buildOutputPayload(state).slide.id === 'rom-8-29'` (verse promoted, not the stale pin). (j) GUARD source-grep on store.ts: `setSlides` reducer's set() payload regex `setSlides:\s*\(s\)\s*=>\s*set\(\{([^}]*)\}\)` MUST match BOTH `pinnedPreviewSlide:\s*null` AND `liveSlide:\s*null` (preserves v0.7.203 invariant). (k) symmetric flow — verse on Preview via setSlides, single-click video → `pinPreviewSlide(vid)` → assert `(pinnedPreviewSlide ?? slides[previewSlideIndex])?.id === 'vid-storm'` (Preview swaps instantly); slides[0] verse survives in the deck (just shadowed by the pin); live untouched (`isLive=false`, `liveSlide=null`). **Test results**: 11/11 PASS in the extended file (was 8/8 on v0.7.212 — +3). Full suite **638/638 PASS** (was 635/635 on v0.7.212 — +3). 0 regressions. **Replit runtime proof**: test (i) exercises the EXACT 3-call sequence the production paths run (`pinPreviewSlide` → `setSlides` → `setLiveSlideIndex+setIsLive`); the assertion `pinnedPreviewSlide === null` post-setSlides directly verifies the store mutation; the final `buildOutputPayload` assertion mirrors the renderer's read path. Test (j) source-grep enforces the fix lives at the store reducer, not in a wrapper that could be bypassed. Same hybrid proof shape as v0.7.210/v0.7.212. **GUARD-RAIL (A) — store.ts `setSlides` reducer's set() payload MUST include BOTH `liveSlide: null` (v0.7.203) AND `pinnedPreviewSlide: null` (v0.7.213)**: removing either field re-opens a regression class. Without `liveSlide: null`, the v0.7.208 AI direct ref survives a new deck import and stale-renders on live (v0.7.210's lead bug). Without `pinnedPreviewSlide: null`, a stale Media pin survives the verse-stage path and v0.7.212's `goLive` promotes the wrong thing (this release's lead bug). Test (j) source-grep enforces both. Audit: `rg -n "setSlides:\s*\(s\)\s*=>\s*set\(" src/lib/store.ts` MUST show BOTH fields in the payload. **GUARD-RAIL (B) — Any future "hard-reset" reducer that takes ownership of the Preview surface MUST clear `pinnedPreviewSlide` AND `liveSlide`**: the pattern is established at L1442 (`setSelectedScheduleItemId(null)`), L1463 (other hard-reset), and L1190 (`setSlides` as of v0.7.213). New reducers like `loadDeck`, `importPresentation`, `resetState` etc. MUST follow the same pattern. **PROCESS GR — When introducing a "pin-first" read pattern (`pinnedPreviewSlide ?? slides[...]`), EVERY writer to the fallback (slides[] via setSlides, setPreviewSlideIndex, etc.) MUST also clear the pin**: v0.7.201 introduced pin-first read in the Preview pane (logos-shell L967). v0.7.212 extended to `goLive`. But the symmetric "writer clears pin" rule was only applied at clearScheduleItem/clearSelection sites, NOT at `setSlides`. Future audit when introducing a new shadowing primitive: enumerate ALL writers to the shadowed field; default to "clear the shadow" unless writer semantics explicitly preserve operator intent (`setPreviewSlideIndex` correctly preserves the pin for arrow-key navigation through the same deck). **PROCESS GR — When operator escalates a follow-up symptom IMMEDIATELY after a release ships, do NOT assume the prior release was wrong; the new symptom is often the NEXT layer of the same bug class**: v0.7.212's pin-first branch in goLive was correct in isolation. The follow-up symptom was caused by a DIFFERENT site (writer didn't clear the pin) — a layer deeper. The right diagnostic question is "given my fix is correct in isolation, what UPSTREAM/DOWNSTREAM site does NOT yet know about the new invariant?" — this produced the 5-minute store.ts fix instead of an unnecessary `goLive` rewrite.

-   **v0.7.212 — Single-click on another Media tile stops the live video AND Go Live button does not promote preview-pinned video to live (one-line GR)**: `output-preview.tsx` `sendNow` MUST hold `lastSentFpRef` and compute a CHEAP identity fingerprint via `JSON.stringify({t:type,b:blanked,l:isLive,dm:displayMode,sl:{id,ty,ti,su,co,mk,mu:mediaUrl.length,mp:mediaPaused},st:settings,au:audio})` — early-return `if (fp === lastSentFpRef.current)` BEFORE `revRef.current += 1` and `postMessage` so identical mirrorLive payloads don't tear down the iframe `<video>`. Fingerprint MUST exclude raw `s.mediaUrl` (only `mediaUrl.length` proxy). v0.7.204 IS_PREVIEW handler `lastRenderKey=''` force-reset MUST stay intact at route.ts L2212. Both `goLive` functions (easyworship-shell + logos-shell) MUST check `pinnedPreviewSlide` and `setLiveAuto(pinned)` BEFORE the `!slides.length` toast guard. PROCESS GR: when a new primitive is introduced, EVERY downstream reader/writer of the shadowed state MUST be audited in the same release. A "subscribe fires on every store change" pattern is ONLY safe when receiver is idempotent for unchanged payloads — dedup at SOURCE not SINK. Full text in `artifacts/imported-app/CHANGELOG.md`.<!-- TRIMMED v0.7.215: was v0.7.212 incumbent full -->
