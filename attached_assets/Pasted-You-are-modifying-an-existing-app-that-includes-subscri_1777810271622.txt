You are modifying an existing app that includes subscriptions and a Live Transcription feature.

Implement the following subscription and usage system changes:

---

1. BASE PLAN CHANGES

* Locate the current subscription priced at **GHS 200**
* Change the price to **GHS 170**
* Remove any reference to **"31 days"**
* Replace it with:
  → "25 hours/month transcription time"

---

2. USAGE TRACKING SYSTEM

* Implement a **monthly transcription usage system**

* Each user on this plan gets:
  → 25 hours per month (**1500 minutes**)

* When the subscription is activated:

  * Start a countdown timer tracking remaining transcription time
  * Store usage in the database (e.g., `remaining_minutes`)

* During live transcription:

  * Deduct usage **in real-time (per second)** from remaining_minutes

---

3. HARD LIMIT ENFORCEMENT

* When remaining transcription time reaches **0**:

  * Automatically **disable/lock the Live Transcription feature**
  * Display message:
    → "You have used your monthly transcription limit."

* Prevent further transcription until:

  * A new billing cycle starts, OR
  * The user upgrades their plan

---

4. MONTHLY RESET

* At the start of each billing cycle:

  * Reset transcription time to the plan allocation

* Default reset values:

  * Base plan: **25 hours/month**
  * Yearly plan: **30 hours/month**

---

5. MULTI-MONTH OPTION

* Allow users to select additional months when subscribing

* For each additional month:

  * Add **80% of the base plan (25 hours)** to the monthly limit

Formula:
→ total_hours = 25 + (number_of_extra_months × (25 × 0.8))

Correct Examples:

* 1 month = 25 hours

* 2 months = 25 + (1 × 20) = 45 hours/month

* 3 months = 25 + (2 × 20) = 65 hours/month

* Clearly display the updated monthly transcription hours before checkout

---

6. REMOVE UNUSED PLANS

* Delete ALL subscription options from:
  → 2 months to 6 months (predefined plans)

* Keep only:

  * 1 month (with optional add-on months)
  * 1 year plan

---

7. YEARLY PLAN CHANGES

* Keep the **1-year subscription**, but:

  * Remove any “25% discount” references
  * Set transcription time to:
    → 30 hours/month

* Apply the same monthly reset logic

---

8. UI / UX REQUIREMENTS

* Display:

  * Remaining transcription time (hours + minutes)
  * A progress bar showing usage

* Update values **in real-time** during transcription

---

9. BACKEND LOGIC

* Ensure:

  * Accurate time tracking (server-side, not frontend-only)
  * Protection against refresh/reset exploits
  * Subscription tied to authenticated user account

---

10. DATABASE STRUCTURE (if needed)
    Add fields:

* subscription_type
* remaining_minutes
* monthly_limit_minutes
* billing_cycle_start
* billing_cycle_end

---

11. TESTING
    Simulate:

* Subscription activation
* Live transcription countdown
* Lock when limit reaches zero
* Monthly reset behavior

Ensure everything works end-to-end without breaking existing authentication or billing logic.
