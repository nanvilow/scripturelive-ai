import { BrightnessPreview } from "./_shared";

export default function Max() {
  return (
    <BrightnessPreview
      label="BRIGHTEST (proposed)"
      bgOpacity={0.85}
      scrimAlpha={0.05}
    />
  );
}
