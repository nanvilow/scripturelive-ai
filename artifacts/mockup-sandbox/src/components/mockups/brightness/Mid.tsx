import { BrightnessPreview } from "./_shared";

export default function Mid() {
  return (
    <BrightnessPreview
      label="BRIGHTER (proposed)"
      bgOpacity={0.72}
      scrimAlpha={0.12}
    />
  );
}
