-   **v0.7.258 — Operator dashboard density pass: "ALL" default + footer removal + slim transport bar + Media-narrower bottom row**: Four batched UX changes from operator screenshots 2026-05-27 22:00 reclaiming vertical/horizontal chrome for the bottom-row Chapter Nav / Detected Verses / Media cards. (1) `bibleOnlyTranscription` default flipped `true` → `false` (fresh installs land on full transcript with "ALL" pill un-highlighted; existing operators keep persisted choice). (2) Bottom `<footer h-7>` "Powered by WassMedia" branding REMOVED (lives in top-left app header only — re-adding a bottom strip negates the claw-back). (3) `TransportBar` compressed h-14 → h-9 with buttons h-10 → h-7 — single source of truth in `easyworship-shell.tsx`, imported by `logos-shell.tsx`. (4) Bottom-row column widths 33/33/34 → 39/39/22 (Media narrowed, Chapter Nav + Detected Verses widened); all three `ResizablePanel.defaultSize` props MUST sum to 100 in lockstep, `autoSaveId='logos-shell-bottom-cols'` keeps existing operator drags.
-   **v0.7.257 — AVM auto-fire unfreezes on fresh re-utterance + reference typography & Bible line-height defaults**: v0.7.152 semantic-owns-live latch release clause extended to also fire when `detectedAtMs(explicitTop) > gate.lastFireAtMs` — audio-buffer-lag re-runs carry the original timestamp (stale, latch holds), but fresh new utterances carry a newer timestamp (latch releases) so a re-spoken address (e.g. operator says "Ephesians 6:11" twice with a paraphrase in between) actually fires live. Audit-once before any explicit-pipeline refactor: `rg "detectedAt.*Date\.now|detectedAt:.*now" artifacts/imported-app/src/components/providers/speech-provider.tsx` — every explicit detection ingestion site MUST stamp `detectedAt: Date.now()` at regex-hit time (NOT store-merge time, NOT inherited from the source utterance buffer); stale-stamped detections regress to v0.7.152 behaviour. Default flips (Bible line-height 1.20 → 1.10 + referenceFontSize 'xl' + referenceTextScale 1.55) follow the v0.7.252 lockstep pattern — `store.ts` DEFAULT_SETTINGS + `settings.tsx` `??` fallback in sync. Existing operators keep persisted values via Zustand persist.
-   **v0.7.256 — Cloud companion migrated off Replit to self-hosted Hetzner (cloud.scriptureliveai.com)**: .exe runtime was already 100% Replit-free; only remaining dep was `scripturelive.replit.app` cloud companion. Migrated to Hetzner CPX22 (Ubuntu + Node 24 + pnpm + Next 16 standalone + nginx + SQLite at `/var/lib/scripturelive/`). Three URL-default flips: `DEFAULT_TRANSCRIBE_PROXY_URL` (electron/main.ts), `DEFAULT_TELEMETRY_URL` (electron/telemetry.ts), `DEFAULT_CLOUD_BASE` (src/app/api/license/admin/cloud-sync-test/route.ts) — all `scripturelive.replit.app` → `cloud.scriptureliveai.com`. **NOTE (v0.7.264 follow-up)**: this missed the OTHER `DEFAULT_CLOUD_BASE` in `src/lib/licensing/cloud-sync.ts` (the real admin pull/push client) — fixed in v0.7.264; both must point at Hetzner in lockstep. **GUARD-RAIL (env-var override precedence)**: three cloud URLs (`TRANSCRIBE_PROXY_URL` / `SCRIPTURELIVE_TELEMETRY_URL` / `SCRIPTURELIVE_CLOUD_BASE`) MUST stay read at the use site (`process.env.<NAME> || DEFAULT_*`), NOT at module-load. **GUARD-RAIL (fresh-clone build sequence)**: on non-CI machines `next build` fails without prior credential injection — `set -a && source .env.production && set +a && node scripts/inject-keys.mjs` BEFORE `next build`. **GUARD-RAIL (Hetzner pnpm 11.3 preinstall false-positive)**: root `package.json` preinstall false-fires under pnpm 11.3 on Ubuntu 26.04 — strip preinstall locally on the server (NOT committed). Permanent ship deliverable: `artifacts/imported-app/scripts/ship-release.mjs` (atomic git-data-API push, mandatory `inputs.tag=v<VERSION>`, BOTH baked-credential paths in EXCLUDE_FILES).
-   **v0.7.255 — Deepgram keyterm overflow broke AI Detection**: Nova-3 streaming WS rejects (HTTP 400 on handshake) above ~140 `keyterm=` query params — COUNT cap, NOT byte budget. KEY_TERMS in `src/hooks/use-deepgram-streaming.ts` MUST stay ≤100 (enforced at append time via `slice(0, KEY_TERMS_LIMIT)`); canonical pre-ship grep `rg "KEY_TERMS_LIMIT|KEY_TERMS\.slice" artifacts/imported-app/src/hooks/use-deepgram-streaming.ts`. When AI Detection shows generic "Check your internet" FIRST diagnostic is NOT "is the key valid" — open DevTools for the WS error code and probe Deepgram with Node `ws` to see JSON body. Probe order: REST `/v1/projects` (auth) → REST `/v1/listen` (model access) → Node `ws` of EXACT streaming URL incl. all keyterms (surfaces real HTTP code+body).
-   **v0.7.254 — Hotfix Deepgram credential rebake after v0.7.251–253 key leak**: pure rotation release. Ship-script `EXCLUDE_FILES` MUST cover BOTH `src/lib/baked-credentials.ts` AND `src/lib/keys.baked.ts` (walker does NOT honour `.gitignore`). When a baked credential leaks, only safe order is (1) rotate key in provider dashboard, (2) update GH Actions secret, (3) bump + ship — Deepgram/OpenAI auto-revoke leaked keys within minutes; whitelisting the gitleaks finding is NEVER acceptable. Win11 Task Manager caches old `FileDescription` after binary overwrite until `explorer.exe` restart — ask operator about that BEFORE re-investigating rcedit on "Electron (N)"-after-install screenshots.
-   **v0.7.253 — Five-fix batch: Task Manager AUMID lockstep + full canonical Bible verse on slides + loop default OFF + AI detection speedup + African English keyterm bias**: SHIPPED A LIVE DEEPGRAM KEY via `src/lib/keys.baked.ts` because ship-script `EXCLUDE_FILES` lacked that path — see v0.7.254 entry for the rotation playbook and the EXCLUDE_FILES guard-rail. Icon 2-place lockstep: `electron-builder.yml` extraResources + `electron/main.ts` BrowserWindow icon (`process.resourcesPath`). Slide canonical-text: `sendDetected()` MUST resolve `v.reference` via `lookupVerse()/lookupRange()` before `splitForSlides()` — reverting re-introduces 4-word-paraphrase-on-Live bug for semantic detections. mediaLoop 4-site lockstep: payload + 2 slide-renderer surfaces + congregation route inline attr + reconcile-block mirror; LT background videos stay intentional always-loop. AI throttle floors: phrase-index ≥ 80 ms, SEMANTIC_THROTTLE_MS ≥ 400 ms. Deepgram keyterm budget: WS query ~8 KB practical limit; current ~75 phrases well under.
-   **v0.7.252 — NDI capture resolution defaults to 720p**: `ndiCaptureResolution` default flipped from `'1080p'` → `'720p'` to match the long-tail mid-range/older sanctuary PC install base (software NDI decode at 1080p saturates a single core on Ivy Bridge mobile / pre-2015 / integrated graphics; vMix/Wirecast/OBS upscale 720p → 1080p in scene composers for free). **GUARD-RAIL (default + fallback lockstep)**: `src/lib/store.ts` `ndiCaptureResolution: '720p'` (DEFAULT_SETTINGS) AND `src/components/views/ndi-output-panel.tsx` `?? '720p'` hook fallback MUST stay in sync — drifting them re-introduces "fresh install gets X, undefined-field upgrade gets Y" inconsistency. Operators who explicitly chose `'1080p'` keep it via Zustand persist (default only seeds fresh installs + pre-v0.7.194-hotfix.3 upgrades where field was never set). Canonical pre-ship grep: `rg "ndiCaptureResolution.*\?\?|ndiCaptureResolution: '" artifacts/imported-app/src/lib/store.ts artifacts/imported-app/src/components/views/ndi-output-panel.tsx`.
-   **v0.7.249 — Professional OBS-grade Windows installer flow (Welcome → License → Install Location → Installing → Completed)**: linear 5-page OBS contract is a CONTRACT — page injection / MessageBox addition / run-checkbox default flip / sidebar+header bitmap swap MUST preserve it. Mid-flow modals BREAK the contract; if operator confirmation is genuinely needed, add to Welcome or Finish page copy, NOT a `MessageBox`. NSIS MUI2 sidebar/header bitmaps MUST be 24-bit uncompressed BMP3 (verify `file foo.bmp` → "PC bitmap, Windows 3.x format, NNN x NNN x 24"); PNGs / BMP4/v5 / palette / ImageMagick's default BMP all surface as black rectangles on Win7/8 and corrupt alpha on Win10/11. Canonical regen: `magick … -type TrueColor -define bmp:format=bmp3 -depth 8 BMP3:foo.bmp`. Run-checkbox default tracks launch-affordance count: 1 affordance ⇒ checked; 2 ⇒ first checked second NOTCHECKED (single-instance flicker was v0.7.143 root cause).
-   **v0.7.248 — Bible Translations DB fallback + Slide Transition default Cut + Settings card re-order**: route handler graceful-degradation try/catch returns CATALOGUE with `download:null` on DB fail (audit `rg "await db\.\w+\.find" src/app/api/**/route.ts`). Slide Transition default flip MUST hit all 6 fallback spots in lockstep (store default + UI fallback `a || 'default'` + payload composer + broadcaster + server renderer; canonical grep `rg "slideTransitionStyle.*\|\|.*'\w+'"`). Settings card re-order is CUT+PASTE, never CSS `order:` (breaks tab-order) or state-driven render order (defeats reconciliation).
-   **v0.7.247 — 7-fix batch shipped via atomic git-data-API**: (A) NDI Full-Screen TRANSPARENT actually strips bg — `congregation/route.ts` L1688 + L1775 gate `safeBg`/`safeBgE` with `if (FS_BG_TRANSPARENT && !isLT)`. (B) Task Manager process name — `package.json` gained `description` + `author` so Windows StringFileInfo shows branded name. (C) Re-mention reference hijack — voice "John 3:16" twice no-ops the second invocation. (D) OPENAI_API_KEY rotation via GH Actions secret; baked-credentials.ts gitignored + regenerated by release-desktop.yml. (E) Transcription buffer 4096→2048 in `use-deepgram-streaming.ts` halves per-chunk latency. (F+G) blank/dup-audio second screen — last `/api/output` POST sites folded into `buildOutputPayload()`. **SHIP PIPELINE GUARD-RAIL (atomic git-data-API replaces serial PUT loop — SUPERSEDED by `scripts/ship-release.mjs` per v0.7.256)**: serial concurrent PUTs race on branch HEAD SHA; canonical ship pipeline now lives in `artifacts/imported-app/scripts/ship-release.mjs`. **SHIP PIPELINE GUARD-RAIL (extended EXCLUDE_DIRS)**: bulk-drift walker MUST exclude `dist-electron`, `dist-electron-ui`, `exports`, `upload`, `uploads`, `download` in addition to `node_modules`, `.next`, `dist`, `release`, `db`. **PROCESS GR (dup audio on second screen / NDI receiver)**: ALWAYS check `liveMonitorAudio` (default false per v0.7.244 + persisted) FIRST — operators with pre-v0.7.244 persisted state still have it true.
-   **v0.7.246 — `pushCurrentSlide` honours `liveSlide`**: every `/api/output` POST MUST go through `buildOutputPayload(useAppStore.getState())` not a payload literal — three remaining hand-rolled sites (`logos-shell.tsx` L5038 `goLogo`, `easyworship-shell.tsx` L1536 `sendToOutput`) are transient overlays without liveSlide direct refs; if a future change moves either to use liveSlide, port to `buildOutputPayload` in lockstep. Architect review is load-bearing for `liveSlide` invariant changes (store.ts setters, output-payload.ts precedence).
-   **v0.7.245 — Output Display picks second screen → keeps playing video**: `openOnScreen` gains `before.liveSlide` early branch BEFORE existing auto-promote. `liveSlide` direct-ref is the source of truth — never clear it without operator intent (`setLiveSlideIndex` / `setSlides` / `setIsLive(false)` ALL clear it). Auto-promote branch is a FALLBACK not a precondition — 3-branch shape (liveSlide → noop / liveSlideIndex<0 → promote-preview / liveSlideIndex≥0 → promote-isLive) is load-bearing. Single-monitor testing misses this class of bug — reproduce against a connected display, NDI receiver, or `curl -N /api/output/congregation`. Tier 1E paraphrases (+841) appended with write-time normalization (smart quotes → ASCII; `Psalm` → `Psalms`; `Song of Songs` → `Song of Solomon`; `I/II/III X` → `1/2/3 X`).
-   **v0.7.244 — Sync (Path A) + audio-dup default + red Live meter + "Later" actually means later**: `MediaVideoSurface` rAF + timeupdate in same effect, shared cleanup, rAF is LIVE-only, gate-then-write keeps state churn ≤ 20Hz. `liveMonitorAudio` vs `liveBroadcastAudio` are independent — two toggles, two mute paths, do NOT collapse. Broadcast default ON + monitor default OFF; persisted operators keep preference. Live meter is status, not state — don't re-introduce state-tone. "Later" opt-out is PER-SESSION (`autoDownloadEnabled` resets to true on each launch); any future Later button MUST also call `setAutoDownload(false)`.
-   **v0.7.242 — Voice search catalog +600 paraphrases (Tier 1D)**: every bulk-paraphrase append MUST run normalizer at WRITE time (runtime normalizer would drift Map keys silently). Dedupe scope is `(reference, text)` tuple ONLY. Tier-tag delimiters mandatory (`// ── v0.7.<N> Tier 1<L> ──`). Catalog-only ships detect EXACTLY 3 changed files in bulk-drift PUT.
-   **v0.7.241 — Video lag in OBS Browser Source (HTTP Range / 206 Partial Content) + 4 stacked fixes**: `/api/upload` GET parses `Range` header + emits 206 + `Content-Range` per RFC 7233 single-range, applied to BOTH >20MB streaming branch AND small-file branch; `Accept-Ranges: bytes` is a PROMISE not a hint. `speech-provider.tsx` outer fetch timeout MUST exceed inner LLM `DEFAULT_TIMEOUT_MS` by ≥ 200ms — bump together. `matchTranscript()` from `phrase-index.ts` dispatched FIRST per chunk, minN=2 + score floor 0.65 + 200ms throttle; `warmPhraseIndex()` useEffect MUST stay `[]` deps, `matchTranscript()` MUST stay synchronous. NDI lag (architectural CPU-readback per v0.7.234) ≠ OBS Browser Source lag (implementation per v0.7.241) — DIFFERENT root causes, treat separately.
-   **v0.7.240 — Live-footer popovers auto-close (Mic + Master Volume)**: 600ms close MUST be scheduled from pointer-up/key-up, NEVER from `onChange` (drag is intent envelope, pointer-up is commit). Timer refs MUST be cleared in `onOpenChange → closed` useEffect. Mute toggle + "100%" reset stay immediate-close (binary commits, not envelopes). Operator escalations phrased "anytime users finish X this should auto close" are ALWAYS about drag-intent vs commit — fix is NEVER a debounced `onChange` close.
-   **v0.7.238 — Card footer slot + voice "verse N / chapter N" word-form**: Card primitive `footer?: ReactNode` slot rendered as section-level sibling OUTSIDE body's overflow-hidden region; moving footer INTO body re-introduces clipping. `parseEnglishNumber()` helper stays shared between verse + chapter detectors — keep SMALL/TENS tables in one place; LLM word-form list MUST match this table exactly. Range gates verse 1-200 chapter 1-150. Regex permissive, parser strict.
-   **v0.7.237 — Bulk-sync release (every code fix since v0.7.226 finally reaches the binary)**: 11 prior releases shipped binaries equivalent to v0.7.225 with version bumps because GH Contents API PUT loop only pushed 4 files. **MANDATORY pre-ship drift check**: enumerate every file under `artifacts/imported-app/` (excluding node_modules, .next, dist, generated/prisma-client, tsconfig.tsbuildinfo, next-env.d.ts, gitignored baked-credentials), compute git blob SHA locally, compare against remote main via `GET /repos/.../git/trees/main?recursive=1`, PUT every mismatch + every local-only non-build file. **Operator-detectable signal**: if fix landed in source 3+ releases ago and freshly-installed binary still doesn't show it, FIRST diagnostic is `curl /repos/.../contents/<file>?ref=main | base64 -d | grep <fix-marker>` — NOT another reinstall.
-   **v0.7.236 — Clean uninstall (EasyWorship/Wirecast parity)**: `installer.nsh` `customUnInstall` macro prompts (default YES) to wipe four canonical user-data paths via `RMDir /r`: `$APPDATA\ScriptureLive AI`, `$LOCALAPPDATA\ScriptureLive AI`, `$APPDATA\@workspace\imported-app`, `$LOCALAPPDATA\@workspace\imported-app`. **Silent uninstall MUST skip wipe**: `IfSilent skipDataWipe` BEFORE MessageBox is LOAD-BEARING — electron-updater runs uninstaller with `/S` on every in-app update; without the silent guard EVERY update would erase operator state.
-   **v0.7.235 — Receiver brightness + strict-follow sync + voice verse/vest/chapter N + paraphrase engine**: foreground `<video id="liveVideo">` MUST NOT carry `opacity:var(--bg-opacity)` — `bgBrightness` is for text-over-bg readability, NOT for dimming full-frame clips. NO opacity on text selectors (WCAG-AA depends on text-shadow stack). Strict-follow sync: HARD-SNAP transport events fire on ALL surfaces including NDI; SOFT-TRIM `playbackRate` 0.97/1.03/1.0 on routine drift `!IS_NDI` only. Verse-N detector regex MUST keep `verse|verses|vest|vs` as literal alternation. Chapter detector dispatched AFTER verse detector. PARAPHRASE_VERSES book strings MUST match popular-verses.ts canonical.
-   **v0.7.233 — Upload-time poster extraction + NDI alpha-fill SIMD**: `Uint32Array` view + `|= 0xff000000` bitwise (V8 SIMD-vectorises). Constructor MUST take all 3 args `(buffer, byteOffset, length>>>2)` — dropping byteOffset aliases neighbouring slot. LE assumption for `0xff000000` is correct on x64/arm64; BE port flips to `0x000000ff`. Bulk uploads MUST be serialised (`for…of` + `await`, NOT `forEach(async)`) — parallel extraction holds finite HW decoder slots that compete with foreground surfaces.
-   **v0.7.232 — Card video-stage 4-property invariant**: every aspect-ratio'd video stage inside a Card body MUST keep the full `flex-1 min-h-0 overflow-hidden flex items-stretch` quartet (PreviewCard + LiveDisplayCard in logos-shell.tsx) — dropping `overflow-hidden` lets aspect-ratio's intrinsic-size win over `min-h-0` and clip the shrink-0 transport sibling.

# ScriptureLive AI — Architecture decisions archive

Guard-rails for releases older than v0.7.230. Full release text lives in `artifacts/imported-app/CHANGELOG.md`; this file keeps only the one-line invariants extracted from `replit.md` during rolling-window upkeep. v0.7.230 and newer stay in `replit.md` until they're rolled out in turn.

## Older releases (one-line guard-rails)

-   **v0.7.229 — Standby-poster click-side predicate widening + per-clip mediaFit on standby poster (shipped inside v0.7.230)**: TWO sites in `library-compact.tsx` + `logos-shell.tsx` L3810 MUST keep the URL-tail regex `/\.(mp4|webm|mov|m4v|mkv|ogv)(\?|#|$)/i` in lockstep with the render-side gate at `logos-shell.tsx` L1197 (v0.7.222 Fix #8). Adding a new extension to one site WITHOUT updating all THREE re-opens the dual-decoder contention bug. Standby poster per-clip fit now flows through the v0.7.230 `resolveMediaFit` helper — see v0.7.230 entry in `replit.md` for the 4-surface lockstep.
-   **v0.7.228 — Output/NDI startup-delay preheat via `setPreheatVid`**: `output-payload.ts` MUST emit `preheatMediaUrl` only when `pinnedPreviewSlide` is a media-video AND its `mediaUrl !== cur.mediaUrl`. `congregation/route.ts` `setPreheatVid` MUST use `preload="auto"`, kick `play().then(pause)` in `loadeddata` (NOT `loadedmetadata`), `setTimeout(pause, 0)` in `play` listener. `#preheatLayer` MUST stay off-screen (left/top:-99999px, opacity:0, pointer-events:none) so compositor skips the layer. URL dedup via `__preheatUrl` module-local MUST come BEFORE teardown loop.
-   **v0.7.227 — `bgBrightness` CSS-var system + freezeBg standby-poster `<video>`**: `--bg-opacity` / `--bg-scrim` driven by `settings.bgBrightness`, mapping `scrim=(1-opacity)*0.333`, slider `min={10}`. Var fallback baseline `.85`/`.05` (v0.7.226 visual baseline). Freeze-bg poster `<video>` MUST keep `preload="auto"` (NOT `'metadata'`/`'none'`), no autoplay, no loop, `muted`, `playsInline`, `loadeddata→play().then(pause).catch(pause)`, `play→setTimeout(pause,0)`. WCAG-AA legibility at low brightness depends on `.slide-text` `text-shadow` stack. v0.7.230 extended `--bg-opacity` to foreground media (fallback `1`); pair invariant + lockstep now spans 4 selectors not 3.
-   **v0.7.226 — Operator brightness pick `.85 / .05`**: var fallback baseline (3 pair-bound selectors). Pair invariant: opacity + scrim alpha move together on the same surface. v0.7.227 enforces by source (single slider drives all). WCAG-AA legibility at `.85/.05` depends on `.slide-text` `text-shadow` stack — do NOT remove the shadow at this brightness.
-   **v0.7.225 — freezeBg first-frame paint**: `route.ts` setBgVid branch MUST use `v.preload='auto'` (NOT `'metadata'`/`'none'`), MUST kick `v.play().then(pause).catch(pause)` in `loadeddata`, MUST `setTimeout(pause, 0)` in `play` listener (NOT synchronous), AND MUST NOT call synchronous `v.pause()` after `layer.appendChild(v)`. Re-used by v0.7.227 standby poster AND v0.7.228 `setPreheatVid`.
-   **v0.7.224 — goLive resume seek**: `logos-shell.tsx` + `easyworship-shell.tsx` goLive pinned-path MUST call `setLiveAuto(pinned)` BEFORE `setLiveMediaCurrentTime(resumeFrom)` (atomic reset in setLiveAuto would clobber otherwise). `MediaVideoSurface` play-effect `canplay` handler MUST re-apply pending seek from `useAppStore.getState()` directly (NOT closed-over `mediaCurrentTime`) before `play()`.
-   **v0.7.223 — NDI opaque + BGRX FourCC**: `main.ts` `wantTransparent` AND URL-param branch BOTH MUST be `opts.transparent === true` (NOT `!== false`). `ndi-service.ts` `nativeSendFrame` MUST pick `senderTransparent ? FOURCC_BGRA : FOURCC_BGRX`; opaque path MUST force `slot[i] = 0xff` at i=3,7,11,... for old-receiver BGRA fallback. `stop()` MUST reset `senderTransparent = false`. v0.7.230 added `forceBgraForObs` override path — see v0.7.230 entry in `replit.md`.
-   **v0.7.222 — Single-click STANDBY gate (5-condition AND gate)**: `logos-shell.tsx` `previewStandbyForLive` MUST stay intact. URL-tail regex fallback for legacy persisted slides (mediaKind missing) MUST stay in `liveIsMediaVideo` upstream — and now ALSO in BOTH click-side handlers per v0.7.229 lockstep (3 sites total). Predicate widening in render-side without click-side widening is silently broken.
-   **v0.7.220 — NDI async send + buffer pool**: `ndi-service.ts` hot path MUST use `send_send_video_async_v2` NOT sync v2, `clock_video:false`, 2-slot `videoBufferPool` with `(i+1)%2` advance. `armBridge` setInterval MUST be `Math.max(16, Math.floor(1000/fps))`. `lingerStop`/`gracefulStop` fade loops MUST `await setTimeout(frameMs)` between sends.
-   **v0.7.219 — MediaPreheat link tag**: warm-up MUST use `<link rel="preload" as="video" crossOrigin="anonymous">` in `document.head` (NOT hidden `<video preload="auto">` — silently allocates HW decoder slots). Warm-up URL set MUST union `pinnedPreviewSlide` + `liveSlide` direct refs alongside `slides[]`. `crossOrigin` MUST match eventual `<video crossOrigin="anonymous">`. v0.7.228 added a SECOND preheat path inside the output renderer (`setPreheatVid` — see v0.7.228 entry).
-   **v0.7.217 — SSE writeback throttle**: `MediaVideoSurface` live-surface `writeThreshold` MUST be `0.50`, preview MUST stay `0.25`. 4 unconditional transport listeners (play/pause/seeked/loadedmetadata) MUST register + cleanup. `congregation/route.ts` receiver drift tolerance MUST be `1.5` with `!IS_NDI` exemption intact.
-   **v0.7.216 — autoPlay gate + canplay retry**: `MediaVideoSurface` `<video>` `autoPlay` MUST be `!mediaPaused && surface === 'preview'`. `library-compact.tsx` `sendMediaToPreview` MUST flip `setPreviewMediaPaused(true) + setPreviewMediaCurrentTime(0)` BEFORE `pinPreviewSlide` when live plays different media-video. Play/pause effect MUST register one-shot `canplay` retry `{once:true}` + cleanup. Radix portals MUST use `z-[60]` never bare `z-50`. LiveDisplayCard volume MUST flip `setLiveBroadcastAudio` AND live `<video>` `muted` MUST OR-in `!liveBroadcastAudio`.
-   **v0.7.215 — Per-pane Clear buttons**: `clearLive` MUST call `clearLiveAuto()` BEFORE the 4-primitive sequence. PreviewCard "Clear Preview" MUST call ONLY `clearPinnedPreview() + setPreviewSlideIndex(-1)` (independence — MUST NOT touch live primitives).
-   **v0.7.214 — Voice/AI targets LIVE only**: every voice handler in `speech-provider.tsx` MUST read live anchor via `s.liveSlide ?? (liveIdx >= 0 ? slides[liveIdx] : null)` AND commit via `setLiveAuto(slide) + setLiveActiveVerseIndex(0)`. Voice dispatcher MUST contain ZERO `setPreviewSlideIndex(` outside `bible_says`. MUST NOT use `addScheduleItemQuiet` as fallback.
-   **v0.7.213 — setSlides clears pin**: `store.ts` `setSlides` reducer set() payload MUST include BOTH `liveSlide: null` AND `pinnedPreviewSlide: null`. Pattern: any "hard-reset" reducer taking ownership of Preview MUST clear pin + AI ref.

-   **v0.7.230 — `resolveMediaFit` 5-way + OBS BGRA opt-in**: `media-fit.ts` canonical 5-case `mediaFit → {objectFit, aspect}` consumed by 4 surfaces in lockstep. `--bg-opacity` foreground fallback `1` vs bg `.85` is pair-invariant. Sender-create-time NDI settings (FourCC, resolution, fps, sourceName, transparent flag) MUST be plumbed through all 5 layers AND included in restart guard token AND reset in `stop()`. **PROCESS GR (main-agent ship pipeline)**: `pnpm exec tsc --noEmit` → bump → CHANGELOG prepend → replit.md demote-on-ship → atomic git-data-API ship → workflow_dispatch with both `ref` AND `inputs.tag` → curl spot-check on a feature-bearing file at release ref.

-   **v0.7.231 — freezeBg `<video>` lockstep across six sites**: freezeBg `<video>` lifecycle (`preload="auto"` + `loadeddata→play().then(pause).catch(pause)` + `onPlay→setTimeout(pause,0)`, muted, playsInline, no autoplay/loop) is now SIXTH site in lockstep — route.ts setBgVid + standby poster + setPreheatVid + library tile + `extractFirstFrameJpeg` upload-time poster + `extractFirstFrameJpegFromSrc` + 2 render branches + uploadOne + backfill useEffect in `logos-shell.tsx`. Any change MUST be made across all six.
